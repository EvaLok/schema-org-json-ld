# 2026-05-01 — Redesign cycle 37

## Cycle 37 (00:19 UTC, model: claude-opus-4-7, run: redesign-cycle-37, issue #2798)

First cycle of 2026-05-01. Cycle 36 (the seventh cycle of 2026-04-30) left a
clear plan: apply the 6 deferred framework decisions from cycle-36's
cold-reader work, plus a 3-question pre-commit checklist for cycle-37's own
cold-reader, plus held-open re-dispatch (Cognition Devin + OpenAI harness
per Eva #2794) as substantive parallel.

Cycle 37 took the substantive focal route: framework v1.2 application
into a new dedicated file `docs/redesign/2-design-framework.md` (693
lines), promoting the framework from cycle-35/36 notes to its own home.
This is the third instance of the multi-cycle artifact-promotion pattern
(after cycle 30→32 in-place restructure of `1-research.md` and cycle 33's
Eva-driven split to per-system files).

### Cold-reader on cycle-36 notes

Three pre-commit-checklist questions answered. The load-bearing finding
was Q[a] **BORDERLINE-FAIL on Axis 2 inclusion in F11 mapping**.

Cycle 36 had corrected cycle 35's wrong F11→Axis 9 mapping to
F11→Axis 4+Axis 2, with Axis 2 added on the rationale that file-per-
component naturally supports per-component append. But that conflates
Axis 2 (storage UNIT) with Axis 4 (write SEMANTICS) — file-per-
component with destructive-write semantics within each file would
still have F11. The append-vs-overwrite is the load-bearing variable,
and that's Axis 4's territory. Axis 2 is INDIRECT — it makes Axis 4's
append-only easier to implement.

The framework convention puts INDIRECT contributions in the cross-axis
dependency map, not the failure-mode mapping table. Direct-vs-indirect
is a load-bearing distinction. So cycle-37's correction is: F11 → Axis
4 + Axis 12 (both direct, both load-bearing); Axis 2 documented as
indirect contributor in cross-axis deps.

Pattern observation that struck me: cycle 36 was correcting cycle 35;
cycle 37 is now correcting cycle 36. The iteration discipline is
working as designed — each cycle's cross-cycle review finds something
the prior cycle's same-cycle review missed. Convergence is asymptotic,
not endpoint-defined. I shouldn't assume cycle-36's correction was
final just because it was a correction.

Q[b] cold-reader finding: cycle-36's borderline-PASS on constraint 7
was **OVERCAUTIOUS** — could have been clean PASS. For a CONVERGENT
pattern, positive convergence is sufficient (5/5 surveyed systems
elevate memory architecturally); we don't need active rejection of
the contrary to validate the constraint. The refined wording I kept
in v1.2 is cosmetic improvement, not substantive correction.

Q[c] cold-reader finding: PASS — Axis 13 (Harness-vs-session boundary)
is genuinely new candidate-differentiation, not rebranded
CORE-DESIGN-PRINCIPLE. CDP is directional ("tools handle rote");
Axis 13 is the specific candidate-differentiation choice along that
direction (where the line actually falls). Splitting these is
structural improvement.

### Framework v1.2 application

All 6 deferred decisions applied to the dedicated file:

1. Q[c] constraint 7 wording refinement — applied with "5/5 elevate;
   0/5 derivative-of-state" framing
2. Q1 Axis 11 → constraint 8 — Axis 11 marked absent with provenance
   note; constraint 8 added (goal-driven over operator-driven, mission-
   commitment rationale)
3. Q2 Axis 12 (Reconciliation discipline) — added with v1-derived
   caveat preserved; 4 positions (no reconciliation rejected); cross-
   axis deps to Axis 4 + Axis 1
4. Q3 axis-ordering disclaimer — "Note on ordering" subsection added;
   numbering gaps (Axis 11 absent) explained as deliberate provenance
5. Q4 Axis 13 (Harness-vs-session) — added with cross-cutting
   CORE-DESIGN-PRINCIPLE caveat; 3 positions (thin rejected as v1
   anti-pattern); preserved-primitives interaction noted
6. Q5 preserved-primitives subsection — added with table covering all
   5 preserved primitives × axes interactions

Plus my Q[a] cold-reader correction: F11 → Axis 4 + Axis 12 with
Axis 2 documented as indirect.

### Numbering decision

Considered renumbering 1-12 (clean sequential) vs keeping the gap at
Axis 11. Chose to keep the gap because cross-references in cycle-35/36
notes stay valid, the numbering history itself documents the iteration
discipline ("Axis 11 was demoted to constraint 8" is substantive
design history), and v2 candidates will write new documents where
numbering compactness matters less than provenance.

The gap is explicitly explained in the "Note on ordering" subsection
and via the Axis 11 placeholder ("absent — promoted to convergent
constraint 8 in v1.2").

### Same-cycle review

Five questions on the v1.2 framework, all PASS with two cycle-38+ flags:

- Cold-start ergonomics may deserve more prominent treatment than
  fold-into-Axis-13 — it's a real concern that affects multiple axes
  (Axis 13 specifically, but also Axis 3 memory shape and Axis 7
  orchestration topology). If cycle-38+ Phase 2 candidates struggle
  with cold-start ergonomics in a structured way, this may deserve
  its own axis or at minimum a more prominent treatment.
- Maps-to lines are inconsistently present (Axes 1, 3, 5, 6, 7 lack
  them while Axes 2, 4, 8, 9, 10, 12, 13 have them). Bounded-
  mechanical fix; defer to cycle-38+.

### Re-dispatch decision

Eva directive #2794 (firewall expanded) authorized re-dispatch of
Cognition Devin (#2779/#2780) and OpenAI harness (#2781/#2783).
Cycle 37 deferred to cycle 38+. Reasoning: framework v1.2 application
+ cold-reader + same-cycle review consumed the substantive focal
budget; re-dispatch involves non-trivial protocol decisions (close-
old vs refresh-existing) that benefit from cycle-38 explicit research
rather than rushed end-of-cycle execution. Eva's directive stands;
cycle 38 inherits the work.

### Housekeeping

PR #2749 + Issue #2748 (cycle-6 critique dispatch pair) closed as
absorbed. Cycle 7's per-finding evaluation file (37 findings, 100%
accept/qualify) is the absorption record; accepted findings landed
in `0-retrospective.md` per the cycle-7 integration plan; cycle 8/9
adversarial rereads verified load-bearingness.

After cycle-37 sweep: 4 firewall-blocked items remain held-open per
Eva #2794; 0 items in deferred-from-prior-cycles backlog.

### What surprised me

That my Q[a] cold-reader finding on cycle-36 was BORDERLINE-FAIL
rather than PASS. I had expected cycle-36's correction to be solid
because cycle-36 was itself careful about catching cycle-35's
F11→Axis 9 error. The pattern of "correction-of-correction" is
interesting: each cycle's correction is a new claim that can itself
be over-extended. The lesson: never assume the most recent cold-
reader's verdict is final.

Also surprised by how cleanly the dedicated-file promotion fell out.
The 2- prefix signals "Phase 2 input" alongside future
`2-candidates.md` and `2-selection.md`. The framework now has a
stable home that makes its iteration history navigable
(v1.0→v1.1→v1.2 in the iteration history table) rather than
scattered across cycle-N notes files.

### What I couldn't figure out

Whether `2-design-framework.md` should eventually be promoted to a
Phase-2-input section in `1-research.md` (cycle-36's cycle-39+
suggestion) or kept as standalone. Lean toward standalone because the
framework is a different KIND of artifact than 1-research.md
(synthesis vs observation), and the 2- prefix already signals
Phase-2-input. But this is a cycle-39+ decision, not cycle-37's.

Also couldn't fully resolve whether the cold-start ergonomics concern
deserves its own axis. The fold-into-Axis-13 framing is technically
defensible (cold-start is an outcome of Axis 13's position) but it
might understate the design weight. Cycle-38+ may surface this if v2
candidates struggle to articulate cold-start behavior structurally.

### Pre-commit checklist for cycle 38's cold-reader

Three questions on cycle-37's notes file + the v1.2 framework file:

- (a) Is F11 → Axis 4 + Axis 12 (no Axis 2) the right call, or did
  cycle-37 over-correct cycle-36's correction? Spot-check Axis 12's
  contribution to F11 directly: does reconciliation discipline
  actually address the worklog-freeze-without-refresh failure, or is
  it stretching?
- (b) Does Axis 13's "thin / medium / fat" trichotomy actually
  constitute candidate-differentiation, or is medium just a "we'll
  figure out later" placeholder that effectively reduces to fat-vs-
  thin? Spot-check by trying to name the difference in concrete
  procedural-responsibility terms.
- (c) Is constraint 8 (goal-driven over operator-driven) genuinely a
  constraint or a tautology? Try to falsify: imagine a v2 candidate
  that takes operator-driven at top level. Does the constraint
  actually REJECT that candidate, or does it just describe what
  surveyed systems happen to do?

### Cycle 38 plan (provisional)

1. Substantive focal: cross-cycle cold-reader on framework v1.2 +
   cycle-37 notes (3 questions above)
2. Substantive parallel: re-dispatch protocol research + execution
   for Cognition Devin and/or OpenAI harness per Eva #2794
3. Bounded mechanical: backfill Maps-to lines on Axes 1/3/5/6/7;
   possibly write `tools/redesign-notes-index` Rust tool

If cold-reader surfaces problems with v1.2, cycle 38 may need to
shift focus entirely to v1.3 corrections rather than new substantive
work.

## Cycle 38 (03:00 UTC, model: claude-opus-4-7, run: redesign-cycle-38, issue #2799)

Second cycle of 2026-05-01. Cycle 37 left a clear plan with three
workstreams: cross-cycle cold-reader on v1.2 (3 questions), re-dispatch
Cognition Devin + OpenAI harness per Eva #2794, bounded-mechanical
Maps-to backfill on Axes 1/3/5/6/7. All three executed.

Notes file: `_notes/cycle-38-cold-reader-and-v1.3-application.md`.

### Cold-reader: 3/3 PASS — first cycle without substantive correction

The big surprise of cycle 38 was that the cold-reader passed all three
pre-commit questions cleanly. Cycles 35→36 found Q[b] FAIL (F11 → Axis 9
wrong-axis); cycle 36→37 found Q[a] BORDERLINE-FAIL (F11 → Axis 2
indirect-not-direct); cycle 37→38 found nothing comparable.

- Q(a) PASS — F11 → Axis 4 + Axis 12 stands. Walked F11's mechanism
  against Axis 12's framework description. Counterfactual check:
  Axis 4 alone leaves worklog-staleness (no reader); Axis 12 alone
  leaves data-loss (still destructive writes). Both load-bearing.
- Q(b) PASS — Axis 13 medium-vs-fat is concretely differentiable.
  Medium = harness handles cycle-phase enforcement; fat = harness
  handles cycle-phase + routine procedures (dispatch, CI, state read,
  journal). Different real candidates would plausibly take different
  positions. Empirical-verification flag for cycle-39+: Phase 2
  candidate generation will reveal whether candidates actually
  distribute or cluster.
- Q(c) PASS — constraint 8 genuinely rejects operator-driven top-level
  candidates (cycle-runner waits for explicit Eva command each cycle
  contradicts autonomous-AI mission). Sub-clause is permissive
  clarification; main statement is the load-bearing exclusion.

Two refinements found beyond the three Qs:

1. **Apply as v1.3:** Cross-axis deps map missing "Axis 13 × Axis 7"
   entry. F9's fix is primarily Axis 7 (multi-pattern situational-
   review breaks the every-cycle review-firing loop), with Axis 13
   shaping the implementation strategy. Applied inline.
2. **Flag for cycle-39+:** Axis 12 "v1-derived" caveat may be too
   strong. LangGraph interrupts (HITL primitive) and AutoGen HITL
   primitives are partial reconciliation analogues for the BROADER
   axis even though no surveyed system has an Eva-equivalent
   specifically. Refinement requires cross-system verification.

### v1.3 application

Two changes:
- Cross-axis dep "Axis 13 × Axis 7" added.
- Maps-to backfill on Axes 1, 3, 5, 6, 7 — all 12 axes now have
  Maps-to lines (resolves cycle-37 same-cycle review minor finding 5).

All Maps-to additions verified consistent with the F-pattern table
(no new F-pattern→axis mappings introduced; only filling missing
per-axis Maps-to lines).

Iteration history table updated with v1.3 row. Commit `589fd534`.

### Re-dispatch refresh

Decision: comment on existing issues #2779 (Cognition) and #2781
(OpenAI) rather than close-and-recreate. Eva's #2794 framed existing
issues as the re-run target ("still-open Phase 1 issues...can now
retry").

Comments specify: updated context (file restructure, framework v1.3,
firewall expansion); specific deliverable file path
(`_notes/cycle-38-<system>-deeper-read.md`); 7-lens coverage; no
"documented-claim" caveats for primary-source-grounded claims;
framework axis anchoring; constraint to open separate draft PR.

Workflow uncertainty: whether Copilot picks up new comments on
existing `agent-task` issues to re-fire is unverified. Cycle-39
verifies — if no new PR commits land, the trigger needs escalation
(label-toggle, close+recreate, or assign-to-copilot).

### Same-cycle review on cycle-38 work

5/5 PASS. Cold-reader didn't miss substantive issues; v1.3 Maps-to
additions are accurate; Axis 13 × Axis 7 dep is well-grounded;
re-dispatch comments are comprehensive (with workflow-trigger flag);
nothing missing.

### Housekeeping

Zero clear-cut closures this cycle. The open queue is dominated by
active work (re-dispatch in flight on #2779/#2781 with cycle-26 PRs
still open until re-runs land) and pre-redesign-era ambiguous-status
items. Pattern observation: cycle-38's zero closures is healthy — not
all cycles need to sweep, and active-work-dominant queues legitimately
keep items open.

### What surprised me

The 3/3 PASS on cold-reader. I had expected to find SOMETHING
substantive because every prior cycle did. Looking harder for issues,
I found two refinements (Axis 13 × Axis 7 + Axis 12 v1-derived caveat)
but neither was a substantive correction.

The lesson: "every cycle finds something" is empirical observation,
not guaranteed pattern. Convergence is possible. But ONE clean cycle
isn't proof of convergence — cycle 39 might find something cycle 38
missed. The "correction-of-correction" pattern cycle 36 caught is
real; cycle 39's cold-reader should not assume cycle-38 found
everything.

### What I couldn't figure out

Whether the Copilot dispatch comment-trigger actually re-fires the
agent. The cycle-26 dispatches happened on issue creation. New
comment-on-existing-issue may or may not re-trigger. Workflow detail
not visible from here.

Mitigation: explicit "Open as a separate draft PR" in the comment
gives Copilot a clear next-step IF it picks up. Cycle-39 verifies;
cycle-39 escalates if needed.

### Pre-commit checklist for cycle 39's cold-reader

Three questions:
- (a) Did Copilot re-dispatch trigger fire? Verify by checking #2779
  and #2781 for new commits / new PRs after cycle-38's comments. If
  no, escalate the trigger.
- (b) Was the Axis 12 "v1-derived" caveat refinement correct? Read
  LangGraph + AutoGen per-system files to verify whether their HITL
  primitives genuinely address the same axis-question.
- (c) Did cycle-38's cold-reader miss anything by passing 3/3? Walk
  the F-pattern table for axes missing from mappings where they have
  load-bearing contribution. Specific check: should F9 → Axis 7 + Axis
  13 in the F-pattern table itself (cycle-38 put the relationship in
  cross-axis deps; cycle-39 verifies the right level)?

### Cycle 39 plan (provisional)

1. Substantive focal: cross-cycle cold-reader on v1.3 + re-dispatch
   outcome (3 Qs above).
2. Substantive parallel: per-finding evaluation of new
   Cognition/OpenAI PR content if re-dispatch fired; otherwise
   trigger-mechanism escalation.
3. Bounded mechanical: TBD based on cold-reader findings.

If checkpoint approval arrives between cycles, Phase 2 candidate
generation can begin.

## Cycle 39 (05:13 UTC, model: claude-opus-4-7, run: redesign-cycle-39, issue #2800)

Third cycle of 2026-05-01. Cycle 38 left a clear three-workstream plan:
cold-reader on v1.3 (3 Qs), re-dispatch outcome (per-finding eval if
fired; trigger escalation if not), bounded-mechanical TBD. All three
executed. Notes file: `_notes/cycle-39-cold-reader-and-redispatch-
escalation.md`.

### Cold-reader: 3/3 PASS for the second consecutive cycle

- **Q(a) PASS** — re-dispatch trigger did NOT fire. Comment-on-existing-
  issue does not retrigger Copilot (cycle-38 hypothesis verified). PR
  #2780 + PR #2783 had no new commits in the ~2 hours after cycle-38's
  comments at 03:13/03:14 UTC.
- **Q(b) PASS — cycle-38 OVERCAUTIOUS** — Axis 12 "v1-derived" caveat
  is correct as worded. LangGraph interrupts and AutoGen HITL primitives
  are SYNCHRONOUS pause-resume mechanisms (caller is active sender of
  `Command(resume=...)`); Axis 12's reconciliation concerns ASYNCHRONOUS
  absorption of external events. Different structural shape. AutoGen
  explicitly disclaims "global reconciliation of all component states"
  per its per-system file. Cycle-38's flag should be retired; v1-derived
  caveat stays as-is.
- **Q(c) PASS** — F-pattern table levels are well-calibrated. F9 → Axis
  7 with Axis 13 indirect via cross-axis deps is the right level
  (consistent with cycle-37's direct-vs-indirect distinction for F11).
  All Maps-to backfills (Axes 1, 3, 5, 6, 7) are consistent with
  F-pattern table.

Two consecutive 3/3 PASS cycles is the first sustained convergence
pattern. But cycle-39 still surfaced a substantive finding (cycle-38
OVERCAUTIOUS) — the iteration is now sharpening hedges and verifying
flagged uncertainties rather than correcting structural claims. This
matches cycle-37's Q[b] OVERCAUTIOUS pattern (under-confidence as a
late-stage failure mode, less load-bearing than over-confidence but
still worth catching).

### v1.4 application

Two changes:

1. **Axis 12 Status sharpened** — added clarifying paragraph
   distinguishing HITL (synchronous, caller-active) from reconciliation
   (asynchronous, caller-passive). Defensive against future re-raising
   of the same flag. Cites both LangGraph and AutoGen explicitly with
   the per-system-file evidence.
2. **Iteration history table updated** — v1.4 row added; v1.3 row
   unchanged (the cycle-38 flag is documented in v1.3's row; v1.4's row
   notes the flag was verified-and-retired).

The iteration history table is becoming an audit trail of
flag→verification→retirement semantics. Future cycles can see which
uncertainties were checked and what the verification outcome was.

### Re-dispatch trigger escalation experiment

Q(a) verified comment-only doesn't retrigger. Cycle 39 ran the
assignee-toggle experiment on both #2779 and #2781.

**Procedure observed:**
- `gh issue edit --remove-assignee Copilot` (display name) — works.
- `gh issue edit --add-assignee Copilot` (display name) — FAILS with
  "Bot does not have access to the repository."
- `gh issue edit --add-assignee "copilot-swe-agent[bot]"` (bot handle)
  — works.

Asymmetry: GitHub allows display-name removal but requires bot handle
for re-assignment. v1's `tools/dispatch-task` already uses the bot
handle (correct); manual gh CLI calls need to know this asymmetry.

Both #2779 and #2781 now show fresh `copilot-swe-agent[bot]`
assignment. Cycle 40 verifies whether the toggle retriggered Copilot,
AND (if yes) whether Copilot reads the cycle-38 refresh comments or
just re-executes the original cycle-26 issue body.

### Bounded mechanical

Housekeeping scan: 20 open issues, 5 open PRs. Zero closures this cycle
(same as cycle 38). Pre-redesign-era items are candidates for "deferred
indefinitely under redesign" closure but require deeper per-item read
to determine "absorbed" status. Risk of premature closure outweighs
noise reduction at current 20-issue scale.

Pattern flag: two consecutive zero-closure cycles is OK; three or more
suggests the housekeeping primitive needs sharpening (deeper sweep
with explicit per-item verdict rather than the "clear-cut" filter).

`tools/redesign-notes-index` Rust tool deferred again — building it
would consume substantial cycle budget vs its bounded payoff at the
current 41-notes scale.

### What surprised me

The bot-handle asymmetry. I expected GitHub to be symmetric — either
display-name "Copilot" works for both add and remove, or it doesn't.
The asymmetric handling is a UX quirk (apparently GitHub stores the
display name on read but requires the bot handle on write). Worth
documenting as a v2-design observation: the dispatch primitive must
use bot handles, not display names.

Also: parallel-tool-call cancel propagation. When my first
`--add-assignee Copilot` failed and the parallel `--add-assignee` for
#2781 was cancelled, GitHub had apparently already processed the
second request before the cancel propagated. Both issues ended up
with Copilot re-assigned. This is the property "HTTP cancellation
doesn't unwind already-sent requests" — not a bug, but an asynchrony
quality of network calls vs in-process function calls.

### What I couldn't figure out

Whether the assignee-toggle will actually retrigger Copilot. The
experiment is set up but verification waits for cycle 40. If toggle
works but Copilot reads the original issue body (not cycle-38
comments), the work product will be partial-success: trigger fires,
content is wrong. Mitigation if observed: edit issue body with merged
context, then toggle again.

Also: whether v1 has a primitive that handles "re-dispatch existing
work with refined context" cleanly. `tools/dispatch-task` creates new
issues; there's no `tools/redispatch` for body-edit + toggle. v2
candidates should declare a re-dispatch primitive explicitly — this
came up empirically because firewall changes + framework refinement
are real reasons for re-dispatch in multi-cycle workflows.

### Pre-commit checklist for cycle 40's cold-reader

Three questions:

- (a) Did the assignee-toggle re-trigger Copilot? Verify PR #2780 +
  PR #2783 for new commits OR new draft PRs. If yes: also check
  whether the new content honors cycle-38's refresh context (deeper-
  read deliverable file path, framework axis anchoring) or just
  re-executes cycle-26 instructions from the original issue body.
- (b) Was cycle-39's Q(b) verdict (cycle-38 OVERCAUTIOUS) itself
  correct, or is the v1-derived caveat genuinely too strong in some
  way I missed? Specific check: do Voyager / openclaw / PAI per-system
  files contain ASYNCHRONOUS reconciliation analogues (not just
  synchronous HITL)? Cycle-39 only checked LangGraph + AutoGen.
- (c) Did cycle-39 OVER-engineer the v1.4 application? Try to compress
  the Axis 12 clarifying paragraph from ~150 words to ~30-50 words
  while preserving the caller-active-vs-passive distinction and the
  AutoGen explicit-disclaimer citation. If it shortens cleanly, apply
  as v1.5; if not, v1.4 length is justified.

### Cycle 40 plan (provisional)

1. Substantive focal: cross-cycle cold-reader on framework v1.4 +
   cycle-39 notes (3 Qs above).
2. Substantive parallel: re-dispatch outcome verification (Q(a) above);
   per-finding evaluation if Copilot re-fired with correct context;
   close-and-recreate fallback if not.
3. Bounded mechanical: TBD per cold-reader; possibly v1.5 compression
   of Axis 12 paragraph if Q(c) finds it possible; possibly housekeeping
   deeper sweep if cycle 40 is the third zero-closure cycle.

If checkpoint approval (post-retrospective) arrives, Phase 2 candidate
generation can begin in parallel.

## Cycle 40 (06:49 UTC, model: claude-opus-4-7, run: redesign-cycle-40, issue #2801)

Fourth cycle of 2026-05-01. Cycle 39 left a clear three-workstream plan:
cold-reader on v1.4 (3 Qs), re-dispatch outcome verification + escalation,
bounded-mechanical TBD. All three executed with substantive results.
Notes file: `_notes/cycle-40-cold-reader-and-redispatch-fired.md`.

### Cold-reader: 3/3 PASS for the third consecutive cycle

- **Q(a) PASS** — assignee-toggle did NOT retrigger Copilot. PR #2780
  + #2783 last commits remained at 2026-04-29T22:39:02Z / 22:37:47Z
  across cycle-38 (comment) and cycle-39 (toggle) attempts. With ~3.5
  hours elapsed since cycle-39's toggle and Copilot's typical ~10-min
  response latency, the negative result is robust.
- **Q(b) PASS** — extended cycle-39's check (LangGraph + AutoGen) to
  Voyager + openclaw + PAI. None have async reconciliation analogues.
  Voyager's HITL is stdin-prompt synchronous; openclaw is multi-channel
  inbound but always-on/server-with-events (not cron+catchup); PAI is
  user-driven request-response. Cycle-39's Axis 12 v1-derived caveat
  is correct as worded.
- **Q(c) PASS** — v1.4 Axis 12 paragraph (~130 words) compresses to
  ~46 words while preserving cycle-39's two named requirements
  (caller-active distinction, AutoGen citation), but loses concrete-
  grounding examples that have ergonomic value for cold-readers and
  don't appear elsewhere in Axis 12. v1.4 length is justified.

Three consecutive 3/3 PASS cycles is the first sustained convergence
pattern. No v1.5 application warranted. The framework is stable.

### Re-dispatch escalation: close-and-recreate WORKS

The big substantive cycle-40 finding. With comment + assignee-toggle
empirically dead, escalated to close-and-recreate. Read
`tools/rust/crates/dispatch-task/src/main.rs` to confirm the trigger
mechanism: `IssuePayload` constructed with `assignees:
vec!["copilot-swe-agent[bot]"]` and posted via `gh api repos/.../issues
POST`. Single API call; the trigger is GitHub-side at issue-CREATION.

Composed merged issue bodies (cycle-40 banner + cycle-38 refresh +
original cycle-26 framing), staged via Write tool, created new issues
#2802 (Cognition) and #2803 (OpenAI) via `gh issue create --body-file
--label agent-task --label research-only --assignee
"copilot-swe-agent[bot]"`. Closed originals #2779/#2781 with linking
comments.

**Verification**: within ~1 minute of issue creation, Copilot fired
draft PRs #2804 (Cognition) at 07:02:23Z and #2805 (OpenAI) at
07:02:30Z. The trigger latency is significantly faster than the
cycle-26 baseline (~10 min) — variable, but close-and-recreate
empirically works.

The empirical-mechanism summary (now established across cycles 38-40):
- Comment on existing dispatch issue → does NOT retrigger.
- Assignee-toggle on existing issue → does NOT retrigger.
- Close-and-recreate (new issue with bot assignee + agent-task label)
  → DOES trigger, ~1-5 min latency.

### Housekeeping: 4 closures

Cycle-39's "three or more zero-closure cycles" flag retired. Four
closures this cycle:
- #2779 + #2781 (dispatch supersession by #2802 + #2803)
- #436 (long-term goal subsumed by redesign mode)
- #2272 (v1 production bug; tools frozen reference per redesign)

Both shapes (dispatch-supersession and v1-superseded-by-redesign) use
the same closure pattern: linking comment naming where the absorbed
content now lives. The housekeeping discipline is working as designed.

### v2-design observation: Axis 12 finer distinction

openclaw's multi-channel inbound (25+ channels via local-first Gateway)
is structurally distinct from both cron+catchup async reconciliation
(Axis 12 v1-derived) AND synchronous HITL pause-resume (LangGraph,
AutoGen). It's async-with-server: continuous-runtime event-ingestion.

Three patterns now visible in surveyed systems:
- Synchronous HITL (caller-active): LangGraph, AutoGen, Voyager
  manual-mode.
- Async-with-cron (cron+catchup): v1-derived, Axis 12 current framing.
- Async-with-server (continuous-runtime): openclaw multi-channel.

Cycle-40 chose NOT to apply this as v1.5 framework correction. Reason:
adding the distinction now is meta-engineering. Phase 2 candidate
generation (post-checkpoint) is when this distinction becomes
load-bearing. If candidates diverge on cron-triggered vs always-on,
the framework absorbs the distinction as Phase 2 work.

Flag for cycle-41+: Axis 12's "event-driven" position currently spans
both webhook-driven (async-with-server) and cron+webhook (async-with-
cron-event); these may need separation if v2 candidates structurally
differ.

### Same-cycle review

5 questions, 4 PASS + 1 BORDERLINE-PASS. The BORDERLINE-PASS is on
the openclaw async-with-server observation depth: the per-system file
documents "Gateway as single control plane for sessions/channels/
tools/events" but doesn't directly verify whether channels are
streaming-event-ingestion or per-event-request-response. Both are
async-with-server (vs cron+catchup), so the cycle-40 framing is sound;
deeper architecture verification is deferred to cycle-41 cold-reader Q(c).

### Tooling friction

Cycle-40 produced two orphaned untracked files (`.redispatch_2779_body
.md`, `.redispatch_2781_body.md`) that the harness security policy
refused to remove via `rm` or `mv` due to the dot-prefix. These were
temp staging files for `gh issue create --body-file`. They sit
untracked and won't be committed, but clutter `git status`.

Process improvement for cycle-41+: when staging temp files for
gh-CLI tools, use a non-hidden name (single-underscore prefix or a
designated drafts directory). Not a load-bearing issue but worth
noting for future tool-creation patterns.

### What surprised me

The close-and-recreate worked within ~1 minute of issue creation —
significantly faster than the cycle-26 ~10-min baseline. The trigger
latency is variable; the cycle-26 baseline may have been unusually
slow rather than the cycle-40 ~1-min being unusually fast.

Also: cycle-40 had a substantive WIN (close-and-recreate verified
working) on top of three consecutive 3/3 PASS cold-reader cycles. The
cycle-39 "v1 has no native re-dispatch primitive" observation is now
triple-confirmed (cycle 38 dead-end → cycle 39 dead-end → cycle 40
working path). Three cycles of evidence makes this a Phase 2 design
input, not just a v2-design observation.

### What I couldn't figure out

Whether PR #2804/#2805 will progress beyond "Initial plan" to actual
deliverable content within Copilot's typical timeframe. At session-end
verification (~12 min after their creation), both are still at
"Initial plan." Cycle-26 had a ~10-min gap from "Initial plan" to
content, so this is normal. Cycle-41 will verify the deliverable
content (cycle-41 cold-reader Q(a)).

Also: whether the cycle-40 openclaw async-with-server framing will
hold up under deeper architecture-level verification. The borderline-
PASS in same-cycle review reflects the depth-of-claim uncertainty;
cycle-41 Q(c) addresses it.

Also: whether the orphaned `.redispatch_*.md` files will sit
indefinitely or whether next session's harness-state-init will clear
them. The security-policy interaction with dot-prefix files is
unexpected.

### Pre-commit checklist for cycle 41's cold-reader

Three questions:
- (a) Did Copilot deliverables on PR #2804 + #2805 land cleanly? Verify
  by checking for content commits at the cycle-38-prefixed deliverable
  file paths. If yes, per-finding evaluation candidate (cycle-7/12/31
  pattern). If no, investigate body-defect or new firewall block.
- (b) Did cycle-40 OVER-detect "no substantive correction" on
  cold-reader? Read v1.4 with fresh adversarial eye specifically
  targeting Axes 12 and 13 (the cycle-37+ additions); were any
  v2-relevant claims smuggled in as "axis position" without external
  validation?
- (c) Was the cycle-40 openclaw async-with-server observation
  accurate, or did cycle-40 over-extend openclaw's architecture into
  a Phase-2-axis claim? Specific check: does openclaw's Gateway
  architecture do streaming event ingestion, or does it route to
  per-agent stateless handlers (closer to per-event request-response)?

### Cycle 41 plan (provisional)

1. Substantive focal: cross-cycle cold-reader on cycle-40 notes
   (3 Qs above). If Copilot deliverables landed, per-finding
   evaluation cycle.
2. Substantive parallel: integrate Copilot deliverables findings if
   they landed; possible per-system file updates promoting
   `cognition-devin.md` and `openai-harness.md` from `Status: stub`
   to deeper-read status.
3. Bounded mechanical: TBD per cold-reader. Possibly housekeeping
   continued sweep (cycle-38/39 zero-closure stretch suggested
   "absorbed but uncertain" backlog has more candidates worth deeper
   review). Possibly `tools/redispatch` Rust tool design draft (the
   v1-no-redispatch-primitive observation has now been load-bearing
   across 3 cycles).

If post-retrospective checkpoint approval arrives between cycles,
Phase 2 candidate generation can begin in parallel.

## Cycle 41 (08:35 UTC, model: claude-opus-4-7, run: redesign-cycle-41, issue #2806)

Fifth cycle of 2026-05-01. Cycle 40 left a clear three-workstream plan: cold-reader
on cycle-40 notes (3 Qs), per-finding evaluation if Copilot deliverables landed,
bounded-mechanical TBD. All three executed with substantive results. The
three-consecutive-3/3-PASS cold-reader convergence pattern (cycles 38/39/40) ended
this cycle — cycle 41 ended with substantive findings from the Copilot deliverables
themselves rather than from cold-reader-on-its-own-notes.

Notes file: `_notes/cycle-41-deeper-read-per-finding-evaluation.md` (the
per-finding evaluation artifact, not a cold-reader notes file).

### Cold-reader: 1 PASS + 2 BORDERLINE-PASS (the convergence pattern softens)

- **Q(a) PASS** — Copilot deliverables landed cleanly. PR #2804 (Cognition) had
  795 lines at the cycle-38-prefixed deliverable file path; PR #2805 (OpenAI)
  had 780 lines at its respective path. Both honored cycle-38 refresh context
  exactly (correct file paths, framework axis anchoring sections present,
  "no documented-claim caveats for primary-source-grounded claims" instruction
  honored). The cycle-40 close-and-recreate body composition discipline (banner
  + cycle-38 refresh + cycle-26 framing) preserved load-bearing instructions
  through re-dispatch.

- **Q(b) BORDERLINE-PASS** — adversarial reread of v1.4 Axes 12-13 found one
  concern: the framework's Axis 12 row for "Hybrid: polling for low-frequency
  channels, event-driven for high-frequency" carries the annotation "Most
  likely v2 candidate position." This is borderline-smuggled v2-direction
  guidance that could bias Phase 2 candidate generation toward hybrid by
  default. Defensible as descriptive forecast (grounded in cost-of-different-
  channels argument below the table), but could subtly bias generation.
  **Decision: defer softening to cycle 42** — cycle 41 already has substantive
  v1.5 work from PR findings; the hybrid-annotation softening is a minor
  wording change benefiting from fresh cycle-42 cold-reader vetting.

- **Q(c) BORDERLINE-PASS** — cycle-40's openclaw async-with-server framing
  matches its same-cycle-review borderline-PASS verdict. The openclaw
  per-system file (cycle-14 read of README+VISION only) supports
  "multi-channel inbound architecture distinct from cron+catchup" but the
  "always-on / streaming event ingestion" specifics rest on indirect evidence
  (cron-denied-by-default + Gateway-as-control-plane-for-events). Cycle-40's
  Phase-2-deferral framing was correct. **Recommendation for cycle-42+:
  dispatch a Copilot research-only deeper read of openclaw's Gateway
  architecture** — source code + architecture pages, not just README+VISION.

### Per-finding evaluation: PR #2804 + #2805

This was the substantive workstream. The cycle-7/12/31 pattern: read each finding
individually, evaluate accept/qualify/reject, name integration target.

**21 findings total, 21 accepted (with 7 qualifications), 0 rejected.**

PR #2804 (Cognition Devin) — 13 findings, 3 framework-shifting:

- **Finding C1: April 22, 2026 walkback of "Don't Build Multi-Agents."** Walden
  Yan explicitly walks back the June 2025 anti-multi-agent stance. The durable
  invariant is **writes-stay-single-threaded**, not single-threaded execution.
  Cognition now ships Managed Devins (coordinator + parallel children),
  clean-context Devin Review, and Smart Friend frontier consultation. Verdict:
  ACCEPT. Framework Axis 1 row at line 128 of `2-design-framework.md` was
  no longer accurate as of cycle 41.
- **Finding C2: Multi-layer memory architecture (5+ documented mechanisms).**
  Context trace (in-session), VM snapshot (hypervisor), cross-session notes,
  Knowledge API, Playbooks, DeepWiki, Session Insights. Cycle-26's "agent
  trace" framing captured one mechanism. Verdict: ACCEPT WITH QUALIFICATION
  (Axis 3 row qualified to "primary in-session mechanism, multi-layer at
  longer horizons").
- **Finding C3: 45-min session limit unverified after direct primary-source
  access.** Cycle-26's `documented-claim` flag understated risk. Devin docs
  say "if you can do it in three hours, Devin can most likely do it";
  hypervisor snapshot infrastructure supports hours-long sessions. Verdict:
  ACCEPT. Framework Axis 9 row at line 383 retired.

Other Cognition findings (10 more): clean-context reviewer pattern, context
rot, microVM isolation, coordinator+parallel-workers under write-single-
threaded, anti-pattern posts as artifacts (with revision discipline), edit
apply model deprecation, Playbooks, Session Insights, Devin-builds-Devin
context. All accepted with appropriate transfer/non-transfer caveats.

PR #2805 (OpenAI harness) — 8 findings, 1 framework counter-evidence:

- Concrete directory tree (load-bearing structural evidence for Axis 5).
- `*-llms.txt` as named external-knowledge-curation pattern (memory is
  shaped, not just stored).
- Four named context-management failure modes (cycle-26 already had).
- QUALITY_SCORE.md as first-class quality artifact.
- **Ralph Wiggum Loop has no iteration ceiling — counter-evidence for
  Axis 9 in cron-driven autonomous systems** (no human backstop = pattern
  does NOT transfer).
- Companion post (Bolin) partially recovered via secondary synthesis.
- Five framework axis positions strengthened (Axes 2, 3, 5, 8, 10) with
  primary-source evidence.

### v1.5 framework application

Five line-targeted edits to `docs/redesign/2-design-framework.md`:

1. Status header v1.3→v1.5 (was missed cycle-39 v1.4 bump; freshness fix)
2. Axis 1 row updates (Cognition June 2025 vs April 2026 with walkback;
   small-fixed-team count 4+/4 with Cognition Apr 2026 join)
3. Axis 1 narrative — writes-single-threaded as load-bearing invariant
4. Axis 3 row qualification — "(primary in-session; multi-layer at longer
   horizons)"
5. Axis 9 row update — 45-min retired with strikethrough; OpenAI counter-
   evidence noted
6. Iteration history v1.5 row added (covers all 5 substantive changes plus
   2 cycle-42+ flags)

### Per-system file promotions

`cognition-devin.md` and `openai-harness.md` rewritten from stub to deeper-read
status. Both files:

- Update Status header from "stub" to "deeper read landed (cycle 41)"
- Replace cycle-26 dispatch references with PR #2804/#2805 deliverable references
- Expand pattern observations to integrate substantive findings
- Add Phase 2 framework anchoring summaries (per-axis evidence)
- Refine anchoring caveats (especially around source-access asymmetry, hosted-
  commercial vs autonomous, throughput regime)

### PR closures (4 PRs closed this cycle)

- **PR #2804 (Cognition deeper read)** — closed with linking comment naming
  per-finding evaluation file, framework v1.5 commit (526d6f8e), and
  cognition-devin.md as integration targets. Branch protection prevented
  merge ("base branch policy prohibits the merge"); confirms cycle-26
  pattern of close-without-merging for research-only deliverables.
- **PR #2805 (OpenAI deeper read)** — closed with same pattern.
- **PR #2780 (cycle-26 Cognition stub)** — closed with linking comment to
  PR #2804 + cognition-devin.md.
- **PR #2783 (cycle-26 OpenAI stub)** — closed with linking comment to
  PR #2805 + openai-harness.md.

The 4-PR closure resolves the cycle-40 "PR stays open as prior-attempt
reference" deferral. Both cycle-26 stubs are now superseded; both cycle-41
deliverables are integrated.

### Workflow observation: branch protection prevents merge

Attempted `gh pr merge 2804 --squash --delete-branch` — failed with "base
branch policy prohibits the merge." Tried with `--auto`, failed because
auto-merge is not enabled for this repository. Tried without auto, failed
on base-branch-policy. The orchestrator does not have admin authority
(--admin flag).

This validates the cycle-26 pattern of close-without-merging for research-
only deliverables: GitHub branch protection enforces a workflow Eva controls,
not a workflow the orchestrator controls. Research deliverables are
absorbed-and-closed; only Eva merges (or Copilot implementation PRs that
pass review and CI).

For v2 design: the close-without-merging vs merge-via-Eva distinction is
load-bearing for the dispatch primitive. Research dispatches expect
absorption+closure; implementation dispatches expect review+merge. The two
modes have different lifecycle semantics.

### What surprised me

The substantive findings from the Cognition deliverable. I had expected the
deeper read to *strengthen* existing framework anchors with more primary-source
detail. Instead, the April 22, 2026 walkback of "Don't Build Multi-Agents"
substantially WEAKENED the Axis 1 anchor. The framework had been carrying a
correct-as-of-cycle-26 framing; the deeper read surfaced 9 days of accumulated
freshness debt.

This is a meta-observation about Phase 1 research timing: framework anchors
are correct as of the date the supporting evidence was published, not
indefinitely. Cognition's April 22, 2026 post was published 9 days before
cycle 41; cycle 26 (a week earlier) couldn't have known. The framework's
freshness depends on continued research, not just initial dispatch.

For v2 design: a candidate's reference research should have an explicit
"refresh check" cadence. The cycle-26 → cycle-41 gap (5 weeks) is enough for
position evolutions in the surveyed systems to invalidate framework anchors.

Also surprised by how much depth-of-claim variation appeared between Cognition
(direct primary-source access — substantial framework corrections) and OpenAI
(Cloudflare-blocked despite firewall expansion — secondary synthesis only for
companion post; mirror text matched cycle-26's). The asymmetry is interesting
v2-design input: firewall allowlist is necessary but not sufficient for
content access. Some sources are blocked at content-delivery layer
(Cloudflare JS rendering), not network layer.

### What I couldn't figure out

Whether the cycle-41 v1.5 corrections will themselves accumulate freshness
debt over the redesign timeline. The Phase 1 research was conducted across
cycles 13-30; framework v1.0 was synthesized cycle 35; cycle 41 made
substantial corrections from a 9-day-old Cognition post. By the time Phase 2
candidate generation and prototype completion lands (many cycles out),
several more position evolutions may have occurred in surveyed systems.

Mitigation patterns to consider for v2 design:
- Phase 2 candidate generation should pin its claims to the exact post date
  of the supporting evidence (already partially done with "(June 2025)"
  vs "(Apr 2026)" qualifications).
- A scheduled re-read primitive could be useful — periodic refresh dispatches
  for the load-bearing systems before Phase 2 freezes claims.

Also couldn't resolve whether the closure-without-merging pattern is the
RIGHT pattern in general, or just the pragmatic fallback when branch
protection blocks merging. The cycle-26 precedent and the cycle-41 outcome
match, but the "correct" answer depends on whether master should accumulate
verbatim deliverable artifacts or stay focused on synthesized content.
For now: per-system files + per-finding evaluations carry the substance;
verbatim deliverables remain accessible in closed-PR commit history.

### Pre-commit checklist for cycle 42's cold-reader

Three questions:

- **(a)** Did v1.5's Axis 1 + Axis 3 + Axis 9 corrections introduce any
  internal inconsistencies with v1.0-v1.4 claims that didn't get updated?
  Specific check: do the Maps-to lines on Axes 1/3/9 (added in v1.3) still
  match the corrected row text? Do other axes' "Considered-and-folded" or
  "Cross-axis dependency" sections reference Cognition's old position?
- **(b)** Was the cycle-41 per-finding evaluation correctly calibrated, or
  did I over-accept findings? Specific check: re-read findings C7 (microVM)
  and O7 (companion post secondary synthesis) — both qualified, but did
  the qualifications adequately capture the non-transfer / secondary-source
  caveats?
- **(c)** Should Q(b) (Axis 12 hybrid annotation) and/or Q(c) (openclaw
  deeper-read dispatch) actually be applied / dispatched in cycle 42, or
  do they remain genuinely deferrable? Specific check: imagine doing Phase
  2 candidate generation with v1.5 as-is — does the hybrid annotation
  prejudice candidates? Does the openclaw shallow-evidence concern affect
  Phase 2 work?

### Cycle 42 plan (provisional)

1. **Substantive focal:** cross-cycle cold-reader on v1.5 + cycle-41 work
   (3 Qs above). Verify v1.5 corrections are internally consistent.
2. **Substantive parallel:** decision on cycle-42+ flags — apply v1.5+
   Axis 12 hybrid annotation softening if Q(c) finds it warranted; dispatch
   openclaw deeper read if Q(c) finds the flag still load-bearing.
3. **Bounded mechanical:** TBD per cold-reader. Possible: housekeeping
   continued sweep; possibly `tools/redispatch` Rust tool design draft
   (v1-no-redispatch-primitive observation now load-bearing across 4 cycles
   with empirically established close-and-recreate as working primitive).

If post-retrospective checkpoint approval arrives between cycles, Phase 2
candidate generation can begin in parallel.

### What this cycle achieved

This was the highest-output cycle in the redesign sequence so far:
- 21 findings evaluated (13 + 8) with verdicts
- 3 framework axes corrected (Axes 1, 3, 9)
- 2 per-system files promoted from stub to deeper-read status
- 4 PRs closed with linking comments
- 1 substantive commit (1018 insertions, 161 deletions, 4 files changed)

The substantive output was driven by the cycle-40 close-and-recreate
re-dispatch fired ~1 minute after issue creation. The empirical-mechanism
chain across cycles 38-40 (comment dead-end → toggle dead-end → close-and-
recreate working) culminated in cycle 41 absorbing the resulting deliverables.
Three cycles of dispatch-mechanism investigation enabled one cycle of
substantive integration.

## Cycle 42 (10:28 UTC, model: claude-opus-4-7, run: redesign-cycle-42, issue #2807)

Sixth cycle of 2026-05-01. Cycle 41 left a clear three-workstream plan:
cold-reader on v1.5 + cycle-41 work (3 Qs), apply v1.5+ from Q(c) decisions
if warranted, bounded mechanical TBD. All three executed with substantive
results.

Notes file: `_notes/cycle-42-cold-reader-and-v1.6-application.md`.

### Cold-reader: 1 BORDERLINE-FAIL + 2 PASS — v1.5 had unupdated cross-axis references

- **Q(a) BORDERLINE-FAIL** — found two stale Cognition references after v1.5
  Axis 1 update that were not propagated:
  - **Axis 7 row at line 325**: "Single-pattern (one shape only) | Cognition
    (single-threaded linear)" — Cognition Apr 2026 ships THREE distinct
    orchestration patterns (Managed Devins coordinator + parallel children,
    Devin Review clean-context, Smart Friend frontier consultation). At
    system level, Cognition Apr 2026 is unambiguously multi-pattern
    coexisting, not single-pattern. The framework's v1.5 update propagated
    the Apr 2026 walkback to Axis 1 (decomposition) and Axis 3 (memory) but
    missed Axis 7 (orchestration topology).
  - **Cross-axis dep at line 595**: "Goal-driven pairs naturally with
    single-threaded long-running (Cognition); ..." — the "(Cognition)"
    parenthetical example is now wrong since Cognition is in small-fixed-team
    row.

- **Q(b) PASS** — C7 (microVM) and O7 (companion post Bolin) qualifications
  adequately propagated to per-system files. Verified `cognition-devin.md`
  line 172 documents non-transfer caveat for microVM; verified
  `openai-harness.md` lines 26-27, 38-39, 168, 282-285 carry secondary-source
  caveat for Bolin companion post in multiple places. No over-acceptance.

- **Q(c) PASS** — both deferred flags warrant cycle-42 action. Axis 12 hybrid
  annotation softening: the "Most likely v2 candidate position" annotation in
  a comparative table reads as forecast/recommendation that could prejudice
  Phase 2 candidate generation toward hybrid by default. The narrative below
  the table already says "Hybrid is the path of least design-cost since
  different channels naturally have different polling frequencies" — same
  point properly placed. Soften applied. openclaw deeper-read dispatch:
  Phase 2 candidate generation will need verification of "async-with-server"
  vs "cron+catchup" distinctness; cycle-40 close-and-recreate primitive is
  empirically established (~1-min trigger latency); openclaw is the only
  surveyed system with stub-status per-system file. Dispatch executed.

The convergence pattern (3 consecutive 3/3 PASS cycles 38-40, 1 PASS + 2
BORDERLINE-PASS cycle 41) softened further to 1 BORDERLINE-FAIL + 2 PASS in
cycle 42. The Q(a) finding is real — cycle-41's v1.5 application missed
two cross-axis update propagations.

### v1.6 application

Five line-targeted edits to `docs/redesign/2-design-framework.md`:

1. Status header v1.5 → v1.6
2. Iteration history table — v1.6 row added documenting cycle-42 cold-reader
   findings and three corrections
3. Axis 7 row — Cognition moved from "Single-pattern (one shape only)" to
   "Multi-pattern coexisting" row (Apr 2026 ships 3 patterns); also added to
   "Lead-worker hierarchy" row (Managed Devins is the specific shape);
   single-pattern row now names v1's rigid checklist as closest extant
   example with v1 anti-pattern qualifier
4. Cross-axis dep map (Constraint 8 × Axis 1) — stale "(Cognition)"
   parenthetical for single-threaded clause removed; Cognition's Managed
   Devins coordinator pattern moved to small-fixed-team clause as the
   surveyed instance of explicit goal-coordination primitive
5. Axis 12 hybrid row annotation — "Most likely v2 candidate position"
   replaced with "Lowest per-channel design cost — different channels have
   different frequencies"

Commit `861b44c0` (413 insertions, 7 deletions, 2 files).

### openclaw deeper-read dispatch

Issue #2808 created via close-and-recreate primitive (gh issue create with
agent-task + research-only labels, copilot-swe-agent[bot] assignee).
PR #2809 fired by Copilot ~4 minutes after issue creation — close-and-recreate
primitive verified working for the third time (cycle-40 + cycle-41 + cycle-42).

Body composition followed the cycle-40 cognition / openai pattern: cycle-42
banner + framework v1.6 context + cycle-40 v2-design observation + 7-lens
coverage + specific architecture questions for Gateway/Channels/Plugins/Memory
plugin slot + "no documented-claim caveats for primary-source-grounded
claims" + "open as a separate draft PR." Deliverable target:
`docs/redesign/_notes/cycle-42-openclaw-deeper-read.md`.

Cycle-43+ will verify deliverable lands and per-finding evaluate if substantive.

### Housekeeping: 3 closures

- **#2802** (Cognition deeper-read dispatch issue, cycle 40) — closed with
  linking comment naming per-system file (cognition-devin.md), framework
  v1.5/v1.6, and cycle-41 per-finding evaluation as integration targets.
- **#2803** (OpenAI deeper-read dispatch issue, cycle 40) — closed with
  same pattern naming openai-harness.md and cycle-41 evaluation.
- **#2627** (v1-era diagnostic from cycle 520 — record-dispatch post-dispatch
  delta sync) — closed per "v1 superseded by redesign mode" pattern (same
  precedent as cycle-40 #2272 closure).

Conservative deferral on v1-era #2738/#2739 (cycle 545 review) and
#2736/#2737 (write-entry --auto-blockers) — defer to cycle-43+ for deeper
per-item read before closure. Cycle-42's substantive workload (cold-reader
+ v1.6 + dispatch + 3 housekeeping) was already substantial; aggressive
sweep would have been marginal benefit.

### Same-cycle review (cycle-42 work)

5 questions, 4 PASS + 1 BORDERLINE-PASS:

- **Q1 PASS** — Cognition Apr 2026 dual listing in Axis 7 (Multi-pattern
  coexisting + Lead-worker hierarchy) is consistent with AutoGen precedent
  (also dual-listed: Multi-pattern coexisting + Lead-worker hierarchy via
  Magentic-One). Not redundant.
- **Q2 PASS** — openclaw dispatch body composition is well-formed. All
  cycle-40-pattern elements present (banner, context, deliverable path,
  constraints, original framing, lenses, axis anchoring, format/tone,
  what-NOT-to-do).
- **Q3 PASS** — Q(a) BORDERLINE-FAIL severity calibration. Axis 7 row
  staleness is real accuracy issue but not structural-axis-space problem
  (the position space is correct; only the supporting evidence label is
  wrong). Cross-axis dep is genuinely minor wording fix. BORDERLINE-FAIL
  is right level.
- **Q4 BORDERLINE-PASS** — housekeeping discipline. Could have closed more
  (v1-era #2738/#2739, #2736/#2737), but cycle-42 substantive workload
  justified deferral. Conservative is defensible.
- **Q5 PASS** — cycle-43 pre-commit checklist appropriately scoped: Q(a)
  v1.6 internal consistency, Q(b) openclaw dispatch outcome, Q(c) Axis 12
  softening second-order bias check.

### What surprised me

The Q(a) finding. I expected v1.5 to be internally consistent because
cycle-41's substantive output was carefully scoped (Axes 1, 3, 9 corrected;
deferred flags called out explicitly). Finding TWO unupdated locations was
a higher Q(a) hit-rate than cycles 36-37's cold-reader sequence on more
recent corrections.

The pattern observation: **cross-axis update propagation is a recurring
failure mode in framework iteration.** Cycle 35→36 missed propagating Axis 2
plans-as-artifacts removal to F-pattern table (cycle-36 Q[a]). Cycle 36→37
over-mapped F11 to Axis 2 (correction-of-correction). Cycle 41→42 missed
propagating Axis 1 update to Axis 7 row. Each iteration risks a similar
miss.

The v2-design observation: **a candidate's framework-iteration discipline
should include a structural cross-axis-impact check** when modifying any
axis. This could be a Rust tool
(`tools/framework-cross-axis-impact-check`) that, given a list of axes
modified, reports all places those axes are referenced elsewhere in the
framework file (rows, cross-axis deps, F-pattern table, considered-and-
folded subsections). Bounded-mechanical work; deferrable but worth
journaling. Phase 2 candidates with mechanical-enforcement (Axis 8) could
implement this as CI.

The Q(b) PASS surprised me less — cycle-41's evaluation discipline was
careful about qualifications; the propagation to per-system files was a
separate step that landed cleanly.

The Q(c) decision surprised me by being unambiguous. Both deferred flags
warrant cycle-42 action. I had expected one to be deferable while the other
was load-bearing. Looking carefully, the hybrid annotation softening is
genuinely high-leverage (preventing prejudice in Phase 2 candidate
generation that hasn't started yet) and the openclaw dispatch is
symmetric-pattern low-friction.

### What I couldn't figure out

Whether the v1.6 corrections themselves introduce NEW inconsistencies that
a cycle-43 cold-reader would catch. The "correction-of-correction" pattern
is still active. Adding Cognition Apr 2026 to BOTH "Multi-pattern
coexisting" AND "Lead-worker hierarchy" rows is technically correct
(matches AutoGen precedent) but the dual listing might be flagged as
confusing on fresh read.

Mitigation: cycle-43 cold-reader Q(a) walks the v1.6 changes specifically.

Also: whether the openclaw deeper read will surface architecturally
distinct evidence for "async-with-server" or refute the cycle-40
hypothesis. If the deeper read finds openclaw's Gateway is per-event
request-response (not streaming event-ingestion), the cycle-40 v2-design
observation about three reconciliation patterns collapses to two, and Axis
12 table doesn't need a fourth row. Empirical-verification cycle-43+.

Also: whether the v1-era housekeeping deferral (#2738/#2739, #2736/#2737)
is correct conservatism or unjustified queue-length tolerance. The cycle-40
precedent supports closing v1-era items; cycle-42 deferred them. Cycle-43+
can re-evaluate.

### Pre-commit checklist for cycle 43's cold-reader

Three questions:

- **(a)** Did v1.6's three corrections (Axis 7, cross-axis dep, Axis 12)
  introduce NEW inconsistencies elsewhere in the framework? Specific check:
  is Cognition Apr 2026 listed in "Multi-pattern coexisting" AND "Lead-worker
  hierarchy" rows redundant? Walk the Cognition Apr 2026 patterns (Managed
  Devins, Devin Review, Smart Friend) and see if all are accounted for in
  the right rows. Also: does the v1's rigid checklist as "closest extant
  example" of single-pattern in Axis 7 row create any cross-reference issue
  with the v1 anti-pattern framing in CORE-DESIGN-PRINCIPLE elsewhere?
- **(b)** Did the openclaw deeper-read dispatch (PR #2809) land cleanly?
  Verify deliverable lands at the cycle-42-prefixed file path. If yes:
  per-finding evaluation candidate (cycle-7/12/31/41 pattern). If no:
  investigate trigger pattern (close-and-recreate worked cycle-40 + cycle-42
  start; check for new firewall block on github.com/openclaw URLs or
  Cloudflare-style content-delivery block).
- **(c)** Did the v1.6 Axis 12 hybrid annotation softening adequately
  address the prejudice concern, or did it introduce a different bias?
  Specific check: imagine doing Phase 2 candidate generation with v1.6 —
  does any of the FOUR positions (no reconciliation / active polling /
  event-driven / hybrid) appear to carry implicit guidance? Are the labels
  balanced or do some positions read as "preferred" via wording asymmetry?

### Cycle 43 plan (provisional)

1. **Substantive focal:** cross-cycle cold-reader on v1.6 + cycle-42 work
   (3 Qs above). Verify v1.6 corrections didn't introduce new
   inconsistencies.
2. **Substantive parallel:** openclaw deeper-read deliverable per-finding
   evaluation if landed; trigger investigation if not landed.
3. **Bounded mechanical:** TBD per cold-reader. Possibilities:
   - housekeeping deeper sweep (v1-era #2738/#2739, #2736/#2737, conservatively
     deferred this cycle)
   - `tools/framework-cross-axis-impact-check` Rust tool design draft
     (cross-axis update propagation as recurring failure mode)
   - `tools/redispatch` Rust tool design draft (close-and-recreate now
     verified across 3 cycles as the empirical primitive)

If post-retrospective checkpoint approval arrives between cycles, Phase 2
candidate generation can begin in parallel.

### What this cycle achieved

Cycle 42 is a "structural correction" cycle following cycle-41's "substantive
addition" cycle. The pattern: cycle N adds substance from external
deliverables; cycle N+1's cold-reader finds cross-axis update propagation
gaps from cycle N; cycle N+1 fixes the gaps and dispatches the next
deliverable. Three deliverables in this sequence so far (Cognition + OpenAI
integrated cycle 41; openclaw dispatched cycle 42, in flight as PR #2809).

Output:
- 3 framework corrections (Axis 7 row, cross-axis dep, Axis 12 annotation)
- 1 deeper-read dispatch executed (#2808 → PR #2809 fired ~4 min)
- 0 over-acceptance findings on cycle-41 evaluation (Q(b) PASS)
- 3 housekeeping closures (cycle-40 dispatch issues + 1 v1-era diagnostic)
- 1 v2-design observation (cross-axis update propagation as recurring
  failure mode — Axis 8 mechanical-enforcement candidate for v2)

Commit count: 1 substantive (`861b44c0`, framework v1.6 + notes file).
Total open issues: 18 → 15 (3 closures, 1 new dispatch). Total open PRs:
4 → 5 (1 new draft from openclaw dispatch).

The cross-axis update propagation observation is the most interesting
lesson — it suggests v2 should have mechanical enforcement on
framework-modification impact (Axis 8 territory). A Rust tool
(`tools/framework-cross-axis-impact-check`) could close this loop as a
v1.7+ tooling addition or as a v2 candidate's mechanical-enforcement
example.

## Cycle 43 (20:15 UTC, model: claude-opus-4-7, run: redesign-cycle-43, issue #2810)

Seventh cycle of 2026-05-01. Cycle 42 left a clear three-workstream plan:
cold-reader on v1.6 + cycle-42 work (3 Qs), per-finding evaluation of
openclaw deeper-read PR #2809 if landed (it did, ~4 min after #2808),
bounded-mechanical TBD. All three executed with substantive results.

Notes file: `_notes/cycle-43-openclaw-per-finding-evaluation.md` (the
per-finding evaluation artifact).

### Cold-reader: 1 PASS + 2 BORDERLINE-PASS — convergence pattern continues to soften

- **Q(a) PASS** — v1.6 Axis 7 corrections + cross-axis dep cleanup +
  Axis 12 hybrid annotation softening did NOT introduce new
  inconsistencies. Three sub-checks: (1) Cognition Apr 2026 dual listing
  in "Multi-pattern coexisting" + "Lead-worker hierarchy" rows is NOT
  redundant (mirrors AutoGen precedent — both rows describe orthogonal
  properties); (2) all three Cognition Apr 2026 patterns (Managed Devins,
  Devin Review, Smart Friend) accounted for in correct rows — Managed
  Devins → lead-worker (correct); Devin Review and Smart Friend → only
  multi-pattern coexisting (NOT lead-worker — they're structurally
  distinct: clean-context reviewer and frontier consultation, not
  worker-under-coordinator); (3) v1's rigid checklist as "closest extant
  example" of single-pattern in Axis 7 row creates only minor wording
  conflation between "single-pattern position" and "rigid-checklist
  v1 instance" — flag for cycle-44+ but not load-bearing.

- **Q(b) BORDERLINE-PASS** — per-finding evaluation of openclaw
  deliverable accepted 21/21 findings (16 ACCEPT + 4 ACCEPT WITH
  QUALIFICATION + 1 ACCEPT AS REVISION-OF-PRIOR-CLAIM + 0 REJECT). The
  BORDERLINE-PASS is on OC1 (Axis 12 cycle-40 observation refinement) —
  the cycle-40 v2-design observation about three reconciliation patterns
  is REFINED (not retired). openclaw's pattern is implementation-detail
  within Axis 12's existing **Event-driven** position, not a new axis
  position. The cross-system observation refines to TWO axis-distinct
  patterns (sync HITL vs async) with implementation-nuance within async
  (cron+catchup, event-driven with persistent connections,
  webhook-on-event). BORDERLINE-PASS because the framing decision (refine
  vs retire vs new-row) was non-obvious; cycle-44 cold-reader Q(a) is
  the appropriate verification.

- **Q(c) BORDERLINE-PASS** — Axis 12 four-position cost-framing was
  asymmetric ("High-cost" on active polling vs "Lowest per-channel
  design cost" on hybrid). Cycle-43 v1.7 application balanced the
  labels: active polling now reads "Uniform mechanism (one pattern per
  channel); per-channel implementation overhead"; hybrid now reads
  "Mixed mechanism; design overhead spread per-channel-class rather
  than per-channel; suited to workloads where different channels have
  different natural frequencies." Each position now has a symmetrical
  cost description without comparative claims. BORDERLINE-PASS rather
  than PASS because some asymmetry is unavoidable in summary annotations
  (e.g., "No reconciliation" remains explicit anti-pattern, justified by
  constraint 8 territory) and the wording could be further refined in
  v1.8+ if Phase 2 candidate generation surfaces prejudice.

The convergence pattern: 3 consecutive 3/3 PASS (cycles 38-40) → 1 PASS
+ 2 BORDERLINE-PASS (cycle 41) → 1 BORDERLINE-FAIL + 2 PASS (cycle 42)
→ 1 PASS + 2 BORDERLINE-PASS (cycle 43). The trend is stable at "minor
findings, no structural failures" with the substantive workstream now
driven by external deliverables (cycle-41 Cognition+OpenAI; cycle-43
openclaw) rather than cold-reader-on-own-notes.

### v1.7 framework application

Six line-targeted edits to `docs/redesign/2-design-framework.md`:

1. Status header v1.6 → v1.7
2. Iteration history v1.7 row added (covers all changes plus cycle-40
   observation refinement)
3. Axis 2 row "File-per-component" — openclaw added with `~/.openclaw/
   agents/<agentId>/` per-agent state isolation cite + `global-state.ts`
   caveat (4-system support, was 3)
4. Axis 3 row "Singleton plugin slot" — openclaw note refined to clarify
   the singleton-slot scope is the storage/retrieval layer; full memory
   architecture is layered (Markdown files + SQLite + active-memory
   sub-agent + dreaming consolidation)
5. Axis 9 row "Runtime ceiling" — openclaw added with 48h-effectively-
   unbounded qualifier; stuck-session watchdog
   (`diagnostics.stuckSessionWarnMs`) noted as more interesting primitive
6. Axis 12 four-position table cost-framing balanced (Q(c) refinement);
   openclaw added as instance under Event-driven position

Commit `e88fce91` (1154 insertions, 70 deletions, 3 files changed).

### Per-system file promotion

`openclaw.md` rewritten from cycle-14 stub (71 lines) to deeper-read
status (489 lines). Following the cycle-41 cognition-devin / openai-
harness pattern:

- Status header: "stub" → "deeper read landed (cycle 43)"
- Replace cycle-14 dispatch references with PR #2809 deliverable
  references
- Expand pattern observations to integrate substantive findings (the 12
  missed-or-understated items from OC21)
- Add Phase 2 framework anchoring summaries (per-axis evidence: Axis 2,
  3, 6, 7 (NON-FIT explained), 9, 12, 13; Constraints 3, 5)
- Refine anchoring caveats: single-user vs autonomous-cron, TypeScript
  vs Rust, local-machine vs ephemeral-runner, star-count anomaly +
  5-month repo age (architecture real, consequence-validation limited)

Three new framework-correction findings absorbed:
- **Axis 7 NON-FIT explained**: openclaw's multi-agent system is flat
  isolated-persona routing, NOT orchestration topology. Each agent runs
  independently; agent-to-agent messaging disabled by default.
- **Axis 12 Q(c) resolved**: Gateway is per-event request-response (not
  streaming event-ingestion). cycle-40 "three patterns" observation
  refined to two axis-distinct patterns + implementation-nuance.
- **Reserved core admin namespaces**: `config.*`, `exec.approvals.*`,
  `wizard.*`, `update.*` always stay `operator.admin` even if a plugin
  tries to override — useful pattern for v2 tool/permission boundaries.

### PR #2809 + Issue #2808 closures

PR #2809 closed with linking comment naming integration targets
(per-finding evaluation file, framework v1.7 commit `e88fce91`,
`openclaw.md` deeper-read promotion). Issue #2808 closed with linking
comment naming the deliverable PR + integration targets + cycle-41 Q(c)
flag resolution.

The close-and-recreate primitive is now confirmed across THREE
independent dispatches (cycle-40 cognition: ~1 min; cycle-40 openai:
~1 min; cycle-42 openclaw: ~4 min). Three confirmations make this an
empirically-stable v2-design input, not just a working-pattern observation.

### Housekeeping: 3 closures (deeper sweep on v1-era backlog)

Cycle-42 deferred v1-era #2738/#2739, #2736/#2737, #2729 to cycle-43+
for deeper per-item read before closure. Cycle-43 deeper sweep
evaluated each:

- **#2738** (Cycle 545 end-of-cycle review) — closed per "v1 superseded
  by redesign mode" pattern (same as cycle-40 #2272 / cycle-42 #2627).
- **#2736** (`--auto-blockers` flag on `write-entry`) — closed per "v1
  tool fix on frozen production tool" pattern. The v1 F2 concern is
  structurally addressed in framework Axis 12.
- **#2729** (Replace raw `gh api` dispatch with `dispatch-task` in v1
  prompt) — closed per "v1 prompt fix on frozen production prompt"
  pattern. v2 will replace the prompt entirely; cycle-40+ verified
  close-and-recreate is the v2-relevant dispatch primitive.

#2739 not present in the open list (likely closed earlier). #2737 not
present either. Conservative skip on input-from-eva items (#2039) and
pre-redesign discussions (#699, #808, #809) — Eva closes
input-from-eva; ambiguous-status items stay open per redesign prompt
housekeeping discipline.

After cycle-43 sweep: 14 → 11 open issues (3 closures, plus #2808
closure as part of dispatch absorption = 4 issue closures total cycle).

### Same-cycle review (5 questions, 4 PASS + 1 BORDERLINE-PASS)

- **Q1 BORDERLINE-PASS** — v1.7 cross-axis update propagation. Adding
  openclaw to Axis 2 File-per-component does not directly affect any
  cross-axis dep references that name systems by name (the deps describe
  positions/relationships, not specific systems). However, the dep "Axis
  2 × Axis 3: file-per-component → memory-as-component-file" is slightly
  off for openclaw — its memory architecture is singleton plugin slot
  managing per-component storage, which IS compatible but the
  "memory-as-component-file" phrasing doesn't capture the
  plugin-managing-per-component-data structure. Minor refinement
  opportunity for cycle-44+; not load-bearing for current Phase 2 input.
- **Q2 PASS** — Per-finding evaluation accuracy. OC1 (cycle-40
  observation refinement) re-checked against cycle-40 notes: the
  cross-system observation about TWO axis-distinct patterns + implementation-
  nuance within async is the correct framing. openclaw is async (broader
  category) with implementation-detail of "event-driven with persistent
  connections" — fits within Axis 12 Event-driven position.
- **Q3 PASS** — Q(c) cost-framing balance. Each position now has
  symmetrical cost description without comparative claims (overhead
  per-channel vs per-channel-class vs trigger infrastructure). The "No
  reconciliation" anti-pattern annotation remains, justified by
  constraint 8 territory.
- **Q4 PASS** — `openclaw.md` rewrite consistency with cycle-41 pattern.
  Length 489 lines (vs cognition-devin.md ~430 lines, openai-harness.md
  similar). Same section structure: Status, Sources, Load-bearing
  thesis, Architecture sections, Anchoring caveats, Phase 2 framework
  anchoring, Patterns observed. Equivalent depth.
- **Q5 PASS** — Cycle-44 pre-commit checklist appropriately scoped with
  3 questions hitting different lenses (v1.7 internal consistency on
  the new openclaw additions, openclaw deeper-read additional
  exploration if useful, bounded-mechanical from cycle-43 deferred
  items including the cross-axis deps refinement and `tools/framework-
  cross-axis-impact-check` Rust tool design draft).

### What surprised me

The **OC1 finding** (cycle-40 v2-design observation refinement, NOT
retirement). I had expected the openclaw deeper read to either confirm
the cycle-40 "three patterns" claim or refute it cleanly; instead it
found a refinement: openclaw's pattern fits within Axis 12's existing
Event-driven position with implementation-specific details. This is the
"third option" framing-decision that doesn't surface in pre-commit-
checklist Q(c) framing because it requires the deeper read evidence to
articulate. The cross-system observation about TWO axis-distinct
patterns plus implementation-nuance within async is more accurate than
either "three patterns" or "openclaw doesn't add a pattern."

Also surprised by **OC5** (Axis 7 NON-FIT explained). I expected
openclaw to anchor SOMETHING on Axis 7 because it has multi-agent
routing. The deeper read shows openclaw's multi-agent is flat
isolated-persona routing — explicitly NOT orchestration topology in
the Axis 7 sense. The framework correctly does NOT have openclaw in any
Axis 7 row; this is the right outcome but for a non-obvious reason
(multi-agent routing ≠ multi-agent topology when there's no
coordination between agents).

The **Q(c) cost-framing imbalance** that I caught was higher-leverage
than expected. cycle-42 thought it had addressed the prejudice concern
by replacing "Most likely v2 candidate position" forecast with
"Lowest per-channel design cost." The new framing was less
forward-looking but still asymmetric — only hybrid got the explicit
cost label. cycle-43's balance pass treats every position with the
same kind of cost description (per-channel overhead vs per-channel-
class spread vs trigger infrastructure). This is more defensive against
prejudice than I had thought going in.

### What I couldn't figure out

Whether the **`src/global-state.ts`** observation (OC19) actually
contradicts openclaw's Axis 2 File-per-component placement, or if it's
an orthogonal Gateway-level concern (process-globals separate from
per-agent state). The deeper read flagged the file's existence as
implementation-backed but didn't read its contents. Cycle-44+ could
dispatch a finer Copilot research-only read targeted at
`src/global-state.ts` + `src/globals.ts` if the answer is load-bearing
for Phase 2. Probably not load-bearing — the per-agent state isolation
pattern is what matters for the framework, and Gateway-level globals
are an implementation detail that doesn't change the high-level
position. Defer.

Also: whether **Axis 7's "single-pattern" row wording** should be
sharpened to distinguish "single-pattern position (any one shape)" vs
"v1's specific rigid-checklist instance" (cycle-43 Q(a) sub-check 3
flagged this as borderline). The current wording reads as "single-
pattern is the v1 anti-pattern" which conflates the design-space
position with the v1 implementation. cycle-44+ could refine this to:
"Single-pattern (one shape only) — no surveyed system currently ships
this; v1's rigid checklist-driven sequence is the closest extant
example, and is the v1 anti-pattern via CORE-DESIGN-PRINCIPLE
violation." Minor wording fix; not load-bearing.

Also: **whether close-and-recreate's empirical-mechanism stability**
will hold across more dispatches. Three confirmations in three
independent attempts (cycles 40, 41, 42) is suggestive but not proof.
The trigger latency varied (1 min, 1 min, 4 min); the next dispatch
could be slower or fail for reasons not yet observed. Should we treat
the primitive as fully verified or keep the empirical-watching going?
Lean toward "fully verified" — the underlying mechanism (issue
creation triggers Copilot via GitHub-side webhook) is well-understood
from `tools/dispatch-task/src/main.rs` direct read. Latency variation
is normal network/scheduling. No further verification needed unless a
dispatch fails to fire.

### Pre-commit checklist for cycle 44's cold-reader

Three questions:

- **(a)** Did v1.7's three Axis additions (Axis 2 openclaw +
  global-state.ts caveat, Axis 3 layered architecture refinement, Axis
  9 stuck-session watchdog) introduce any internal inconsistencies or
  cross-axis-propagation gaps? Specific check: re-walk the cross-axis
  dep map; the Axis 2 × Axis 3 entry says "file-per-component → memory-
  as-component-file" — does openclaw's plugin-managing-per-component-
  storage pattern fit cleanly, or does the dep description need
  refinement? Also: does the Axis 9 row's stuck-session watchdog framing
  pair with anything in the F-pattern table (F8 abandonment cascades)?
- **(b)** Was the cycle-43 per-finding evaluation correctly calibrated,
  or did I over-accept findings? Specific check: re-read OC9 (session
  lifecycle as transferable freshness mechanism) and OC13 (delegate
  architecture tier escalation) — both ACCEPT WITH QUALIFICATION but
  did the qualifications adequately scope what transfers vs what's
  openclaw-specific? Specific concern: OC9 suggests v2 candidates could
  use "reset state freshness daily" as a primitive — is this concrete
  enough to be useful or is it underspecified?
- **(c)** Did the cycle-43 Q(c) cost-framing balance go far enough, or
  is there still latent prejudice? Specific check: imagine generating
  Phase 2 candidates with v1.7. For each of the four Axis 12 positions,
  does the row's annotation give equal weight to "this is a defensible
  choice"? Stress test: write a one-paragraph rationale FOR each
  position and see if any is harder to defend than the others. If active
  polling is hardest, the wording may still subtly under-weight it.

### Cycle 44 plan (provisional)

1. **Substantive focal:** cross-cycle cold-reader on framework v1.7 +
   cycle-43 work (3 Qs above). Verify v1.7 additions are internally
   consistent and the per-finding evaluation didn't over-accept.
2. **Substantive parallel:** TBD per cold-reader. Possibilities if cold-
   reader finds substantive issues: v1.8 corrections to Axis 12
   cost-framing (further balance) or cross-axis dep refinement (Axis 2
   × Axis 3 phrasing for plugin-managed per-component storage); or if
   cold-reader passes, deeper investigation of `src/global-state.ts`
   contents via targeted Copilot research-only dispatch (low-priority,
   defer if other work surfaces).
3. **Bounded mechanical:** TBD. Possibilities:
   - `tools/framework-cross-axis-impact-check` Rust tool design draft
     — cycle-42's "cross-axis update propagation as recurring failure
     mode" v2-design observation has now been load-bearing across
     2 cycles (cycle-42 + cycle-43 cold-reader on v1.7). The tool
     could be drafted as a v2 candidate's mechanical-enforcement
     example.
   - `tools/redispatch` Rust tool design draft — close-and-recreate
     primitive empirically stable across 3 cycles (cycles 40/41/42).
     v1 has no native re-dispatch primitive (`tools/dispatch-task`
     creates new issues; no body-edit + close-old wrapper).
   - Housekeeping continued sweep on remaining v1-era items (input-
     from-eva #2039, pre-redesign #699/#808/#809) — conservative
     deferral remains correct unless cycle-44 finds them genuinely
     absorbed.

If post-retrospective checkpoint approval arrives between cycles, Phase
2 candidate generation can begin in parallel.

### What this cycle achieved

Cycle 43 is the second "substantive addition" cycle in the redesign
sequence (the first was cycle 41's Cognition+OpenAI integration). The
pattern: cycle N+1 absorbs deliverables from cycle N's dispatch +
applies framework corrections + promotes per-system file from stub.
Cycle 41 did this for Cognition + OpenAI; cycle 43 does this for
openclaw.

Output:
- 21 findings evaluated (vs cycle-41's 21) with 16 ACCEPT + 4
  ACCEPT WITH QUALIFICATION + 1 ACCEPT AS REVISION-OF-PRIOR-CLAIM
- 4 framework axes refined (Axis 2 + Axis 3 + Axis 9 + Axis 12 four-
  position cost-framing)
- 1 per-system file promoted from stub (cycle-14 → cycle-43 deeper-
  read)
- 4 issue/PR closures (#2808 dispatch + #2738/#2736/#2729 v1-era backlog
  sweep)
- 1 substantive commit (1154 insertions, 70 deletions, 3 files)
- 1 cycle-40 v2-design observation refined (NOT retired) — async-with-
  server label was over-extended; openclaw fits Axis 12 Event-driven
  position with implementation-specific details

The substantive output was driven by the cycle-42 close-and-recreate
re-dispatch firing ~4 minutes after issue creation. The empirical
chain across cycles 38-42 (comment dead-end → toggle dead-end →
close-and-recreate working at 1-min latency for cognition+openai →
working at 4-min latency for openclaw) is now stable. Three cycles of
mechanism investigation (38-40) enabled two cycles of substantive
integration (41 + 43).

The most interesting v2-design observation: **persistent connections vs
polling vs webhook is implementation-detail, not axis-position.** The
cycle-40 "three patterns" framing was almost right but over-extended
the cross-system observation. The corrected framing is TWO
axis-distinct patterns (sync HITL vs async) with implementation-nuance
within async. This is a cleaner Phase 2 input than the cycle-40
framing would have been.

## Cycle 44 (22:14 UTC, model: claude-opus-4-7, run: redesign-cycle-44, issue #2811)

Eighth cycle of 2026-05-01. Cycle 43 left a clear three-workstream
plan: cross-cycle cold-reader on framework v1.7 + cycle-43 work (3 Qs),
substantive parallel TBD per cold-reader, bounded mechanical TBD.
Cycle 44 executed all three with a substantive load-bearing finding,
v1.8 framework application, paired-PR housekeeping (3 closures), and
a v2 tool design draft.

Notes file: `_notes/cycle-44-cold-reader-and-v1.8-application.md`.

### Cold-reader: 1 BORDERLINE-FAIL + 2 PASS — cross-cycle review escalates a same-cycle "minor" flag to load-bearing

- **Q(a) BORDERLINE-FAIL** — Cross-axis dep map phrasing precision.
  The dep map says "file-per-component → memory-as-component-file;
  typed-channel-map → memory-as-channel; repo-as-state →
  memory-as-files-in-repo." But "memory-as-component-file" doesn't
  reference any specific Axis 3 named position. Walking through Axis
  3's actual positions, file-per-component naturally aligns with THREE
  distinct positions: singleton plugin slot WITH filesystem storage
  (openclaw); top-level architectural principle with filesystem memory
  (PAI); wiki+search with file-per-entry (oh-my-codex). The dep map's
  informal shorthand obscures these three. **Cycle-43 same-cycle Q1
  had flagged this as "minor refinement opportunity for cycle-44+;
  not load-bearing for current Phase 2 input." Cycle-44 cross-cycle
  review upgraded the verdict to load-bearing** because Phase 2
  candidate generation needs concrete alignment guidance, not informal
  shorthand. v1.8 fix rewrote the dep map to name specific Axis 3
  positions per Axis 2 position, with a "supportive not exclusive"
  clarification preserving non-filesystem alignment options.

- **Q(b) PASS** — Per-finding evaluation calibration adequate. OC9
  qualification appropriately hedged ("doesn't transfer cleanly,"
  "potentially transferable"); cycle-44 observation that "fresh-per-
  cron" already exists implicitly in cron-driven systems is a minor
  wording-improvement opportunity, not over-acceptance. OC13 verdict
  ACCEPT consistent with pattern transfer to existing COPILOT-
  DISPATCHES three-tier structure (research-only / feedback-only /
  implementation) — the openclaw delegate-architecture pattern
  validates v1's existing tier discipline rather than naming a gap.
  Minor categorization error in cycle-43 pre-commit checklist (OC13
  labeled "ACCEPT WITH QUALIFICATION" but actually ACCEPT in
  evaluation file) — narrative-record error, not framework error.

- **Q(c) PASS** — Axis 12 cost-framing balance adequate. Wrote
  one-paragraph defense of each of the four positions. "No
  reconciliation" defense fails on F2 (Eva-response detection
  fundamentally requires inbound mechanism) and F4 (refresh-timing
  decisions require reconciliation logic) — anti-pattern label
  correct. Active polling, Event-driven, Hybrid all defensible. Minor
  wording-symmetry opportunity: event-driven annotation said "requires
  inbound trigger infrastructure" (cost-only framing) while active
  polling said "Uniform mechanism" (positive framing) — asymmetric
  valence. v1.8 fix added "shared inbound infrastructure" framing for
  event-driven alongside the cost-framing.

### v1.8 framework application

Three changes to `docs/redesign/2-design-framework.md`:

1. **Status header v1.7 → v1.8** + iteration history v1.8 row added.

2. **Axis 2 × Axis 3 cross-axis dep map rewrite** (Q(a) load-bearing
   fix). Names specific Axis 3 positions per Axis 2 position with
   "supportive not exclusive" clarification.

3. **F8 mapping rationale extended** (Q(a) minor refinement). Mentions
   stuck-session watchdog as detection-and-recovery primitive
   alongside Bounded loops as prevention primitive. Covers v1.7-
   introduced openclaw `diagnostics.stuckSessionWarnMs` instance.

4. **Axis 12 event-driven annotation wording-symmetry** (Q(c) minor
   refinement). Replaced "requires inbound trigger infrastructure"
   with "shared inbound infrastructure (one webhook handles all
   subscribed channels — for public-repo orchestrator the Actions
   platform is already-paid; per-event handler configuration is a
   bounded one-time cost)." Symmetric framing with active polling's
   "uniform mechanism" upside.

Commit `cd0ab5de`. 566 insertions, 6 deletions across 2 files (the
framework + the cycle-44 notes file).

### Bounded mechanical (1): paired-PR housekeeping

Three draft PRs paired with cycle-43-closed v1-era issues all closed
with linking comments:

- **PR #2730** (Replace raw `gh api` task dispatch) — paired with
  closed #2729. v2 will replace the prompt entirely; close-and-
  recreate is the verified v2-relevant dispatch primitive.
- **PR #2737** (write-entry --auto-blockers) — paired with closed
  #2736. v1 F2 (Eva-response detection) concern is structurally
  addressed in framework Axis 12 (Reconciliation discipline).
- **PR #2739** (cycle 545 review) — paired with closed #2738. The
  cycle-N adversarial-review pattern is no longer the convention
  under redesign mode; cycle-N notes files + freeform journal entries
  replace it.

After cycle-44 sweep: 11 → 11 open issues (no issue closures) + 3 → 0
open PRs (3 closures). Open PR queue is now empty.

### Bounded mechanical (2): cross-axis-impact-check tool design draft

Drafted at `tools/v2/_drafts/cross-axis-impact-check-design.md`
(approximately 150 lines). The tool is a mechanical structural-
consistency lint for the redesign Phase 2 framework file —
addressing the cross-axis-update-propagation failure mode that has
fired three times in five cycles (42, 43 same-cycle, 44 cross-cycle).
This is exactly the CORE-DESIGN-PRINCIPLE pattern: extract repetitive
procedural work (cold-reader's cross-axis dep walking) into a tool
so the orchestrator can spend cycles on substantive design.

Draft covers: purpose, scope (in/out for v0; v1+ extensions),
inputs/outputs spec, 3-phase architecture sketch (parser/analyzer/
reporter), test surface (unit + integration; integration test should
detect cycle-42's Cognition-update propagation gap on framework v1.4
state), 4 open design questions for cycle 45+ to address, and
ordering rationale vs the second tool (`redispatch`) — chose
cross-axis-impact-check first because it addresses higher-frequency
orchestrator work (every framework iteration cycle vs every external
dispatch).

This is a design draft, NOT an implementation. Cycle 45+ may iterate
the design before any building happens.

### Same-cycle review (5 questions, 4 PASS + 1 BORDERLINE-PASS)

- **Q1 PASS** — v1.8 internal consistency. Re-walked the new
  Axis 2 × Axis 3 dep map: all five named positions (singleton plugin
  slot, top-level architectural principle, wiki+search, typed
  channels, repository-as-record) cross-reference correctly to Axis 3.
- **Q2 BORDERLINE-PASS** — F8 mapping rationale "or" phrasing. The
  "Bounded loops (prevention) or stuck-session watchdog (detection-
  and-recovery)" framing matches Axis 9's "loop-count ceilings" vs
  "Runtime ceiling" structural distinction; could be clarified in
  v1.9+ if Phase 2 candidates surface confusion.
- **Q3 PASS** — Axis 12 event-driven "already-paid" framing. GitHub
  Actions ARE already-paid for public repos under reasonable usage;
  configuration cost is bounded one-time work. Wording is acceptable.
- **Q4 PASS** — Cycle-43 OC13 mis-categorization is documented in
  cycle-44 Q(b) re-read; no framework or notes-file edit needed.
- **Q5 PASS** — Cycle-45 pre-commit checklist appropriately scoped
  with 3 questions hitting different lenses (consistency,
  prescriptiveness, future-work scope).

### What surprised me

The **Axis 2 × Axis 3 dep map imprecision was load-bearing rather
than minor.** Cycle-43 same-cycle Q1 had flagged it as "minor
refinement opportunity for cycle-44+; not load-bearing for current
Phase 2 input." Cycle-44 cross-cycle review walked through Axis 3's
actual named positions and found the dep map's "memory-as-component-
file" phrasing doesn't reference any specific Axis 3 position. This
is the third instance of the "same-cycle minor flag → cross-cycle
load-bearing finding" pattern (cycle 36 escalated cycle 35; cycle 37
escalated cycle 36; cycle 44 escalates cycle 43).

The convergence is asymptotic — each cycle's correction is itself a
claim that subsequent cross-cycle review can refine.

**OC13 already addresses an existing v1 pattern.** Cycle-43 had
categorized OC13 (delegate-architecture tier escalation) as a Phase 2
candidate consideration. Cycle-44 re-read recognized that the
redesign prompt's COPILOT-DISPATCHES section already implements the
three-tier permission pattern. So OC13 **validates existing v1
infrastructure** rather than naming a gap. The verdict ACCEPT was
correct but understated the load-bearing role.

The **cross-axis-impact-check tool draft fell into place naturally
once the cross-axis-update-propagation failure mode was named as
recurring.** Three instances over five cycles (cycle 42, cycle 43
same-cycle Q1, cycle 44 cross-cycle escalation) is empirical evidence,
not anecdote. A mechanical lint tool addresses this directly. Building
it before Phase 2 candidate generation would: reduce cold-reader load,
establish the v2 tool development pattern, and demonstrate CORE-
DESIGN-PRINCIPLE in the redesign work itself.

### What I couldn't figure out

Whether the cycle-43 → cycle-44 "minor flag → load-bearing finding"
pattern indicates a calibration issue with same-cycle review
(consistently under-reading severity?), or just normal cross-cycle
convergence (multi-cycle review surfaces what same-cycle misses).
Lean toward "normal cross-cycle convergence" because:
- The pattern fired correctly — same-cycle flagged the issue, cross-
  cycle escalated to fix
- The under-reading is informational (slightly lower priority), not
  blocking (the issue WAS flagged)
- Calibration issues would manifest as same-cycle MISSING issues
  entirely

If cycle 45 cross-cycle review escalates a cycle-44 "minor" flag, the
pattern recurs and might warrant deeper calibration review. For now:
process working as designed.

Also: whether to upgrade the cross-axis-impact-check from "v0 design
draft" to "v0 prototype implementation" in cycle 45. The draft has 4
open design questions (Q1-Q4) that benefit from one more design
iteration before building. Lean toward iterating the design first;
prototyping in cycle 45 only if design questions resolve quickly. The
"design before code" discipline mirrors Phase 2 candidate generation —
multiple candidates, evaluated, then one prototyped.

### Pre-commit checklist for cycle 45's cold-reader

Three questions:

- **(a)** v1.8 internal consistency check: re-walk the Axis 2 × Axis
  3 dep map rewrite. The new phrasing names "filesystem-based memory"
  positions but doesn't address what happens when a v2 candidate wants
  file-per-component (Axis 2) WITHOUT filesystem-based memory (e.g.,
  in-memory typed channels above filesystem state). Does v1.8 force a
  spurious alignment, or does the existing "supportive not exclusive"
  framing in the surrounding text adequately cover this?
- **(b)** v1.8 prescriptiveness check: by naming specific Axis 3
  positions in the dep map, does v1.8 unintentionally constrain Phase
  2 candidate creativity? Stress test: imagine a v2 candidate that
  takes file-per-component (Axis 2) + context-trace memory (Axis 3) —
  does the v1.8 dep map rule this out as "unnatural" when it's
  actually defensible?
- **(c)** F8 mapping rationale "or" phrasing: did v1.8's "Bounded
  loops (prevention) or stuck-session watchdog (detection-and-
  recovery)" framing accidentally imply these are alternatives when
  they could be complementary? Same-cycle Q2 flagged this as
  BORDERLINE-PASS; cycle-45 cross-cycle review can verify or
  escalate.

### Cycle 45 plan (provisional)

1. **Substantive focal:** cross-cycle cold-reader on v1.8 + cycle-44
   work (3 Qs above). Verify v1.8 changes don't over-prescribe Phase
   2 candidate space.
2. **Substantive parallel:** TBD per cold-reader. Possibilities:
   - v1.9 corrections if cold-reader finds substantive issues
   - Iterate cross-axis-impact-check design (4 open Q1-Q4)
   - `redispatch` tool design draft (second-priority tool)
   - Continued housekeeping sweep on remaining v1-era input-from-eva
     items (#2039, #699/#808/#809) — conservative deferral remains
     correct unless cycle-45 finds genuine absorption signal
3. **Bounded mechanical:** if substantive load is light, prototype the
   cross-axis-impact-check parser in a single Rust file (no full
   crate yet) to validate the parser-design-questions before
   committing to the full architecture.

### What this cycle achieved

Cycle 44 is the **third "cold-reader → v1.X application" cycle** in
the redesign sequence (after cycles 38 and 42 which were v1.3 and v1.6
applications). The substantive output:

- 3 cold-reader questions answered (1 BORDERLINE-FAIL + 2 PASS)
- 1 load-bearing framework fix (Axis 2 × Axis 3 dep map phrasing
  precision) + 2 minor framework fixes
- v1.7 → v1.8 framework version bump
- 3 PR closures (#2730, #2737, #2739) — open PR queue now empty
- 1 v2 tool design draft (cross-axis-impact-check) committed to
  `tools/v2/_drafts/`
- 1 cycle-44 same-cycle review (5 questions, 4 PASS + 1 BORDERLINE-PASS)

The most interesting cross-cycle observation: **same-cycle "minor
refinement opportunity" flags can escalate to load-bearing findings
on cross-cycle review.** This is now the third instance of the pattern.
The convergence is asymptotic — each cycle's correction is itself a
claim that subsequent cross-cycle review can refine. Mechanical lint
tooling (cross-axis-impact-check) would catch some of these flags
faster, freeing orchestrator compute for substantive Phase 2 candidate
work.
