# 2026-04-28 — redesign cycle 8

## Cycle 8 — adversarial re-read of cycle-7 Copilot integrations

Cycle 7 (a20e6a8c) integrated Copilot feedback PR #2749 — 27 accept / 10 qualify / 0 dismiss across 37 findings, retrospective 1034 → 1330 lines. The cycle 7 evaluation document (`_notes/cycle-7-copilot-feedback-evaluation.md`) explicitly queued five adversarial checks for cycle 8. Cycle 4 ran the equivalent on cycle 3's audit-#442 integration; the discipline is the same.

The audit repo posted nothing new since #442 (cycle 202 was its last cycle, 2026-04-27 04:31Z; it committed to drafting `docs/redesign/0-audit-retrospective.md` no later than audit cycle 204; that hasn't landed yet). No external feedback to integrate this cycle.

**Findings.** Two substantive issues plus two minor ones from five checks. ~40% issue-find rate, vs cycle 4's ~11% on cycle 3 integrations. The higher rate is consistent with cycle 7 being a larger, less-mature integration than cycle 3 was.

Check 1 (failure-family refactor): the post-cycle-7 "Reconciliation asymmetry and defense accretion: families in detail" section was duplicating F-section evidence. About 85 lines of the 110-line section were restatements of what's in F2/F3/F4/F11 sections; only ~26 lines were genuinely new content (F5 intersection note, F8 placement note, two design-implication blockquotes). The cycle-7 integration plan's intent had been "give the family framing; F-sections hold the evidence" — what got written gave both. Condensed to a 65-line "Cross-family notes and v2 design implications" section keeping only the new content plus four design implications (one per family).

Check 2 (freeze-vs-refresh F1 strain): the cycle-7 integration plan promised to "preserve the wider observation that the same bug shows up at multiple scales as a separate adjacent point" but the F11 architectural-implication paragraph as written collapsed F1/F12/F5 into a single upstream chain ending at the freeze-vs-refresh timing collision. F1 is structurally a response-shape pattern (constraints-instead-of-tools), not a timing pattern. Reworked the paragraph to clarify F1/F12/F5 as parallel sibling manifestations of defense-accretion, each with its own local mechanism, sharing the family tag because the same reflex (encode-the-failure-into-the-system) produces all of them.

Check 3 (success-criteria demotions): no actionable issue. The smell-test framing preserves the failure-direction without overclaiming the success-direction; the 9 candidate v2 measure-shapes are correctly Phase-2 inputs.

Check 4 (self-congratulation sweep): one location of meta-residue in the working-notes-pattern entry — a "Note: observations about X belong in iteration log" sentence that was itself the kind of process commentary it was complaining about. Deleted. Iteration plan section's cycle-by-cycle progress accounting is appropriate as iteration log; kept.

Check 5 (glossary): two small issues. C5.5 entry forward-references F4 (rephrased to operational terms). F-pattern entry was circular ("F-pattern: one of the F1-F12 patterns") — rewritten to give an operational hook. Added 4 missing entries (worklog, review agent, step comments, pipeline-check). The full legibility sweep through retrospective body remains queued.

**Honest about what wasn't done.** The retrospective is now 1312 lines (down from 1330; -18 net after the F11 paragraph rework added some lines back and the glossary expanded). The full refactor-for-length sweep through the F-pattern sections themselves remains queued. F-section evidence is the artifact's primary evidence base; compressing it requires more scrutiny than a single cycle should attempt while also doing the adversarial re-read. The journal-entry self-congratulation sweep (Copilot lens 4.D second pass) was also deferred — cycle 8 work was already substantive enough.

**What surprised me.** I expected the adversarial re-read to find perhaps one issue (cycle-4 pattern). Two substantive ones plus two minor edits is more than I anticipated. On reflection, cycle 7 integrated 37 findings in one cycle — cycle 3 integrated ~13 — and the bigger integration had less per-item time to settle. The duplicated families-in-detail section is the kind of thing that probably wouldn't have shown up if cycle 7 had paused to read the integration as a whole; the section was added as a deliberate output of the cycle-7 plan, but the plan didn't anticipate that the per-section family annotations *plus* the family table would already carry most of what the section needed to convey. The freeze-vs-refresh F1 strain is similar: the integration plan committed to preserving F1 as a separate adjacent point, but the paragraph as written collapsed it. Both are honest mistakes in cycle 7's hands; cycle 8's re-read caught them; that's the discipline working. The pattern argues for shorter integration cycles in the future — cycle 7 was unusually large.

**What I'm still uncertain about.** Whether the families-in-detail condensation went too far. The original section did serve as a "consolidated reading" anchor for someone wanting to see the family-level claim without reading all 12 F-sections. The condensed section assumes the reader has read the family table and the per-section annotations; if they haven't, the four design-implication blockquotes alone may be too compressed to bootstrap. Cycle 9+ should re-read the condensed section against this concern (pre-committed in the cycle-8 notes file). If the cold-reader test fails, expand the section back somewhat — but synthesizing across patterns, not restating evidence.

**Persistence-mechanism observations.** Ten cycle-N notes files now under `_notes/`, plus the iteration log table in README. The pattern continues to work at this scale. Notable: the cycle-8 adversarial re-read was made tractable by the cycle-7 evaluation document explicitly listing the five checks for cycle 8. Without that pre-commit, cycle 8 would have had to re-derive what to scrutinize. The pre-commit-checks-for-next-cycle convention is doing real work.

**Cycle 9+ candidate work.** Beyond the queued cycle-7 deferrals (legibility sweep, F6/F8/F9 measurements, impact ranking, cost/economics, parallelism, deeper preservation sweep), cycle 8's pre-commits add: (a) cold-reader test on the condensed families section, (b) journal-entry self-congratulation sweep, (c) optional second-Copilot-feedback dispatch on the post-cycle-8 retrospective. Phase 0 is on its 9th iteration cycle and the artifact is approaching steady state on the load-bearing structural claims; the iteration is now mostly in clean-up and validation against external lenses. The post-retrospective checkpoint is approachable but not yet ready — Eva-side, not orchestrator-side, decision.

## Cycle 9 — adversarial-on-adversarial of cycle-8 edits + emergent citation sweep

Executed cycle-8 pre-commits: cold-reader test on condensed Cross-family notes section, F1-vs-defense-accretion check on F11 paragraph rework, glossary-decode check. Two of three checks turned up findings; one of the findings was a class-level pattern that prompted a sweep substantially wider than the cycle-8 pre-commit anticipated. Net: retrospective 1312 → 1284 lines (-28).

**Findings.** Check 1 (cold-reader on Cross-family notes): real and substantive. Cycle 8 condensed the section ~110 → ~63 lines but ~38 of the remaining ~63 still duplicated content already in the family preamble at lines 145-186. Most egregious: F5 dual-membership note in the section was nearly word-for-word what's at preamble lines 145-148. Two of four design-implication blockquotes restated the preamble's three-clause sentence about reconciliation/defense-accretion design implications. Cycle 8's "kept the genuinely new content" framing missed that two of three "new" items (F5 intersection, F8 placement) were either duplicates or redundant with preamble.

Restructured the section. Reframed its role as "collected v2 design implications per family" rather than "things not covered elsewhere" (which was cycle 7's framing inherited by cycle 8). Deleted F5 dual-membership note. Compressed F8 placement to two sentences. Rewrote F1/F12/F5 sibling-manifestations as "Defense accretion appears at four substrates" — now explicitly includes F11 (which the cycle-8 paragraph implicitly excluded by listing only F1/F12/F5), points back to F11 section for substrate-by-substrate breakdown rather than restating it. Kept all four design-implication blockquotes as the canonical per-family v2 reference even though two duplicate preamble (a tradeoff: cold reader looking up "v2 reconciliation-asymmetry implication" probably benefits from finding it in the design-implications section even if duplicated). Section now ~45 lines.

Check 2 (F1-vs-defense-accretion in F11 paragraph): held up on substance. Family membership unambiguously preserved by cycle-8 correction — three explicit affirmations within the paragraph ("parallel manifestations of defense accretion at different substrates," "sibling manifestations of the same family," "share the family tag"). The over-separation worry from cycle-8 pre-commit did not materialize. But the check surfaced a second-order issue: the paragraph closed with "(cycle 8 correction per `_notes/...` check 2)" — process-commentary, the same anti-pattern cycle 8 deleted from a single instance in the working-notes-pattern entry. This recurrence triggered Sweep 4 (below).

Check 3 (glossary decode): 5 of 6 cycle-8 glossary edits decode operationally. One issue: pipeline-check entry used "most of them defense-flavored per F12 sub-(b)" — same forward-reference pattern cycle 8 fixed in the C5.5 entry. Rewrote: "most of which exist to detect specific past failure modes (the F12 catalog characterizes the pattern)." Glossary header had its own process-commentary ("Added cycle 7 (`_notes/...`, lens 7) to make F-pattern claims legible ...") — same anti-pattern class. Removed.

Sweep 4 (emergent from checks 1+2): the cycle-meta-citation anti-pattern recurs at 12+ body-text locations across the artifact, not just the one cycle-8 caught. Each cycle-7 integration left behind a "(cycle 7 X per Copilot feedback PR #2749 lens Y)" parenthetical; cycle 8 added two of its own. Cycle 9 swept 13 distinct instances. Distinction matters: Category A citations (footnote-style, citing `_notes/cycle-N-foo.md` for empirical measurements) are kept — they let a reader trace claims to evidence. Category B citations (process-commentary describing WHO made the edit and WHY, often "per Copilot lens X") are anti-pattern — they don't add substance, they reveal sausage-making, and they will rot as referenced PRs become harder to find. One Category-B-style citation kept in iteration plan section's "Connect across patterns" bullet — that section uses cycle-by-cycle history format intentionally.

**Honest about what wasn't done.** Journal-entry self-congratulation sweep (deferred from cycles 7 and 8) deferred again. Second Copilot feedback dispatch deferred to cycle 10+. F6/F8/F9 measurements still queued. Refactor-for-length sweep on F-pattern sections themselves still queued. Cycle 9's citation sweep was the unexpectedly-large work this cycle; preventing scope creep beyond it was the right call.

**What surprised me.** I expected the cycle-8 pre-committed checks to mostly find that cycle 8's edits held up (cycle-4-style 11% issue-find rate). Instead two of three found real issues, and check 2 surfaced a class-level anti-pattern (cycle-meta citations in body text) that turned out to span 13 instances. The cycle-8 self-congratulation sweep had caught one instance of meta-residue and treated it as isolated; cycle 9 found it was actually pervasive. The cycle-N-pre-commits-cycle-N+1-check pattern is producing real findings; but the findings are sometimes wider than the pre-commit anticipated — both cycle 4 (F8 placement was wrong in the shared-root preamble, beyond a single cite check) and cycle 9 (citation anti-pattern was 13 instances, not 1) extended their checks into class-level sweeps.

**What I'm still uncertain about.** Whether keeping all four design-implication blockquotes is right when two duplicate preamble. Argued for keeping (per-family canonical reference) but cycle 10 cold-reader test could re-decide. Whether the four-substrate framing (F1/F12/F5/F11) is stronger or weaker than cycle-8's three-substrate framing (F1/F12/F5 with F11 implied separate) — I think stronger because it makes defense-accretion family complete in one frame, but the substrate-listing paragraph in the F11 section still uses three; this is now slightly inconsistent across the two locations. Cycle 10 should reconcile.

**Persistence-mechanism observations.** README iteration log + per-cycle `_notes/cycle-N-*.md` files continue to work. Notable cycle-9 observation: cycle-8's pre-commit explicitly listed three checks for cycle 9; cycle 9 executed exactly those checks, and two of them produced findings the pre-commit had identified as worth checking even though the pre-commit author (cycle 8 itself) thought the underlying edits were sound. This is the discipline working: making the cycle-N self check the cycle-N edits adversarially in cycle N+1 catches what cycle-N's own self-eval missed. The cycle-7-evaluation-document-pre-commits-cycle-8-checks-pre-commits-cycle-9-checks chain is now four cycles deep with no breakdown. Worth noting for v2 design: this kind of multi-cycle commitment chain is something v1 has no native support for (the chronic-category mechanism is the closest analog and has all the F9 problems); v2 should make multi-cycle pre-commit / verify / propagate cycles a first-class pattern.

**Cycle 10+ candidate work.** Pre-commits in `_notes/cycle-9-adversarial-reread.md`: (a) cold-reader on cross-family notes restructure (was keeping all four design-implication blockquotes correct?), (b) substantive-vs-stylistic check on the citation sweep (did any deleted paren carry context that mattered?), (c) F-pattern naming-and-tagging consistency check (after 9 cycles, drift is possible). Plus the long-deferred items: journal-entry self-congratulation sweep, second Copilot feedback dispatch (waiting for stabilization), F6/F8/F9 measurements, refactor-for-length on F-section evidence, impact ranking, cost/economics, parallelism. Phase 0 still iterating; post-retrospective checkpoint approachable but Eva-side, not orchestrator-side, decision.

## Cycle 10 — adversarial-on-adversarial of cycle-9 + substrate-count reconciliation

Executed cycle-9 pre-commits: cold-reader on cross-family notes 4-blockquote choice (Check A), substantive-vs-stylistic on the 13-instance citation sweep (Check B), F-pattern naming-and-tagging consistency across artifact (Check C). Plus the cycle-9-noted substrate-count reconciliation (3 vs 4) that wasn't framed as a numbered check but was explicitly named for cycle 10.

Cycle 10 came back substantially smaller than cycle 9 (28 lines net) or cycle 7 (296 lines net). The artifact appears to be approaching steady state on load-bearing structural claims; only consistency edits this cycle, no new substantive findings.

**Findings.** Check A (cold-reader on 4 blockquotes): cycle-9's choice HOLDS. Audit method: compared each blockquote to family preamble lines 174-179. Blockquote 1 (Reconciliation-asymmetry) is verbatim duplicate; Blockquote 2 (Defense-accretion) has ~50% overlap with the second-sentence "load-bearingness" caveat being genuinely new content; Blockquotes 3 (Procedure/review-overhead) and 4 (Tooling-fragility) are NEW because the preamble doesn't address those families' implications. The section heading "Cross-family notes and v2 design implications" plus the section intro signal the subsection is a per-family digest; duplication with preamble is by-design; cutting blockquotes 1 and 2 would break per-family-completeness. No edit. (Cycle 11+ flag: if the artifact ever gets a per-family implications-column added to the family table, the cross-family digest section becomes redundant — flag for Phase 2 design-document shape.)

Check B (citation sweep substantive-vs-stylistic): all 13 deletions PASS. Constructed a per-deletion table verifying body-text content survival vs iteration-log attribution preservation. Three highest-stakes deletions (F11 freeze-vs-refresh attribution, F12 load-bearingness qualification, F11 parallel-siblings cycle-8-correction): substantive content survives in body; lost attributions are in iteration log. The artifact reads cleaner; the cost (lost inline lineage to cycle-7-Copilot-lens-X for some load-bearing reframings) is bounded. Cycle 11+ flag: iteration log is now the load-bearing attribution surface; future log condensation must preserve cycle-7-Copilot and cycle-8-correction lineage.

Check C (naming/tagging consistency): three real findings, two borderline non-edits. The substrate-count inconsistency cycle 9 flagged was the headline finding — three locations discussed defense-accretion's substrate-manifestations with different counts: cross-family notes said "four substrates" (F1/F12/F5/F11), F11 architectural-implication paragraph said "three" (F1/F12/F5 with F11 implicit), F12 hypothesis said "three substrates" with F12 listed as one of its own three then "fourth observation at temporal layer" added clumsily. Edit applied: F11 paragraph now reads "F1, F12, F5, and F11 (this section) are parallel manifestations of defense accretion at four substrates" with F11's local mechanism (freeze-vs-refresh) added to the local-mechanism listing alongside F1/F12/F5; F12 hypothesis substrate paragraph rewritten to "Defense accretion appears at four substrates within the family. F1 manifests at the prompt/checklist layer; F5 at the state-shape layer; F12 at the cross-substrate cataloging level; and F11 at the temporal layer" — F12 is now framed as peer-substrate-of-cross-substrate-cataloging, not as meta-observer-of-self. Third finding: F12 binding sentence said "binds F11 to F1+F12 (named tools + named fields + observable trigger timing)" — F5 was missing despite the named-fields binding being precisely F5's write-mostly-state-fields pattern. Edit: "binds F11 to F1, F5, and F12 (named tools fire post-close, named fields are mutated, the catalog confirms cross-substrate spread)." All three locations now consistently use four-substrate framing.

Two borderline non-edits with documented reasoning: (a) family-table-vs-F11-header dual-membership representation asymmetry — table places F11 primary in reconciliation-asymmetry row with parenthetical to defense-accretion; F11 header leads with defense-accretion. F5's table-and-header pair is consistent (defense primary in both); F11's is asymmetric. Both representations are valid; forcing a single primary breaks the table's per-row convention or the header's both-equal convention. Acceptable mild asymmetry; flagged for cycle-11+ reconsideration. (b) F8 cross-family note over-defends against a classification F8 doesn't have (note exists because cycle 4 corrected F8's earlier inclusion in the shared-root preamble). Note is short, serves reader-orientation cue, removal cost > keep cost. Borderline.

**Honest about what wasn't done.** Journal-entry self-congratulation sweep deferred again (now four cycles in a row: 7, 8, 9, 10). F6/F8/F9 measurements still queued. Refactor-for-length on F-section evidence still queued. Cycle-10's work was consistency reconciliation; doing more would have been scope creep for what was meant to be an adversarial-on-adversarial check cycle.

**What surprised me.** I expected the cycle-9 pre-committed checks to mostly find issues (cycle 9's checks found 2-of-3 with substantive issues). Cycle 10 came in lower: 0-of-2 substantive findings on Checks A and B (both held entirely), only Check C produced edits and those were consistency edits not new findings. The convergence is consistent with cycle 9's prediction that the artifact was approaching steady state on load-bearing structural claims. We're approaching, not at — there are still queued items (measurements, length-refactor, journal sweep) and the second Copilot dispatch hasn't run.

The substrate-count reconciliation surfaced one issue I didn't anticipate: F12 had an awkward self-reference ("F12 is the meta-pattern that appears in three substrates: F1 ... F5 ... F12 ...") where F12 was listed as one of its own three substrates. This was a residue of cycle 5's original framing of F12 as a meta-pattern, not fully reconciled when cycle 7 added the family framing. Catching it required actually trying to align the substrate-count across three locations; reading F12 in isolation, the self-reference was easy to miss.

**What I'm still uncertain about.** Whether the family table should be updated to put F11 in defense-accretion row primarily, matching the F11-section's foregrounding of F11 as a peer substrate. Argued above for not editing — the table's per-row convention is acceptable mild asymmetry — but cycle-11 cold-reader test could re-decide. Whether the post-cycle-10 artifact is now ready for the post-retrospective checkpoint, or whether the queued items (measurements, length-refactor, journal sweep) are checkpoint-blocking. My read: not blocking. The retrospective at 1286 lines captures the structural claims with measurement evidence; the queued items are refinements, not load-bearing gaps. Eva-side decision; not orchestrator-side.

**Persistence-mechanism observations.** The cycle-N-pre-commits-cycle-N+1-checks chain is now five cycles deep (cycle 7 evaluation → cycle 8 checks → cycle 9 checks → cycle 10 checks → cycle 11 checks pre-committed). No breakdown. Each cycle's work is bounded by what its predecessor pre-committed plus whatever class-level patterns emerge from the checks (cycle 4's F8 placement, cycle 9's citation anti-pattern). Cycle 10 didn't surface a class-level pattern — the substrate-count reconciliation was localized to three places, all named in cycle 9's pre-commit. This bounded scope is itself a stability signal.

**Cycle 11+ candidate work.** Pre-commits in `_notes/cycle-10-adversarial-on-adversarial.md`: (a) cold-reader on family-table-vs-F11-paragraph after substrate-promotion, (b) verify F12 hypothesis rewrite preserves load-bearingness qualification's prominence, (c) test F11 paragraph self-reference phrasing for elegance, (d) **execute second Copilot feedback dispatch** — artifact has stabilized enough that fresh external lens will add signal beyond self-checks. Plus the long-deferred items: journal-entry self-congratulation sweep, F6/F8/F9 measurements, refactor-for-length, impact ranking, cost/economics, parallelism, family-table representation reconsideration.

## Cycle 11 — adversarial-on-adversarial of cycle-10 + second Copilot feedback dispatch

Cycle 10 (commit `e37e778e`) made consistency edits reconciling defense-accretion's substrate-count to four (F1/F5/F12/F11) across F11 paragraph, F12 hypothesis, and cross-family notes. Cycle 10 pre-committed three adversarial-on-adversarial checks for cycle 11 plus the long-deferred second Copilot feedback dispatch as the highest-leverage remaining item. Cycle 11 executed all four. Two of three self-checks produced minor edits; one held entirely. Dispatch posted as issue #2755 (gpt-5.5).

**Findings.** Check 1 (cold-reader on family-table-vs-F11-paragraph after cycle-10 substrate-promotion): real inconsistency. Cycle 10 promoted F11 to peer in three locations (F11 paragraph line 727, F12 hypothesis line 869, cross-family notes line 924) but did not propagate to the family table at line 138, which still had "F1, F5, F12 (with F11 as the temporal stage)" — F11 parenthetical-secondary in defense-accretion. Cross-axis observation: F5 was also asymmetric (peer-in-defense, parenthetical-in-reconciliation); cycle 10 had specifically declared F5's table-and-header pair "consistent" via the table-vs-header axis, which was correct for that axis but missed the cross-row asymmetry. Edit applied: promoted both F5 and F11 to peer in both rows; added dagger marker (†) for dual-membership; consolidated explanatory bullets under a single dagger-footnote line ("Both readings are correct, neither reduces the other"). Two cycles examined the same table, reached opposite verdicts on F5 via different lenses, and both were locally correct — interesting epistemically: an artifact can be "consistent" along one axis and "inconsistent" along another simultaneously, and which judgment is load-bearing depends on which axis the reader privileges.

Check 2 (F12 hypothesis load-bearingness qualification preservation): HOLDS. The qualification "**The catalog proves accumulation; it does not prove load-bearingness.**" is preserved with maximum prominence — bold formatting, lead position in Hypothesis paragraph, ~10-line elaboration before substrate-discussion begins, separate paragraph for substrate-discussion, echo at end ("confirming, not load-bearing on its own"). F12 meta-character preserved by the "cross-substrate cataloging level" framing — pipeline-checks/polling-tools/gates/cutoff-cycles spanning multiple substrates IS the meta-observer role, just named concretely (what F12 actually does: catalogs across substrates) rather than abstractly (it's the meta-pattern). Arguably an improvement over the pre-cycle-10 phrasing because a fresh reader can now identify the substrate-tools, which is the kind of operational legibility the cycle-7 glossary was added for. No edit.

Check 3 (F11 paragraph "(this section)" self-reference elegance): real (mild) issue. The cycle-10-added "(this section)" was redundant with the elaboration ("F11 names the temporal stage") later in the same sentence, and read as meta-text pointing at the structural document rather than at the substantive claim. Considered three alternatives (drop entirely / "(this section's subject)" / restructure to lead with F11). Chose drop entirely as minimal edit preserving four-substrate-completeness — the listing already contains F11; the elaboration already names F11's role. Edit applied. Borderline editorial choice; flagged in cycle-12 pre-commits for cold-reader re-read on whether the deletion lost a navigational cue for a reader skipping ahead.

Check 4 (second Copilot feedback dispatch): EXECUTED. Issue #2755 opened with `agent-task` + `feedback-only` labels, `Copilot` assigned, gpt-5.5 (cycle 6 used gpt-5.4 — different model for cycle 11 to broaden perspective). Seven lenses calibrated against cycle-6's dispatch to maximize fresh-eye signal: lenses 1/2/5 target post-cycle-6 content (failure-family preamble, four-substrate framing, cross-family notes section); lens 3 re-checks the freeze-vs-refresh formulation (cycle-7 adopted from cycle-6, has it held up?); lens 4 targets the demoted thresholds and 9 candidate measure-shapes; lens 6 explicitly NEW self-congratulation in post-cycle-6 content; lens 7 full-artifact length/digestibility (cycle-6 didn't address). Procedural deviation from cycle-6: used `gh issue create --body-file ...` instead of the cycle-6 `jq | gh api ... --method POST --input -` pipeline (which has the `agent_assignment` field); the deviation was environmental (the parallel-tool pipeline was blocked), not a procedural choice. Visible fields look identical to cycle-6's #2748 — same labels, same `Copilot` assignee. If no PR appears within ~24 hours, cycle 12 must re-issue via the cycle-6 procedure. Flag for cycle 12.

**Honest about what wasn't done.** Journal-entry self-congratulation sweep deferred again (now five cycles in a row: 7/8/9/10/11). F6/F8/F9 measurements still queued. Refactor-for-length on F-section evidence still queued (this is the lens-7 question of the dispatch — Copilot's response will inform whether cycle 12+ should attempt it). Family-table F8 placement note borderline finding from cycle 10 still not edited.

**What surprised me.** The cycle-11 self-checks found two real issues (Checks 1 and 3) and validated one rewrite (Check 2). I had expected lower yield given cycle 10's "approaching steady state" framing — but the substrate-promotion in cycle 10 created a downstream inconsistency (table-vs-paragraph) that cycle 10 itself didn't catch because cycle 10 was checking the table-vs-header axis, not the table-vs-paragraph axis. The cycle-N-pre-commit-cycle-N+1-check discipline is exactly designed to catch this kind of cross-axis inconsistency. Pre-commit (a) for cycle 11 explicitly asked "did the substrate-promotion introduce new inconsistencies?" and the answer was yes. The discipline working again.

What was less expected: the F5 representation also turned out to be asymmetric. Cycle 10 had declared F5's table-and-header pair "consistent" (via table-vs-header axis); cycle 11's check on a different axis (table-vs-paragraph) revealed the table's row-vs-row asymmetry for F5 too. Same table, two cycles, opposite verdicts on F5 via different lenses, and both were locally correct. That's worth noting epistemically: which judgment matters depends on which axis the reader privileges. The cycle-11 edit makes the table symmetric on both axes; a future cold-reader test (cycle 12 pre-commit 1) will check whether the consolidation lost prose nuance.

The dispatch was the long-deferred item from cycle 5 (originally proposed) → cycle 6 (executed first) → cycles 7/8/9/10 (deferred or replaced by integration work) → cycle 11 (executed second). Six-cycle cadence on a deferred item is itself a pattern: it takes that long for the artifact to stabilize enough for a fresh dispatch to be high-signal. Cycle 9 originally said "deferred to cycle 10+" and cycle 10 said "deferred to cycle 11+" — cycle 11 honors the binding by executing.

**What I'm still uncertain about.** Whether the cycle-11 dispatch will produce a PR. The procedural deviation from cycle 6 (using `gh issue create` instead of `gh api ... --method POST` with the `agent_assignment` field) introduced uncertainty. If no PR lands within ~24 hours, the dispatch must be re-issued. This is a flag for cycle 12.

Whether the F11 paragraph "(this section)" deletion was the right call vs the alternative of restructuring the paragraph to lead with F11. The deletion is the minimal edit; restructuring would be more elegant but loses the parallel-listing structure that the four-substrate framing depends on. Borderline editorial choice; flagged in cycle-12 pre-commits for re-read.

Whether the family-table dagger-footnote consolidation lost some prose nuance. The pre-cycle-11 form had longer per-pattern explanations; the cycle-11 form is more compact. Cycle 12 cold-reader test (pre-commit 1) will check.

Whether the post-cycle-11 artifact is now ready for the post-retrospective checkpoint, or whether the long-deferred items (measurements, length-refactor, journal sweep) are checkpoint-blocking. My read: still not blocking. The cycle-11 dispatch is the last named outstanding item before checkpoint other than the long-deferred ones; after the PR lands and cycle 12+ integrates, the artifact may genuinely be ready for Eva's review. But that is Eva's call to make, not the orchestrator's.

**Persistence-mechanism observations.** The cycle-N-pre-commits-cycle-N+1-checks chain is now six cycles deep (cycle 7 → 8 → 9 → 10 → 11 → 12 pre-committed). No breakdown. The chain has produced real findings every cycle since cycle 8. Cycle 11's combined-notes-file form (Checks 1-3 + dispatch metadata in one file) differs from cycle 6's (which had separate files for dispatch and mechanism work). The combined form is appropriate when dispatch and self-checks share the same cycle's compute and dispatch metadata is small (~50 lines); separate files would have been overhead. For cycle 12+ when the PR arrives, the integration work will be a separate notes file (cycle-7 model: per-finding evaluation table with accept/qualify/dismiss disposition).

**Cycle 12+ candidate work.** Pre-commits in `_notes/cycle-11-adversarial-and-dispatch.md`: (a) cold-reader test on family-table dagger-footnote consolidation (lost nuance?), (b) cold-reader test on F11 paragraph "(this section)" deletion (lost navigational cue?), (c) cycle-11 dispatch status check + integration if PR landed, (d) F-section-header order convention check after table symmetric treatment. Plus long-deferred: journal-entry self-congratulation sweep (5 cycles deferred), F6/F8/F9 measurements (cycle 7+), refactor-for-length (cycle 8+), family-table F8 placement note (cycle 10 borderline). If the dispatch PR arrives: cycle 12 (or whichever cycle sees it) becomes cycle-7-style per-finding evaluation and integration. The seven lenses are calibrated to find new issues; integration may be substantial if Copilot finds class-level patterns the way cycle-9's citation sweep did.

## Cycle 12 — cycle-11 Copilot dispatch PR landed; per-finding evaluation + Tier-1 integration

The dispatch did produce a PR. #2756 opened at 07:00 UTC (~1.7 hours before this cycle started), 439 lines, single new file at `_notes/cycle-11-copilot-feedback.md` per the dispatch's hard constraints. Notable: the file declares model `claude-sonnet-4.6` despite the dispatch body requesting `gpt-5.5`. The cycle-11 deviation from cycle-6's procedure (using `gh issue create` instead of `jq | gh api ... --method POST` with the `agent_assignment.model` field) does produce a PR, but does not propagate the model-selection field. So the assignee-only path works for triggering Copilot but not for model control. Worth recording for the v2 dispatch tool spec — model selection should be an explicit, verifiable parameter.

The critique itself is high-quality regardless of which model produced it. 25 findings across 7 lenses. Per-finding evaluation in `_notes/cycle-12-copilot-feedback-evaluation.md`: 22 accept / 3 qualified / 1 already-resolved (1.E) / 0 dismiss. Compare cycle 6: 27/10/0 across 37 findings.

**Most consequential finding: 3.B.** Both "defenses run too late" and "artifacts freeze too early" are equally consistent with the freeze-vs-refresh evidence. The artifact picks the second framing and builds the v2 design implication from it ("cycle boundaries should be checkpoint markers on a continuously-evolving state"), without arguing against the first. This is motivated reasoning — and it is the kind of motivated reasoning that would propagate into Phase 2 candidates if not caught now. Catching it before Phase 2 is high-leverage.

**Most validating finding: 1.E.** Copilot independently arrived at exactly the cycle-11 dagger-footnote fix. Cycle 11's self-check observed the table-vs-paragraph asymmetry and made both F5 and F11 peer in both rows; Copilot's lens 1 made the same observation with the same recommendation. When the orchestrator's self-check and an external lens converge, the change has both internal and external warrant. This is what the dispatch is designed to produce — and one of the lens-validations cycle 11 had hoped for.

**Most actionable cleanup: lens 5.** Cross-family notes section had a systemic duplication problem larger than cycle 10's check A captured (cycle 10 examined this section and decided to keep most of it). Three findings (5.A blockquote 1 verbatim duplicate; 5.B F8 placement note as cycle-history; 5.C defense-accretion four-substrate restatement duplicating F12) bundle into a section-level cleanup. Cycle 12 applied the cleanup as a single restructure: the section was retitled "v2 design implications by family" and reduced to its three genuinely-new design-implication blockquotes plus an opening pointer to where the load-bearing claims live (preamble + F11 paragraph + F12 hypothesis).

**Cycle 7 lesson revisited.** Cycle 7 integrated 37 findings in one cycle and produced two substantive issues that cycle 8 had to fix. Cycle 12 split the 25 findings into Tier-1 (bounded mechanical, applied this cycle: 1.C, 5.A+5.B+5.C bundled, 6.A, 6.D, 1.E confirmation) and Tier-2 (load-bearing prose / structural change, deferred with explicit cycle-13+ pre-commits). Tier-2 spans 9 grouped pre-commits each suitable for one cycle's compute. The split is the explicit answer to cycle 7's lesson: per-cycle integration capacity is bounded; over-extending produces cycle-N+1 cleanup work; spreading out preserves quality.

**Tier-1 details.** 1.C "guarantees" → "structurally produces" at lines 169 and 717: 3-cycle evidence supports "structurally produces" but not "guarantees." 5.A+5.B+5.C cross-family notes restructure: section retitled, F8 placement note cut, defense-accretion four-substrate restatement cut, Reconciliation-asymmetry blockquote cut, opening replaced with a pointer to load-bearing-claim locations, three v2-design-implication blockquotes retained as the per-family-digest content that lens 5 acknowledged is genuinely new. 6.A glossary "F-pattern": "since they are not equally independent" replaced with "as a hypothesis under iteration ... not a settled taxonomy" — the former forecloses the question the body holds open. 6.D working-notes-pattern: caveated as "redesign-era addition, not a v1 working feature." Net: 1286 → 1277 lines (-9).

**Tier-2 deferrals.** 9 pre-commit groups: family-summary rewrite (1.B+2.A+2.C — defense-accretion family summary at lines 167-170 is F11's local mechanism mis-elevated, F12 substrate-categorization strain, sibling-not-upstream qualification); review/disposition substrate (2.B+1.D — possible 5th substrate; F9 dual-membership consideration); freeze-vs-refresh framing alternative (3.A+3.B — make "run defenses earlier" alternative explicit; bump OQ8 priority; abnormal-cycle caveat on 3-cycle measurement); nine measures rework (4.A+4.B+4.C — gameability critique; M1/M2-vs-M4 tension; F2/F8/F9/F1 missing measure classes); iteration plan move to README (6.B+7.A — 44 lines of process-commentary in the deliverable body); resolved open questions collapse (7.B); preserved-through-cutover disposition (6.C — promote vs cut); F-section length sweep (7.C+7.D+3.C+7.E — F6 compression, F10 truncation, F4/F11 cross-reference, ~150-200 line target); F8 singleton-family acknowledgment (1.A — taxonomy smell honestly named).

**Cycle-11 pre-commits resolved.** (a) Cold-reader on family-table dagger consolidation: passes (per-pattern bullets retain dual-membership claims; lens 1.E independently arrived at the same fix). (b) Cold-reader on F11 paragraph "(this section)" deletion: passes (preceding paragraph at line 724 names F11 explicitly as "the *temporal* face of the defense-accretion family" — no navigational cue lost). (c) Dispatch PR landed. (d) F-section header order convention: bundled into Tier-2 group 8 (length sweep). All four pre-commits closed.

**Honest about what wasn't done.** Long-deferred items still deferred: journal-entry self-congratulation sweep (now 6 cycles deferred), F6/F8/F9 measurements (cycle 7+), refactor-for-length (cycle 8+ — partially addressed by Tier-2 group 8 deferral, but still deferred). The eight Tier-2 pre-commits add to the deferred list rather than reduce it. This is honest: cycle 12 made progress on bounded items and deferred load-bearing prose work for dedicated cycles. Cycle 13+ has 9 pre-commit groups available to draw from.

**What surprised me.** Three things. (1) The dispatch worked despite the procedural deviation from cycle 6 — the assignee-only `gh issue create` path produces PRs. The model field, however, did not propagate. (2) Lens 1.E independently arriving at the cycle-11 dagger-footnote fix is the strongest evidence yet that the cycle-N-pre-commits-cycle-N+1-checks discipline produces real results — the cycle-11 fix was internally warranted by the substrate-promotion inconsistency, and externally warranted by Copilot's fresh-lens reading of the same table. (3) Lens 3.B (motivated-reasoning on freeze-vs-refresh) is the deepest finding in the entire critique. It does not just say "this prose is weak" — it says "this prose is hiding a choice the artifact didn't argue, and the choice would propagate into Phase 2 candidates." Catching this now, before Phase 2 design candidates exist, is exactly the kind of high-leverage external-lens finding the dispatch was meant to surface.

**What I'm still uncertain about.** Whether the Tier-1 restructure of cross-family notes went too far. The applied edit is slightly more aggressive than the three named findings — it retitled the section and rewrote the opening, beyond just cutting the three named items. The justification: lens 5 explicitly named the section as having "a systemic duplication problem more systemic than cycle 10 captured." So a section-level rewrite is consistent with the spirit of the findings. But a cycle-13 cold-reader should verify the rewrite reads coherently to a fresh reader.

Whether the family-table dagger consolidation's pre-commit (a) check was sufficiently rigorous. I cold-read the table and concluded the per-pattern bullets carry the load-bearing dual-membership claims, but cycle 11 had flagged "the cycle-11 form is more compact" — could compactness have lost more than I noticed? The convergence with lens 1.E ("Making both peers in both rows is the logically consistent choice") gives me confidence the direction is right. The exact wording could be revisited.

Whether 1.A (F8 singleton-family taxonomy smell) should have been Tier-1 instead of Tier-2. It's a relatively bounded edit — a short honest acknowledgment in the family preamble or F8 section. I deferred because the wording requires care, but a one-line acknowledgment could be drafted in a few minutes. Cycle 13 should consider promoting 1.A to a quick Tier-1-style edit if cycle 13 picks Tier-2 group 1 (family-summary rewrite) — they're related.

Whether the cycle-12 evaluation file is too long (~350 lines). Cycle-7 evaluation was similar size. The per-finding table is the load-bearing structure; the disposition column is what cycle-13+ verifies against. Length is justified by usefulness, but if cycle 13 consults the evaluation file and finds it unwieldy, the next dispatch's evaluation should compress.

**Persistence-mechanism observations.** The cycle-N-pre-commits-cycle-N+1-checks chain is now seven cycles deep (cycle 7 → 8 → 9 → 10 → 11 → 12 → 13 pre-committed). No breakdown. The dispatch + integration cycle pair (cycle 6 + cycle 7; cycle 11 + cycle 12) is itself a sub-pattern. Cycle 6 dispatched, cycle 7 integrated +296 lines in one cycle (over-extended). Cycle 11 dispatched, cycle 12 integrated -9 lines in Tier-1 + 9 Tier-2 group deferrals. The Tier-1/Tier-2 split appears to be a meaningful improvement in integration-cycle discipline; cycle 13+ Tier-2 work will test whether the deferred items actually get done or whether they accumulate as new "deferred since cycle 12" items.

The dispatch + integration sub-pattern is also worth naming explicitly for v2: a v2 "external feedback request" surface should pair with a v2 "integration cycle" surface, and the integration surface should default to multi-cycle Tier-1/Tier-2 splits rather than one-cycle-eat-it-all.

**Cycle 13+ candidate work.** Tier-2 pre-commit groups (1-9) per evaluation file. Pick by leverage — my read: family-summary rewrite (group 1) is the largest single load-bearing prose change and addresses the deepest cluster of findings (1.B + 2.A + 2.C); iteration plan move (group 5) is mechanically bounded and addresses the cross-cutting "two jobs" finding. Either could be cycle 13's focus. The other 7 groups carry forward.

Plus the cycle-13 cold-readers from Tier-1 edits: did the cross-family notes rewrite read coherently? Did "structurally produces" convey what "guarantees" was meant to? Did the 6.A glossary tightening avoid the opposite-extreme problem? Did the 6.D working-notes caveat read naturally?

The post-retrospective checkpoint is approachable but not yet there — Tier-2 group 1 (family-summary rewrite) and group 3 (freeze-vs-refresh framing alternative) are load-bearing changes that should land before Eva's checkpoint review. After those, the remaining Tier-2 groups are quality refinements rather than load-bearing gaps. Eva's call to make on whether those quality-refinement deferrals are checkpoint-blocking; orchestrator's read is that they are not, but the load-bearing groups (1 and 3) are.

## Cycle 13 — cold-readers on cycle-12 Tier-1 + Tier-2 group 1 (family-summary rewrite)

Cycle 12 left five pre-commits for cycle 13: four cold-reader checks on the Tier-1 edits applied last cycle (cross-family notes restructure; "structurally produces" wording; glossary "F-pattern" tightening; working-notes-pattern caveat), plus a Tier-2-group selection-and-execution. Cycle 13 ran all five. The four cold-readers passed with two minor flags; cycle 13 selected group 1 (family-summary rewrite, findings 1.B + 2.A + 2.C) and executed it across five retrospective locations.

**The cold-readers all passed.** Cross-family notes section (now "v2 design implications by family") reads coherently as a per-family digest; opening pointer is functional; three blockquotes are genuinely per-family content. "Structurally produces" sits correctly between "produces" (too weak) and "guarantees" (too strong) — names the mechanism without claiming universality. Glossary "F-pattern" entry's "as a hypothesis under iteration ... not a settled taxonomy" framing avoids both the foreclosing problem and the opposite-extreme problem. Working-notes-pattern caveat is mechanically correct and honest. Two minor flags: the iteration-plan section at line 1287-1289 still has a historical "guarantees" quote (bundle with Tier-2 group 5 when iteration plan moves); minor redundancy between "(cycle 3 addition)" parenthetical and italicized caveat in the working-notes-pattern bullet (bundle with Tier-2 group 8). Comparing yield to cycle 8's checks on cycle 7 (2 of 3 substantive issues found) and cycle 9's checks on cycle 8 (~38 lines of duplication missed in cycle 8): cycle 13's checks on cycle 12 yielded only minor flags. Mild evidence the Tier-1/Tier-2 split is sizing integration work better than cycle 7's all-at-once approach.

**Tier-2 group 1 picked over groups 3 and 5.** Cycle 12's notes were explicit: group 1 (family-summary rewrite) is the largest single load-bearing prose change; group 5 (iteration plan move) is mechanically bounded; group 3 (freeze-vs-refresh framing alternative) is the deepest finding still open. Cycle 13 picked group 1 because it sets the framing that cycle 14+ groups 2 and 3 build on. Group 3 specifically depends on where freeze-vs-refresh lives in the artifact — if it's the family summary, then "alternative framings" is a family-level question; if it's F11's local mechanism, then "alternative framings" is an F11-local question. Group 1 makes that decision before group 3 has to.

**Five edits, four locations + table consistency.** The pre-commit listed three locations (family preamble; F11 paragraph; F12 hypothesis). Working through the rewrite surfaced two more: the v2-design-implications section pointer at line 938-942 referenced the now-superseded "four-substrate breakdown" framing, and the family table parenthetical at line 140 listed three example defense types where the new family-summary listed four. Tier-2 work has more cross-section coupling than I estimated. Pre-commit for cycle 14+: explicit "ripple-effect check" when executing Tier-2 work.

**The freeze-vs-refresh insight is preserved, not demoted out of existence.** The cycle-7 framing ("v1 defenses are end-of-cycle/next-cycle refreshers; artifacts freeze before refreshers finish; architecture [guarantees → structurally produces] post-close divergence") was load-bearing in three ways: as F11's local mechanism, as the family summary, and as a cross-cutting architectural observation about the v1 cycle-boundary model. Cycle 13's rewrite says: it is F11's local mechanism and a cross-cutting observation, but it is not the family summary. The framing now appears in F11 architectural-implication paragraph (lines 729-740, "structurally produces" intact), the family-overlap paragraph in the preamble (lines 184-194, "structurally producing post-close divergence" intact, attributed to F11's local mechanism), the F12 hypothesis (line 901, "F11's local mechanism"), the v2-design-implications section ("Defense accretion implication" blockquote at lines 945-950), and the cycle-7-history quote in the iteration plan section (which will move to README in group 5). Five locations. The demotion is from "family summary" to "F11 specifically plus cross-sectional appearance," which is more accurate per finding 1.B.

**A partial integration of finding 2.B fell out.** While writing the F12 catalog description in the F11 paragraph, I needed to name what F12 catalogs — and that turned out to include the review-disposition surface (audit_processed, deferred_findings, review_agent.history fields). F12's catalog explicitly spans "the prompt/state/temporal substrates plus the review-disposition surface" in the F11 paragraph and F12 hypothesis. This is partial credit on Tier-2 group 2 (review/disposition substrate) — the deeper question of whether review-disposition is a fifth substrate, or F5's framing should be broadened, or F9 dual-membership at this surface is the right framing, remains open. Cycle 14+ group 2 work.

**Honest about what wasn't done.** Eight Tier-2 groups remain (2-9). Long-deferred items: journal-entry self-congratulation sweep (now 7 cycles deferred), F6/F8/F9 measurements (cycle 7+), refactor-for-length F-section sweep (cycle 8+, also Tier-2 group 8). The deferred list is steady but not growing significantly — cycle 13 added ~1 deferred item (cold-reader 2 flag and cold-reader 4 flag both bundled into existing groups) while resolving 5 (the four cold-readers + group 1). Cycle 13 did not address the most consequential single finding from the cycle-11 dispatch (lens 3.B, freeze-vs-refresh framing alternative) — that's group 3, queued for cycle 14+.

**What surprised me.** Three things. (1) Cross-section coupling: the pre-commit listed three locations to update; the work needed four (plus table consistency, making five). Tier-2 work touches more of the artifact than the per-finding analysis suggests. (2) The cold-readers on cycle 12 yielded only minor flags. Compare cycle 8's check on cycle 7's integration: 2 of 3 substantive issues. Cycle 9 found 38 lines of duplication cycle 8 missed. The Tier-1/Tier-2 discipline does seem to size cycles better — cycle 12's per-cycle integration scope was substantially smaller than cycle 7's, and the smaller scope gets checked more carefully. (3) Partial credit on group 2 (review-disposition surface) emerged unplanned from edit 2's prose. F12 catalogs more than just the three substrates I had in my head when I started — pipeline-checks include review-disposition checks. Working through the rewrite surfaced a structural observation I would have missed in pure planning.

**What I'm still uncertain about.** Whether the family-summary's parenthetical "(a constraint, a state field, a cycle-boundary check, an additional pipeline-check)" mixes substrate-defense (the first three) with catalog-example (the fourth) in a way that might be confusing on a fresh read. Cycle 14+ cold-reader on the family-summary should examine whether to (a) keep the four-item list (mixing substrate examples and catalog examples), (b) reduce to three (just substrate examples — drop "additional pipeline-check"), or (c) restructure to be explicit ("a substrate-specific defense [examples...]; F12 catalogs the accumulation"). Whether moving freeze-vs-refresh from family-summary to F11-local-plus-cross-sectional reduces its prominence too much in practice. The framing now appears in five locations — that's more, not fewer — but the demotion from family-summary affects how a fresh reader encounters the claim. Whether cycle 14+ should pick group 3 (freeze-vs-refresh alternative) or group 5 (iteration plan move) first. Group 3 is the most consequential content question; group 5 is the highest-leverage structural cleanup. My weak read: group 3, because the content question affects v2 candidate design and Phase 2 candidates will inherit whichever framing the artifact commits to.

**Persistence-mechanism observations.** The cycle-N-pre-commits-cycle-N+1-checks chain is now eight cycles deep. No breakdown. The Tier-1/Tier-2 split has held through one full evaluation cycle (cycle 12 deferred 9 groups; cycle 13 executed 1 + partial 0.5 of group 2 + minor table consistency). The "deferred list" tracking is becoming the mechanism's weak point: it's distributed across notes files (cycle 7, cycle 12, various "long-deferred items" mentions in journals) rather than consolidated. Tier-2 group 5 (iteration plan move to README) would partially address this; a future cycle should explicitly consolidate the outstanding-Tier-2-groups view in the README. Flag for cycle 14+ as a persistence-mechanism enhancement.

**Cycle 14+ candidate work.** Pre-commits in `_notes/cycle-13-cold-readers-and-family-summary.md`: (1) cold-reader on the cycle-13 family-summary rewrite at lines 167-194 — does demoting freeze-vs-refresh from family-summary to F11-local lose architectural prominence? does the four-item parenthetical mix substrate-and-catalog confusingly? (2) cross-section consistency check across family preamble + F11 paragraph + F12 hypothesis (substrate-count claim, local-mechanism inventory, sibling-not-upstream caveat placement); (3) family-table consistency check; (4) Tier-2-group selection — group 3 (freeze-vs-refresh framing alternative, deepest open finding) or group 5 (iteration plan move, mechanically bounded structural cleanup); (5) long-deferred items roll-call. Plus the long-deferred items: journal-entry self-congratulation sweep, F6/F8/F9 measurements, refactor-for-length, and the persistence-mechanism deferred-list consolidation.

## Phase 1 initiation — external research survey (parallel with Phase 0)

Phase 1 authorized via [#2759](https://github.com/EvaLok/schema-org-json-ld/issues/2759) (input-from-eva, 2026-04-28). Eva's signal: the retrospective is "approaching steady state on load-bearing structural claims" per cycle 13's own assessment; the cycle-7 condition ("after the Copilot feedback PR has landed and been integrated") has been satisfied twice (PR #2749 cycle 7, PR #2756 cycle 12); continued serial Phase 0 → Phase 1 means Phase 1 reading begins cold after checkpoint approval when it could be priming now.

**What was done.** Initial architecture-level survey of seven external AI agent/orchestrator systems: PAI (most detailed — repo accessible with rich architectural patterns), LangGraph, AutoGen, Semantic Kernel, Voyager, Devin, CrewAI. openclaw not accessible (repo appears private or nonexistent; needs retry with network access). Output: `docs/redesign/1-research.md`, organized by cross-cutting concern rather than by system.

**Seven cross-cutting patterns identified.** (1) Cold-start persistence — PAI's PRD-as-session-contract with three-tier state (durable/ephemeral/historical) is the richest model; LangGraph's per-node checkpointing is the most automatic. (2) Defense-accretion prevention — Voyager's strict admission gate (verify before storing) and LangGraph's compaction-node pattern are complementary mechanisms; no surveyed system has v1's constraint-accumulation pattern. (3) Tool-vs-judgment separation — universal pattern: the AI decides what; tools handle how; no system embeds fixed procedural sequences in the prompt. (4) Self-improvement loops — PAI's closed feedback loop and Voyager's curriculum-verify-store cycle are the two validated models; both require objective verification. (5) State representation — two axes: flat/tiered and ambient/opt-in; v1's flat-ambient is the worst combination for defense-accretion. (6) Review mechanisms — review criteria should be defined before execution, not discovered after (PAI's ISC). (7) Cycle boundary handling — surveyed systems mostly avoid hard cycle boundaries; the actionable pattern may be making the boundary a clean serialization point rather than an artifact-freeze point.

**Anchoring tensions documented.** Six anchoring-risk observations noted inline in the research document, per the operational guidance in #2759. The highest-risk anchoring pattern: PAI's PRD model is appealing and could prematurely anchor v2 toward a "single structured document" pattern that replicates state.json problems at a different layer. The pattern to extract is the tiered separation, not the specific format. Similarly, Voyager's strict admission gate is clean in Minecraft (objective verification) but orchestrator constraint verification requires counterfactual reasoning that is harder to automate.

**Notable absences from surveyed systems.** Three things v1 has that no surveyed system has: (a) a constraint-accumulation mechanism like recurrence-escalation — every other system either gates admission strictly or keeps rules minimal by design; (b) a multi-step procedural checklist embedded in the AI's system prompt — procedure lives in the framework/tools in every other system; (c) chronic-category tracking as a first-class concern — review categories are dispositioned or discarded, not carried forward as maintenance obligations.

**What surprised me.** The universality of the tool-vs-judgment separation. I expected to find at least one system that embeds procedural steps in the prompt the way v1 does. None do. This is the strongest independent validation of the redesign prompt's CORE-DESIGN-PRINCIPLE — not because these systems are authoritative, but because the convergence across architecturally diverse systems (graph-based, conversation-based, catalog-based, curriculum-based) suggests the pattern is robust rather than paradigm-specific.

PAI's depth was also unexpected. The Algorithm's seven-phase structure with closed feedback loop, ISC splitting test, and effort-tier calibration is the most directly applicable model to this orchestrator's concerns. Worth deeper reading in subsequent Phase 1 cycles.

**What I'm still uncertain about.** Whether reading multiple systems before the retrospective is fully stable introduces the premature-anchoring risk the deferral was designed to prevent. Eva's authorization addresses this by changing the mitigation from "defer reading" to "read multiple systems before drafting any Phase 2 candidate shapes, journal tensions rather than committing to them." The six inline anchoring-tension notes are the implementation of that mitigation. Whether the tensions I identified are the right ones is itself uncertain — a cold reader might flag different anchoring risks.

Whether the notable-absences section is observation or advocacy. "No surveyed system has X" is a factual observation; the implied "therefore v2 should not have X" is a design conclusion that Phase 1 should not be drawing. The research document is careful to present these as observations, but future cycles should verify the framing hasn't drifted into implicit advocacy.

**Phase 1 next steps.** openclaw access (required per prompt definition). Deeper reading on PAI's implementation detail (hook mechanics, ISC schema, JSONL formats). Reflexion paper for empirical evidence on when self-critique helps vs. hurts. TaskWeaver for plan/verify loops. Temporal.io for durable execution patterns. These can be own-compute reading or Copilot research-only dispatches — operational shape per standing autonomy in #2759.

**Phase 0 next steps (unchanged).** Tier-2 group 3 (freeze-vs-refresh framing alternative) remains the highest-priority load-bearing Phase 0 item per cycle 12 evaluation and cycle 13 assessment. Phase 1 reading does not change Phase 0 priorities.
