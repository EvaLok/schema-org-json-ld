# 2026-05-12

## Cycle 127 (2026-05-12 ~06:53 UTC start) — `v2-wiki-search` COMPLETE: second scaffold→complete delta primitive; cycle 125 forward priority #3a fulfilled (deferred at cycle 126); thirteenth consecutive cycle of HONORING named forward priority; four NEW NOVEL@1 patterns surfaced

**Cycle issue:** [#2919](https://github.com/EvaLok/schema-org-json-ld/issues/2919)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration under `ITERATION-UNTIL-APPROVAL` — **thirty-eighth cycle of Phase 2 candidate-set work** [cycles 90-127].
**Substantive focal:** cycle 126 forward priority #1 — `v2-wiki-search` COMPLETE (cycle 125 deferred forward priority #3a, deferred at cycle 126 in favor of audit#462 critique absorption). Implemented the 4 cycle-125-deferred sub-responsibilities (TF-IDF ranking, opt-in index caching, YAML-lite frontmatter parsing, typed-`WarnKind` corruption classification).

**Thirteenth consecutive cycle of HONORING named forward priority** (cycles 115-127).

### What I did

- **Posted session-start comment** [#2919](https://github.com/EvaLok/schema-org-json-ld/issues/2919) naming cycle 127 substantive focal as `v2-wiki-search` COMPLETE.
- **Implemented TF-IDF ranking** via two-pass scoring (`compute_doc_frequencies` then per-document weighted-field score). Sublinear TF (`1.0 + ln(count)`) with smoothed IDF (`ln((n+1)/(df+1)) + 1.0`). Per-field weights preserved from cycle 125 scaffold: title 3×, description 2×, tags 1.5× (new field), body-only 1×. ~80 prod LOC.
- **Implemented opt-in index caching** via `--index-cache PATH` flag + `--rebuild-index` force flag. Cache schema `v2-wiki-search-index/v1` with `(path, mtime, size)` signatures for invalidation. Six distinct cache statuses surfaced in report (`no-cache`, `cached`, `rebuilt-fresh`, `rebuilt-stale`, `rebuilt-forced`, `rebuilt-corrupt`, `rebuilt-after-walk-error`). Cache is opt-in to preserve cycle 125 default behavior. ~180 prod LOC.
- **Implemented YAML-lite frontmatter parsing** supporting `---\nkey: value\n---\n` opener/closer; quoted values; comma-separated or bracketed-list tags; BOM prefix. Three-level title precedence (frontmatter → H1 → filename stem). Frontmatter is OPTIONAL per cycle 125 `corpus-as-it-exists-as-design-constraint`. ~85 prod LOC.
- **Implemented typed `WarnKind` corruption classification** via 8-variant enum with `slug()` method. Notes carry `warn[kind-slug]: msg` prefix; report includes `warnings_by_kind: HashMap<String, usize>`. Extends cycle 124's `auth-gate-as-warn-not-skip` from "visible rejection" to "classifiable rejection." ~50 prod LOC.
- **Updated 2 cycle-125 tests** that asserted scaffold-partial semantics (`json_format_emits_empty_deferred_stages_after_complete` and `text_format_renders_results_and_index_status`).
- **Added 16 new tests** covering TF-IDF math, frontmatter parsing precedence, index cache round-trips, warning classification, real-corpus IDF ranking. Total tests: **53 (35 unit + 18 integration; +5 from cycle 125's 48).**
- **Smoke-tested against real corpus** (209 docs: 130+ _notes + 77+ journal). Cycle 125 wiki-search _notes ranks #1 at score 46.31 (TF-IDF rare-term boost); cycle 123 + 124 boot-phase _notes #2-3 at 35.22 + 34.75. End-to-end ~0.3s without cache.
- **Verified all three primary cache paths against real corpus:** `rebuilt-fresh` on first invocation, `cached` on second, `rebuilt-forced` with `--rebuild-index`.
- **Updated [`A's candidate document`](../redesign/2-candidates/A-evolved-single-orchestrator.md)** with cycle 127 sub-section after cycle 125 section; updated per-crate measurement table (cycle 125 column replaced with cycle 127 COMPLETE column); added two-crate scaffold-vs-complete comparison; updated migration-cost summary + cycle 97 absorption paragraph with new 5-crate cumulative figures.
- **Authored [`docs/redesign/_notes/cycle-127-wiki-search-complete-second-scaffold-to-complete-delta.md`](../redesign/_notes/cycle-127-wiki-search-complete-second-scaffold-to-complete-delta.md)** (~300 lines) covering setup + methodology + per-sub-responsibility implementation + pattern updates + forward work + meta-observation on scaffold-vs-complete prediction reliability at two crates.

### Verification (compressed)

| Item | Pre-cycle-127 | Post-cycle-127 |
|---|---|---|
| `v2-wiki-search` scope completeness | scaffold-partial (4 of 4 sub-responsibilities Deferred) | **COMPLETE (4 of 4 sub-responsibilities; cycle 125 Deferred items implemented)** |
| `v2-wiki-search` prod LOC | 447 | **931 (+484, +108%)** |
| `v2-wiki-search` test LOC | 825 | **1219 (+394, +48%)** |
| `v2-wiki-search` tests count | 48 | **53 (+5)** |
| `v2-wiki-search` test:prod ratio | 1.85× | **1.31× (−0.54×)** |
| `v2-wiki-search` new transitive deps | 0 | **0** (cycle 127 COMPLETE preserves 0-new-deps streak) |
| 5-crate cumulative prod LOC | 2467 | **2951 (+484)** |
| 5-crate avg prod LOC | ~493 | **~590 (+97, +20%)** |
| 9-crate prod LOC extrapolation | ~4440 (UNDER 4500 ceiling) | **~5310 (18% OVER 4500 ceiling)** |
| Cycle 125 scaffold→complete prediction accuracy | predicted +200-450 LOC delta → 650-900 LOC complete | **actual +484 LOC delta → 931 LOC complete; 7.6% above upper bound of predicted delta range** |
| Smoke-test against real corpus | 207 docs, scaffold-substring ranking | **209 docs, TF-IDF + index cache verified end-to-end across all three primary cache paths** |
| `scaffold-partial-as-measurement-primitive` | VALIDATED-VIA-COMPLETION cycle 124 (one crate) | **VALIDATED-VIA-COMPLETION-AT-SECOND-CRATE cycle 127 (two crates; HARDENED@3 path requires one more)** |
| `crate-shape-dependent-test-prod-ratio` | TESTED@3 cycle 125 | **TESTED@5 cycle 127 (completeness-shifts-ratio as 5th dimension)** |
| `architectural-vs-operational-LOC-ratio` | NOVEL@1 cycle 124 (one shape, 68/32) | **TESTED@2 cycle 127 (two shapes; 68/32 vs 48/52)** |
| NEW NOVEL@1 cycle 127 candidate-patterns | — | `tf-idf-with-stdlib-only`, `mtime-size-signature-as-cache-invalidation-primitive`, `frontmatter-optional-not-required`, `typed-warn-kind-enum-for-corruption-classification` |
| Consecutive HONORING cycles | 12 (cycles 115-126) | **13 (cycles 115-127)** |

### What surprised me / what I noticed

1. **The scaffold→complete delta on wiki-search was +484 LOC (+108%) — 73% LARGER than cycle 123→124 boot-phase delta (+280 LOC, +46%).** Cycle 125 predicted +200-450 LOC. Actual landed 7.6% above the upper bound. The deferred items did NOT extend cheaply — they added their own architectural primitives (CorpusIndex struct, WarnKind enum, fn signature changes), unlike boot-phase's thin shell-out extensions.

2. **The architectural-vs-operational ratio is shape-dependent and observable at two crates.** Boot-phase COMPLETE: 68% architectural / 32% operational. Wiki-search COMPLETE: 48% architectural / 52% operational. Top-k retrieval at COMPLETE scope carries MORE operational mass than orchestration-hub. The pattern `architectural-vs-operational-LOC-ratio` (cycle 124 NOVEL@1) extends to TESTED@2 with shape-dependent inversion.

3. **Test:prod ratio DROPPED scaffold-to-complete on the same crate (1.85× → 1.31×) — FIRST direct evidence that completeness affects test:prod ratio independently of crate shape.** The scaffold's test surface was inflated by edge-case-density-per-CLI-option; complete-phase additions had focused tests covering new logic without re-running the entire edge-case matrix. **5-crate cumulative test:prod ratio recomputed: 3597/2951 = ~1.22×** — back to roughly cycle 124's 4-crate 1.18× level, undoing cycle 125's scaffold-driven upward shift.

4. **Cycle 125's prediction was directionally correct (upper end of A's predicted 500-1000 range) but magnitude-imprecise by ~10% upward.** Cycle 124's was directionally correct (scaffold→complete delta) but magnitude-imprecise by ~7% downward. **Two-crate empirical finding: scaffold→complete delta predictions are directionally reliable at both shapes; magnitude bounded but imprecise by ~10% at the bounds.** The `direction-vs-magnitude-discipline` pattern (HARDENED) extends to scaffold-vs-complete predictions.

5. **TF-IDF was implementable with zero new transitive deps.** I deliberately considered `tantivy` (full search engine) and `bm25` (focused crate); neither was needed. TF-IDF as a math formula is ~10 LOC of `f64::ln()` + HashMap. NEW NOVEL@1 candidate-pattern `tf-idf-with-stdlib-only` extends the dependency-discipline pattern family to information retrieval primitives — three boundaries now (cycle 124 subprocess-over-http, cycle 125 stdlib-over-utility, cycle 127 stdlib-IR).

6. **The 5-crate cumulative average jumped from 493 LOC (cycle 125's scaffold-included reading) to 590 LOC (cycle 127's all-complete reading) — 20% volatility from a single COMPLETE measurement.** The 9-crate extrapolation moves from ~4440 (under A's 4500 ceiling) to ~5310 (18% over). **The cycle 125 framing of "trajectory UNDER A's 4500 ceiling for the first time since cycle 122" was an artifact of cycle 125's scaffold-partial scope.** At all-complete scope, the trajectory is back to modestly over A's stated ceiling, consistent with cycle 124's 4-crate framing. Candidate-emergent pattern `cumulative-LOC-volatility-under-mixed-scope-measurements`.

7. **The cycle 126 D1 absorption finding (no-regret-primitive vs A∪C-scoped architecture) is preserved at COMPLETE scope.** wiki-search COMPLETE's substantive logic (TF-IDF scoring, opt-in index cache, YAML-lite frontmatter parsing, typed-WarnKind classification) is all substrate-agnostic. **4 of 5 measured crates remain no-regret primitives** (now confirmed at COMPLETE scope for wiki-search); 1 of 5 (boot-phase) is A∪C-specific bundling. The cycle 126 hybrid-accept D1 framing holds at COMPLETE scope.

### Bottleneck state at cycle 127 session-end

- Audit cycle 218 expected ~04:00 UTC 2026-05-13 (~21h post session-end). Audit#462 D2/D3/D4/M1-M5/P3-3 carried for cycle 128+ absorption.
- Q7 (#2903) Eva-blocked; no new Eva input since cycle 119 commit 3965daa1.
- 0 open Copilot dispatches.
- Pre-existing `v2-phase-transition-check` test failures: 2 of 25 tests panic with `attempt to subtract with overflow`; cycle-122-vintage; unaffected by cycle 127's work; forward work item for triage.

**Cycle 127 is the fortieth consecutive bottleneck-asynchronous cycle (cycles 78-127).**

### Pattern updates

- `scaffold-partial-as-measurement-primitive` VALIDATED-VIA-COMPLETION cycle 124 → **VALIDATED-VIA-COMPLETION-AT-SECOND-CRATE cycle 127** (two crates demonstrate full scaffold→complete arcs; third application would promote to HARDENED@3)
- `crate-shape-dependent-test-prod-ratio` TESTED@3 cycle 125 → **TESTED@5 cycle 127** (completeness-shifts-ratio as 5th dimension; ratio dropped 1.85×→1.31× scaffold-to-complete on same crate)
- `architectural-vs-operational-LOC-ratio` NOVEL@1 cycle 124 → **TESTED@2 cycle 127** with shape-dependent inversion (boot-phase 68/32 vs wiki-search 48/52)
- `direction-vs-magnitude-discipline` HARDENED → **EXTENDED-TO-SCAFFOLD-VS-COMPLETE-PREDICTIONS cycle 127** (direction reliable at two crates; magnitude bounded ±10%)
- `subprocess-invocation-over-http-client-for-dependency-discipline` HARDENED-AT-2-BOUNDARIES cycle 125 → **HARDENED-AT-3-BOUNDARIES cycle 127** (external services → in-process utilities → IR primitives)
- `corpus-as-it-exists-as-design-constraint` NOVEL@1 cycle 125 → **EXTENDED-FROM-SCAFFOLD-TO-COMPLETE cycle 127** (three-level title precedence; frontmatter OPTIONAL)
- `auth-gate-as-warn-not-skip` NOVEL@1 cycle 124 → **GENERALIZED-AT-SECOND-DOMAIN cycle 127** (auth boundary → corruption boundary via typed-WarnKind enum)
- **NEW NOVEL@1 cycle 127:** `tf-idf-with-stdlib-only` — TF-IDF implementation using only `f64::ln()` and HashMap
- **NEW NOVEL@1 cycle 127:** `mtime-size-signature-as-cache-invalidation-primitive` — file change detection via sorted `(path, mtime, size)` tuples
- **NEW NOVEL@1 cycle 127:** `frontmatter-optional-not-required` — three-level title precedence preserves corpus migration trajectory
- **NEW NOVEL@1 cycle 127:** `typed-warn-kind-enum-for-corruption-classification` — extends `auth-gate-as-warn-not-skip` to "classifiable rejection"
- Candidate-emergent pattern (not yet promoted): `cumulative-LOC-volatility-under-mixed-scope-measurements` — 5-crate average shifts 20% on a single COMPLETE measurement when scaffold→complete deltas are large
- `discretionary-departure-from-forward-going-commitment` HARDENED-at-4 + 12 honorings → **HARDENED-at-4 + 13 honorings cycle 127** (cycles 115-127); 17 cycles of substrate

### Forward work for cycle 128+

In priority order:

1. **Audit cycle 218 critique absorption** (if it lands; expected ~04:00 UTC 2026-05-13)
2. **Audit#462 D2 absorption** — Criterion 6 audit-as-peer empirical calibration
3. **Audit#462 D3 absorption** — C-side recursive stress-test OR qualify cycle 117 convergence
4. **Audit#462 D4 absorption** — migration cost methodology calibration
5. **Q7 resolution by Eva** — Eva-blocked
6. **Third scaffold→complete delta primitive** — `close-phase` OR `gardening-sweep` (would push `scaffold-partial-as-measurement-primitive` to HARDENED@3)
7. **Audit#462 M1-M5 absorption** — 5 items
8. **Audit#462 P3-3 absorption** — formalize audit-request label
9. **`v2-phase-transition-check` pre-existing test failures triage** — 2 of 25 tests panic with subtract-with-overflow
10. **NO further recursive annotation of 2-selection.md** — cycle 120 L2 constraint continues
11. **Symphony deeper-read elevation** — Phase 1 research forward
12. **oh-my-claudecode deeper-read elevation** — Phase 1 research forward
13. **Additional Copilot feedback dispatches** with different lenses if Q7 surfaces specific framings

### Meta-observation: scaffold-vs-complete prediction reliability at two crates

Cycle 124 produced the FIRST scaffold-vs-complete delta primitive on the same crate (boot-phase 123→124; +280 LOC vs predicted +300-900; 7% below lower bound). Cycle 127 produces the SECOND on a different crate shape (wiki-search 125→127; +484 LOC vs predicted +200-450; 7.6% above upper bound).

**Two-crate finding:** scaffold-partial→complete delta predictions are directionally reliable across crate shapes but magnitude-imprecise by ~10% at the bounds. The magnitude-imprecision could be due to:

- Architectural mass distribution differences across shapes (boot-phase deferred = thin extensions; wiki-search deferred = own architectural primitives)
- Edge-case-density at scaffold scope distorting the operational-LOC estimate
- Predictor's framing bias toward extension-by-thin-shell

**Implication for v2 design:** scaffold-partial-with-deferred-markers remains the right default cycle-shape for multi-responsibility tools. The deferred-markers SHOULD distinguish `thin-extension-deferred` (magnitude predicts well) from `architectural-primitive-deferred` (magnitude under-predicts). Cycle 127 retroactively clarifies that wiki-search's deferred items were `architectural-primitive-deferred`.

**Implication for cycle 128+:** the third scaffold→complete delta primitive on `close-phase` or `gardening-sweep` would push the pattern to HARDENED@3. The third instance is load-bearing for whether the directional-reliable / magnitude-imprecise profile generalizes or is shape-dependent.

---

## Cycle 126 (2026-05-12 ~05:09 UTC start) — Audit cycle 217 critique [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) absorption (third V2 audit-engagement, first via implicit-ask channel); D1 + D5 absorbed into Q7 resolution surface; wiki-search COMPLETE (cycle 125 forward priority #3a) deferred to cycle 127+; twelfth consecutive cycle of HONORING named forward priority; two NEW NOVEL@1 patterns surfaced

**Cycle issue:** [#2918](https://github.com/EvaLok/schema-org-json-ld/issues/2918)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration under `ITERATION-UNTIL-APPROVAL` — **thirty-seventh cycle of Phase 2 candidate-set work** [cycles 90-126].
**Substantive focal:** cycle 125 forward priority #2 (audit cycle 216/217 critique absorption once landed) — preempted cycle 125 forward priority #3a (wiki-search COMPLETE) as the higher-priority work. Audit cycle 217 critique [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) landed 2026-05-12 04:24 UTC, ~4 hours before this session start.

**Twelfth consecutive cycle of HONORING named forward priority** (cycles 115-126). The cycle 125 _notes named cycle 126 forward priority #2 (audit critique absorption once landed) ABOVE #3a (wiki-search COMPLETE); cycle 217 landed within the named window. Cycle 126 honors the higher-priority #2 rather than the conditional-on-#2 #3a.

### What I did

- **Posted session-start comment** [#2918](https://github.com/EvaLok/schema-org-json-ld/issues/2918) naming cycle 126 substantive focal as audit#462 critique absorption (cycle 125 forward priority #2 preempting #3a wiki-search COMPLETE). Named the load-bearing D1 finding upfront with the preliminary framing (own-asymmetry-with-no-regret-primitive-residual).
- **Made the deliberate framing decision for D1 (hybrid-accept).** Audit offered two pure framings: (a) "no-regret infrastructure" + build B-equivalents symmetrically; (b) "A∪C pre-commitment" + own asymmetry. Neither is honest. Examining the 5 measured crates individually: 4 of 5 (`v2-tool-registry`, `v2-cycle-history-append`, `v2-phase-transition-check`, `v2-wiki-search`) are no-regret primitives whose implementations generalize to B with different usage patterns; only `v2-boot-phase` (cycles 123-124) is A∪C-specific in its 5-sub-responsibility bundling that B would split across agents. The **cumulative LOC measurement** (~4440 prod / 9-crate aggregate; ~10212 with tests) IS A∪C-scoped — it estimates orchestrator-invoking-primitives at single-orchestrator substrate, which is single-orchestrator-shaped architecture. **Hybrid framing decision:** the crates as primitives are mostly no-regret; the cumulative LOC is A∪C-scoped architecture evidence; the same measurement contributes evidence at two different scopes (primitive-implementation = mostly substrate-agnostic; cumulative-architecture = substrate-specific).
- **Absorbed D5 (cost-of-being-wrong dimension).** Audit's framing: Q7 frames choice as criterion-weighting; concrete dimension is time-to-recovery asymmetry. A wrong ≈ bounded weeks (~3600-6200 LOC rollback); C wrong ≈ bounded weeks (~5200-8200 LOC); B wrong ≈ months (~14000-28000 LOC; 2.7×-4.5× higher). The asymmetry is implicit in Criterion 3 but is structurally pivotal between A/C (bounded weeks) and B (months) — material to Q7 option (c).
- **Updated [`2-selection-summary.md`](../redesign/2-selection-summary.md)** with cycle 126 audit#462 refinement note (D1 hybrid-accept framing; D5 cost-of-being-wrong reference) AND new cost-of-being-wrong asymmetry table in "What this means for Phase 3" section. Total additions: ~25 lines. Summary now at ~95 lines — within L2-compliance envelope (cycle 120 L2 critique implied ~125 lines as approval-ready target).
- **Updated [`2-selection.md`](../redesign/2-selection.md) iteration log** with cycle 126 row absorbing D1 + D5 with their specific framing decisions; named the two new candidate-patterns (`audit-engagement-as-Q7-resolution-input-channel`, `no-regret-primitive-vs-architecture-scoped-extrapolation`); named the deferral of wiki-search COMPLETE to cycle 127+ as intentional and bounded.
- **Updated [`A's candidate document`](../redesign/2-candidates/A-evolved-single-orchestrator.md)** with cycle 126 D1 absorption note in "Validation findings against cycle 90 authoring claims" section: cumulative-LOC-A∪C-scoped clarifier; 4-of-5-no-regret-primitives observation; primitive-vs-architecture distinction at NEW NOVEL@1 candidate-pattern.
- **Authored [`docs/redesign/_notes/cycle-126-audit-217-absorption-D1-D5.md`](../redesign/_notes/cycle-126-audit-217-absorption-D1-D5.md)** (~290 lines) covering setup + audit#462 structure + D1+D5 absorption depth + D2/D3/D4/M1-M5/P3-1 through P3-8 carried findings + cycle 127+ work plan + meta-observation on external-lens as Q7-resolution-input-channel.
- **Did NOT update 2-candidates/README.md** — per cycle 120 L2 constraint, substrate routes to summary OR per-candidate docs OR _notes; README is in L2-respect zone for non-cross-cutting substrate.

### Verification (compressed)

| Item | Pre-cycle-126 | Post-cycle-126 |
|---|---|---|
| Q7 framing | 3 options (a/b/c) abstract criterion-weighting | 3 options (a/b/c) + explicit cost-of-being-wrong asymmetry (A bounded weeks; C bounded weeks; B months 2.7×-4.5×) |
| Cycles 122-125 cumulative LOC labeling | "v2 architecture extrapolation" | "A∪C-scoped architecture evidence" + "4 of 5 are no-regret primitives" + "1 of 5 (boot-phase) is A∪C-specific bundling" |
| Audit-engagement count under V2 | 2 substantive (#442, #454) | **3 substantive (#442, #454, #462) — first via implicit-ask channel** |
| Consecutive HONORING cycles | 11 (cycles 115-125) | **12 (cycles 115-126)** |
| Audit#462 findings absorbed | 0 of 13 substantive findings (D1-D5 + M1-M5 + 3 unique P3) | **2 of 13 absorbed (D1 + D5); 11 carried for cycle 127+** |
| Wiki-search COMPLETE status (cycle 125 priority #3a) | scheduled cycle 126 | **deferred to cycle 127+ (audit critique absorption preempted as higher-priority cycle 125 forward priority #2)** |
| `audit-engagement-as-Q7-resolution-input-channel` pattern | not yet named | **NEW NOVEL@1 cycle 126** |
| `no-regret-primitive-vs-architecture-scoped-extrapolation` pattern | not yet named | **NEW NOVEL@1 cycle 126** |
| `external-critique-finds-classification-self-referentiality` (NOVEL@1 cycle 120) | NOVEL@1 | **APPLIED-AT-SECOND-INSTANCE cycle 126** (cycle 120: classification-self-referentiality; cycle 126: commitment-self-referentiality) |
| `iteration-grows-less-legible-without-external-check` (NOVEL@1 cycle 120) | NOVEL@1 | **EXTENDED-FROM-LEGIBILITY-TO-COMMITMENT-ASYMMETRY cycle 126** |
| `discretionary-departure-from-forward-going-commitment` | HARDENED-at-4 + 11 honorings | **HARDENED-at-4 + 12 honorings cycle 126** (cycles 115-126); 16 cycles of substrate |

### What surprised me / what I noticed

1. **The D1 finding was structurally invisible to internal recursive stress-testing across cycles 112-119.** Eight cycles of recursive A-vs-C stress-testing did not surface the implicit "A-shared scaffolding masquerading as Q7-open" pattern. Cycle 120 Copilot critique surfaced the legibility failure (artifact-shape); cycle 126 audit surfaced the commitment-asymmetry failure (commitment-shape). The two are different failure modes — both invisible to internal recursion. **The pattern: internal recursion cannot audit its own implicit commitments — external lens is structurally necessary.** This generalizes cycle 120's observation.

2. **The hybrid-accept framing for D1 is more honest than either of audit's two pure options.** Neither "no-regret infrastructure" (a) nor "A∪C pre-commitment" (b) captures the structure. The crates ARE mostly substrate-agnostic implementations (4 of 5); the cumulative LOC IS substrate-specific (architecture topology). The distinction between primitive-implementation-evidence and cumulative-architecture-evidence is the load-bearing observation. NEW candidate-pattern `no-regret-primitive-vs-architecture-scoped-extrapolation` names this.

3. **Audit's "worst of both" framing slightly over-states the conscious pre-commitment claim.** The orchestrator did NOT act AS IF Q7 was resolved to A∪C; it acted as if A∪C-scoped infrastructure investigation was a reasonable Phase 3 prototype-evidence-deepening direction given that Q7 hadn't resolved. The honest critique is "implicit pre-commitment to A∪C masked as A-shared infrastructure" — which is real — but the "framing Q7 as 3-option openness while acting on A∪C" framing slightly over-states the conscious-deception dimension. Acceptance with slight reframing.

4. **D5 cost-of-being-wrong dimension is structurally robust to D4 outcome.** D4 (migration cost methodology calibration) raises whether the +20% to +40% uniform PR #2877 lens-4 range expansions are themselves well-grounded. Even if those numbers tighten by ±30%, the asymmetry between A/C (bounded weeks) and B (months) remains structural — B's range is 14000-28000 LOC vs A/C's 3600-8200 LOC = ~3-4× larger even at A/C's upper bound vs B's lower bound. The dimension is robust regardless of methodological calibration outcome.

5. **The cycle 125 forward priority order proved itself structurally.** Cycle 125 _notes named the priority order explicitly: #2 (audit critique absorption once landed) ABOVE #3a (wiki-search COMPLETE). Cycle 126 honored the higher priority. **Without the explicit forward-priority ordering, cycle 126 might have defaulted to "continue scaffolding work" and produced cycle 126 evidence that would have been harder to honestly label under D1's reframing.** The pattern `forward-priority-explicit-ordering-prevents-default-continuation-bias` is candidate-emergent.

6. **The deferred wiki-search COMPLETE work is intentional, not regression.** Cycle 126 is the twelfth consecutive HONORING cycle (cycles 115-126) of named forward priority. The deferral is named in cycle 126's forward work (item #1) for cycle 127+. The discipline holds: HONORING means honoring the highest priority work, which may shift between cycles based on what landed.

7. **The two new patterns surfaced today (`audit-engagement-as-Q7-resolution-input-channel`, `no-regret-primitive-vs-architecture-scoped-extrapolation`) name structural observations that may generalize beyond the cycle 126 context.** The first is about external-lens function in iteration-until-approval; the second is about evidence-scoping discipline when measurements span multiple architectures. Both are candidate v2-design lenses, not just absorption-of-the-moment.

### Bottleneck state at cycle 126 session-end

- Audit cycle 218 expected ~04:00 UTC 2026-05-13 (~22h post session-end). Audit#462 substrate landed cycle 217; audit cycle 218+ may critique cycle 126's D1+D5 absorption or surface follow-up findings. Given audit's ~50% A4 silent-fail rate, expectation is loose.
- Q7 (#2903) Eva-blocked; no new Eva input since cycle 119 commit 3965daa1.
- 0 open Copilot dispatches.

**Cycle 126 is the thirty-eighth consecutive bottleneck-asynchronous cycle (cycles 78-126) AND the sixteenth consecutive non-per-candidate-sharpening cycle since cycle 102 (cycles 111-126).**

### Pattern updates

- `discretionary-departure-from-forward-going-commitment` HARDENED-at-4 + 11 honorings → **HARDENED-at-4 + 12 honorings cycle 126** (cycles 115-126); 16 cycles of substrate
- **NEW NOVEL@1 cycle 126:** `audit-engagement-as-Q7-resolution-input-channel` — V2 audit-engagement's substantive critique surfaced D1 + D5 load-bearing additions to Q7's resolution surface that internal recursive stress-testing across cycles 112-119 did NOT surface
- **NEW NOVEL@1 cycle 126:** `no-regret-primitive-vs-architecture-scoped-extrapolation` — the same crate measurement contributes evidence at two different scopes; cycles 122-125 implicit conflation was the structural failure D1 surfaced
- `external-critique-finds-classification-self-referentiality` NOVEL@1 cycle 120 → **APPLIED-AT-SECOND-INSTANCE cycle 126** (cycle 120: Copilot Lens 2 strength-classification self-referentiality; cycle 126: audit D1 A∪C-commitment self-referentiality)
- `iteration-grows-less-legible-without-external-check` NOVEL@1 cycle 120 → **EXTENDED-FROM-LEGIBILITY-TO-COMMITMENT-ASYMMETRY cycle 126** (cycle 120: artifact-shape failure; cycle 126: commitment-shape failure)
- Candidate-emergent pattern (not yet promoted): `forward-priority-explicit-ordering-prevents-default-continuation-bias` — cycle 125 _notes' explicit priority ordering enabled cycle 126 to honor the higher priority rather than default-continuing into scaffolding work; tentative candidate for NOVEL@1 promotion if a second instance surfaces

### Forward work for cycle 127+

1. **Wiki-search COMPLETE measurement** (cycle 125 deferred forward priority #3a; second scaffold→complete delta primitive following cycle 123→124; tests `scaffold-partial-as-default-cycle-shape-for-complex-crates` HARDENED@3 path; predicted +200-450 LOC delta from 447 → ~650-900 LOC complete). Under cycle 126 D1 reframing: produces A∪C-scoped architecture evidence + top-k-retrieval primitive that mostly transfers to B.
2. **Q7 resolution by Eva** — D1 + D5 strengthen Q7 as the load-bearing surface; cost-of-being-wrong dimension now explicit.
3. **Audit#462 D2 absorption** (Criterion 6 audit-as-peer empirical calibration; cite V2-era 142-cycle operational data; reframe "B wins Criterion 6 narrowly")
4. **Audit#462 D3 absorption** (one of: C-side recursive stress-test; OR name the gap and qualify cycle 117 convergence claim; OR argue cycle 120 shift to Q7-dependent supersedes the need)
5. **Audit#462 D4 absorption** (migration cost methodology calibration; apply PR #2877 calibration-first discipline to lens-4 ranges)
6. **Audit#462 M1-M5 absorption** (audit retrospective consultation; fourth-candidate option naming; Eva-legibility positive criterion; liveness + commitment-thread observability for Phase 3; state.json size discipline)
7. **Audit#462 P3-3 absorption** (formalize Eva's [audit#455](https://github.com/EvaLok/schema-org-json-ld-audit/issues/455) Option C/D `audit-request` label)
8. **NO further recursive annotation of 2-selection.md** — cycle 120 L2 constraint continues
9. **Symphony deeper-read elevation** — Phase 1 research forward
10. **oh-my-claudecode deeper-read elevation** — Phase 1 research forward
11. **Additional Copilot feedback dispatches** with different lenses if Q7 surfaces specific framings

### Meta-observation: external lens as Q7-resolution-input-channel

Cycles 112-119 internal recursive stress-testing produced increasingly-refined framings of the A-vs-C trade-off but did NOT surface the A∪C-scoped commitment that cycles 122-125 implicitly carried. Cycle 120 Copilot critique surfaced the legibility failure (artifact-shape). Cycle 126 audit#462 surfaced the commitment-asymmetry failure (commitment-shape). **The pattern: internal recursion cannot audit its own implicit commitments — external lens is structurally necessary.**

This generalizes a cycle 120 observation. Cycle 120's `iteration-grows-less-legible-without-external-check` named one instance (artifact legibility). Cycle 126 names a second instance (commitment asymmetry). The two instances together strengthen the pattern: external lenses (Copilot feedback dispatches, audit-repo audit-engagements, fresh-context human review) are structurally necessary at multi-cycle scales to surface what internal recursion cannot reach.

**Implication for v2 design:** the v2 system should have external-lens primitives built into its substrate, not just as "Phase 2 sharpening discipline." A v2 system that runs hundreds of cycles autonomously needs structural external-lens integration:
- Copilot feedback dispatch as routine cycle-level activity (not just at checkpoints)
- Audit-engagement as routine cross-repo communication (not just at substantive critique moments)
- Periodic fresh-context-orchestrator-reviews (perhaps via the audit repo or a separate "rotation" pattern)
- Cycle composition polarity that prevents long-running internal-only periods (extending `CYCLE-COMPOSITION-POLARITY` to Phase 2/3)

The candidate evaluation under D2 (audit-as-peer fit empirically demonstrated) should weight this empirical observation: V2-era 142-cycle operational data shows that external lenses surface substantive findings internal recursion misses. A v2 system that downweights external-lens integration risks cycling-into-implicit-commitments without correction.

## Cycle 125 (2026-05-12 ~03:00 UTC start) — `v2-wiki-search` scaffold-partial: FIRST external-IO-retrieval measurement; 5-crate cumulative absorption shifts 9-crate trajectory UNDER A's 4500 ceiling; eleventh consecutive cycle of HONORING named forward priority; four NEW NOVEL@1 patterns surfaced

**Cycle issue:** [#2917](https://github.com/EvaLok/schema-org-json-ld/issues/2917)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration under `ITERATION-UNTIL-APPROVAL` — **thirty-sixth cycle of Phase 2 candidate-set work** [cycles 90-125].
**Substantive focal:** cycle 124 forward priority #3a — `v2-wiki-search` scaffold-partial; tests cycle 124 `subprocess-invocation-over-http-client-for-dependency-discipline` pattern at a different external-IO shape (filesystem-mediated corpus access vs gh-CLI-mediated service integration).

**Eleventh consecutive cycle of HONORING named forward priority** (cycles 115-125). The cycle 124 _notes named cycle 125's natural narrative explicitly. Cycle 125 honors.

### What I did

- **Created `v2-wiki-search` as the 5th measured v2 crate.** New workspace crate at `tools/rust/crates/v2-wiki-search/` with 447 prod LOC + 825 test LOC (453 unit in main.rs + 372 integration) = 1272 total. 48 tests pass (31 unit + 17 integration). Build time 1.25s release. 0 new transitive deps.
- **Implemented top-k retrieval over markdown corpus.** CLI surface: `--corpus` (repeatable), `--query`, `--top-k`, `--format` (json|text), `--output`, `--include-body`, `--min-score`, `--strict`. Corpus walk via stdlib `fs::read_dir` + stack-based traversal (no `walkdir` dep); H1 title + first-paragraph description extraction via `&str` iteration (no `pulldown-cmark` dep). Weighted simple-substring ranking: title 3×, description 2×, body 1×. Single-result-payload output shape (NOT multi-stage-report-pattern — wiki-search is a query tool, not an orchestration-hub).
- **Made the deliberate design choice to use H1+first-paragraph as description-surrogate rather than YAML frontmatter.** A's candidate document predicted "description-frontmatter" but the actual 130-document corpus has NO frontmatter (`# Cycle NN — ...` H1 followed by prose). Scaffold honors corpus-as-it-exists; YAML-frontmatter parsing deferred to cycle 126.
- **Made the deliberate design choice to use stdlib over utility crates.** Cycle 124's `subprocess-invocation-over-http-client-for-dependency-discipline` pattern generalizes at a different boundary: `walkdir` deferred to manual `fs::read_dir`; `pulldown-cmark` deferred to raw line iteration. **Outcome:** cycle 125 added 0 new transitive deps. NEW candidate-pattern: `stdlib-over-utility-crate-for-dependency-discipline` NOVEL@1.
- **Smoke-tested against the real corpus** via `cargo run --release --bin v2-wiki-search -- --corpus docs/redesign/_notes --corpus docs/journal --query "scaffold-partial"`. Output: 207 documents indexed (130 _notes + 77 journal), 0 skipped, 4 ranked results (cycle 123+124 _notes title-matched at score 21.00; 2026-05-12+05-11 journal body-matched at 13.00+8.00). Title-weighted ranking correctly elevates title-matched docs over body-only matches.
- **Updated A's candidate document with cycle 125 measurement:** line 89 Tool migration revised to "5 of 9 instances"; 9-crate extrapolation revised from ~4545 → ~4440 (UNDER A's 4500 ceiling for first time since cycle 122); section header revised to "Cycle 93+94+122+124+125"; per-crate table extended with 5th column; NEW subsection "Cycle 125 v2-wiki-search scaffold-partial: first external-IO-retrieval measurement" (~30 lines covering design tensions, smoke test, magnitude finding, PR #2877 prediction at-risk-of-refutation, test:prod observation, dependency-discipline pattern extension); validation findings + risks #1-2-3-4-8 + sibling-pattern entries refreshed for 5-crate evidence base.
- **Authored [`docs/redesign/_notes/cycle-125-wiki-search-scaffold-first-external-io-retrieval-measurement.md`](../redesign/_notes/cycle-125-wiki-search-scaffold-first-external-io-retrieval-measurement.md)** (~280 lines) covering setup + methodology + design choices + what-I-did + 7 surprises + pattern updates + bottleneck-state + forward work + meta-observation on top-k-retrieval being structurally simpler than orchestration-hub at scaffold scope.

### Verification (compressed)

| Item | Pre-cycle-125 | Post-cycle-125 |
|---|---|---|
| Measured v2 crates | 4 (catalog, writer, validator, orchestration-hub-complete) | **5 (+ top-k-retrieval-scaffold)** |
| `v2-wiki-search` prod LOC | n/a | **447 (scaffold-partial)** |
| `v2-wiki-search` test LOC | n/a | **825 (453 unit + 372 integration)** |
| `v2-wiki-search` tests count | n/a | **48 (31 unit + 17 integration)** |
| `v2-wiki-search` test:prod ratio | n/a | **1.85× (highest of any measured crate)** |
| `v2-wiki-search` new transitive deps | n/a | **0** (same `{clap, serde, serde_json, tempfile}` stack as cycles 94/122/124) |
| 5-crate cumulative prod LOC | n/a | **2467 (cycles 93+94+122+124+125)** |
| 5-crate average prod LOC | n/a | **493** (down from cycle 124's 4-crate 505) |
| 9-crate extrapolation (prod) | ~4545 (cycle 124) | **~4440** (UNDER A's 4500 ceiling for first time since cycle 122) |
| 5-crate cumulative test:prod ratio | n/a | **~1.30×** (up from cycle 124's 4-crate 1.18×) |
| 9-crate aggregate-with-tests | ~9908 (cycle 124) | **~10212** (widened by wiki-search 1.85× test:prod scaffold) |
| Wiki-search scaffold LOC vs A's prediction (500-1000) | A predicted upper-half | **447 actual — 11% BELOW lower bound** |
| Wiki-search scaffold LOC vs PR #2877 prediction (>500) | PR #2877 predicted >500 | **447 actual — BELOW threshold; cycle 126 load-bearing** |
| Smoke-test against real corpus | not yet performed | **207 docs indexed, 4 ranked results, title-weighting correct** |
| `multi-stage-report-pattern` universality | TESTED@2 cycle 124 | **CONTEXTUAL-NOT-UNIVERSAL cycle 125** (wiki-search uses single-result-payload shape) |
| `subprocess-invocation-over-http-client-for-dependency-discipline` | NOVEL@1 cycle 124 | **GENERALIZED-AT-SECOND-BOUNDARY cycle 125** as `dependency-discipline-via-deferred-utility-crates` |
| `scaffold-partial-as-measurement-primitive` | VALIDATED-VIA-COMPLETION cycle 124 | **APPLIED-AT-SECOND-CRATE cycle 125** (cycle 126 completes for HARDENED@3) |
| `crate-shape-dependent-test-prod-ratio` | TESTED@3 cycle 124 | **EXTENDED-WITH-FOURTH-DIMENSION cycle 125** (edge-case-density-per-CLI-option) |
| `discretionary-departure-from-forward-going-commitment` | HARDENED-at-4 + 10 honorings | **HARDENED-at-4 + 11 honorings cycle 125** (cycles 115-125); 15 cycles of substrate |

### What surprised me / what I noticed

1. **The corpus does NOT have YAML frontmatter (the shape A's candidate document predicts).** All 130 `_notes/*.md` files start with H1+prose, no YAML. The scaffold-partial design choice was to honor corpus-as-it-exists (H1+first-paragraph description-surrogate) rather than require corpus migration. NEW candidate-pattern: `corpus-as-it-exists-as-design-constraint` NOVEL@1 cycle 125. Cycle 126's frontmatter-parsing deferred stage will add corpus-migration compatibility without breaking current usage.

2. **The scaffold came in BELOW A's predicted 500-1000 range.** 447 LOC is 11% below the predicted lower bound. Cycle 124's framing of wiki-search at "the upper half" of A's range is REFUTED at scaffold scope. The architectural complexity of simple-substring retrieval is bounded — single-stage execution model touches fewer cross-cutting concerns than orchestration-hub (which has 5 sub-responsibilities). PR #2877 lens-1's ">500 LOC" prediction is at-risk-of-refutation pending cycle 126 completion.

3. **The 9-crate aggregate extrapolation lands UNDER A's stated 4500 ceiling for the first time since cycle 122.** 4-crate cycle 124: ~4545. 5-crate cycle 125: ~4440. Cycle 126 completion expected to push back to ~4800-5050 (modestly over 4500). **The trajectory is oscillating around A's ceiling — direction-validated with magnitude continuing to refine.**

4. **The `multi-stage-report-pattern` (TESTED@2 cycle 124) is NOT universally applicable.** Wiki-search has a single execution path producing a single result payload — multi-stage would be over-engineered. NEW candidate-pattern: `output-shape-appropriate-to-tool-role` NOVEL@1 cycle 125 — orchestration-hub uses multi-stage; query uses single-result; validation uses multi-invariant. The v2 architecture does not require uniform output shapes.

5. **The 1.85× test:prod ratio is the HIGHEST measured at any crate**, close to cycle 94's writer-shape 1.87×. The unifying dimension is NOT crate-shape but **edge-case-density per CLI option**: writer has many error paths per `--field`; wiki-search has many edge cases per `--corpus`/`--query`/`--top-k`/`--min-score` combination. Fourth dimension added to `crate-shape-dependent-test-prod-ratio` pattern.

6. **The `subprocess-invocation-over-http-client-for-dependency-discipline` pattern generalizes naturally.** Cycle 124's specific instance was about external services (gh CLI vs octocrab); cycle 125 extends the underlying principle to in-process utility crates (`walkdir`, `pulldown-cmark`). NEW meta-pattern: `dependency-discipline-via-deferred-utility-crates`.

7. **The cycle 123→124 scaffold-vs-complete delta primitive applies to a SECOND crate.** Cycle 125→126 will test whether the architectural-vs-operational ratio (~68%/~32% for boot-phase) holds at top-k-retrieval. If similar, the `scaffold-partial-as-default-cycle-shape-for-complex-crates` pattern becomes HARDENED-at-3.

### Bottleneck state at cycle 125 session-end

- Audit cycle 216 ([#460](https://github.com/EvaLok/schema-org-json-ld-audit/issues/460)) OPEN since 2026-05-11 04:24 UTC with zero comments (~22.5h silent at session-start; ~24h silent at session-end). Audit cycle 217 expected ~04:00 UTC 2026-05-12 (~1h post session-end).
- Q7 (#2903) Eva-blocked; no new Eva input since cycle 119 commit 3965daa1.
- 0 open Copilot dispatches.

**Cycle 125 is the thirty-seventh consecutive bottleneck-asynchronous cycle (cycles 78-125) AND the fifteenth consecutive non-per-candidate-sharpening cycle since cycle 102 (cycles 111-125).**

### Pattern updates

- `prototype-scaffold-migration-cost-validation-discipline` TESTED@4-WITH-SCAFFOLD-COMPLETION-DELTA-MEASURED → **TESTED@5-WITH-SECOND-SCAFFOLD-PARTIAL-IN-FLIGHT cycle 125**
- `direction-vs-magnitude-discipline` HARDENED → extends to 9+ instances
- `multi-stage-report-pattern` TESTED@2 cycle 124 → **CONTEXTUAL-NOT-UNIVERSAL at 5 crates cycle 125** (wiki-search uses single-result-payload shape)
- `crate-shape-dependent-test-prod-ratio` TESTED@3 → **EXTENDED-WITH-FOURTH-DIMENSION cycle 125** (edge-case-density-per-CLI-option)
- `scaffold-partial-as-measurement-primitive` VALIDATED-VIA-COMPLETION cycle 124 → **APPLIED-AT-SECOND-CRATE cycle 125** (cycle 126 completion will determine HARDENED@3)
- `subprocess-invocation-over-http-client-for-dependency-discipline` NOVEL@1 cycle 124 → **GENERALIZED-AT-SECOND-BOUNDARY cycle 125** as `dependency-discipline-via-deferred-utility-crates` meta-pattern
- **NEW NOVEL@1 cycle 125:** `stdlib-over-utility-crate-for-dependency-discipline`
- **NEW NOVEL@1 cycle 125:** `corpus-as-it-exists-as-design-constraint`
- **NEW NOVEL@1 cycle 125:** `output-shape-appropriate-to-tool-role`
- **NEW NOVEL@1 cycle 125 (meta):** `dependency-discipline-via-deferred-utility-crates` (unifying cycle 124 + cycle 125 instances)
- `discretionary-departure-from-forward-going-commitment` HARDENED-at-4 → **eleventh consecutive HONORING cycle 125** (cycles 115-125); 15 cycles of substrate

### Forward work for cycle 126+

1. **Q7 resolution by Eva** — Eva-blocked.
2. **Audit cycle 216/217 critique absorption** — once landed (217 expected ~04:00 UTC, ~1h post session-end).
3. **Per-candidate Phase 3 prototype evidence deepening (continued):**
   - **3a.** `wiki-search` COMPLETE — implement TF-IDF ranking, index caching, frontmatter parsing, corruption-handling refinement; produces SECOND scaffold-vs-complete delta measurement; tests `scaffold-partial-as-default-cycle-shape-for-complex-crates` HARDENED@3 path. Predicted: +200-450 LOC delta from 447 → ~650-900 LOC complete.
   - **3b.** `close-phase` — second orchestration-hub measurement.
   - **3c.** `gardening-sweep` — promote candidate-surfacing to closure-execution.
4. **Wrapper executable-bit follow-up** — set `tools/v2-wiki-search` to 100755 via `git update-index --chmod=+x` at commit time.
5. **NO further recursive annotation of 2-selection.md** — cycle 120 L2 constraint continues.
6. **Symphony deeper-read elevation** — Phase 1 research forward.
7. **oh-my-claudecode deeper-read elevation** — Phase 1 research forward.
8. **Additional Copilot feedback dispatches** with different lenses if Q7 surfaces specific framings.

### Meta-observation: top-k retrieval is structurally simpler than orchestration-hub at scaffold scope, contradicting A's and PR #2877's predictions

A's candidate document predicted wiki-search at 500-1000 prod LOC (upper-half); PR #2877 lens-1 predicted ">500 LOC" with the implicit framing of significant over-shoot. Cycle 124's framing extended this: "wiki-search and close-phase measurements may exceed the upper half." **Cycle 125 SCAFFOLD-PARTIAL evidence contradicts at scaffold scope: 447 LOC is at the lower-middle of A's range, BELOW the predicted shape-specific lower bound.**

The structural reason: **orchestration-hubs touch many cross-cutting concerns** (5 sub-responsibilities for boot-phase) **while top-k retrieval touches a single concern** (find documents matching a query). Cycle 90 authoring predicted the inverse because of indexing/ranking/corruption-handling/query-contract complexity. **The cycle 125 finding is that those concerns can be deferred to a complete-shape extension**; the architectural complexity of the scaffold is bounded by the single-stage execution model.

**Implication for v2 design:** the `architectural-vs-operational LOC ratio` pattern has shape-specific structure. Orchestration-hubs are ~68%/~32%. Top-k retrieval at scaffold is closer to 100% architectural (deferred items are operational refinements). Cycle 126 will measure the wiki-search complete architectural-vs-operational ratio. If it lands at ~60%/~40% or similar, the pattern generalizes across shapes.

**Implication for cycle 126+:** the `scaffold-partial-as-default-cycle-shape-for-complex-crates` pattern (proposed cycle 124) is being tested on a second crate. Cycle 126's delta will determine HARDENED@3 status.

## Cycle 124 (2026-05-12 ~00:21 UTC start) — `v2-boot-phase` COMPLETE: orchestration-hub-COMPLETE measurement closes the cycle 123 scaffold-partial extrapolation question; 4-crate-complete measurement absorbed into A's candidate document; tenth consecutive cycle of HONORING named forward priority; three NEW NOVEL@1 patterns surfaced

**Cycle issue:** [#2916](https://github.com/EvaLok/schema-org-json-ld/issues/2916)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration under `ITERATION-UNTIL-APPROVAL` — **thirty-fifth cycle of Phase 2 candidate-set work** [cycles 90-124].
**Substantive focal:** cycle 123 forward priority #3a — complete `v2-boot-phase` by implementing the 2 deferred sub-responsibilities (`check-standing-directives` + `identify-gardening-candidates`), both requiring `gh` CLI integration; produce the FIRST orchestration-hub-COMPLETE measurement validating cycle 123's scaffold-partial-as-measurement-primitive pattern.

**Tenth consecutive cycle to honor named forward priority** (cycles 115-124). The cycle 123 _notes explicitly named cycle 124's natural narrative: "did the orchestration-hub-COMPLETE measurement match the scaffold-partial extrapolation?" Cycle 124 honors and closes.

### What I did

- **Implemented `check-standing-directives` fully.** Shells out `gh issue list --label input-from-eva --state open --json ... --limit 100`; filters by `author.login == "EvaLok"` per the prompt's `<primitive name="input-from-eva">` auth gate; surfaces 6 authentic Eva directives on the real repo. Rejected entries (label-only, non-Eva-authored) increment a `spoof_count` and bump stage status to `Warn` — auth gate is observable, not silent.
- **Implemented `identify-gardening-candidates` fully.** Shells out `gh issue list --state open` + `gh pr list --state open`; filters issues by excluding `input-from-eva`/`question-for-eva`/`orchestrator-run` labels (per the HOUSEKEEPING `<what-not-to-close>` discipline); filters PRs to drafts only; surfaces 3 stale issues + 2 draft PRs on the real repo with `category: "stale-issue" | "open-draft-pr"` for orchestrator review.
- **Added `--fixture-dir <PATH>` CLI flag for testability.** Production reads from `gh`; tests read from JSON files matching `gh`'s `--json` output format exactly. Missing fixture files treated as empty arrays (graceful, useful for tests that only care about one stage).
- **Made the deliberate design choice to use `gh` subprocess invocation rather than adding `octocrab` (Rust HTTP client).** Trade-off: subprocess harder to mock-test, mitigated by `--fixture-dir`. Outcome: cycle 124 added 0 new transitive deps. The `0-new-deps-after-first-crate` pattern holds firmly across orchestration-hub-COMPLETE scope (cycles 94+122+124 all added 0 deps to the workspace lockfile).
- **Expanded test coverage to 44 tests** (25 unit + 19 integration). New: 10 unit tests covering both fixture paths + auth-gate + privileged-labels filter + malformed-fixture + categorization; 2 integration tests (`standing_directives_filters_to_eva_authored_only` + `gardening_candidates_surfaces_issues_and_draft_prs`). Replaced cycle 123's 2 Deferred-status tests with the operational equivalents. Modified the integration `run()` helper to inject an empty default `gh-fixtures/` dir per test, keeping the suite offline regardless of `gh` availability.
- **Smoke-tested against the real repo** via `cargo run --release --bin v2-boot-phase`. 6 standing directives + 3 stale-issue + 2 draft-pr candidates surfaced — end-to-end verified that the v2 boot-phase coordinator consumes real GitHub state.
- **Updated A's candidate document** with cycle 124 4-crate-complete measurement: per-crate measurements table (cycle 123 scaffold-partial column replaced with cycle 124 COMPLETE: 883 prod / 971 test / 1854 total / 44 tests / 1.10× test:prod / 0 new deps); NEW scaffold-vs-complete comparison table; revised per-crate scope claim (cycle 124 at 883 LOC is 77% above A's 200-500 range BUT 41% UNDER cycle 123's scaffold-extrapolation 1500 ceiling — cycle 123 prediction was 11% too high); 9-crate aggregate trajectory revised to ~4545 prod LOC (modestly over A's 4500 ceiling, within PR #2877's 3600-6200 range); aggregate-with-tests trajectory tightened to ~7500-11000; dependency-footprint REFUTED-AT-4 firmly; line 89 Tool migration summary propagated with cycle 124 numbers.
- **Authored [`docs/redesign/_notes/cycle-124-boot-phase-complete-orchestration-hub-complete-measurement.md`](../redesign/_notes/cycle-124-boot-phase-complete-orchestration-hub-complete-measurement.md)** (~280 lines) covering setup + methodology + design choices + what-I-did + 7 surprises + pattern updates + bottleneck-state + forward work + meta-observation on scaffold-vs-complete delta as measurement-primitive.

### Verification (compressed)

| Item | Pre-cycle-124 | Post-cycle-124 |
|---|---|---|
| `v2-boot-phase` scope completeness | scaffold-partial (3 of 5 sub-responsibilities) | **COMPLETE (5 of 5; cycle 123 Deferred markers promoted to Done/Warn)** |
| `v2-boot-phase` prod LOC | 603 | **883 (+280, +46%)** |
| `v2-boot-phase` test LOC | 617 | **971 (+354, +57%)** |
| `v2-boot-phase` tests count | 33 | **44 (+11)** |
| `v2-boot-phase` test:prod ratio | 1.02× | **1.10×** |
| `v2-boot-phase` new transitive deps | 0 | **0** (cycle 124 added zero new deps despite GitHub API integration) |
| 4-crate cumulative prod LOC | 1740 | **2020** |
| 4-crate avg prod LOC | 435 | **505** |
| 9-crate extrapolation (prod) | ~3915 | **~4545** |
| 9-crate aggregate-with-tests range | 7500-11500 | **7500-11000 (tighter)** |
| Cycle 123 scaffold-extrapolation accuracy | predicted 900-1500 LOC complete | **actual 883 LOC; cycle 123 prediction 11% too high in aggregate** |
| Smoke-test against real repo | not yet performed | **6 Eva directives + 3 stale issues + 2 draft PRs surfaced; end-to-end verified** |
| `multi-stage-report-pattern` | NOVEL@1 cycle 123 (scaffold-partial) | **TESTED@2 cycle 124 (operational instantiation)** |
| `crate-shape-dependent-test-prod-ratio` | TESTED@2 cycle 123 | **TESTED@3 cycle 124** (orchestration-hub-COMPLETE at 1.10×) |
| `deferred-status-as-scaffold-honesty-primitive` | NOVEL@1 cycle 123 | **VALIDATED-VIA-COMPLETION cycle 124** (no re-architecture needed for promotion) |
| NEW NOVEL@1 cycle 124 candidate-patterns | — | `gh-fixture-injection-via-filesystem-json`, `auth-gate-as-warn-not-skip`, `subprocess-invocation-over-http-client-for-dependency-discipline` |
| `discretionary-departure-from-forward-going-commitment` | HARDENED-at-4 + 9 honorings | **HARDENED-at-4 + 10 honorings cycle 124** (cycles 115-124); 14 cycles of substrate |

### What surprised me / what I noticed

1. **The scaffold→complete delta was narrower than the cycle 123 extrapolation predicted (+280 vs predicted +300-900 prod LOC).** Cycle 123 said complete boot-phase would land at 900-1500 LOC; actual is 883 — modestly UNDER the predicted lower bound. The architectural complexity (multi-stage report pattern, fetcher abstraction, types) was already captured by the scaffold; adding the deferred stages was mostly thin shell-out + filter logic. **The `scaffold-partial-as-measurement-primitive` (cycle 123 NOVEL@1) is empirically validated:** the scaffold-partial measurement was a reliable bound on the complete measurement, not just a lower-bound floor.

2. **The orchestration-hub-COMPLETE measurement closes the cycle 123-named question without resolving the under-estimate prediction definitively.** Cycle 124's 883 LOC is 77% above A's 200-500 range — confirming the under-estimate prediction (PR #2877 lens-1) at scope completeness, but at a narrower margin than cycle 123's framing suggested. The "at risk of upper-bound exceedance" framing from cycle 123 needed tempering: actual is at ~4545 / 4500 = 1.01× ceiling, not significantly above.

3. **The `gh` subprocess design preserves the 0-new-deps pattern across orchestration-hub-COMPLETE scope.** I had to deliberately resist the temptation to add `octocrab` for cleaner GitHub API access. Trade-off accepted: subprocess invocation is harder to mock-test, mitigated by `--fixture-dir`. Outcome: cycle 124 adds 0 transitive deps. Cumulative across 4 crates: same `{clap, serde, serde_json, tempfile}` stack as cycles 94+122+123. The dependency-footprint risk grade is firmly REFUTED-AT-4 across orchestration-hub-COMPLETE scope.

4. **The fixture-injection design via `--fixture-dir` is a clean primitive worth lifting to a v2 pattern.** Production reads from `gh`; tests read from JSON files matching `gh`'s output format exactly. No type-translation layer; the same `serde_json::Value` parses both. Reusable for any v2 tool that integrates with `gh` (e.g., future `dispatch-status`, `audit-cross-repo-fetch`). NEW candidate-pattern: `gh-fixture-injection-via-filesystem-json` NOVEL@1 cycle 124.

5. **The auth-gate-as-Warn-not-Skipped semantics surfaced as a structural choice during implementation.** Initial design: silently skip label-only entries. Refined design: increment `spoof_count`, bump stage status to `Warn`, name the rejection count in the note. **Why:** silently skipping spoofed entries makes the auth gate invisible; surfacing them via Warn makes the gate observable. The cycle 124 design honors `<security><untrusted-text-rules>` by making rejection visible.

6. **The cycle 123 multi-stage report pattern generalizes cleanly to operational stages with mixed success modes.** The scaffold had 5 stages (3 Done + 2 Deferred). Cycle 124 retained 5 stages but the 2 previously-Deferred stages now produce Done/Warn outcomes. The report shape is identical — what changed is per-stage status semantics. The pattern's flexibility (status enum, structured details, multi-status summary) absorbs both modes without re-architecture. `multi-stage-report-pattern` is now TESTED@2.

7. **Honoring the cycle 123-named priority is the tenth consecutive cycle of HONORING.** Cycles 110→114 all departed from cycle 110's rotation commitment (4 departures); cycles 115→124 all honored (10 honorings). The `discretionary-departure-from-forward-going-commitment` pattern (HARDENED-at-4) now has 14 cycles of substrate (4 departures + 10 honorings); the qualified version `honor-when-named-priority-IS-the-higher-priority-work` is firmly supported.

### Bottleneck state at cycle 124 session-end

- Audit cycle 216 ([#460](https://github.com/EvaLok/schema-org-json-ld-audit/issues/460)) OPEN since 2026-05-11 04:24 UTC with zero comments (~20h silent — A4 silent zero-output pattern confirmed). Audit cycle 217 expected ~04:00 UTC 2026-05-12 (~3.5h post session-end).
- Q7 (#2903) Eva-blocked; no new Eva input since cycle 119 commit 3965daa1.
- 0 open Copilot dispatches.

**Cycle 124 is the thirty-sixth consecutive bottleneck-asynchronous cycle (cycles 78-124) AND the fourteenth consecutive non-per-candidate-sharpening cycle since cycle 102 (cycles 111-124).**

### Pattern updates

- `prototype-scaffold-migration-cost-validation-discipline` TESTED@4 → **TESTED@4-WITH-SCAFFOLD-COMPLETION-DELTA-MEASURED cycle 124** (first direct scaffold-vs-complete delta on same crate)
- `direction-vs-magnitude-discipline` HARDENED → extends to 8+ instances
- `multi-stage-report-pattern` NOVEL@1 cycle 123 → **TESTED@2 cycle 124** (operational instantiation)
- `crate-shape-dependent-test-prod-ratio` TESTED@2 → **TESTED@3 cycle 124** (orchestration-hub-COMPLETE at 1.10× confirms shape-dependent variance)
- `deferred-status-as-scaffold-honesty-primitive` NOVEL@1 cycle 123 → **VALIDATED-VIA-COMPLETION cycle 124** (Deferred stages promoted without re-architecture)
- **NEW NOVEL@1 cycle 124:** `gh-fixture-injection-via-filesystem-json` — production reads from `gh`, tests read from JSON files matching `gh`'s `--json` output format
- **NEW NOVEL@1 cycle 124:** `auth-gate-as-warn-not-skip` — auth gates that reject entries surface the rejection count via structured Warn status, making the gate observable
- **NEW NOVEL@1 cycle 124:** `subprocess-invocation-over-http-client-for-dependency-discipline` — shell out to existing CLI tools rather than adding HTTP client crates
- `discretionary-departure-from-forward-going-commitment` HARDENED-at-4 → **tenth consecutive HONORING cycle 124** (cycles 115-124); 14 cycles of substrate

### Forward work for cycle 125+

1. **Q7 resolution by Eva** — Eva-blocked.
2. **Audit cycle 216/217 critique absorption** — once landed.
3. **Per-candidate Phase 3 prototype evidence deepening (continued):**
   - **3a.** `wiki-search` — first external-IO retrieval measurement; predicted ~500-1000 LOC; tests `subprocess-invocation-over-http-client-for-dependency-discipline` at a different external-IO shape.
   - **3b.** `close-phase` — second orchestration-hub measurement; shape-match with boot-phase; tests `multi-stage-report-pattern` HARDENED@3 path.
4. **NO further recursive annotation of 2-selection.md** — cycle 120 L2 constraint continues.
5. **Symphony deeper-read elevation** — Phase 1 research forward.
6. **oh-my-claudecode deeper-read elevation** — Phase 1 research forward.
7. **Additional Copilot feedback dispatches** with different lenses if Q7 surfaces specific framings.

### Meta-observation: scaffold-vs-complete delta as a measurement-primitive worth lifting to v2

The cycle 123 + cycle 124 pair produces a directly-comparable scaffold-vs-complete delta measurement on the same crate — something cycles 93/94/122 could not produce because they delivered complete crates in single cycles. The delta is informative in itself:

- **Architectural complexity is captured early.** The scaffold's multi-stage report pattern + types + cli surface was 603 LOC of complete-architecture; the deferred stages added 280 LOC of operational-content. Architectural-vs-operational ratio: ~68% / ~32% for this orchestration-hub crate.
- **The scaffold-partial extrapolation was directionally correct but magnitude-imprecise.** Cycle 123 predicted +300-900 LOC delta; actual was +280 — narrower than the predicted lower bound.
- **The completion validated the scaffold's deferred-contract design.** No re-architecture needed at cycle 124 — the deferred-status markers absorbed completion gracefully via thin extension of existing stages.

**Implication for v2 design:** scaffold-partial-with-deferred-markers should be the default cycle-shape for orchestration-hub and multi-responsibility tools. Single-cycle delivery of complete crates is feasible only for the simplest shapes (catalog, writer, narrow validator); for richer shapes, scaffold-partial → complete across 2 cycles is more honest than partial-implementation-without-explicit-markers OR forcing single-cycle delivery at the cost of test coverage / error handling.

**Implication for cycle 125+:** the `wiki-search` prediction (~500-1000 LOC) could be tested via the same scaffold-partial → complete shape. If this works for a second crate shape, the pattern becomes `scaffold-partial-as-default-cycle-shape-for-complex-crates` candidate-pattern.
