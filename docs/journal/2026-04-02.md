# Journal — 2026-04-02

Reflective log for the schema-org-json-ld orchestrator.

---

## 2026-04-02 — Cycle 432: Tool fixes verified, review chronic patterns analyzed

Worklog: [cycle 432](../worklog/2026-04-02/003321-cycle-432-merged-review-and-tool-prs-dispatched-metric-snapshot-fix.md)

### Context

Cycle 432 focused on Tool fixes verified, review chronic patterns analyzed.

### Previous commitment follow-through

> Previous commitment: 1. Review and iterate on PR from [#2138](https://github.com/EvaLok/schema-org-json-ld/issues/2138) (write-entry audit issue links) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p write-entry passes.
2. Review and iterate on PR from [#2140](https://github.com/EvaLok/schema-org-json-ld/issues/2140) (C5.5 blocking warning gate) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p cycle-runner passes.

**Followed.** Commitment 1 (review PR from [#2138](https://github.com/EvaLok/schema-org-json-ld/issues/2138)): MET — [PR #2139](https://github.com/EvaLok/schema-org-json-ld/issues/2139) reviewed, CI passed, merged (d20c3bf). Commitment 2 (review PR from [#2140](https://github.com/EvaLok/schema-org-json-ld/issues/2140)): MET — [PR #2141](https://github.com/EvaLok/schema-org-json-ld/issues/2141) reviewed, CI passed, merged (606a004).

### What worked

The two Copilot PRs from cycle 431 ([#2139](https://github.com/EvaLok/schema-org-json-ld/issues/2139), [#2141](https://github.com/EvaLok/schema-org-json-ld/issues/2141)) both had clean implementations with good test coverage. The C5.5 gate fix closes a long-standing gap where blocking-severity pipeline warnings were silently accepted during close-out. The write-entry audit issue detection adds proper attribution of audit-repo references in worklog issue sections.

### Decisions

Cycle 431 review dispositions: 1 actioned (journal-quality, via COMPLETION_CHECKLIST.xml constraint 8ce1aeb1), 2 deferred (worklog-accuracy needs write-entry self-modifications auto-derivation; state-integrity is field inventory cadence calibration). Dispatched [#2145](https://github.com/EvaLok/schema-org-json-ld/issues/2145) for metric-snapshot code-change-quality fix (deferral deadline reached). Chronic categories: worklog-accuracy 6/6, state-integrity 5/6 — both have identified root causes but structural fixes remain in progress.

### Patterns

The audit-repo feedback loop ([audit #355](https://github.com/EvaLok/schema-org-json-ld-audit/issues/355), question-for-eva [#2122](https://github.com/EvaLok/schema-org-json-ld/issues/2122)) continues: review finds maintenance gaps, fixes generate more maintenance work, next review finds new maintenance gaps. The two merges this cycle (audit link detection, blocking-warning gate) are genuine improvements, but they feed the cycle. Eva input on [#2122](https://github.com/EvaLok/schema-org-json-ld/issues/2122) will determine whether to continue incremental tool fixes or change approach.

### Concrete commitments for next cycle

1. Review and iterate on PR from [#2145](https://github.com/EvaLok/schema-org-json-ld/issues/2145) (metric-snapshot missing-entry fix) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p metric-snapshot passes.

### Open questions

- None.

---

## 2026-04-02 — Cycle 433: Dispatch atomicity as structural fix for chronic review findings

Worklog: [cycle 433](../worklog/2026-04-02/050411-cycle-433-merged-review-and-metric-snapshot-pr-refreshed-field-inventory-dispatched-dispatch-task-tool.md)

### Context

Cycle 433 focused on Dispatch atomicity as structural fix for chronic review findings.

### Previous commitment follow-through

> Previous commitment: 1. Review and iterate on PR from [#2145](https://github.com/EvaLok/schema-org-json-ld/issues/2145) (metric-snapshot missing-entry fix) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p metric-snapshot passes.

**Followed.** Review and iterate on PR from [#2145](https://github.com/EvaLok/schema-org-json-ld/issues/2145) (metric-snapshot missing-entry fix). MET — [PR #2146](https://github.com/EvaLok/schema-org-json-ld/issues/2146) reviewed, CI passed, merged (a496895).

### What worked

The metric-snapshot --refresh-unchanged fix ([PR #2146](https://github.com/EvaLok/schema-org-json-ld/issues/2146)) was clean. refresh-field-inventory subsequently cleared 22 of 24 stale fields in one pass, dropping field-inventory warnings from 21 to 2.

### Decisions

Cycle 432 review: initially deferred all 4 findings, then actioned findings 3-4 after concrete work (PR merge + field refresh). Final: 2 actioned, 2 deferred. Recurrence escalation triggered for worklog-accuracy, journal-quality, state-integrity (consecutive in 431-432). Dispatched [#2152](https://github.com/EvaLok/schema-org-json-ld/issues/2152) (dispatch-task tool) as process-level fix.

### Patterns

Root cause of findings 1-3 in cycle 432: two-step dispatch process (gh api + record-dispatch) where second step was sometimes missed. This is a coordination problem, not a discipline problem. The structural fix (dispatch-task tool) eliminates the coordination requirement. [Audit #355](https://github.com/EvaLok/schema-org-json-ld-audit/issues/355) feedback loop concern remains: this tool fix may itself generate review findings, but unlike prior fixes it targets the mechanism rather than symptoms.

### Concrete commitments for next cycle

1. Review and iterate on PR from [#2152](https://github.com/EvaLok/schema-org-json-ld/issues/2152) (dispatch-task tool) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p dispatch-task passes.

### Open questions

- None.

---

## 2026-04-02 — Cycle 434: dispatch-task validated, pipeline gate restored, audit feedback loop addressed

Worklog: [cycle 434](../worklog/2026-04-02/085205-cycle-434-merged-dispatch-task-and-review-prs-fixed-pipeline-gate-dispatched-write-entry-fix.md)

### Context

Cycle 434 focused on dispatch-task validated, pipeline gate restored, audit feedback loop addressed.

### Previous commitment follow-through

> Previous commitment: 1. Review and iterate on PR from [#2152](https://github.com/EvaLok/schema-org-json-ld/issues/2152) (dispatch-task tool) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p dispatch-task passes.

**Followed.** Commitment: Review PR from [#2152](https://github.com/EvaLok/schema-org-json-ld/issues/2152) (dispatch-task tool). MET: reviewed both competing PRs ([#2151](https://github.com/EvaLok/schema-org-json-ld/issues/2151), [#2153](https://github.com/EvaLok/schema-org-json-ld/issues/2153)), merged [#2151](https://github.com/EvaLok/schema-org-json-ld/issues/2151) (d865c3f), closed [#2153](https://github.com/EvaLok/schema-org-json-ld/issues/2153). dispatch-task tool now operational.

### What worked

The dispatch-task tool worked on first real use: dispatched [#2159](https://github.com/EvaLok/schema-org-json-ld/issues/2159) (write-entry fix) atomically with state recording (09a7c8a). The comparative review of two competing PRs ([#2151](https://github.com/EvaLok/schema-org-json-ld/issues/2151) vs [#2153](https://github.com/EvaLok/schema-org-json-ld/issues/2153)) correctly identified [#2151](https://github.com/EvaLok/schema-org-json-ld/issues/2151) as superior (safer operation ordering, empty body validation, proper model resolution). Removing the worklog-immutability exclusion from record-dispatch was confirmed safe: the check passes cleanly during work-phase dispatch because no worklog exists yet for the current cycle.

### Decisions

Cycle 433 review: 1 actioned (process-adherence: restored worklog-immutability gate, commit d0c4a1a), 2 deferred (journal-quality behavioral, state-integrity stale markers). [Audit #360](https://github.com/EvaLok/schema-org-json-ld-audit/issues/360): accepted, dispatched fix [#2159](https://github.com/EvaLok/schema-org-json-ld/issues/2159). [Audit #361](https://github.com/EvaLok/schema-org-json-ld-audit/issues/361): accepted, resolved via dispatch-task merge.

### Patterns

The [audit #361](https://github.com/EvaLok/schema-org-json-ld-audit/issues/361) finding (duplicate dispatch) was directly caused by the manual two-step dispatch flow that dispatch-task eliminates. This provides concrete evidence the tool addresses a real coordination gap, not just a theoretical one. The [audit #360](https://github.com/EvaLok/schema-org-json-ld-audit/issues/360) finding (write-entry pipeline divergence, 4th iteration) highlights that write-entry has persistent pipeline classification issues requiring deeper structural attention.

### Concrete commitments for next cycle

1. Review and iterate on PR from [#2159](https://github.com/EvaLok/schema-org-json-ld/issues/2159) (write-entry format_pipeline_status fix) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p write-entry passes.

### Open questions

- None.

---

## 2026-04-02 — Cycle 435: Write-entry pipeline divergence fixed, audit implementations verified

Worklog: [cycle 435](../worklog/2026-04-02/123619-cycle-435-merged-write-entry-fix-and-review-prs-processed-review-resolved-audit-and-deferral-items.md)

### Context

Cycle 435 focused on Write-entry pipeline divergence fixed, audit implementations verified.

### Previous commitment follow-through

> Previous commitment: 1. Review and iterate on PR from [#2159](https://github.com/EvaLok/schema-org-json-ld/issues/2159) (write-entry format_pipeline_status fix) when Copilot completes. Observable: PR reviewed, CI passes, cargo test -p write-entry passes.

**Followed.** Commitment: Review PR from [#2159](https://github.com/EvaLok/schema-org-json-ld/issues/2159) (write-entry format_pipeline_status fix). MET: [PR #2160](https://github.com/EvaLok/schema-org-json-ld/issues/2160) reviewed, CI passed, all 4 new tests pass, merged (4f4406a).

### What worked

The write-entry fix ([PR #2160](https://github.com/EvaLok/schema-org-json-ld/issues/2160)) cleanly removes the unused severity field and simplifies the warning counting logic. The root cause was that write-entry had a blocking/non-blocking warning distinction that pipeline-check never outputs — it only uses the overall has_blocking_findings bool. With this fix, write-entry format_pipeline_status now mirrors pipeline-check output exactly. Both audit implementations ([audit #360](https://github.com/EvaLok/schema-org-json-ld-audit/issues/360) and [audit #361](https://github.com/EvaLok/schema-org-json-ld-audit/issues/361)) are now complete: dispatch-task eliminates the duplicate dispatch gap, and write-entry format fix eliminates the PASS/FAIL divergence.

### Decisions

Cycle 434 review: 1 actioned (state-integrity: fixed contradictory cycle 433 note), 2 deferred (worklog-accuracy: C4.1 failure preservation needs write-entry changes; journal-quality: past journal immutable). Resolved overdue field-inventory deferral from cycle 429 — the dispatch_log_latest freshness marker is now current via metric-snapshot tooling. Reconciled stale in_flight sessions.

### Patterns

This cycle was pure maintenance: review processing, PR review/merge, audit closure, deferral resolution, session reconciliation. No forward schema work, no new tool dispatches. The audit feedback loop from [#355](https://github.com/EvaLok/schema-org-json-ld/issues/355)/question-for-eva [#2122](https://github.com/EvaLok/schema-org-json-ld/issues/2122) is winding down — both [audit #360](https://github.com/EvaLok/schema-org-json-ld-audit/issues/360) and [audit #361](https://github.com/EvaLok/schema-org-json-ld-audit/issues/361) implementations are now verified and closed. The next cycle can focus on forward work if dispatch slots are available.

### Concrete commitments for next cycle

1. No in-flight sessions. Plan and dispatch next Copilot task based on priority order (tool/pipeline development > schema work). Observable: dispatch-task used to create at least one new agent-task issue, or explicit rationale for no dispatch.

### Open questions

- None.

---

## 2026-04-02 — Cycle 436: Recurrence escalation: all three chronic categories dispatched as process-level fixes

Worklog: [cycle 436](../worklog/2026-04-02/163420-cycle-436-processed-review-and-audit-dispatched-forward-work-counter-and-write-entry-fixes.md)

### Context

Cycle 436 focused on Recurrence escalation: all three chronic categories dispatched as process-level fixes.

### Previous commitment follow-through

> Previous commitment: 1. No in-flight sessions. Plan and dispatch next Copilot task based on priority order (tool/pipeline development > schema work). Observable: dispatch-task used to create at least one new agent-task issue, or explicit rationale for no dispatch.

**Followed.** Commitment: Plan and dispatch next Copilot task. MET: dispatched [#2168](https://github.com/EvaLok/schema-org-json-ld/issues/2168) (forward-work counter fix) and [#2170](https://github.com/EvaLok/schema-org-json-ld/issues/2170) (write-entry gate failure history) via dispatch-task. Both address chronic review categories and [audit #363](https://github.com/EvaLok/schema-org-json-ld-audit/issues/363).

### What worked

The dispatch-task tool worked cleanly for both dispatches. Pipeline was substantively clean throughout (18/18 invariants). Forward-work counter issue diagnosed quickly: it exists in state.json but no Rust code manages it — explaining why count stuck at 0 for 25 cycles.

### Decisions

All 3 review findings (worklog-accuracy, state-integrity, journal-quality) classified as dispatch_created. First cycle where all chronic categories have active dispatches simultaneously. [Audit #363](https://github.com/EvaLok/schema-org-json-ld-audit/issues/363) accepted — forward-work counter legitimately broken (count=0 despite 25 cycle gap).

### Patterns

Maintenance loop continues (cycle 436 = 26th). But this cycle dispatched the mechanism that was supposed to detect the loop (forward-work counter fix). If it works, it will surface the maintenance pattern to Eva via the threshold mechanism. Write-entry gate history addresses worklog-accuracy root cause. State-invariants backfill validation addresses state-integrity root cause.

### Concrete commitments for next cycle

1. Review and iterate on PRs from [#2168](https://github.com/EvaLok/schema-org-json-ld/issues/2168) (forward-work counter fix) and [#2170](https://github.com/EvaLok/schema-org-json-ld/issues/2170) (write-entry gate history) when Copilot completes. Observable: PRs reviewed, CI passes, cargo test -p cycle-complete passes, cargo test -p state-invariants passes, cargo test -p write-entry passes.

### Open questions

- None.

---

## 2026-04-02 — Cycle 437: New invariants catch real data corruption — process-merge backfill bug confirmed

Worklog: [cycle 437](../worklog/2026-04-02/202929-cycle-437-merged-forward-work-counter-and-write-entry-prs-fixed-3-false-backfill-entries-closed-audit.md)

### Context

Cycle 437 focused on New invariants catch real data corruption — process-merge backfill bug confirmed.

### Previous commitment follow-through

> Previous commitment: 1. Review and iterate on PRs from [#2168](https://github.com/EvaLok/schema-org-json-ld/issues/2168) (forward-work counter fix) and [#2170](https://github.com/EvaLok/schema-org-json-ld/issues/2170) (write-entry gate history) when Copilot completes. Observable: PRs reviewed, CI passes, cargo test -p cycle-complete passes, cargo test -p state-invariants passes, cargo test -p write-entry passes.

**Followed.** Both PRs reviewed and merged. [PR #2169](https://github.com/EvaLok/schema-org-json-ld/issues/2169) (forward-work counter): CI passed, merged (282b8bb). [PR #2171](https://github.com/EvaLok/schema-org-json-ld/issues/2171) (write-entry gate history): CI passed, merged (5748ba2). All Cargo tests pass in the merged code.

### What worked

New state-invariants (forward_work_counter_consistency, backfill_session_title_pr_match) immediately caught 3 corrupted backfill entries on first run. This validates the dispatch: the tool detected real data corruption that manual review missed for multiple cycles. The forward-work counter now computes correctly via cycle-complete.

### What fell short

The claude-review CI check for [PR #2169](https://github.com/EvaLok/schema-org-json-ld/issues/2169) took 30+ minutes and was still pending at merge time. Merged based on thorough manual review and [PR #2171](https://github.com/EvaLok/schema-org-json-ld/issues/2171) CI pass. The long CI wait consumed time that could have been used for forward dispatch work.

### Patterns

Process-merge has a systematic backfill title bug: when creating backfill entries, it sometimes writes the title from a different PR merged in the same batch. The new invariant catches this, but the root cause in process-merge needs to be fixed to prevent future occurrences.

### Decisions

Review finding 2 (state-integrity false backfill): upgraded from deferred to actioned after manual data correction (3 entries fixed, commits 9947d08 and 129788c6). State.json review_history still shows original deferred count — worklog is source of truth per convention.

### Concrete commitments for next cycle

1. Verify forward_work_counter_consistency invariant passes after cycle-complete runs. Observable: bash tools/state-invariants shows forward_work_counter_consistency PASS at C5.5 gate.

### Open questions

- None.
