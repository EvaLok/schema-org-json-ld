# Journal — 2026-03-03

## Cycle 104: When a threshold becomes a uniform rule

**Eva's [#340](https://github.com/EvaLok/schema-org-json-ld/issues/340) resolves a design tension the audit identified but didn't act on.** The audit journal noted constructor mismatches during QC parity testing — ItemList and ContactPoint using positional params that confused the QC's test authors. The audit correctly classified these as "test construction issues, not library bugs." True, but incomplete: the underlying problem is API ergonomics, not test code quality.

The original convention (audit #20, accepted cycle 72) was: options-object for types with >5 optional properties, positional for simpler types. This created a dual pattern — consumers had to know which pattern each class used. PHP doesn't have this problem because named parameters make all constructors position-independent. TypeScript's lack of named parameters means the only way to match PHP's ergonomics is to use options objects everywhere.

**Eva saw what the orchestrator and audit didn't.** The threshold rule optimised for code size (positional params are more concise). Eva optimised for consumer experience. `new Brand({ name: 'ACME' })` is two characters longer than `new Brand('ACME')` but eliminates a category of error: passing arguments in the wrong order. When you're writing `new ContactPoint('+1-800-123-4567', null, null, null, 'en')` to set only telephone and language, the options pattern isn't a style preference — it's a usability fix.

**This is a breaking change and that's fine.** The package isn't published yet. The QC constructor mismatches that triggered this observation exist only in QC test code, not in external consumers. This is exactly the right time to standardise: after the port is complete but before the first release. Once published, changing constructor signatures requires a major version bump.

**Decomposition strategy:** 3 sequential batches (A-D, E-M, O-V). Sequential is mandatory per audit #29 (barrel file conflicts). The refactoring is mechanical — extract positional params to options interface, update constructor body, update tests. The agent has handled similar batch sizes (9-12 types) during the TS port. 16 classes per batch should be comfortable.

**Note on audit #56:** QC-targeted, about the QC's Copilot dispatch pipeline. No action needed from main orchestrator. The audit is now filing recommendations about the QC's agent workflow — a sign that the QC is evolving from a pure validation tool to a development participant.

**Update (01:27Z):** All 3 batches completed and merged within a single cycle (~40 minutes wall clock). 53 classes converted, zero revision rounds needed from any of the 3 Copilot sessions. The mechanical nature of the refactoring made it ideal for agent delegation — the pattern is regular, the rules are clear, and the test suite catches any mistakes. This is the largest single-cycle code change in the project's history: ~160 files across 3 PRs. Eva directive #340 closed.

## Cycle 105: Infrastructure debt surfaces when you look for it

Eva's [#348](https://github.com/EvaLok/schema-org-json-ld/issues/348) is a meta-directive: don't wait for work to come to you, go find it. The immediate practical consequence is removing the idle cycle mechanism (consecutive_idle_cycles, skip logic) from the startup checklist. But the deeper message is about orientation — the system should always be improving, not just responding.

**The path errors were embarrassing.** AGENTS.md — the single most important file for Copilot agent quality — still referenced `src/v1/Schema/` instead of `php/src/v1/Schema/`. This has been wrong since Phase 0 restructured the repo on March 1. That's 2 days and ~30 agent sessions where agents were reading incorrect directory paths. The agents probably figured it out from context (the actual files exist at the correct paths), but every bit of friction in AGENTS.md translates to lower agent reliability. The schema-implementation PHP skill had the same errors.

**The ts-schema-implementation skill was still documenting a convention that no longer exists.** The dual pattern (positional for ≤5 properties, options for >5) was superseded by Eva directive #340 in cycle 104. I updated AGENTS-ts.md but forgot the skill. This is a systematic gap: when a convention changes, ALL documentation of that convention must be updated. The current approach (manually remembering which files document a convention) doesn't scale. Worth thinking about whether there's a structural fix — perhaps a convention registry, or at minimum a checklist of "files to update when X changes."

**Cross-repo cooperation as proactive value.** Instead of just waiting for the QC to reach 86/86, I posted a type categorization on QC-REQUEST #331 that groups the remaining 47 types by testing strategy. This won't make the QC's job faster directly, but it might help them batch more efficiently. It also demonstrates the kind of proactive cooperation Eva's directive calls for — don't just open a door and wait, hold it open and hand them a map.

**Pattern: convention change → documentation sweep.** Every convention change should trigger a sweep of: AGENTS.md, AGENTS-ts.md, all skills in `.claude/skills/`, README.md, and any ADRs that reference the old convention. Adding this to the worklog template as a checklist item.

## Cycle 106: The parity denominator gap returns

**Audit [#58](https://github.com/EvaLok/schema-org-json-ld-audit/issues/58) catches a familiar pattern.** The QC orchestrator is actively expanding E2E coverage (49/86, up from 39/86) but isn't expanding parity testing — and parity is the publish gate. This is a variant of the denominator problem the audit caught in [#49](https://github.com/EvaLok/schema-org-json-ld-audit/issues/49). The mechanism is different but the risk is the same: the QC could reach 86/86 E2E while parity stays at 39/86, creating a misleading completion signal.

The root cause is that the QC's Copilot dispatch specs include E2E generate scripts and unit tests, but not parity tool updates. Parity lives in a separate tool (`tools/ts-parity-check.ts`) that doesn't get updated as part of the coverage expansion workflow. Two process fixes are possible: (1) add a post-merge step to the QC's PR review pipeline, or (2) include parity test creation in the Copilot dispatch spec. Either works; the key requirement is that parity advances in lockstep with E2E.

**What I can do about it:** Post a clear alert on QC-REQUEST [#331](https://github.com/EvaLok/schema-org-json-ld/issues/331). Create an audit-inbound issue acknowledging the finding. The actual fix is QC-side infrastructure — I can't modify their pipeline, but I can make the gap visible.

**Property gap audit.** A proactive scan of 5 high-traffic types (Article, Recipe, Event, Product, FAQ) against Google's current docs revealed two actionable gaps:

1. `Review.positiveNotes` and `Review.negativeNotes` (`ItemList` type) — powers Google's Pros/Cons rich results for product reviews. Missing entirely. High visibility feature.
2. `JobPosting` beta properties (educationRequirements, experienceRequirements, experienceInPlaceOfEducation) — recommended by Google, missing. Dispatched to Copilot this cycle.

Two design-tension observations (not bugs): `Question.acceptedAnswer` is nullable because `Question` serves both FAQPage (where it's required) and QAPage (where it's optional). `Place.address` is nullable because `Place` is generic but Google requires it specifically for Event locations. Both are correct as designed — the library shouldn't enforce Google's context-specific requirements at the type level. Documentation is the right response.

**Convention change sweep formalized.** Added to STARTUP_CHECKLIST. The pattern identified in cycle 105 is now a documented process step: every convention change triggers a sweep of all files that reference the old convention. This should prevent the multi-file staleness problem that caused cycle 105's infrastructure cleanup.

## Cycle 107: Systematic property gap auditing as a maintenance practice

**The property gap audit is now systematic.** Cycle 106 scanned 5 types and found 2 actionable gaps. This cycle scanned 10 more types in two parallel batches and found the coverage picture is remarkably good across most types, with a few targeted gaps worth fixing.

**Coverage scorecard (15 types audited total):**
- **100% covered (10 types):** Article, Recipe, Event, Product, FAQ, Organization, LocalBusiness, SoftwareApplication, Movie, ProfilePage (class itself)
- **Near-complete with targeted gaps (5 types):** VideoObject (missing SeekToAction), Course (deprecated Google docs), Dataset (missing hasPart/isPartOf), JobPosting (QuantitativeValue.unitText), DiscussionForum (type mismatches)

**The SeekToAction gap is interesting.** It's a recommended property for Key Moments seek markup — the feature that lets Google deep-link into specific video timestamps. The codebase already has the exact pattern needed (MathSolver + SolveMathAction with PROPERTY_MAP for hyphenated JSON-LD keys). This is a great example of how incremental auditing reveals gaps that aren't obvious from the schema.org spec alone — you need the Google docs to know that `potentialAction` with `SeekToAction` is the recommended way to implement seek markup.

**QuantitativeValue.unitText vs unitCode.** A subtle distinction: `unitCode` uses UN/CEFACT machine codes (KGM, LBR), while `unitText` is the human-readable equivalent (HOUR, YEAR). Google's JobPosting docs specifically use `unitText` for salary periods. We have the former but not the latter. Non-breaking addition, but important for correctness.

**Course's 12 missing properties are a false alarm.** The `/course-info` Google docs page was deprecated in September 2025. The active `/course` page only formally requires `name` and `description` with `provider` recommended. The missing properties were from the deprecated page. This is a good reminder to verify the current state of Google's documentation, not just the cached knowledge.

**Audit #60 (QC dispatch metrics) is primarily QC-targeted.** The main orchestrator has 21/21 dispatch-to-PR conversion — no failures. But the audit's recommendation to track `dispatch_to_pr_rate` separately from `pr_merge_rate` is sound defensive practice. Added both metrics to state.json and stale dispatch detection to the startup checklist.

**Observation on the QC pipeline.** QC parity is at 49/86 (57%), up from 39/86 at cycle 106. The expansion rate is ~10 types per QC cycle. At this rate, reaching 86/86 will take ~4 more QC cycles. Meanwhile, each main orchestrator cycle is now primarily a maintenance cycle — property gap auditing, infrastructure refinement, and audit processing. This is fine; the rate-limiting factor is the QC, and rushing won't help.

## Cycle 108: The mixed-array bug and type classification

**First revision round in 23 dispatches.** PR [#364](https://github.com/EvaLok/schema-org-json-ld/issues/364) exposed a pre-existing bug in `JsonLdGenerator::AddPropertiesToObject()`: it checked only the first array element's type to decide serialization strategy for the entire array. When `Dataset.hasPart` contained `[Dataset object, "https://url"]`, the first element was TypedSchema so all elements got passed to `SchemaToObject()`, crashing on the string.

The bug has been latent since the JsonLdGenerator was written — no existing property used mixed arrays before `Dataset.hasPart`. Interestingly, the TypeScript serializer already handled this correctly by dispatching per-element. The PHP code was checking `$v[0] instanceof TypedSchema` and treating the result as gospel for all elements. Copilot fixed it in 4 minutes by flattening the branching into a single loop with per-element type checking.

**Lesson:** The PHP serializer's assumption of array homogeneity was never tested because all existing array properties were homogeneous. The test-driven approach (write the expected behavior, then make it work) is what caught this — without the mixed-array test case, the bug would have shipped. This validates the importance of expanding property types beyond what's been tested historically.

**Type classification was high-value.** Audit [#62](https://github.com/EvaLok/schema-org-json-ld-audit/issues/62) correctly identified that the QC orchestrator's coverage expansion pipeline was dispatching building-block types for standalone E2E testing. These types are designed to be nested — standalone validation will always fail.

The classification I posted on QC-REQUEST [#331](https://github.com/EvaLok/schema-org-json-ld/issues/331) splits the 88 schema types into ~28 standalone-testable, ~58 building-block, and 12 enums. This reframes the QC's publish gate: E2E testing should target the 28 standalone types, not all 88. Parity testing should still cover all 88. The QC orchestrator can use this to skip building-block types in its expansion pipeline, avoiding wasted Copilot sessions.

**Property gap audit is now complete.** All 23 standalone-testable types have been audited against Google's current documentation. 16/23 at 100% coverage. The remaining gaps are low-priority: QAPage's `Question.image`/`video` and `Answer.image`/`video` (recommended, not required), and VacationRental's `Review.contentReferenceTime` (French listings only). Every actionable gap found in cycles 106-108 has been either fixed (PRs #355, #357, #362, #364) or dispatched (#370).

**AggregateRating.itemReviewed is a legitimate library enhancement.** The QC report [#160](https://github.com/EvaLok/schema-org-json-ld-qc/issues/160) wasn't a bug in the traditional sense — AggregateRating works correctly when nested. But Google requires `itemReviewed` for standalone validation. Adding it as an optional property (defaulting to null) maintains backward compatibility while enabling standalone use. The pattern follows Review's existing `itemReviewed: TypedSchema|null`.

## Cycle 109: Calibrating task complexity for capable models

**Eva's [#372](https://github.com/EvaLok/schema-org-json-ld/issues/372) is a recalibration directive.** The QC repo's [#166](https://github.com/EvaLok/schema-org-json-ld-qc/issues/166) put it bluntly: "The tasks that you have been giving to 5.3 codex are very small and extremely simple." The evidence supports this — 24 dispatches, 24 merges, 1 revision round, zero failures. That's a sign of underutilization, not excellence.

**The zero-revision streak was a false positive for quality.** I was interpreting it as "my issue specs are well-calibrated." Eva and the QC are reading it as "your issue specs are too easy." The difference matters. A model that never struggles is a model that isn't being asked hard questions. The constructor refactoring (cycle 104) hinted at this — 53 classes across 3 PRs, all merged cleanly — but even that was mechanically simple (extract params → options interface, update constructor, update tests). The pattern was regular and the rules were clear.

**What "complex" looks like for this cycle.** I dispatched two tasks that are qualitatively different from previous ones:

1. [#374](https://github.com/EvaLok/schema-org-json-ld/issues/374) is a multi-concern quality sweep: add 4 properties to 2 classes across 2 languages, audit 88 test files for coverage gaps, write 100+ new test methods, and update README. This requires the agent to make many independent decisions about what to test and how to test it, not just follow a recipe.

2. [#375](https://github.com/EvaLok/schema-org-json-ld/issues/375) is a novel tool creation: build a cross-language parity checker that parses PHP constructor signatures AND TypeScript options interfaces, normalises types across language boundaries, and produces a structured comparison report. This has no existing pattern to follow — the agent has to design the parsing strategy, handle edge cases in both language syntaxes, and make the output useful.

**The second directive is more interesting than the first.** Eva said to use 5.3-codex for "auditing and planning related tasks." This isn't just "give it harder code to write." It's "use the model as a thinking tool, not just a typing tool." The parity checker is a tool that helps me do my job better — it catches drift between PHP and TS before it reaches the QC. If it works well, I should look for other places where an agent-built tool could replace manual orchestrator work.

**Predictions for this experiment.** The quality sweep (#374) should succeed — it's high volume but each piece is straightforward. The parity tool (#375) is riskier — regex-based parsing of two different language syntaxes is the kind of task where edge cases accumulate. I expect it to produce a working tool, but it may miss some PHP/TS syntax variations that require a revision round. That would be fine and informative.

**Concurrent dispatch note.** Both tasks are running simultaneously (2/2 slots). They don't touch the same files — #374 modifies schema classes and tests, #375 creates new files in `tools/`. But if #374 adds `image`/`video` to Question/Answer, and #375's parity tool runs against the pre-#374 codebase, the tool will correctly report those properties as missing from TS (since #374 will add them to PHP first). This is a feature, not a bug — it validates that the tool detects real drift.

## Cycle 110: The wheel you reinvented was already a dependency

**Eva's [#378](https://github.com/EvaLok/schema-org-json-ld/issues/378) is about first principles, not just tooling.** The parity tool (PR #377) used regex to parse PHP and TypeScript source files. Eva's feedback: "The instruction to use regex over AST is difficult for me to understand, especially given that we already employ some AST analysis in this project." She's right — TypeScript's compiler API is already installed, and `php-parser` (glayzzle) is a mature, well-maintained npm package. The regex approach was an unnecessary reinvention.

**Why did I instruct the agent to use regex?** Honestly — inertia. I was thinking about the agent's capabilities (it can write regex) rather than the project's dependencies (it already has a TypeScript parser). I also defaulted to "simpler" tooling without evaluating whether regex is actually simpler for this problem. It isn't. Regex for source code parsing is brittle by nature — the `FoodEstablishmentOptions extends LocalBusinessOptions` bug that the regex tool couldn't handle is a perfect example. AST parsers handle `extends` clauses trivially because the grammar is encoded in the parser.

**The broader lesson: "prefer existing tools over novel implementations."** This applies at every level — from the agent's code (use libraries, not regex) to the orchestrator's decisions (use Eva for workflow changes, not workarounds) to the project's architecture (follow established patterns, not ad hoc solutions). Eva explicitly said: "If you need help with installing additional dependencies or enabling/modifying workflows, don't forget that I'm on your team." This is a reminder that asking for help is not a failure mode — it's an efficiency multiplier.

**What I changed:**
1. Dispatched [#380](https://github.com/EvaLok/schema-org-json-ld/issues/380) — complete rewrite of the parity tool using TypeScript Compiler API + php-parser AST
2. Created `.claude/skills/tool-creation-guidelines/SKILL.md` — encodes "AST over regex" as a firm convention
3. Noted that Eva is an asset for dependency management and workflow changes — not just a reviewer

**Prediction for #380:** This should work well. The TypeScript Compiler API is well-documented and the agent can reference the existing TypeScript codebase for guidance. The `php-parser` npm package is mature. The main risk is that the php-parser API has quirks around PHP 8.1+ syntax (constructor promotion, backed enums) that might need some exploration. But this is a genuine parsing task, not a regex maze — the agent should be able to iterate more reliably.

## Cycle 111: The 34:1 documentation ratio

**The README tells a story about what this project values.** 34 PHP code examples, 1 TypeScript example. For a library that now has perfect dual-language parity (88/88 types in both PHP and TS), this ratio is jarring. It's not that the TypeScript port was recent — it's been complete since March 1, 3 days ago. The README was simply never updated to reflect the dual-language reality.

**This is a visibility problem, not a correctness problem.** The TypeScript code works. The tests pass. The package builds correctly. But a TypeScript developer visiting this README sees a PHP library with a TypeScript afterthought. The Quick Start has one TS example; every other section is PHP-only. The API Reference describes PHP method signatures. The Contributing section says "Add or update schema classes in `php/src/v1/Schema/`." The Testing section only mentions `composer run test-unit`.

**Why did this slip through?** The TypeScript port was executed at high speed (12 agent sessions in ~3.5 hours). The focus was correctly on code correctness and test coverage, not documentation. PR [#376](https://github.com/EvaLok/schema-org-json-ld/issues/376) was supposed to update the README as Part 3, but it only added the Quick Start TS example and the dual-language mention in the header. The 25 Usage Example sections were untouched.

**Dispatched [#383](https://github.com/EvaLok/schema-org-json-ld/issues/383) to close the gap.** This is a good test of the agent's ability to handle a large, repetitive-but-not-trivial task. Each of the 25 sections needs a TypeScript example that:
1. Uses the options-object constructor pattern (not the old positional style)
2. Imports from the correct package name (`@evabee/schema-org-json-ld`)
3. Uses the correct method name (`schemaToJson` not `SchemaToJson`)
4. Matches the same property values as the PHP example

The repetition makes it automatable; the per-section differences (different types, different nested objects, different property sets) prevent pure copy-paste.

**Edge-case test expansion ([#385](https://github.com/EvaLok/schema-org-json-ld/issues/385)) is the second dispatch.** The 10 weakest-covered standalone types each have only 2-3 PHP tests and 3 TS tests. The target is ~52 new tests across both languages, each exercising a distinct edge case. This follows up on #376's underdelivered Part 2 and strengthens the test suite ahead of npm publish.

**Both tasks dispatched concurrently.** #383 only modifies README.md. #385 only modifies test files. Zero shared file overlap. This is the first cycle with two genuinely complex concurrent dispatches — previous concurrent pairs were either one complex + one trivial, or both mechanical (like constructor refactoring batches).

**Observation on maintenance cycles.** This is the 8th consecutive cycle without an Eva directive or QC report to process. The main blocker (QC parity at 49/86) is external. But each cycle has found genuine improvement work: property gap auditing (cycles 106-108), convention documentation sweep (cycle 105), AST tool rewrite (cycle 110), and now README + test expansion (cycle 111). Eva's directive in [#348](https://github.com/EvaLok/schema-org-json-ld/issues/348) — "there are no idle cycles" — has been validated: there is always something to reassess, explore, or refine.

## Cycle 112: Review-heavy cycle with a documentation quality lesson

**Both cycle 111 dispatches completed and merged this cycle.** [PR #386](https://github.com/EvaLok/schema-org-json-ld/issues/386) (52 edge-case tests) merged cleanly — genuine edge cases, good PHP/TS parity, minor import ordering fix needed post-merge. [PR #384](https://github.com/EvaLok/schema-org-json-ld/issues/384) (README TS examples) needed 1 revision round: 6 examples would have caused TypeScript compilation errors due to missing required constructor properties.

**Documentation examples need the same rigor as code.** The Copilot agent wrote TS examples that looked correct at a glance but omitted required constructor properties (MathSolver, JobPosting, Recipe, Offer, VacationRental). These are "simplified" examples — a natural tendency when writing documentation — but TypeScript's strict type system makes simplified examples that compile in PHP (where everything is nullable) fail in TS. The lesson: README examples for a dual-language library must be validated against BOTH language's type systems. The agent likely would have caught this if the issue spec had said "ensure every example compiles" rather than just "add TS examples."

**Audit recommendations drove useful infrastructure improvements.** Audit [#67](https://github.com/EvaLok/schema-org-json-ld-audit/issues/67) caught a stale audit-inbound issue (#353) that had been open for 5+ cycles. The fix was a 1-line enhancement to the housekeeping checklist. Audit [#68](https://github.com/EvaLok/schema-org-json-ld-audit/issues/68) caught a denominator mismatch in the pre-publish gate (86 vs 76 testable types). Both are good examples of the audit system catching cross-recommendation inconsistencies that the main orchestrator missed.

**Milestone: 29/29 dispatches merged (100% merge rate).** All Copilot agent sessions have produced mergeable code with only 2 requiring revision rounds (#364 for a mixed-array serialization bug, #384 for missing required props). The workflow is genuinely reliable at this point.

## Cycle 113: Tools finding what humans missed

**The parity-check tool justified its existence.** Running `tools/parity-check.ts` — the AST-based tool rewritten in cycle 110 per Eva's directive — revealed that `WebPageElement` is the only remaining class still using positional constructor params in TypeScript. All other 88 schema classes were converted during cycle 104's constructor refactoring (Eva directive [#340](https://github.com/EvaLok/schema-org-json-ld/issues/340), PRs #343/#345/#347). WebPageElement was missed.

**Why was it missed?** Cycle 104 organized the refactoring by alphabetical range: batch 1 (A-D), batch 2 (E-M), batch 3 (O-V). "W" falls in batch 3's range (O-V), and the issue spec for [#346](https://github.com/EvaLok/schema-org-json-ld/issues/346) included 22 classes. With that many classes in one batch, the agent likely converted all the options-pattern classes it found but didn't flag WebPageElement because it was already functional (just not using the right pattern). This is a class of error that batch operations are prone to: the work is correct for what was processed, but the inventory wasn't exhaustive.

**The tool caught what manual review didn't.** I reviewed all three cycle 104 PRs before merging. I didn't catch the gap either. This validates Eva's [#372](https://github.com/EvaLok/schema-org-json-ld/issues/372) push toward agent-built tooling — the parity tool is doing automated verification that humans skip under cognitive load.

**Interestingly, the parity tool reported it as a false positive.** The tool said WebPageElement was "missing" `isAccessibleForFree` and `cssSelector` from its TypeScript version. But those properties exist — they're positional constructor params, not options-interface fields. The tool's AST parser for TypeScript only reads the `Options` interface properties, not positional params. After the fix (converting to options-object), the tool will report 89/89 match. So the tool found the right problem for the wrong reason — the missing-property signal was actually a wrong-pattern signal. This is worth noting: even when tools give misleading diagnostics, they can still surface genuine issues.

**Audit [#71](https://github.com/EvaLok/schema-org-json-ld-audit/issues/71) closed a communication gap.** The QC-REPORT feedback loop was broken: fixes were merged but notifications went to closed qc-inbound issues that nobody reads. The fix is simple — post on the active QC-REQUEST thread (#331) that the QC already monitors every session. This is the kind of process fix that's obvious in hindsight but wasn't until the audit pointed it out. The three-orchestrator system continues to catch gaps that each individual orchestrator misses.

**Health check results.** PHP: 423 tests, 1,947 assertions. TS: 409 tests, 92 files. Both green. Biome: 196 files, 0 issues. PHPStan: level max, 0 errors. The codebase is clean. The main blocker remains QC parity validation (49/76), which is external and advancing at ~10 types per QC cycle.
