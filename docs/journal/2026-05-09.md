# 2026-05-09

## Cycle 103 — Author B's counting protocol specification, closing Risk 8 at specification level (deferral arc 7 cycles 96-102; protocol uses structural-decision-points in role artifacts + external-observer enumeration with reproducibility check + ratio-based hard refutation thresholds; 3-candidate falsification completeness milestone reached; NOVEL functional-class shape #21 risk-closure-at-specification-level)

**Cycle issue:** [#2892](https://github.com/EvaLok/schema-org-json-ld/issues/2892)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **fourteenth cycle of Phase 2 candidate-set work** [cycles 90-103]).
**Cycle composition shape:** **risk-closure-at-specification-level** — NOVEL functional-class shape #20 (cycle 102 first instance) → Specification-level closure of a long-deferred candidate-specific risk via external-observer-enumeration methodology specification. **22 functional-class shapes total demonstrated cycles 62-103 at 43 instances.** Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### What I did

- **Specified Author B's counting protocol** in [`docs/redesign/2-candidates/B-decomposed-multi-role.md`](../redesign/2-candidates/B-decomposed-multi-role.md), inserted as new section `### Counting protocol (cycle 103 specification, addressing Risk 8)` between `### Coordination overhead estimate` and `### Validation plan` (~150 lines). Section structure: background (Risk 8 framing) → unit of analysis (structural-decision-points in role artifacts; inclusion/exclusion criteria with boundary cases) → why this unit (4 alignments) → counting method (external enumeration with reproducibility check) → per-role + coordination decomposition → sanity-check secondary unit (operational tool-invocation log with category classification) → hard refutation thresholds (ratio-based 4-band: PASS / PASS-WITH-NOTE / PARTIAL-FLAG / REFUTED) → observational secondary metric (per-role mitigation direction-check) → baseline re-derivation under-the-rubric (with 35-40 estimate spot-check) → re-counting cadence → Risk-8-specific concern-vs-answer table.
- **Replaced validation plan step 5** with explicit hard refutation thresholds. The previous soft "promotes to PASS-WITH-NOTE" / "weakens" language is gone; the threshold is now: PASS ≤ 1.0× v1 baseline; PASS-WITH-NOTE (1.0×, 1.5×]; PARTIAL-FLAG (1.5×, 2×]; REFUTED > 2×. Coordination-overhead is counted as part of aggregate; per-role decision-count is observational direction-check.
- **Updated Risk 8** with `CLOSED at specification level cycle 103` annotation pointing to the Counting protocol section. Notes that Risk 8 remains *open at the operational level* until Phase 3 prototype measurement actually executes the protocol — the dual-level closure framing is itself a novel methodology distinction.
- **Updated [`docs/redesign/2-candidates/README.md`](../redesign/2-candidates/README.md)** in two places: B's row in Load-bearing claims sharpening tracker (sharpening cycle progression "Cycle 92 → Cycle 96 → Cycle 103"; Direction column re-labeled `enumeration-rubric-now-specified` (was `decision-class naming, not enumeration`); Magnitude column re-labeled `ambiguous-without-prototype-but-falsifiable (hard thresholds pre-agreed: PASS ≤ 1.0× v1; PASS-WITH-NOTE (1.0×, 1.5×]; PARTIAL-FLAG (1.5×, 2×]; REFUTED > 2×)`); Forward-work section (cycle 103 closure noted with link to Counting protocol section; B counting protocol removed from second-iteration sharpening targets list).
- **Documented findings** in [`_notes/cycle-103-author-b-counting-protocol.md`](../redesign/_notes/cycle-103-author-b-counting-protocol.md) — setup with deferral-arc framing (cycles 96-102 prioritized PR-absorption + catalogue rebuild over Risk 8 closure per `CYCLE-COMPOSITION-POLARITY`); 5 explicit design choices made with alternatives-considered-and-rejected language (unit, method, threshold-shape, rubric-inclusion-default, rubric-anti-symmetry mitigation); 3 explicit edits to candidate file + 2 edits to README; 7 explicit not-modified items including the cluster catalogue (no catalogue updates owed) + per-system files (not candidate-evaluation work) + 2-design-framework.md (not measurement-methodology) + A's and C's validation plans (already have hard thresholds) + cross-cluster intersections (cycle 104+ deferred work) + MEMORY.md ephemeral-runner observation; verification table; what-surprised-me / what-noticed (5 observations including methodology-vs-design distinction, 7-cycle deferral as load-bearing protection not procrastination, specification-vs-operational closure stratification, 4-band threshold structure vs C's binary, rubric-inclusion non-neutrality acknowledgment); sibling pattern tracking; lexicon entries.

### What surprised me / what I noticed

1. **The cycle 103 work establishes "3-candidate falsification completeness" — a structural milestone.** Before cycle 103, A and C had pre-agreed falsification thresholds (cycle 96 absorption) but B did not. After cycle 103, all three candidates have pre-agreed thresholds. This means **the candidate-set is fully falsifiable for the first time** — Eva can evaluate all three candidates against equally-falsifiable threshold structures at the candidate-selection checkpoint. This isn't just "B caught up to A and C"; it's a candidate-set-level completeness milestone that makes the candidate-selection decision more defensible. Before cycle 103, an Eva-side criticism could plausibly say "B's threshold is softer than A and C's; selection-asymmetry"; after cycle 103, that criticism doesn't apply.

2. **The 7-cycle deferral was load-bearing protection, not procrastination.** Cycles 96-102 prioritized PR absorption and catalogue rebuild over Risk 8 closure — per `CYCLE-COMPOSITION-POLARITY`, research-corpus advance was the substantive focal default. Risk 8 closure was BLOCKED by absorption-arc completion, not just deferred. Cycle 103 is the first cycle where absorption arc has reached natural pause AND catalogue rebuild has completed — this is the first cycle where Risk 8 closure is both unblocked and clearly the highest-priority deferred item. **Pattern observation: deferral driven by `CYCLE-COMPOSITION-POLARITY` polarity discipline can be load-bearing protection.** An undeferred cycle 96 closure attempt might have been weaker than the cycle 103 closure because the cycle 92 sharpening hadn't yet been re-grounded post-PR-absorption-arc.

3. **The protocol's specification-vs-operational closure stratification is a new methodology pattern.** Risk 8 closes at the specification level cycle 103; remains open at the operational level until Phase 3 measurement actually executes the protocol. This is **NOVEL functional-class shape #21: risk-closure-at-specification-level**. Many of B's other risks have similar structure — Risks 1-5 are all named at specification level, remain open at operational level. The pattern is *specification-vs-operational risk-closure stratification*, applicable to many candidate-specific risks.

4. **The rubric's inclusion non-neutrality is a real concern that the protocol explicitly names.** The choice between minimally-inclusive (named steps + named patterns only) vs maximally-inclusive (also prose-procedural decisions) is non-neutral with respect to which candidate is favored — maximally-inclusive inflates v1's baseline more than B's (because v1's prompt is more prose-procedural than B's role-specific prompts will be). Mitigation: name the choice (default minimally-inclusive); apply rubric symmetrically to both candidates; verified-baseline-pre-Phase-3 step requires external observer to verify v1 count under the rubric. **Honest acknowledgment of measurement-design non-neutrality is itself a discipline application** (the cycle 96 PR #2878 lesson "name the choice; verify the choice" applied to a meta-protocol level).

5. **The MEMORY directory referenced in the system prompt does not exist in this container.** Cycle 100's _note claimed `MEMORY.md` was initialized at `/home/runner/.claude/projects/-home-runner-work-schema-org-json-ld-schema-org-json-ld/memory/`; that path does not exist in cycle 103's container. Cycle 100's "initialization" was ephemeral per-runner state and did NOT survive across cron-triggered runs. **The actual cross-cycle persistence mechanism remains `docs/redesign/` + `docs/journal/` + `_notes/`** — those are committed to git and survive across runs. Implication: cycle 100's claim "captures the load-bearing context that future cold-starts need" did not actually persist; future cold-starts (including cycle 103 setup) re-derived context from the committed artifacts, not from MEMORY.md. This is a **falsification of cycle 100's persistence claim**. Honest acknowledgment owed; cycle 103 does not re-attempt MEMORY.md initialization since runner-local persistence is structurally ephemeral.

### Risk 7 partial-closure observation (not modified in candidate file)

The protocol's external-observer requirement extends to v1 baseline re-derivation, which means v1's count will be measured by a non-author observer at Phase 3 — addressing Risk 7's "self-measured by orchestrator that benefits from comparison" concern. Risk 7 is therefore *partially* closed at the specification level cycle 103 alongside Risk 8. The candidate file's Risk 7 entry is NOT modified in this cycle (scope-creep avoidance — cycle 103 substantive focal is Risk 8); cycle 104+ second-iteration sharpening can address Risk 7 explicitly.

### Standing tasks

- **Cluster catalogue update** [completed cycle 102]. Catalogue staleness now RECENT.
- **Author B's counting protocol** [completed cycle 103, specification-level closure]. Risk 8 closes at specification level; remains open at operational level until Phase 3 prototype measurement.
- **Cross-cluster intersection updates** [cycle 104+, MEDIUM PRIORITY] — re-evaluate the 7 cross-cluster intersection patterns from cycles 72/74 with the new cluster A + cluster H sub-shapes from cycle 102. Specific candidate intersections: A↔B (classifier-dispatch storage), F↔H (Sonnet substrate dual-cast), A↔C (task-ingestion-routing as session-start lifecycle).
- **Second-iteration sharpening on candidates A/B/C** [cycle 104+] — with full 4-system absorption + cluster catalogue update (cycle 102) + B's counting protocol (cycle 103) feeding into candidate risk refinement. Remaining sharpening targets: A's tool-registry-growth risk; B's coordination-overhead-magnitude (now bounded by aggregate threshold but standalone magnitude unmeasured); C's plan-authoring-discipline-conditional risk.
- **Restored-polarity dispatching** [cycle 105+] — once second-iteration sharpening absorbs into the candidate set.
- **Symphony deeper-read elevation** [cycle 105+ or based on cluster catalogue findings] — first-pass at cycle 98; could resolve cluster F.9 candidate status.
- **oh-my-claudecode deeper-read elevation** [cycle 106+ or based on cluster catalogue findings] — first-pass at cycle 99; deeper read would confirm/refute the 3-system substrate-edge convergence observation.

### Sibling pattern tracking

- **NOVEL functional-class shape #21 — `risk-closure-at-specification-level`** — cycle 103 first instance. Pattern: a candidate-specific risk named at cycle N is closed at specification level at cycle M (M > N, deferral arc 7+ cycles); operational closure deferred to Phase 3 prototype. Distinguished from `risk-closure-at-operational-level`. **NOVEL at 1 instance.**
- **Functional-class shape #20 (catalogue-rebuild-with-multi-cycle-absorption)** — cycle 102 introduced; cycle 103 NOT a new instance.
- **Functional-class shape #19 (research-deliverable-absorption-with-verification)** — HARDENED at 3 verification-success + 1 verification-mixed instances. Cycle 103 NOT a new instance.
- **Verification-discipline pattern (cycle 96 emergence)** → **6 instances HARDENED**. Cycle 103 NOT a new instance.
- **Pre-agreed-falsification-threshold discipline** (cycle 96 emergence per PR #2878 Finding 18) → cycle 103 application is the **THIRD instance** (A cycle 96 / C cycle 96 / B cycle 103). All three candidates now have pre-agreed thresholds. **3-candidate falsification completeness HARDENED at 1 instance** — structural milestone for candidate-selection-checkpoint readiness.
- **Generalization-level discipline (J-Q(a))** → **19 instances HARDENED**. Cycle 103 application: rubric inclusion non-neutrality (the choice of minimally-inclusive vs maximally-inclusive) is a generalization-level decision named explicitly with reasoning; not a new instance count.
- **Direction-vs-magnitude discipline** → **11 instances HARDENED**. Cycle 103 application: the protocol explicitly separates direction (per-role mitigation direction-check) from magnitude (aggregate-vs-baseline ratio); the two are observed independently and combined for verdict. Discipline applied as protocol structure; not a new instance count.
- **Cycle-composition-polarity discipline (cycle 62 emergence)** → 41 instances HARDENED at cycle 102. Cycle 103 substantive focal IS "v1-system retrospective extraction of lessons not yet absorbed into the redesign" applied to B's specific risk-deferral arc; the cycle 96 discipline-bar-too-low lesson is now applied to the longest-standing under-specified candidate measurement. Cycle 103 application of polarity discipline.
- **Persistence-mechanism falsification observation** — cycle 100's MEMORY.md initialization claim is falsified at cycle 103 (runner-local memory directory does not exist; persistence is structurally ephemeral). NOVEL observation at 1 instance.
- **Audit-as-peer pattern** — 2-instance evidence holds. Audit cycle 213 still silent-failed (no audit-repo commit since cycle 212 at 2026-05-07T04:35Z, ~50+ hours since cycle-101 measurement; ~2 hours added since cycle 102). Cycle 103 NOT a new instance.

### Lexicon entries (cycle 103)

- *Counting protocol* — measurement-methodology specification for B's per-role decision-count metric: structural-decision-points in role artifacts (primary unit), external-observer enumeration with reproducibility check, ratio-based threshold against v1 baseline measured under same rubric. Closes Risk 8 at the specification level.
- *Structural decision-point* — labeled prompt instruction that requires the role's session to choose between alternatives. Distinguished from mechanical instructions (post-comment, commit, file-write) which are excluded from the count.
- *Risk-closure-at-specification-level vs at-operational-level* — NOVEL functional-class shape #21 distinction. Specification-level closure means the methodology is specified; operational-level closure means the measurement has actually executed and produced a verdict.
- *3-candidate falsification completeness* — state where all three candidates have pre-agreed falsification thresholds. Reached at cycle 103. Structural milestone for candidate-selection checkpoint readiness.
- *Rubric inclusion non-neutrality* — measurement-design observation that the choice between minimally-inclusive and maximally-inclusive rubric is non-neutral with respect to which candidate is favored. Mitigation: apply rubric symmetrically + name the choice explicitly.
- *Persistence-mechanism falsification* — observed mismatch between system-prompt-named persistence path and operational reality. Cycle 100's MEMORY.md initialization claim is falsified at cycle 103.

### Cycle 104+ plan

1. **Cross-cluster intersection updates** [cycle 104+, MEDIUM PRIORITY] — re-evaluate the 7 cross-cluster intersection patterns from cycles 72/74 with cycle 102's new cluster A + cluster H sub-shapes
2. **Second-iteration sharpening on candidates A/B/C** [cycle 104+] — with cycle 102 catalogue update + cycle 103 counting protocol fed in; remaining sharpening targets A's tool-registry-growth, B's coordination-overhead-magnitude, C's plan-authoring-discipline
3. **Restored-polarity dispatching** [cycle 105+] — once second-iteration sharpening absorbs
4. **Symphony deeper-read elevation** [cycle 105+ or based on cluster catalogue findings]
5. **oh-my-claudecode deeper-read elevation** [cycle 106+ or based on cluster catalogue findings]

---

## Cycle 102 — cluster catalogue rebuild absorbing PAI + omx deep-dive findings (50 → 54 sub-shapes + 1 candidate sub-axis; cluster A 9 → 11 sub-shapes with NEW task-ingestion routing sub-cluster grouping; cluster H 4 → 6 sub-shapes; cluster F 5 → 6-system convergent + 3-system substrate-edge convergence observation; cluster I substrate-coverage extends to 4-substrate-type map; catalogue-staleness ELEVATED → RECENT)

**Cycle issue:** [#2891](https://github.com/EvaLok/schema-org-json-ld/issues/2891)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **thirteenth cycle of Phase 2 candidate-set work** [cycles 90-102]; **first cycle of post-absorption catalogue rebuild** [cycle 102]).
**Cycle composition shape:** **catalogue-rebuild-with-multi-cycle-absorption** — NOVEL functional-class shape at 1 instance. Cluster catalogue update absorbing 2+ deep-dive landings (PAI cycle 100 + omx cycle 101) deferred for catalogue-staleness elevation; cycle 102 executes the rebuild against the 5 named update points. **21 functional-class shapes total demonstrated cycles 62-102 at 42 instances.** Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### What I did

- **Updated [`docs/redesign/1-research/clusters.md`](../redesign/1-research/clusters.md)** in 19 distinct edit locations across 5 cluster updates (A, F, H, I, comparison summary). File grew from 3459 to 3853 lines (+394 lines, +11.4%).
- **Cluster A**: depth `[6-system clean]` → `[6-system clean + 2-system augmentation cycles 100-101]`; added sub-shape 10 (classifier-mediated mode dispatch from PAI, M1 ABSENT / M2 MODERATE; design work `classify-cycle-task` Rust tool); added sub-shape 11 (deterministic-decision-tree routing from omx, M1 ABSENT / M2 LOW; design work `route-cycle-decision` Rust tool); NEW sub-cluster grouping "Task-ingestion routing" (5th grouping joining phase-boundary semantics, recovery operations, concurrency/queuing, process isolation); substrate-fit summary 6 ABSENT → 8 ABSENT; cost summary 4 LOW + 5 MODERATE → 5 LOW + 6 MODERATE; Phase 2 evaluation extended with 3-way architectural distinction (deterministic-decision-tree vs classifier-mediated vs prompt-driven phasing) + dispatch-shape pipeline observation (classify-then-route).
- **Cluster H**: depth `[4-system convergent]` → `[6-system convergent]`; added sub-shape 5 (feedback-signal-inference from PAI, M1 ABSENT / M2 MODERATE; design work `infer-feedback-signal` Rust tool — distinct from existing sub-shapes by inferring feedback signal from incidental utterance rather than requiring explicit rating event, usage metric, or completion signal); added sub-shape 6 (evaluator-driven keep-discard from omx, M1 ABSENT / M2 LOW; design work `evaluator-driven-consolidate` Rust tool — distinct from sub-shape 2 score-gated consolidation by write-time LLM-critic judgment vs post-hoc usage statistics); substrate-fit summary 2 ABSENT → 4 ABSENT; cost summary 0 LOW + 2 MODERATE + 2 HIGH → 1 LOW + 3 MODERATE + 2 HIGH; Phase 2 evaluation extended with quality-judgment-axis dispatch shape (write-time vs recall-time judgment as distinct primitives).
- **Cluster F**: depth `[5-system convergent]` → `[6-system convergent + 3-system substrate-edge convergence]`; PAI cycle 100 confirmed 6th system with all 8 sub-axes at code level; 3-system substrate-edge convergence observation (omx + PAI + omc thin-wrapper-with-deep-hooks pattern); added candidate sub-axis 9 (output-shape stratification — deterministic-post-processing-of-LLM-output from omx normalize_summary, M1 ABSENT / M2 LOW, **CANDIDATE — 1-system evidence; awaiting 2-system convergence** before promotion to full sub-axis); Phase 2 candidate implication explicit (substrate-edge pattern is substrate-correlated to CLI-with-event-injection-hooks; v1's GitHub Actions substrate is NOT directly transferable but underlying principle is borrow-able).
- **Cluster I**: depth annotation extends with "substrate-coverage extended cycles 100-101"; NEW substantial subsection "Substrate-coverage extension (cycles 100-101 absorption)" naming **4-substrate-type coverage map**: cloud-anchored multi-actor (openclaw + OpenAI harness) + single-user personal-assistant (PAI via Claude Code permission system) + configuration-layer-over-CLI (omx via thin-wrapper-with-deep-hooks) + research-artifact substrate-absent (Voyager). Substrate-correlation reasoning shifts from "v1's substrate is uniquely correlated" to "v1's substrate is one of multiple correlated types where cluster I patterns transfer."
- **Cluster comparison summary table**: totals updated from 50 to 54 sub-shapes (+ 1 candidate sub-axis); aggregate distribution 6 STRONG (12% → 11%) + 25 PARTIAL (50% → 46%) + 19 ABSENT (38% → 43%); cost distribution 16 LOW (32% → 33%) + 27 MODERATE (54%) + 7 HIGH (14% → 13%); substrate-alignment ranking re-ordered (cluster A drops 6th → 8th by ABSENT count; cluster H stays 7th-tier band); self-management cost ranking re-ordered (cluster A stays tied lowest; cluster H stays highest).
- **Updated [`docs/redesign/1-research.md`](../redesign/1-research.md)** in 2 places: quick-reference cluster table sync'd to clusters.md updates (+14 lines, +1.7%); trailing paragraph updated with explicit cycle 102 absorption delta (50 → 54 sub-shapes + 1 candidate; 4 NEW ABSENT; 2 NEW LOW + 2 NEW MODERATE; 0 new HIGH-cost).
- **Documented findings** in [`_notes/cycle-102-cluster-catalogue-update.md`](../redesign/_notes/cycle-102-cluster-catalogue-update.md) — setup with cycle-100/101 catalogue-update-deferral framing; 21 explicit edit locations enumerated; explicit not-modified items (6 items including cross-cluster intersection sections, M3 v1 strengths layer, open structural questions, v1-failure-mode mapping, 2-design-framework.md, 2-candidates A/B/C); verification (line counts, heading counts, table arithmetic); what-surprised-me / what-noticed (5 observations including ABSENT-delta concentration in 2 clusters, no HIGH-cost addition, candidate-vs-full sub-shape distinction is architectural distinctiveness not just system count, cluster A sub-cluster grouping count growth from 4 to 5 closing cycle 71 open structural question, cluster I substrate-coverage extension as most semantically novel update); sibling pattern tracking; cycle 103+ plan; pre-commit checklist.

### Catalogue staleness state transition

**Catalogue-staleness ELEVATED → RECENT.** Cycle 100 left catalogue staleness at NOTABLE (1-cycle debt: PAI deep-dive landing not absorbed); cycle 101 elevated to ELEVATED (2-cycle debt: PAI + omx both not absorbed). Cycle 102 absorbs both into the catalogue and downgrades staleness to RECENT. Next staleness elevation will trigger when the next deep-dive landing absorbs (Symphony deeper-read elevation per cycle 105+ plan or oh-my-claudecode deeper-read elevation per cycle 106+ plan).

### NEW catalogue updates not previously in the catalogue

The catalogue's prior cycle 89 baseline + 4-grouping cluster A structure has now grown to:

| Layer | Pre-cycle-102 | Post-cycle-102 |
|---|---|---|
| Total sub-shapes | 50 | 54 (+ 1 candidate) |
| Cluster A sub-shapes | 9 | 11 |
| Cluster H sub-shapes | 4 | 6 |
| Cluster F sub-axes | 8 | 8 (+ 1 candidate) |
| Cluster A sub-cluster groupings | 4 | 5 |
| Cluster I substrate types | 1 (cloud-anchored multi-actor) | 4 (cloud-anchored + personal-assistant + configuration-layer-over-CLI + research-artifact-absent) |
| Aggregate STRONG / PARTIAL / ABSENT | 6 / 25 / 19 | 6 / 25 / 23 |
| Aggregate LOW / MODERATE / HIGH | 16 / 27 / 7 | 18 / 29 / 7 |

The 4 new sub-shapes (A.10, A.11, H.5, H.6) are all ABSENT (substantial Rust tool design work for Phase 2 candidates); the 1 candidate sub-axis (F.9) is ABSENT but at 1-system evidence (awaiting confirmation). The 4 new sub-shapes split 2 LOW + 2 MODERATE + 0 HIGH on the cost axis — bounded-event mechanisms (per-cycle classification, per-event inference, per-finding evaluation) rather than continuous-background mechanisms.

### What surprised me / what I noticed

1. **The 4 new sub-shapes from cycles 100-101 are all bounded-event primitives, none are continuous-background.** HIGH-cost in the catalogue concentrates on continuous-background mechanisms (consolidation, gardening, semantic-retrieval, dynamic stratification, parallel branches). The cycle 100/101 absorption added classifier-mediated dispatch + deterministic-decision-tree routing + feedback-signal-inference + evaluator-driven keep-discard — all of these run on bounded inputs (per-cycle classification, per-event inference, per-finding evaluation). **Deeper-system absorption tends to surface bounded-event primitives at LOW/MODERATE cost** rather than continuous-background HIGH-cost primitives. Phase 2 candidate implication: the cycle 100-101 absorption shifts the substrate-design work toward more sub-shapes overall but at lower per-cycle cost than HIGH-cost sub-shapes would impose.

2. **Cluster A's sub-cluster grouping count grew from 4 to 5 — first growth since cycle 85.** The cycle 85 audit#454 D3 absorption introduced 4 sub-cluster groupings (phase-boundary semantics, recovery operations, concurrency/queuing, process isolation). Cycle 102 introduces task-ingestion routing as a 5th grouping. **The catalogue is answering the cycle 71 open structural question about cluster A sub-cluster grouping cardinality with new data**: 5 groupings is now data-supported, and the +1 grouping is architecturally orthogonal to the prior 4 (task-ingestion routing happens at session ENTRY before any phase-boundary semantics begin, not as a sub-mechanism of them). The cycle 71 open structural question doesn't close — there could be more groupings as further deep-reads arrive — but the answer "5 groupings, with task-ingestion routing as orthogonal to phase-boundary" is now data-anchored.

3. **Cluster I's substrate-coverage extension is the most semantically novel cycle 102 update.** The cluster I depth held at 2-system convergent (no new sub-shapes), but the substrate-coverage observation expanded from 1 substrate-correlated type to 3 substrate-correlated types. **The cycle 89 substrate-fit weighting hypothesis is RE-CALIBRATED**: cluster I correlation isn't about cloud-substrate uniquely; it's about *any harness with permission-policy enforcement primitives*. v1's GitHub Actions substrate is one of multiple substrate types where cluster I patterns transfer. Implication for Phase 2 weighting (audit#454 P2): cluster I substrate-fit weighting remains valid but the correlation reasoning shifts from "v1 is uniquely positioned for cluster I" to "v1 inherits cluster I primitives the same way other harness-mediated substrates do." This is a **subtle but material recalibration** of the Phase 2 weighting argument.

4. **The +394-line clusters.md growth (+11.4%) is concentrated in the 4 new sub-shape annotations (A.10, A.11, H.5, H.6) at ~80 lines each plus the cluster comparison summary update.** Each new sub-shape annotation follows the established M1/M2 format (substrate-fit + design work + self-management cost + cross-references) which produces a consistent annotation density. **The catalogue's per-sub-shape annotation density is stable at ~75-90 lines each across the 50→54 transition** — no annotation-format drift; the cycle-86-89 M-item annotation discipline holds at cycle-102 absorption.

5. **The candidate sub-axis F.9 (output-shape stratification) is the first explicitly-candidate-status entry in the catalogue.** All prior sub-shapes were full sub-shapes at 1-system origin (per the convention "1-system origin still merits sub-shape status if the mechanism is architecturally distinct"). The cycle 101 absorption explicitly introduced "candidate" status for F.9 because the deterministic-post-processing-of-LLM-output mechanism *might* be idiosyncratic to omx's normalize_summary and not a generally-transferable pattern. **The "candidate vs full sub-shape" distinction is now a catalogue convention** for cases where 1-system evidence raises a transferability question that deeper reads can resolve. Symphony deeper-read or PAI DocIntegrity deeper-read can confirm or refute the F.9 candidacy.

### Standing tasks

- **PR / issue closures from cycles 100-101** — completed cycle 101.
- **Cluster catalogue update** [cycle 102, **HIGH PRIORITY** — staleness ELEVATED] — **completed cycle 102**. Catalogue staleness now RECENT.
- **Author B's counting protocol** [cycle 103, HIGH PRIORITY] — deferred from cycles 96-102.
- **Cross-cluster intersection updates** [cycle 103+] — re-evaluate the 7 cross-cluster intersection patterns from cycles 72/74 with the new cluster A + cluster H sub-shapes; specific candidate intersections: A↔B (classifier-dispatch storage), F↔H (Sonnet substrate dual-cast), A↔C (task-ingestion-routing as session-start lifecycle).
- **Second-iteration sharpening on candidates A/B/C** [cycle 104+] — with full 4-system absorption integration (Symphony + omc first-pass + PAI deep + omx deep) + cluster catalogue update (cycle 102) feeding into candidate risk refinement.
- **Restored-polarity dispatching** [cycle 104+] — once absorption arc + catalogue update + Author B's counting protocol complete.
- **Symphony deeper-read elevation** [cycle 105+ or based on cluster catalogue findings] — first-pass at cycle 98; could resolve cluster F.9 candidate status if Symphony's spec-first BEAM substrate has equivalent deterministic-post-processing primitive.
- **oh-my-claudecode deeper-read elevation** [cycle 106+ or based on cluster catalogue findings] — first-pass at cycle 99; deeper read would confirm/refute the 3-system substrate-edge convergence observation more directly.

### Sibling pattern tracking

- **Functional-class shape #19 (research-deliverable-absorption-with-verification)** — cycle 102 NOT a new instance (no PR absorbed; this is a catalogue-rebuild cycle). The shape stays HARDENED at 3 verification-success + 1 verification-mixed instances.
- **NEW functional-class shape #20 (catalogue-rebuild-with-multi-cycle-absorption)** — cycle 102 first instance. Pattern: cluster catalogue updates absorbing 2+ deep-dive landings deferred for catalogue-staleness elevation; explicit rebuild executes the absorption against named update points. **NOVEL at 1 instance.**
- **Verification-discipline pattern (cycle 96 emergence)** → 6 instances HARDENED. Cycle 102 NOT a new instance (no PR verification this cycle).
- **Generalization-level discipline (J-Q(a))** → 19 instances HARDENED. Cycle 102 application: NEW kind of catalogue-edit-with-explicit-deferral discipline (5 not-modified items with reasoning); not a new generalization-level instance.
- **Direction-vs-magnitude discipline** → 11 instances HARDENED. Cycle 102 NOT a new instance.
- **Cluster catalogue depth-counting convention** (NOVEL cycle 102) — first cycle to extend cluster A and cluster H depths beyond their cluster-mining-base via deep-dive system contributions. Convention: when a deep-dive system contributes new sub-shape evidence to a cluster, the cluster depth extends; when it contributes new sub-axis or substrate-coverage evidence without new sub-shapes, the depth annotation uses augmentation language not raw system-count extension.
- **Per-cluster catalogue-update sequencing pattern** (NOVEL cycle 102) — cluster updates ordered by absorbed-evidence specificity: clusters with NEW sub-shapes (A, H) precede clusters with NEW sub-axes or candidate sub-shapes (F) precede clusters with substrate-coverage extension only (I). NOVEL at 1 instance.
- **Audit-as-peer pattern** — 2-instance evidence holds. Audit cycle 213 still silent-failed (no audit-repo commit since cycle 212 at 2026-05-07T04:35Z, ~48 hours since cycle-101 measurement; ~2 hours added since cycle 101). Cycle 102 NOT a new instance.

### Lexicon entries (cycle 102)

- *Catalogue-rebuild-with-multi-cycle-absorption* — NEW functional-class shape #20: cluster catalogue updates absorbing 2+ deep-dive landings deferred for catalogue-staleness elevation; explicit rebuild executes the absorption against named update points without further dispatching or absorbing.
- *Catalogue-staleness state machine* — RECENT → NOTABLE → ELEVATED state transitions corresponding to deep-dive landing debt; cycle 100 left NOTABLE; cycle 101 elevated to ELEVATED; cycle 102 downgrades to RECENT.
- *Sub-cluster grouping cardinality growth event* — first cluster A sub-cluster grouping count growth since cycle 85's audit#454 D3 absorption introduced the 4-grouping structure; cycle 102 introduces task-ingestion routing as 5th grouping.
- *Substrate-coverage map* — 4-substrate-type coverage map (cloud-anchored multi-actor + single-user personal-assistant + configuration-layer-over-CLI + research-artifact substrate-absent) for cluster I correlation reasoning. Substrate-correlation hypothesis re-calibrated from "v1's substrate is uniquely correlated" to "v1's substrate is one of multiple correlated types where cluster I patterns transfer."
- *Candidate vs full sub-shape catalogue convention* — 1-system origin merits full sub-shape status when mechanism is architecturally distinct; "candidate" status reserves for cases where 1-system evidence raises transferability question that deeper reads can resolve. Cycle 102 first cycle to introduce candidate-status in catalogue.
- *Per-cluster catalogue-update sequencing* — clusters with NEW sub-shapes precede clusters with NEW sub-axes precede clusters with substrate-coverage-only changes; preserves absorbed-evidence specificity ordering.

### Cycle 103+ plan

1. **Author B's counting protocol** [cycle 103, HIGH PRIORITY — deferred from cycles 96-102]
2. **Cross-cluster intersection updates** [cycle 103+] — re-evaluate the 7 patterns with new cluster A + cluster H sub-shapes
3. **Second-iteration sharpening on candidates A/B/C** [cycle 104+] with full 4-system absorption + cluster catalogue update (cycle 102) fed in
4. **Restored-polarity dispatching** [cycle 104+] once absorption arc + catalogue update + Author B's counting protocol complete
5. **Symphony deeper-read elevation** [cycle 105+ or based on cluster catalogue findings] — could resolve F.9 candidate status
6. **oh-my-claudecode deeper-read elevation** [cycle 106+ or based on cluster catalogue findings] — would confirm 3-system substrate-edge convergence

---

## Cycle 101 — research-deliverable absorption with verification mixed: PR #2874 oh-my-codex deeper-read (26 verification claims — 21 EXACT/within-drift + 5 NEW failure mode "structural-fabrication-with-correct-direction"; 3 corrections + 7 confirmations + 8-9 NEW patterns absorbed; omx elevated cycle-26 stub → 10th deep-dive system; absorption arc reaches natural pause)

**Cycle issue:** [#2890](https://github.com/EvaLok/schema-org-json-ld/issues/2890)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **twelfth cycle of Phase 2 candidate-set work** [cycles 90-101]; **sixth cycle of Phase 2 absorption arc** [cycles 96-101]).
**Cycle composition shape:** **research-deliverable-absorption-with-verification-mixed** — FOURTH instance of functional-class shape #19 family but with a NEW sub-variant. Cycle 98 (Symphony) NOVEL with verification-success; cycle 99 (omc) extended to TESTED at 2 instances with verification-success; cycle 100 (PAI) extended to HARDENED at 3 instances with one MAGNITUDE-1-OFF; cycle 101 introduces NEW sub-variant **verification-mixed** (substantial EXACT + structural-fabrication-with-correct-direction). **The shape itself remains HARDENED at 3 verification-success instances; cycle 101 is the first verification-mixed instance.** **20 functional-class shapes total demonstrated cycles 62-101 at 41 instances.** Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### What I did

- **Verified 26 quantitative + structural claims in PR #2874 against `Yeachan-Heo/oh-my-codex` at commit `d1863f72d303857e331865a863ed7f057dbd2cf6` via `gh api`.** 21 of 26 EXACT/within-drift: commit SHA + 11 file sizes (keyword-detector.ts 45590B / generator.ts 50358B / autoresearch/runtime.ts 45592B / mcp/state-server.ts 5352B / wiki-server.ts 8784B / trace-server.ts 10505B / memory-server.ts 15810B / AGENTS.md 24606B / threshold.rs 2151B / exec.rs 2819B all ✓ EXACT) + MCP server count 5 (corrects cycle-26's 3) ✓ EXACT + Rust crate count 5 (corrects cycle-26's 1) ✓ EXACT + Cargo.toml dep ✓ EXACT + 4 model constants ✓ EXACT + 2 constant contents (KEYWORDS_REQUIRING_INTENT 7 entries + should_retry_with_fallback 9 signals) ✓ EXACT + AGENTS.md line-132 directive ✓ EXACT TEXT. **5 substantive inaccuracies clustered as a NEW failure mode**: F1 `KOREAN_IME_MAP` constant FABRICATED (no such constant; inline regex inside `normalizeWorkflowKeyboardTypos()` at line ~535 not ~48); F2 `EXPLICIT_SKILL_RE` constant FABRICATED + regex INACCURATE (no constant; regex inline; actual `/(?:^|[^\w])\$(?:(?:oh-my-codex:)?([a-z][a-z0-9-]*))\b/gi` has `\b` and `gi` PR's pseudo-code omits); F3 `AUTO_COMPLETE_TRANSITIONS` MAGNITUDE-1-MISSING (PR shows 6, actual 7; PR missed `deep-interview->autoresearch`); F4 `ALLOWED_OVERLAP_PAIRS` STRUCTURE-WRONG (PR claims `ultrawork|*` is in set; actual set has only `'ralph|team'`; ultrawork-overlaps-everything is a runtime short-circuit in `isAllowedOverlap()` not set membership); F5 line-number citations approximate to point of non-utility (off by hundreds). **NEW failure mode: structural-fabrication-with-correct-direction** — architectural pattern prose direction-correct, code-citation specifics (constant names, regex literals, set membership locations, line numbers) fabricated, mis-located, or off-by-one. Distinct from cycle 96 fabrication-magnitude (numbers without counting, same direction, large magnitude error), cycle 99 direction-failure (H3 hypothesis directionally wrong), and cycle 100 magnitude-1-off-by-source-vs-runtime (single off-by-one with structural explanation).
- **Rewrote [`docs/redesign/1-research/systems/oh-my-codex.md`](../redesign/1-research/systems/oh-my-codex.md)** from 168-line cycle-33 stub to ~470-line deep-dive parity per cycle-98 / cycle-99 / cycle-100 per-system shape. Sections: verification banner with 26-claim table; sources read so far (substantially expanded with 18+ files); project framing reconciling cycle-26 high-level survey supersedence with required-reads-completion + sister-project parity; hypothesis verdicts (no H1-H3 — dispatch not authored under that frame; instead 3 corrections + 7 confirmations + 9 NEW patterns documented); patterns observed organized into 9 family buckets (state-memory-history, quality-and-discipline, algorithm-and-cycle-internal-phasing, hooks-and-lifecycle, detection-and-routing, configuration-synthesis, agent-architecture, MCP-tool-surface, Rust-substrate-edge, substrate-edge-orchestration); cluster framework anchoring across 4 cluster cells (A deterministic-decision-tree NEW sub-shape; F 3-system substrate-edge convergence with PAI + omc; H evaluator-driven-keep-discard NEW sub-shape; I substrate diversity); 7 anchoring caveats including the structural-fabrication-with-correct-direction adoption-time double-check guidance; 14-item deeper-read queue with priority ordering; cycle-101 absorption record.
- **Updated [`docs/redesign/1-research.md`](../redesign/1-research.md)** in four places: per-system reads table row oh-my-codex: Stub → "Deep-dive (commit `d1863f72`; 9-lens code-level read; 3 corrections + 7 confirmations + 9 NEW patterns; 5-crate Rust workspace, 5 MCP servers verified)" with link to PR #2874; cross-system observations preamble updated 9 → 10 deep-dive systems; further-systems-to-study row updated "Stub from cycle-26 dispatch; cycle-63 deeper read in flight" → "Deep-dive landed (cycle 101)"; cycle plan provisional item 5 updated to add oh-my-codex deeper-read landing alongside openclaw and PAI with expanded resolution-of-asymmetry to cover the cycle-26-vintage dispatch set + Eva-named reads pending elevation note.
- **Documented findings** in [`_notes/cycle-101-oh-my-codex-deeper-read-absorption.md`](../redesign/_notes/cycle-101-oh-my-codex-deeper-read-absorption.md) — setup with cycle-26-supersedence framing; verification table with NEW structural-fabrication-with-correct-direction failure-mode taxonomy entry; per-section verdicts (9 lenses all INTEGRATED); integration outcomes (5 files modified/created with explicit reasoning for each); 4 explicit not-modified items including the load-bearing CLUSTER CATALOGUE deferred-update with cycle 102/103 plan; what-surprised-me / what-noticed (5 observations including the structural-claim-vs-quantitative-claim verification axis, 22-pattern catalogue holding up at code level, 5-MCP-server / 5-Rust-crate corrections as data point about cycle-26 dispatch limits, autoresearch-ledger-vs-state-atomicity asymmetry as borrow-able pattern observation, normalize_summary allowlist as cluster F sub-shape candidate); sibling pattern tracking (5 patterns extended; 1 NEW sub-variant verification-mixed NOVEL at 1; required-reads-completion + cycle-26-dispatch-set parity HARDENED at 2); honest reflection per F1-F5 corrective with self-congratulation audit; pre-commit checklist.
- **Required-reads-completion + cycle-26-dispatch-set parity reached.** Cycle 100 cleared the redesign-prompt's two required reads (openclaw cycle 43 / PAI cycle 100). Cycle 101 elevates the cycle-26-vintage oh-my-codex dispatch from stub to deep-dive parity. Both Eva-named further reads (oh-my-claudecode cycle 99 first-pass / openai/symphony cycle 98 first-pass) remain at first-pass pending elevation. **The absorption arc reaches natural pause at cycle 101.**

### Verification table (compressed)

| Class | Subject | Verdict |
|---|---|---|
| Commit + 11 file sizes (12) | SHA + keyword-detector / generator / autoresearch-runtime / 4 MCP servers / AGENTS.md / threshold.rs / exec.rs | All ✓ EXACT |
| Server/crate counts (2) | MCP 5 (corrects cycle-26's 3) / Rust crates 5 (corrects cycle-26's 1) | Both ✓ EXACT |
| Constants (4) | DEFAULT_SPARK_MODEL / DEFAULT_STANDARD_MODEL / DEFAULT_SUMMARY_TIMEOUT_MS / DEFAULT_FRONTIER_MODEL | All ✓ EXACT |
| Constant content (2) | KEYWORDS_REQUIRING_INTENT 7 entries / 9 retry signals | Both ✓ EXACT |
| AGENTS.md text (1) | line-132 directive | ✓ EXACT TEXT |
| Code structure (5) | KOREAN_IME_MAP / EXPLICIT_SKILL_RE / AUTO_COMPLETE_TRANSITIONS / ALLOWED_OVERLAP_PAIRS / line-number citations | ⚠ STRUCTURAL-FABRICATION-WITH-CORRECT-DIRECTION |

**Net:** 26 quantitative + structural claims verified — **21 EXACT/within-drift + 5 substantive inaccuracies clustered as a NEW failure mode**. Methodologically PR #2874's verification posture is **MIXED-DISCIPLINE**: file sizes / counts / constants verify EXACT (author measured); regex literals / constant names / specific line numbers / set membership locations have substantive inaccuracy (author appears to have authored from memory or pattern-matching). Dispatch-author verification-discipline meta-attribute now stands at **3-success + 2-mixed-success / 1-failure on a 6-instance evidence base** (cycle 96 PR #2878 fabrication-magnitude failure / cycle 97 PR #2877 calibration verified / cycle 98 PR #2873 file-state verified / cycle 99 PR #2876 EXACT across 13 metrics / cycle 100 PR #2875 EXACT across 14 with direction-accurate magnitude-1-off / cycle 101 PR #2874 EXACT across 21 quantitative + structural-fabrication-with-correct-direction across 5 code-citation specifics).

### NEW failure-mode taxonomy entry

The verification-discipline catalogue grows from 3 failure modes to 4:

- **Cycle 96 PR #2878:** *direction-accurate / magnitude-FABRICATED* (5.2× / 1.86× over-counts on workspace-LOC anchors — claimed numbers but didn't count). Quantitative-physical claim layer, large magnitude error in same direction.
- **Cycle 99 PR #2876 H3:** *direction-WRONG* (predicted omc smaller than omx; verified omc 1.26-2.81× larger). Hypothesis-direction layer, opposite asymmetry from claimed.
- **Cycle 100 PR #2875:** *direction-accurate / magnitude-1-OFF-by-source-vs-runtime* (claimed 16 typed memory dirs; 15 source-checked-in + 1 runtime-created; off-by-one with structural explanation). Quantitative-physical claim layer, single off-by-one.
- **Cycle 101 PR #2874:** *structural-fabrication-with-correct-direction* (architectural patterns described in prose are real; code-citation specifics — constant names, regex literals, set membership locations, line numbers — fabricated, mis-located, or off-by-one). Structural-citation claim layer, multiple inaccuracies with direction-correct prose.

The cycle-101 failure mode is methodologically distinct because it surfaces in a different *layer* (structural-citation vs quantitative-physical) than the prior failure modes. The verification-discipline catalogue gets a NEW axis: *structural-claim verification distinct from quantitative-claim verification*. A dispatch can be EXACT on file sizes and constant values while still being unreliable on regex literals and constant names; both axes need verification at adoption time.

### What surprised me / what I noticed

1. **The structural-fabrication-with-correct-direction failure mode is methodologically distinct from prior failure modes in the catalogue.** Earlier failure modes were all in *quantitative* claims; cycle-101 surfaces failure in *code-citation specifics* while the architectural prose-level claims they cite are direction-correct. This is the first failure mode the verification discipline has surfaced in the structural-claim layer; prior failures were all in the magnitude-claim layer. **Verification-discipline gets a new axis** — structural-claim verification distinct from quantitative-claim verification.

2. **The 22-pattern catalogue from cycle-26 holds up well at code level.** Of the 22 patterns enumerated at cycle 26, 3 received corrections (crate count, model name, state file authority — measurement errors, not architectural mistakes), 7 received explicit code-level confirmations, and the remaining 12 were not contradicted. **The cycle-26 high-level survey was directionally accurate at the architectural-pattern level even where it lacked code-level evidence.** Encouraging for the quality of first-pass research dispatches in the omx/omc/Symphony/PAI shape.

3. **The 5-MCP-server / 5-Rust-crate corrections are themselves a data point about cycle-26 dispatch limits.** Both corrections involve counting items in directories — trivial to get right with 1 minute of `gh api` use but easy to get wrong if relying on README-level context or partial directory reads. Pattern: *first-pass surveys produce architecture-correct + count-imprecise outputs*; deeper reads correct the counts without overturning the architecture. This methodology-shape vindicates the cycle-63 / cycle-71 / cycle-77 / cycle-75 deeper-read dispatching strategy.

4. **The autoresearch-ledger-vs-state-atomicity asymmetry is a borrow-able pattern observation.** omx's state layer uses POSIX atomic rename (`writeAtomicFile()`); the autoresearch iteration ledger uses plain `writeFile()`. The asymmetry is documented as a known architectural gap. Pattern observation: any adaptation of an external system's persistence pattern should *audit the atomicity uniformly* across its data files; differences are crash-safety differences, and asymmetry suggests evolutionary addition rather than designed-uniform discipline.

5. **`normalize_summary` allowlist is the strongest single-system evidence for cluster F (substrate-coverage) sub-shape: deterministic-post-processing-of-LLM-output.** omx-sparkshell's contract: model is INSTRUCTED to produce sections `summary:`, `failures:`, `warnings:`; `normalize_summary()` ENFORCES this at parse time regardless of what the model emits. **Deterministic post-processing contract for LLM output**, distinct from prompt-only contracts (PAI's prompt-driven format) and prompt-+-test contracts (omc's prompt-guidance-contract.test.ts regex enforcement). 1-system evidence; potential 2-system convergence if Symphony has a similar layer (deeper read needed).

### Standing tasks

- **PR #2874 + issue #2833 closure** with forward-links pointing to per-system file + absorption note + journal entry — **completed cycle 101**.
- **Cluster catalogue update** — DEFERRED to cycle 102 or 103, **HIGH PRIORITY**. Cycle 100 left catalogue-staleness at NOTABLE (2-cycle debt now); cycle 101 elevates to **ELEVATED**. omx deep-dive contributes evidence to clusters A (deterministic-decision-tree NEW sub-shape candidate distinct from PAI's classifier-mediated routing and openclaw's prompt-driven phasing), F (3-system substrate-edge thin-wrapper-with-deep-hooks convergence: omx + PAI + omc), H (evaluator-driven keep-discard NEW sub-shape candidate distinct from PAI's feedback-signal-inference), I (substrate diversity now spanning configuration-layer-over-CLI as additional substrate type).
- **Author B's counting protocol** [cycle 102 or 103] — deferred from cycles 96-101.
- **Second-iteration sharpening on candidates A/B/C** [cycle 104+] — with full 4-system absorption integration (Symphony + omc + PAI deeper + omx deeper) feeding into cluster catalogue + candidate risk refinement. Catalogue update precedes candidate sharpening.
- **Restored-polarity dispatching** [cycle 104+] — once absorption arc + catalogue update complete; absorption arc reaches natural pause at cycle 101.
- **Symphony deeper-read elevation** [cycle 105+ or based on cluster catalogue findings] — first-pass at cycle 98; strongest "deeper read needed" signal among Eva-named reads if cluster F or A convergence depends on its spec-first BEAM substrate evidence.
- **oh-my-claudecode deeper-read elevation** [cycle 106+ or based on cluster catalogue findings] — first-pass at cycle 99; 2-instance evidence on event-driven-runtime-replacing-polling pattern (omc + Symphony) merits deeper read for elevation.

### Sibling pattern tracking

- **Functional-class shape #19 (research-deliverable-absorption-with-verification)** — verification-success HARDENED at 3 instances (cycles 98 / 99 / 100); cycle 101 introduces NEW sub-variant **verification-mixed** (substantial EXACT + structural-fabrication-with-correct-direction), NOVEL at 1 instance. The shape itself remains structurally stable; the sub-variant captures that verification can succeed at the quantitative-physical layer while surfacing failure at the structural-citation layer.
- **Verification-discipline pattern (cycle 96 emergence)** → **6 instances HARDENED**. Cycle 101 contributes a NEW failure-mode entry (structural-fabrication-with-correct-direction) and a NEW axis (structural-claim verification distinct from quantitative-claim verification).
- **Dispatch-author verification-discipline meta-attribute** → **6 instances HARDENED** (3-success / 2-mixed-success / 1-failure ratio).
- **Generalization-level discipline (J-Q(a))** → **19 instances HARDENED**.
- **Direction-vs-magnitude discipline** → **11 instances HARDENED**. Cycle 101 application: NEW kind of direction-vs-structural-citation drift within the discipline's catalogue.
- **Required-reads-completion + cycle-26-dispatch-set parity** → HARDENED at 2 instances. Cycle 100 first instance with required-reads-only; cycle 101 extends to required-reads + cycle-26-dispatch-set both at deep-dive parity, with Eva-named further reads at first-pass-or-deeper.
- **Sister-project structural-alignment pattern** — extends to **2-pattern-shape evidence**. omx is the senior project; omc is larger by 5 metrics (1.26-2.81×). Sister-project alignment is structural; substrate-investment asymmetry runs the other direction from authoring-time prominence.
- **Sibling-pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 101 NOT a new instance.
- **Audit-as-peer pattern** — 2-instance evidence holds. Audit cycle 213 still silent-failed (no audit-repo commit since cycle 212 at 2026-05-07T04:35Z, ~46 hours since cycle-100 measurement; ~2 hours added since cycle 100). Cycle 101 NOT a new instance.

### Lexicon entries (cycle 101)

- *Structural-fabrication-with-correct-direction* — NEW failure mode: architectural-pattern prose direction-correct AND code-citation specifics (constant names, regex literals, set membership, line numbers) fabricated, mis-located, or off-by-one. Distinct from quantitative-claim failure modes (cycle 96 / 99 / 100).
- *Quantitative-claim vs structural-claim verification axes* — NEW dimension in verification-discipline: dispatch verification can succeed at one axis while failing at the other within a single dispatch.
- *Mixed-success verdict on dispatch-author verification-discipline meta-attribute* — NEW verdict category alongside success / failure; characterizes dispatches that verify EXACT at quantitative-physical layer but surface inaccuracy at structural-citation layer.
- *Required-reads-completion + cycle-26-dispatch-set parity* — state achieved cycle 101: both required reads (openclaw cycle 43 / PAI cycle 100) + all 3 cycle-26-vintage dispatch deepenings (Cognition Devin cycle 41 / OpenAI harness cycle 41 / oh-my-codex cycle 101) now at deep-dive parity.
- *Catalogue-staleness ELEVATED* — state where deferred catalogue updates have accumulated to 2+ cycles; explicit rebuild owed.
- *Mixed-discipline-dispatch* — dispatch where one claim layer (e.g., quantitative-physical) verifies EXACT while another claim layer (e.g., structural-citation) surfaces inaccuracy. Adoption guidance: *re-read source for the inaccuracy-prone layer rather than copy from dispatch's pseudo-code blocks*.

### Cycle 102+ plan

1. **Cluster catalogue update** [cycle 102 or 103, **HIGH PRIORITY** — staleness ELEVATED]
2. **Author B's counting protocol** [cycle 102 or 103]
3. **Second-iteration sharpening on candidates A/B/C** [cycle 104+] with full 4-system absorption + cluster catalogue update fed in
4. **Restored-polarity dispatching** [cycle 104+] once absorption arc + catalogue update complete
5. **Symphony deeper-read elevation** [cycle 105+ or based on cluster catalogue findings]
6. **oh-my-claudecode deeper-read elevation** [cycle 106+ or based on cluster catalogue findings]

---

## Cycle 100 — research-deliverable absorption with verification success: PR #2875 PAI deeper-read (14 quantitative claims verified — 13 EXACT/within-drift + 1 magnitude-1-off-by-source-vs-runtime; H1 REFUTED / H2 CONFIRMED / H3 PARTIALLY CONFIRMED; PAI elevated cycle-14 stub → 9th deep-dive system; shape #19 → HARDENED at 3 instances)

**Cycle issue:** [#2889](https://github.com/EvaLok/schema-org-json-ld/issues/2889)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **eleventh cycle of Phase 2 candidate-set work** [cycles 90-100]; **fifth cycle of Phase 2 absorption arc** [cycles 96-100]).
**Cycle composition shape:** **research-deliverable-absorption-with-verification-success** — THIRD instance of functional-class shape #19. Cycle 98 (Symphony) established the shape as NOVEL; cycle 99 (oh-my-claudecode) extended to TESTED at 2 instances; cycle 100 (PAI deeper-read) extends to **HARDENED at 3 instances**. **20 functional-class shapes total demonstrated cycles 62-100 at 40 instances.** Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### What I did

- **Verified 14 quantitative claims in PR #2875 against `danielmiessler/Personal_AI_Infrastructure` v5.0.0 via `gh api`.** 13 of 14 matched at EXACT precision or within elapsed-time drift: top-level Tools/ 2 source files ✓; PAI/TOOLS/ 72 items ✓ (PR claim "70+"); hooks 38 .hook.ts/.sh ✓ (PR claim 37 within drift); skills 45 directories ✓ EXACT; Algorithm versions LATEST + v5.7.0 + v6.0.0 + v6.1.0 + v6.2.0 + v6.3.0 ✓ EXACT; Algorithm v6.3.0 size 46068 bytes ≈ 46KB ✓ EXACT; Inference.ts 18125 bytes ✓ exists; ContextReduction.hook.sh 11866 bytes ✓ (.sh shell script as PR specified); SatisfactionCapture.hook.ts 18277 bytes in hooks/ ✓ EXACT; IsaFormat.md 33322 bytes ≈ 33KB ✓ EXACT; Releases v5.0.0 + v4.0.3 both present ✓; Packs/ 52 dirs ✓ within "50+" range; main commit SHA `9fb9c86` ✓ EXACT (date drift acceptable). **The 14th claim is MAGNITUDE-1-OFF in a methodologically interesting way:** PR's "16 typed directories" in MEMORY/ → actual 15 source-checked-in (LEARNING/ runtime-created at first SatisfactionCapture/WorkCompletionLearning hook fire). Direction-accurate (multi-tier typed memory IS architecturally present); count off by 1 because LEARNING/ doesn't exist in source release until first hook write. **NEW direction-vs-magnitude failure mode** distinct from cycle 96 fabrication-magnitude (didn't count) and cycle 99 direction-failure (opposite asymmetry from claimed): cycle 100 is direction-accurate / magnitude-1-off-by-source-vs-runtime-distinction.
- **Rewrote [`docs/redesign/1-research/systems/pai.md`](../redesign/1-research/systems/pai.md)** from 84-line cycle-14 README-only stub to deep-dive parity (~470 lines) per cycle-98 / cycle-99 per-system shape. Sections: verification banner with 14-claim table; sources read so far (substantially expanded); project framing reconciling v5.0.0 vs v4.0.3 with "Life Operating System" framing; H1/H2/H3 hypothesis verdicts (REFUTED / CONFIRMED / PARTIALLY CONFIRMED) with evidence; patterns observed organized into 7 family buckets (memory & history, algorithm & cycle-internal phasing, Learn→Improve closure, skills & agents, hooks & lifecycle, Tools layer, documentation honesty); cluster framework anchoring across 10 cluster cells; 10 NEW patterns honestly consolidated to ~5-6 distinct architectural axes per cycle-98 reflection discipline; **17-principle verification table** (13 CONFIRMED with REFINED/RESTRUCTURED qualifications + 2 PARTIALLY CONFIRMED [#7 Spec/Test/Evals First — no automated test suite, no CI test gate; #16 Permission to Fail — no structured "I don't know" return type] + 0 REFUTED); 7 anchoring caveats; 7-item deeper-read queue; cycle-100 absorption record.
- **Updated [`docs/redesign/1-research.md`](../redesign/1-research.md)** in three places: per-system reads table row PAI: First-pass → "Deep-dive (v5.0.0; 9-lens code-level read; H1/H2/H3 verdicts; 17-principle table)" with link to PR #2875; required-reads-remaining from "Deeper reads queued" → "Deeper reads complete" with cycle 43 (openclaw) and cycle 100 (PAI) landing references; cycle plan provisional item 5 updated noting deliverable-size asymmetry resolved with both required reads now at deep-dive parity; cross-system observations preamble updated 8 → 9 deep-dive systems (with Symphony + oh-my-claudecode noted as first-pass pending elevation).
- **Documented findings** in [`_notes/cycle-100-pai-deeper-read-absorption.md`](../redesign/_notes/cycle-100-pai-deeper-read-absorption.md) — setup with H1/H2/H3 hypothesis structure; verification table with NEW direction-vs-magnitude failure-mode taxonomy; per-section verdicts (10 sections + 17-principle table all INTEGRATED); integration outcomes (3+1 files modified/created with explicit reasoning for each); 4 explicit not-modified items including the load-bearing CLUSTER CATALOGUE deferred-update with cycle 102/103 plan; what-surprised-me / what-noticed (5 observations including the source-vs-runtime distinction, the PARTIALLY CONFIRMED principles being the substantive findings, classifier-mediated mode dispatch as architecturally novel, DocIntegrity auto-derivation as transferable mechanical-doc-honesty primitive, dispatch-authoring-quality variability across the corpus); sibling pattern tracking (5 patterns extended; 1 NOVEL required-reads-completion; 1 TESTED→HARDENED shape #19); honest reflection per F1-F5 corrective with self-congratulation audit; pre-commit checklist.
- **Closed PR #2875 + issue #2842** with forward-link comments per housekeeping closure-discipline (same pattern as cycle 96 PR #2878 / issue #2870, cycle 97 PR #2877 / issue #2869, cycle 98 PR #2873 / issue #2851, cycle 99 PR #2876 / issue #2847).
- **Initialized [`MEMORY.md`](/home/runner/.claude/projects/-home-runner-work-schema-org-json-ld-schema-org-json-ld/memory/MEMORY.md)** with cross-cycle redesign baseline. The memory directory has been empty across all 100 cycles of redesign mode; cycle 100 captures the load-bearing context that future cold-starts need (current phase, current cycle composition shape, hand-off pointer to cycle 101+, persistence mechanism location, audit-as-peer protocol). The MEMORY.md initialization is itself a new persistence-mechanism instance worth flagging in the journal.

### Verification table (compressed)

| Class | Subject | Verdict |
|---|---|---|
| Tools layer (3) | top-level Tools/ 2 src / PAI/TOOLS/ 72 / hooks 38 | All ✓ EXACT or within drift |
| Architecture (4) | skills 45 / Algorithm versions 5 / v6.3.0 size 46068 / Inference.ts 18125 | All ✓ EXACT |
| Hooks (2) | ContextReduction.hook.sh 11866 / SatisfactionCapture.hook.ts 18277 in hooks/ | Both ✓ EXACT |
| Substrate (3) | IsaFormat.md 33322 / Releases v5.0.0 + v4.0.3 / Packs 52 dirs | All ✓ EXACT or within range |
| Reference (1) | main commit `9fb9c86` | ✓ EXACT SHA (date drift acceptable) |
| MEMORY/ count (1) | claim "16 typed directories" | ⚠ MAGNITUDE-1-OFF (15 source-checked-in + 1 runtime-created LEARNING/) |

**Net:** 14 quantitative claims verified — 13 EXACT/within-drift + 1 MAGNITUDE-1-OFF (direction-accurate source-vs-runtime distinction). PR #2875's author actually counted file sizes and queried metadata — calibration-first methodology consistent with PR #2873 / PR #2877 / PR #2876 (4-success / 1-failure on 5-instance dispatch-author verification-discipline meta-attribute base, with cycle 96 PR #2878 the only failure).

### H1/H2/H3 verdicts (compressed)

| Hypothesis | Cluster | Verdict | Evidence |
|---|---|---|---|
| H1 | J (semantic-retrieval architecture) | **REFUTED** | BM25 (`Tools/MemoryRetriever.ts`) + ripgrep (THINK phase) only; explicit anti-RAG documented stance since June 2025 |
| H2 | H (Learn→Improve closure as code) | **CONFIRMED** | `WorkCompletionLearning.hook.ts` (SessionEnd structured artifacts) + `SatisfactionCapture.hook.ts` (UserPromptSubmit LLM-inferred implicit rating); cluster H → 5-system convergent + NEW sub-shape "feedback-signal-inference" |
| H3 | A (Algorithm as code construct) | **PARTIALLY CONFIRMED** | 46KB versioned doctrine document + hook-enforced phase boundaries (ISASync, CheckpointPerISC, voice announcements, tier completeness gates, closed-capability enumeration with phantom-as-CRITICAL-FAILURE audit) — NOT a state machine in code sense (no `enum Phase`, no transition function); cluster A → NEW sub-shape "classifier-mediated mode dispatch" |

### What surprised me / what I noticed

1. **The source-vs-runtime distinction is a NEW direction-vs-magnitude failure mode.** PR's "16 typed directories" in MEMORY/ is direction-accurate (multi-tier typed memory is real) AND count-correct against operational state (16 dirs after first hook fire), but off-by-1 against source release (15 source-checked-in dirs; LEARNING/ runtime-created). Distinct from cycle 96 fabrication-magnitude (claimed-numbers-without-counting) and cycle 99 direction-failure (opposite asymmetry from claimed). Verification discipline gets a new failure-mode taxonomy entry: source-release vs operational-state count drift.

2. **PAI's PARTIALLY-CONFIRMED principles are the substantive findings, not the CONFIRMED ones.** 13 of 17 founding principles CONFIRMED; 2 PARTIALLY CONFIRMED — #7 Spec/Test/Evals First (no automated test suite, no CI test gate) and #16 Permission to Fail (no structured "I don't know" return type, no aspirational markings, no anti-pattern catalog with non-permanence caveat). The PARTIALLY CONFIRMED items are the **honesty surface** the deeper read produced: even PAI's "Code Before Prompts" stance has limits, and the principle name overpromises relative to what code delivers. Cycle-14 confirmation-bias warning was prescient — code-level reading produces a more nuanced picture than README implied, but in BOTH directions: alignment IS real for principles 4/5/6/8/11; alignment IS GAPPED for #7 / #16.

3. **Classifier-mediated mode dispatch is architecturally novel and potentially transferable.** PromptProcessing.hook.ts runs Sonnet on every top-level prompt and writes MODE/TIER/REASON/SOURCE before the executor sees the prompt. Executor required to honor the output exactly: "No regex fallback. No model judgment." Fail-safe: classifier error → ALGORITHM E3. **This is deterministic routing applied at task ingestion BEFORE the executor acts** — distinct from existing cluster A sub-shapes which are phase-transition-boundary primitives (mid-cycle gates). Direct application to redesign: a deterministic pre-classifier could route incoming cycle issues by type (research vs absorption vs sharpening vs schema-work) before the orchestrator selects approach.

4. **DocIntegrity auto-derivation of ARCHITECTURE_SUMMARY.md is a mechanical doc-honesty primitive worth borrowing.** PAI cannot have docs drift from authoritative source because the summary is regenerated from the source on every session end via DocIntegrity.hook.ts. Stronger discipline than ours (where doc-vs-code drift is a manual maintenance burden). Direct application: the `docs/redesign/1-research.md` cluster table is currently a manual copy of `docs/redesign/1-research/clusters.md` table; a regeneration script would prevent the drift this absorption note flagged. Pattern: any derived documentation should have a regeneration script + a hook (or CI job) that fires on the right event.

5. **Dispatch-authoring quality is variable across the corpus.** The 9-lens dispatch + H1/H2/H3 hypothesis verdicts + 17 founding-principle verdict table dual structure is stronger than either alone: hypotheses test specific architectural claims; principle table verifies each named principle against code. Cycle-71 dispatch (PAI) has both; cycle-75 dispatch (omc) had hypothesis structure without principle table; cycle-77 dispatch (Symphony) had hypothesis structure without principle table. **Dispatches with explicit hypothesis AND principle structure produce per-system files with the strongest evidentiary base.** Pattern observation worth recording: future dispatch templates should include both axes.

### Standing tasks

- **PR #2875 + issue #2842 closure** with forward-links pointing to per-system file + absorption note + journal entry — **completed cycle 100**.
- **Cluster catalogue update** — DEFERRED to cycle 102/103. PAI's deep-dive evidence merits substantial catalogue updates (cluster A: 9→10 sub-shapes; cluster H: 4-system→5-system convergent + 4→5 sub-shapes; cluster F: 5-system→6-system convergent + sub-axis annotations from PAI's two-category capability taxonomy + Hooks/Skills/Tools/Agents quad; cluster I: substrate-coverage extends to single-user personal-assistant). 3459-line restructure warrants its own dedicated cycle per cycle-99 catalogue-update discipline.
- **Catalogue staleness is now NOTABLE** — pre-PAI-deep-dive cluster counts were "anchored on 8 deep-dive systems" per cycle-99 phrasing; PAI at deep-dive makes 9; updating per-cluster system depth is owed.
- **PR #2874 oh-my-codex deeper-read absorption** [cycle 101, HIGH PRIORITY] — completes next item in absorption arc; oh-my-codex deeper-read elevates omx from cycle-26 stub → deep-dive parity; with omc still at first-pass, sister-project structural-alignment cross-system work has natural pairing once both at deep-dive.
- **Author B's counting protocol** [cycle 102 or 103] — deferred from cycles 96-100.
- **Second-iteration sharpening on candidates A/B/C** [cycle 104+] — with full 4-system absorption integration (Symphony + omc + PAI deeper + omx deeper) feeding into cluster catalogue + candidate risk refinement. Catalogue update precedes candidate sharpening.
- **Restored-polarity dispatching** [cycle 104+] — once absorption arc reaches natural pause and cluster catalogue update is complete.
- **MEMORY.md initialization** — cycle 100 first instance. Future cycles can ADD entries (user role, project phase, cycle-budget context, persistence-mechanism reference) but should NOT duplicate per-cycle context that lives in journals/notes.

### Sibling pattern tracking

- **Functional-class shape #19 (research-deliverable-absorption-with-verification-success)** → **HARDENED at 3 instances** (cycle 98 Symphony / cycle 99 oh-my-claudecode / cycle 100 PAI deeper). Pattern shape is structurally stable across 3 substrate-diverse instances; supports transferability.
- **Verification-discipline pattern (cycle 96 emergence)** → **5 instances HARDENED**. Cycle 100 contributes a NEW failure-mode entry (magnitude-1-off-by-source-vs-runtime).
- **Dispatch-author verification-discipline meta-attribute** → **5 instances HARDENED** (4-success / 1-failure ratio).
- **Generalization-level discipline (J-Q(a))** → **18 instances HARDENED**.
- **Direction-vs-magnitude discipline** → **10 instances HARDENED**. Cycle 100 application: NEW kind of magnitude-off failure mode within the discipline's catalogue (source-release vs operational-state count drift).
- **Required-reads-completion discipline** — NEW pattern observed at cycle 100. Both required reads (openclaw cycle 43 / PAI cycle 100) now at deep-dive parity. Cycle-16 deliverable-size asymmetry worry is RESOLVED. Pattern: NOVEL at 1 instance.
- **Sibling-pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 100 NOT a new instance.
- **Audit-as-peer pattern** — 2-instance evidence holds. Audit cycle 213 still silent-failed (no audit-repo commit since cycle 212 at 2026-05-07T04:35Z, ~44 hours since per cycle-99 measurement). Cycle 100 NOT a new instance.

### Lexicon entries (cycle 100)

- *Source-vs-runtime distinction* — verifiable structural source of count drift; checking source release vs operational state is the discipline for verification.
- *Magnitude-1-off-by-source-vs-runtime* — NEW failure mode within direction-vs-magnitude discipline: claim is direction-accurate AND count-correct against operational state, but off-by-1 against source. Distinct from fabrication-magnitude and direction-failure modes.
- *Required-reads-completion* — state where the redesign prompt's named required reads have all reached deep-dive parity.
- *9-lens-with-hypothesis-and-principles-table* — dispatch authoring discipline observed at cycle 100; combines H1-H3 architectural hypothesis tests with founding-principle verdict table for stronger evidentiary base than either alone.
- *Catalogue-staleness-NOTABLE* — state where deferred catalogue updates have accumulated to the point that an explicit catalog-rebuild cycle is owed.

### Cycle 101+ plan

1. **PR #2874 oh-my-codex deeper-read absorption** [cycle 101, HIGH PRIORITY] — last item in absorption arc.
2. **Cluster catalogue update** [cycle 102 or 103] — substantial restructure absorbing PAI deep-dive findings + (eventually) omx deep-dive findings.
3. **Author B's counting protocol** [cycle 102 or 103].
4. **Second-iteration sharpening on candidates A/B/C** [cycle 104+] — with full 4-system absorption + cluster catalogue update fed in.
5. **Restored-polarity dispatching** [cycle 104+] — once absorption arc + catalogue update complete.

---
