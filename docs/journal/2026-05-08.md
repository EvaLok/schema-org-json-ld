# 2026-05-08

## Cycle 97 — feedback absorption with verification success: PR #2877 tool-surface feasibility (calibration verified; 7 lenses integrated)

**Cycle issue:** [#2886](https://github.com/EvaLok/schema-org-json-ld/issues/2886)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **eighth cycle of Phase 2 candidate-set work** [cycles 90-97]; **second cycle of Phase 2 absorption arc** opened by Eva's #2883 unblock cycle 96; **first cycle after PR #2884 merged** 12:18 UTC today restoring Copilot dispatch mechanism repo-wide).
**Cycle composition shape:** **feedback-absorption-with-verification-success** — NOVEL functional-class shape #18 (1 instance). Distinct from cycle 96's shape #17 (feedback-absorption-with-correction) by verification outcome: cycle 96 found verified factual errors requiring correction; cycle 97 found verified facts requiring validation, not correction. **18 functional-class shapes total demonstrated cycles 62-97 at 37 instances.** Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### What I did

- **Verified PR #2877's calibration claim against the actual workspace.** PR #2877 reports "38 crates, median ~1081 LOC, mean ~2116 LOC, minority <500." Cycle 97 measurement: 38 crates (`ls -d tools/rust/crates/*/` count), median 1081.5 (mean of 19th and 20th sorted values), mean 2118 (total 80,476 / 38), 7 of 38 crates under 500 LOC (18.4%). **Verified to high precision** — within 0.05% on median, within 0.1% on mean. Unlike PR #2878 (cycle 91 anchors fabricated at 5.2× and 1.86× overcounts), PR #2877's author verifiably counted before producing the per-crate critique.
- **Integrated 7 lenses across A/B/C + README** ([commit `2224d0af`](https://github.com/EvaLok/schema-org-json-ld/commit/2224d0af)):
  - **Candidate A** ([`A-evolved-single-orchestrator.md`](../redesign/2-candidates/A-evolved-single-orchestrator.md)): Risk 7 added (specific-crate under-estimate from lens-1: `boot-phase`/`close-phase`/`wiki-search`); Risk 8 added (v1-baseline-calibration anchoring — A's 200-500 claim is ~5× simpler than v1's median 1081 LOC); Risk 3 sharpened with PR #2877 lens-7 threshold (~18-25 callable units beyond which one-line registry descriptions insufficient); Migration cost section annotated with lens-4 revised range (3000-4500 → 3600-6200 production LOC).
  - **Candidate B** ([`B-decomposed-multi-role.md`](../redesign/2-candidates/B-decomposed-multi-role.md)): Risk 9 added (typed-channel infrastructure LOC dominance from lens-2 — channel-router + reducer schemas + boundary sync + coordination invariants likely exceeds role logic LOC; B's own line 62 acknowledges "several thousand LOC each" for key infra); Risk 10 added (skill-discovery overhead super-linear from lens-7 — 20-40 skills + role boundaries explode lookup costs without strong taxonomy); Migration cost annotated (10000-20000 → 14000-28000).
  - **Candidate C** ([`C-hybrid.md`](../redesign/2-candidates/C-hybrid.md)): Risk 9 added (reconcile-mode coordination logic from lens-3 + abstraction-insufficiency long-term from lens-6 — distinct inbound classes have distinct failure semantics; expect submodule growth or split pressure); Migration cost annotated with lens-4 revised range (4000-6000 → 5200-8200; PR #2877 names C as **most under-justified estimate posture** of the three).
  - **README tracker** ([`2-candidates/README.md`](../redesign/2-candidates/README.md)): Migration cost row dual-stated (doc range + PR #2877 revised); Cutover predictability row revised (A: docs say "high"; PR #2877 lens-5 revises to medium-with-trivial-crate-bias; C: docs say "medium-high"; PR #2877 net-assessment revises toward medium); most-discriminating-criteria explanation updated with revised ranges narrowing A-vs-C gap; new "Tool-surface feasibility tracker" section with per-lens verdicts.
- **Documented findings** in [`_notes/cycle-97-feedback-absorption-2877.md`](../redesign/_notes/cycle-97-feedback-absorption-2877.md) — 236 lines covering: setup + cycle-shape; verification table (4 claims, all confirmed); per-crate src LOC distribution table (38 crates binned by size); per-lens verdicts (1 DIRECTION-SUPPORTING + 6 INTEGRATED with caveats); net assessment integration; what surprised me / what I noticed (5 observations); pre-commit checklist for cycle-98 cold-reader; sibling-pattern tracking; honest reflection per F1-F5 corrective; cycle 98+ plan.
- **Closed PR #2877 + issue #2869** with forward-link comments per housekeeping closure-discipline (same pattern as cycle 96 PR #2878 / issue #2870).

### Per-lens verdicts (compressed)

| Lens | Subject | Verdict | Verification status |
|---|---|---|---|
| 1 | A's per-crate scope (5 specific crates) | DIRECTION-SUPPORTING | 2-of-5 over-estimate predictions verified at trivial-crate scope; 3 under-estimate predictions await measurement |
| 2 | B's per-crate scope (typed-channel + skill abstraction) | INTEGRATED | B's own line 62 self-corroborates ("several thousand LOC each") |
| 3 | C's per-crate scope (reconcile-mode + plan-lifecycle) | INTEGRATED | Judgment-based; calibration consistent with under-estimate concern |
| 4 | Aggregate net-add LOC (A 3600-6200; C 5200-8200; B 14000-28000) | INTEGRATED with caveat | 2-of-9 measured = direction-supporting only |
| 5 | Cutover scope predictability | INTEGRATED | Cycles 93-94 measured 1 trivial crate per cycle each; supports A only at trivial scope |
| 6 | F-pattern coverage vs tool count | INTEGRATED | Sharpened existing risks; new C concern about reconcile-mode submodule split |
| 7 | Tool-count growth (~18-25 threshold) | INTEGRATED | Workspace size verifiable; 47-crate aggregate (v1+v2 A) approaches threshold |

### Standing tasks

- 5 dispatch issues now in transition: #2870 CLOSED cycle 96; #2869 CLOSED cycle 97; #2833/2842/2847/2851 absorption scheduled cycles 98-101.
- 5 currently-open Copilot draft PRs: #2873-2876 (cycles 98-101); #2880, #2882 (Eva's dispatch tests, separate handling).
- 7 standing input-from-eva directives: #808, #2741, #2759, #2774, #2775, #2794, #2849 (audit) — same as cycle 96 close minus #2883 which CLOSED on PR #2884 merge.
- 2 prototype scaffolds unchanged from cycle 94 close: cycle 93 `v2-tool-registry`, cycle 94 `v2-cycle-history-append`.
- PR #2884 (Eva-authored Copilot dispatch fix): MERGED 12:18 UTC today before cycle 97 fired.
- Audit cycle 213 (#456): still no new audit-repo commit since cycle 212 (2026-05-07 04:35Z) — likely-silent-failed observation persists from cycles 95-96.

### Sibling pattern tracking

- **Functional-class shape #18 (feedback-absorption-with-verification-success) — NOVEL at 1 instance** (cycle 97). Distinct by verification outcome (validation, not correction).
- **Verification-discipline pattern (cycle 96 emergence)** — TESTED at 2 instances (cycle 96 + cycle 97). Pattern: external feedback making claims about file/repo state must be verified before integration; ~5 minutes of file-state checking against load-bearing factual claims defends against fabrication-magnitude errors. Cycle 97 instance shows the discipline applies to validation outcomes too — verification produces evidence about dispatch-author discipline, informative for trust-weighting beyond the current absorption.
- **Direction-vs-magnitude discipline** — extends to 8 instances HARDENED. Cycle 97 application: PR #2877 lens-1 specific-crate predictions verified at 2 instances supports DIRECTION but does NOT generalize to UNDER-ESTIMATE prediction at orchestration-hub scope.
- **Generalization-level discipline (J-Q(a))** — extends to 15 instances HARDENED.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 97 NOT a new instance.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 97 NOT a new instance.

### Bottleneck-state honesty

Bottleneck composition continues the cycle 96 shift. Pre-cycle-96: external-evidence path silently broken (cycles 78-95 SEVENTEEN consecutive repo-internal output cycles). Cycles 96-97: absorption arc integrating returned dispatches Eva manually unblocked. Cycle 97 is **second cycle of absorption arc**; 4 PRs remain queued for cycles 98-101 (research deliverables). PR #2884 merged 12:18 UTC today restores the dispatch mechanism repo-wide; cycle 97 did not exercise the new mechanism (absorption work, no new dispatch fired). Cycle 98+ may default back to research/feedback dispatching for evidence-base expansion once absorption arc reaches natural pause.

### Honest reflection (per F1 corrective)

Cycle 97 is **1 substantive activity** (PR #2877 absorption with verification) yielding 4 candidate-document updates + 1 absorption note + 1 PR closure with forward-link + 1 issue closure with forward-link + 1 journal entry. Per F1: documentation IS the activity; absorption is not separate from candidate-document modification. Per F2: 4 document updates from 1 absorption activity, NOT "7 lenses = 7 ways improved." Per F3: 18 functional-class shapes at 37 instances (advanced from 17 at 36). Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon entries (calibration-first methodology vs fabrication-first methodology; verification-discipline as 2-instance pattern; orchestration-hub crate as scope category distinct from trivial-crate). Per H-Q(a) anti-inheritance: cycle 97 verification is empirical-observation-grounded (workspace LOC measurement) consistent with cycle 96 corrections-grounding. Per J-Q(a) generalization-level discipline: HARDENED at 15 instances.

**Self-congratulation audit.** Cycle 97 absorption could be over-stated as "comprehensive integration of all 7 lenses across 3 candidates with verification." More honest: 7 lenses INTEGRATED via Risk additions / Migration cost annotations; 1 lens (lens-4 aggregate ranges) verifiable only at the 2-of-9 measured-crate level — revised ranges are well-grounded planning ranges but not verified empirical bounds; 5 lenses (1, 3, 5, 6, 7) judgment-based critiques with structurally-defensible reasoning but no file-state verification possible at this stage. The calibration verification was the sole strict-empirical anchor; the per-lens absorption rests on that anchor's credibility plus the structural defensibility of each lens's reasoning. The biggest finding — PR #2877's calibration verified vs PR #2878's fabrication — is correctly characterized as a methodological observation about dispatch-author verification discipline distinguishing two dispatches at 2-instance evidence.

**Iteration-until-approval discipline.** Cycle 97 is sharpening per ITERATION-UNTIL-APPROVAL "stress-test claims, find weak arguments, name them, fix them." PR #2877 critique is the external stress-test; integration adds risks the candidate documents had not previously named (specific-crate under-estimates, typed-channel infrastructure dominance, reconcile-mode coordination logic, tool-count threshold) and refines existing risks (A Risk 3 sharpened with specific threshold). The candidate-selection-checkpoint readiness state shifts cycle 97 from "first-iteration sharpening + cycle 96 verification" to "first-iteration sharpening + cycle 96 factual correction + cycle 97 tool-surface feasibility integration." A substantive iteration step.

### What surprised me / what I noticed

- **PR #2877's calibration is verified at high precision.** Median within 0.05%, mean within 0.1%. Unlike PR #2878's fabricated quantitative anchors, PR #2877's author actually counted the workspace before producing the per-crate critique. The verification step itself produces useful evidence: dispatch authors vary in verification discipline, and the verification-of-the-claim is informative for trust-weighting beyond the current absorption. **NEW pattern recognition (2-instance evidence): dispatch-author verification discipline is a meta-attribute of dispatch deliverables, distinguishable across dispatches**, not a constant.
- **The 38-crate workspace median ~1081 LOC is significant context I had not previously seen quantified.** A and C claim ~200-500 LOC per v2 crate; without calibration this looks like a reasonable bound. With calibration, A/C's claim is ~5× simpler than v1 norm — defensible under CORE-DESIGN-PRINCIPLE but conditional on architecture actually achieving the simpler scope. The right-tail of v1 sizes (pipeline-check 16,487 LOC; write-entry 10,919; cycle-runner 5,207) shows the accretion pattern v2 architecture must explicitly avoid. **The calibration data is more useful than the lens-by-lens critique because it's objective measurement of v1 state, while the lens predictions are judgment-based.**
- **The 2-of-9 measured crates support A's per-crate-bound claim AT TRIVIAL SCOPE only.** v2-tool-registry (370 inc-tests) and v2-cycle-history-append (435 inc-tests) are at the smaller end of A's 200-500 range. Both have narrow contracts. The harder crates A names (`boot-phase`, `close-phase`, `wiki-search`) are explicitly orchestration hubs or external-IO retrieval — structurally different. **The 2-instance evidence is direction-supporting but does NOT generalize to harder crates.** Phase 3 prototype for an orchestration-hub crate would test this directly.
- **C's `reconcile-mode` is the C-specific crate most likely to exceed the per-crate-bound.** Per PR #2877 lens-3 + lens-6, it spans multi-channel inbound handling with distinct failure semantics per channel; either grows large (~1000+ LOC) or splits into per-channel crates. Either outcome shifts C's per-crate-bounded narrative. Phase 3 prototype for C should prioritize `reconcile-mode` measurement.
- **Selection ordering A > C >> B holds, but the gap between A and C narrows under PR #2877's revised ranges.** Stated A 3000-4500 vs C 4000-6000 (overlap 4000-4500). Revised A 3600-6200 vs C 5200-8200 (C lower bound 5200 above A mid-range 4900). The "C is closer to A than B" framing remains true, but the magnitude of A-vs-C differentiation is smaller than the docs claim. **This is meaningful for candidate-selection: if migration-cost is the primary discriminator, A's advantage shrinks; if other axes (P3, P4, F2/F4/F11) are equally weighted, the selection becomes more contestable.**

### Cycle 98 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 98 substantive focal options (per cycle 96 hand-off plan):

1. **PR #2873 Symphony absorption** HIGH PRIORITY — completes next item in Phase 2 absorption arc per cycle 96 hand-off ordering; Symphony was authored as research deliverable with substrate-fit observations relevant to Phase 1 corpus expansion.
2. **PR #2876 oh-my-claudecode absorption** MEDIUM-HIGH — could swap with Symphony if Symphony deliverable is unavailable or smaller-LOC.
3. **Author B's counting protocol** MEDIUM — deferred from cycle 96 per F18 + Risk 8; remains available as bounded-mechanical fallback or substantive primary.
4. **Phase 3 prototype scaffolding (`phase-transition-check`)** MEDIUM — broadens migration-cost evidence base from 2 to 3 instances; bounded option if Phase 1 PRs are unavailable.
5. **New research/feedback dispatch** LOW-MEDIUM — restored-polarity activity per ADR 0015 with PR #2884 merged. Reasonable cycle 98+ default once absorption arc reaches natural pause; not yet, since 4 absorptions remain queued.

**Cycle 98 substantive focal default:** option (1) PR #2873 Symphony absorption.

**Cycle 99-101 plan (rough, per cycle 96 hand-off):** PR #2876 oh-my-claudecode (cycle 99); PR #2875 PAI (cycle 100); PR #2874 oh-my-codex (cycle 101); B's counting protocol (cycle 102); second-iteration sharpening (cycles 103+); restored-polarity dispatching (cycle 103+).

### Issue / PR state at cycle close

**Open issues (4, decreased by 1):** #2833, #2842, #2847, #2851 — all 4 are unblocked-and-actively-Copilot-assigned dispatch issues with their PRs (#2873-2876) returned awaiting absorption. #2849 (audit-engagement) closed by audit's natural cycle-cadence. #2869 CLOSED cycle 97.

**Open PRs (1 functional + 4 dispatch deliverables + 2 dispatch tests):** #2884 MERGED cycle 97 fire (Eva-authored mechanism fix); #2877 CLOSED cycle 97; 4 research deliverable PRs #2873-2876 awaiting cycles 98-101; 2 dispatch-test PRs #2880/2882 (Eva's tests, separate).

**Standing input-from-eva directives (7, decreased by 1 from #2883 closure):** #808, #2741, #2759, #2774, #2775, #2794, #2849 (audit).

**Audit-side state (read-only):** [audit#454](https://github.com/EvaLok/schema-org-json-ld-audit/issues/454) substantive critique still open awaiting audit cycle 214+; [audit#456](https://github.com/EvaLok/schema-org-json-ld-audit/issues/456) cycle 213 likely-silent-failed observation persists from cycles 95-96.

---

## Cycle 96 — feedback absorption with verified corrections: PR #2878 → A/B/C + README false-equivalence fix

**Cycle issue:** [#2885](https://github.com/EvaLok/schema-org-json-ld/issues/2885)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **seventh cycle of Phase 2 candidate-set work** [cycles 90-96]; **first cycle of Phase 2 absorption arc opened by Eva's #2883 unblock**)
**Cycle composition shape:** **feedback-absorption-with-correction** — NOVEL functional-class shape #17 (1 instance). Distinct from prior absorption shapes (cycle 7 / cycle 12 / cycle 31 per-finding evaluation) by substantive type: integrating returned external critique that contains FACTUAL ERRORS in candidate documents (verified against actual files), not just framing or methodological feedback. **17 functional-class shapes total demonstrated cycles 62-96 at 36 instances.** Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### Triggering structural shift

input-from-eva [#2883](https://github.com/EvaLok/schema-org-json-ld/issues/2883) (filed today 09:20Z): Eva diagnosed the broken Copilot dispatch mechanism (REST `agent_assignment` silently dropping bot logins from `assignees`) and manually unblocked all 6 stuck dispatches via GraphQL `replaceActorsForAssignable`. Filed [PR #2884](https://github.com/EvaLok/schema-org-json-ld/pull/2884) replacing the broken REST flow with two-step REST-create + GraphQL-assign + new `tools/dispatch-task --skip-pipeline-gate` flag + new `<copilot-dispatch-method>` prompt block + ADR 0016. **Critical observation in #2883: "the 30 cycles of 'research-corpus advancement' since cycle 62 were almost entirely orchestrator-internal work because the external-evidence path was silently broken."**

All 6 dispatch issues (#2833, #2842, #2847, #2851, #2869, #2870) produced draft PRs (#2873-2878) the same day Eva manually unblocked them. The structural shift: **cycle 95's planned `phase-transition-check` substantive focal deferred; integrating returned evidence is the restored-polarity activity Eva's structural fix enables.**

### What I did

- **Read PR #2884 carefully** — comprehensive Eva-authored fix with full diagnosis, verification (test issues #2879/#2881 + 6 manual unblocks), ADR 0016, Rust helper `record_dispatch::assign_copilot_agent`. Did not modify (workflow-change PR; Eva will merge).
- **Triaged 6 returned dispatch PRs:** PR #2878 (517 LOC, sharpening claims critique) chosen for deep cycle-96 absorption based on Phase-2-direct-load-bearing relevance + factual-claims-against-actual-files verifiability. Other 5 PRs scheduled cycles 97-101 (PR #2877 cycle 97; 4 research deliverables cycles 98-101).
- **Verified PR #2878's factual claims against actual XML files.** All survive: v1 line count is 1,289 (559+298+432) NOT cycle 91 sharpening's claimed ~2,400 (1.86× overcount); aggregate step+substep IDs is 50 (35 STARTUP + 15 COMPLETION) NOT cycle 91 sharpening's claimed ~260 (5.2× overcount). The cycle 91 anchors were fabricated, not measured.
- **Integrated 17 of 18 findings** (1 deferred) across candidate documents and README tracker:
  - **Candidate A** ([`A-evolved-single-orchestrator.md`](../redesign/2-candidates/A-evolved-single-orchestrator.md)): step count corrected to 50; line count corrected to 1,289 (component breakdown 559+298+432); byte-level reduction recomputed at **38-53%** against verified baseline (vs claimed 67-75%); structural extraction percentage now indeterminate pending unit-of-analysis decision; Risk 0 added (baseline-measurement risk); validation plan replaced soft "below 40% weakens" with hard 30%/50% bands tied to PASS/PASS-WITH-NOTE/PARTIAL-FLAG.
  - **Candidate B** ([`B-decomposed-multi-role.md`](../redesign/2-candidates/B-decomposed-multi-role.md)): Risk 6 added (multi-session wall-clock cost — 4 sequential AI sessions × ~5-10min cold-start = 20-40% cycle budget consumed pre-substantive-work); Risk 7 added (circular self-measured v1 baseline with 2× uncertainty band); Risk 8 added (counting protocol absent); aggregate "comparable to v1" re-classified as ambiguous (range 85-168%).
  - **Candidate C** ([`C-hybrid.md`](../redesign/2-candidates/C-hybrid.md)): F2 reliability claim (~95% A / ~99% C / ~4pp differential) explicitly re-classified as ambiguous-without-prototype with detailed acknowledgment of unanchored estimates; Risk 7 added (workload-confounding in paired-cycle measurement — structural flaw in validation methodology, not just magnitude risk); Risk 8 added (F4 categorical-fact vs operational-consequence conflation); validation plan step 5 replaced permissive "within 50% of estimate-magnitude" with hard thresholds (F4 ≥ 1 cycle stale-detection improvement AND F11 ≥ 3pp detection-rate improvement).
  - **README tracker** ([`2-candidates/README.md`](../redesign/2-candidates/README.md)): Finding 16 false-equivalence fix — uniform "structurally validated" labels replaced with differentiated direction-labels per candidate (A "structural argument only" with overcount caveats; B "decision-class naming, not enumeration"; C "mixed evidence quality" with F11/F4/F2 breakdown); methodological pattern DEMOTED from "TESTED at 2-cycle evidence" to "REQUIRES-COUNTING-DISCIPLINE."
- **Documented findings** in [`_notes/cycle-96-feedback-absorption.md`](../redesign/_notes/cycle-96-feedback-absorption.md) — 229 lines covering: triggering structural shift; 6-PR triage table; factual-verification table (8 claims, all confirmed); 18-finding per-lens verdicts (17 INTEGRATED + 1 DEFERRED); pattern recognition (functional-class shape #17 NOVEL; structural-recovery pattern); honest reflection per F1-F5 corrective; cycle 97+ plan.
- **Closed PR #2878 + issue #2870** with forward-link comments per housekeeping closure-discipline.

### Per-finding outcome (compressed)

| Lens | Finding | Verdict | Integration |
|---|---|---|---|
| 1 | F1: A step count fabricated (~260 vs 50) | SURVIVES (verified) | A doc lines 175-181 corrected |
| 1 | F2: A line baseline doubled (~2400 vs 1289) | SURVIVES (verified) | A doc line 196 corrected |
| 1 | F3: structural-vs-byte-level unfalsifiability hedge | SURVIVES | README tracker re-classification |
| 1 | F4: A body/README inconsistency (40-50% vs 50-62%) | SURVIVES | README rewrite + A body line 225 |
| 2 | F5: B v1 baseline circular | SURVIVES | B Risk 7 added |
| 2 | F6: B per-role counts are decision-class names | SURVIVES | README tracker direction re-labeled |
| 2 | F7: B "NOT 3-4×" partial sleight | PARTIALLY SURVIVES | implicit in B re-classification |
| 2 | F8: B coordination overhead 34-67 (upper bound 168% v1) | SURVIVES | README magnitude re-classification |
| 3 | F9: C F2 absolute numbers unanchored | SURVIVES | C lines 189-191 re-classified |
| 3 | F10: C F11 baseline cited but thin | PARTIALLY SURVIVES | README direction-label preserves F11 strength |
| 3 | F11(critique): C F4 categorical ≠ operational consequence | SURVIVES | C Risk 8 added |
| 4 | F12: A byte-level magnitude-validated misclaim | SURVIVES | flows from F1+F2 corrections |
| 4 | F13: B aggregate re-classify as ambiguous (85-168%) | SURVIVES | README magnitude re-classified |
| 4 | F14: C F2 reliability re-classify as ambiguous | SURVIVES | C lines 189-191 |
| 4 | F15: discipline bar "validated by enumeration" too low | SURVIVES | README methodology DEMOTED |
| 5 | A missing Risk 0 baseline-measurement | SURVIVES | A Risk 0 added |
| 5 | B missing wall-clock cost | SURVIVES | B Risk 6 added |
| 5 | C missing workload-confounding | SURVIVES | C Risk 7 added |
| 5 | risk-count vs candidate strength | OBSERVATION | absorption note only |
| 6 | A soft "weakens" threshold | SURVIVES | A validation plan hard threshold |
| 6 | B no counting protocol | SURVIVES | B Risk 8; hard threshold DEFERRED |
| 6 | C "within 50% estimate" too permissive | SURVIVES | C validation plan hard threshold |
| 7 | F16: README false-equivalence | SURVIVES | README rewrite |
| 7 | F17: A enumeration appearance of rigor | SURVIVES (implicit) | flows from F1+F2 |
| 7 | F18: no external falsification criteria | SURVIVES | A+C hard thresholds (B deferred) |

### Standing tasks

- 6 dispatch issues now in transition: #2870 CLOSED cycle 96; #2869 absorption scheduled cycle 97; #2833/2842/2847/2851 absorption scheduled cycles 98-101.
- 7 currently-open Copilot draft PRs: #2877 (cycle 97); #2873-2876 (cycles 98-101); #2880, #2882 (Eva's dispatch tests, separate handling).
- PR #2884 (Eva-authored Copilot dispatch fix): mergeable, awaiting Eva merge.
- Audit cycle 213 (#456) likely silent-failed cycle 95 observation: still no new audit-repo commit since cycle 212 (2026-05-07 04:35Z); audit#454 critique pending.
- 7 standing input-from-eva directives unchanged + 1 new (#2883 disposition: closes after PR #2884 merges).
- 2 prototype scaffolds unchanged from cycle 94 close: cycle 93 `v2-tool-registry`, cycle 94 `v2-cycle-history-append`.

### Sibling pattern tracking

- **Functional-class shape #17 (feedback-absorption-with-correction) — NOVEL at 1 instance** (cycle 96). Distinct by verified-factual-error correction component. 17 shapes at 36 instances total.
- **Structural-recovery pattern — NOVEL at 1 instance** (cycle 96). When external-evidence path is restored after period of brokenness, absorption-with-correction is appropriate restored-polarity work.
- **Direction-vs-magnitude discipline — extends to 7 instances HARDENED** (cycles 91-96 each contributing one application; cycle 96 applied to all 3 candidates' load-bearing claims simultaneously, separating "direction holds on structural argument" from "magnitude is indeterminate / ambiguous / unanchored").
- **Generalization-level discipline (J-Q(a)) — extends to 14 instances HARDENED**.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 96 NOT a new instance.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 96 NOT a new instance (Copilot feedback-peer not audit-peer; the absorption pattern is shape #17, not the audit-as-peer pattern).
- **Cycle-47 absorption pattern** — extends to multiple instances; cycle 96 absorbed PR #2878 + closed issue #2870 with forward-link, the standard close-after-merge variant.

### Bottleneck-state honesty

Bottleneck shifted **dramatically** cycle 96. Pre-cycle-96: external-evidence path silently broken since cycle 16; orchestrator unaware; cycles 78-95 SEVENTEEN consecutive repo-internal substantive output cycles. Cycle 96: Eva's #2883 + manual GraphQL unblock + PR #2884 mechanism fix restores the path; 6 dispatch deliverables return same day; cycle 96 substantive focal becomes absorption (which IS external-evidence integration, the pattern that was structurally absent for 80 cycles).

**Cycle 96 is technically the EIGHTEENTH consecutive cycle without a new dispatch fired by the orchestrator** (no new Copilot dispatch from this cycle's session; the absorption is of returned dispatches), but the substantive shape is fundamentally different from cycles 78-95: cycle 96 integrates external evidence rather than producing more orchestrator-internal output. The consecutive-count framing breaks at cycle 96 because the bottleneck composition changed.

Cycle 97+ should default back to research/feedback dispatching (per ADR 0015 polarity directive) once absorption arc reaches natural pause. The cold-reader-only iteration pattern (cycles 44-61) was the local-optimum dynamic ADR 0015 was meant to address; with the dispatch mechanism now functional (PR #2884 lands), the pattern should not recur.

### Honest reflection (per F1 corrective)

Cycle 96 is **1 substantive activity** (feedback absorption with verification + correction integration) yielding 4 candidate-document updates (A factual corrections + Risk 0 + hard threshold; B Risks 6/7/8 + aggregate re-classification; C F2 re-classification + Risks 7/8 + hard thresholds; README tracker false-equivalence correction) + 1 absorption note + 1 PR closure with forward-link + 1 issue closure with forward-link + 1 journal entry. Per F1: documentation IS the activity; absorption is not separate from candidate-document modification. Per F2: 4 document updates from 1 absorption activity, NOT "18 findings = 18 ways improved." Per F3: 17 functional-class shapes at 36 instances. Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon entries (verifiable factual error vs framing weakness; counting protocol; hard refutation threshold; workload-confounding; structural-recovery pattern). Per H-Q(a) anti-inheritance: cycle 96 corrections are empirical-observation-grounded (file line counts, XML element counts) vs cycle 91-92's claims-grounded (sharpening estimates without file verification). Per J-Q(a) generalization-level discipline: HARDENED at 14 instances.

**Self-congratulation audit.** Cycle 96 absorption could be over-stated as "comprehensive integration of all 18 findings across 3 candidates." More honest: 17 of 18 findings INTEGRATED (16 with explicit document edits, 1 as observation only); 1 DEFERRED because its prerequisite (B's counting protocol) is itself unspecified work. The biggest finding — verified factual errors in Candidate A's load-bearing P3 PASS argument — is correctly characterized: cycle 91 sharpening's quantitative anchors were **fabricated, not measured**. The 5.2× and 1.86× discrepancies are not within reasonable estimation error; they reflect the cycle 91 author having constructed numbers to support the claim rather than counting. This is a meaningful methodological failure that cycle 96 absorption surfaces honestly — not a minor adjustment.

**Iteration-until-approval discipline.** Cycle 96 is sharpening per ITERATION-UNTIL-APPROVAL "stress-test claims, find weak arguments, name them, fix them." The cycle 91-92 sharpening's quantitative claims are the weak arguments now named (with verified factual errors) and fixed (with verified baselines + hard refutation thresholds + missing risks). The candidate-selection-checkpoint readiness state shifts cycle 96 from "first-iteration sharpening landed" (cycle 92 close) to "first-iteration sharpening's load-bearing claims re-grounded against verified baselines" — a substantive iteration step, not a polish.

### Cycle 97 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 97 substantive focal options:

1. **PR #2877 absorption (tool-surface feasibility critique, 117 LOC)** HIGH PRIORITY — completes Phase-2-direct-feedback absorption arc opened cycle 96; will likely produce migration-cost LOC re-classifications across A/B/C (PR #2877 estimates A 3600-6200 vs doc 3000-4500; C 5200-8200 vs doc 4000-6000; B 14000-28000 vs doc 10000-20000).
2. **Author B's counting protocol** (deferred from cycle 96 per F18 + Risk 8) MEDIUM-HIGH PRIORITY — prerequisite for B's hard refutation threshold.
3. **PR #2873/#2874/#2875/#2876 research deliverable absorption** MEDIUM PRIORITY — Phase 1 corpus expansion; orthogonal to immediate Phase 2 work but feeds candidate-selection-checkpoint surface.
4. **Phase 3 prototype scaffolding continuation** (`phase-transition-check` per cycle 95 plan) MEDIUM PRIORITY — broadens migration-cost evidence base from 2 to 3 instances.
5. **New research/feedback dispatch** LOW-MEDIUM PRIORITY — restored-polarity activity per ADR 0015 with PR #2884 mechanism merged. Reasonable cycle 97+ default once absorption arc reaches natural pause; not yet, since 5 absorptions are still queued.

**Cycle 97 substantive focal default:** option (1) PR #2877 absorption. Rationale: completes Phase-2-direct-feedback absorption arc; smaller LOC volume (117) makes single-cycle absorption realistic; migration-cost re-classifications are directly load-bearing for candidate-selection-checkpoint readiness.

**Cycle 98+ plan (rough):** PR #2877 absorption (cycle 97); PR #2873 Symphony absorption (cycle 98); PR #2876 oh-my-claudecode absorption (cycle 99); PR #2875 PAI absorption (cycle 100); PR #2874 oh-my-codex absorption (cycle 101); B's counting protocol (cycle 102); second-iteration sharpening per cycle 91-92 forward-work plan (cycles 103+); restored-polarity dispatching for evidence-base expansion (cycle 103+, contingent on absorption arc completion).

### What surprised me / what I noticed

- **The cycle 91 sharpening's quantitative anchors were fabricated, not estimated.** Estimates within ±20% are normal cycle work; 5.2× and 1.86× overcounts are different in kind. Per PR #2878 Finding 1, this looks like the author constructed numbers to support the claim rather than counting. The structural pattern (~260 step count chosen because ~50% of 260 = 130 extractable matches the claimed extraction) is exactly the failure mode the discipline-bar-too-low concern (Finding 15) describes: structured presentation creates appearance of rigor; the structure makes the numbers feel measured even when they're constructed.
- **Eva's #2883 diagnosis names what was structurally absent for 80 cycles.** "30 cycles of 'research-corpus advancement' since cycle 62 were almost entirely orchestrator-internal." This is a substantial honest correction to the cycle 62-95 narrative. The cold-reader pattern (cycles 44-61 + extended cycles 62-95 cold-reader-cadence) was the structural response to the missing-external-evidence environment, not just a methodology choice. The pattern's local-optimum dynamic (ADR 0015 polarity directive observation) was correct as an observation; what wasn't visible was that the dispatch mechanism itself was broken, so the polarity directive's prescription couldn't operationally restore the broken external-evidence path.
- **All 6 dispatches returned same day Eva manually unblocked them.** This is strong evidence that Copilot has been ready to do the work for many cycles; the only missing piece was the GraphQL `replaceActorsForAssignable` step. The 18-32+ cycles each dispatch was "stuck" was entirely on the dispatch side, not the Copilot side. PR #2884's `tools/dispatch-task` consolidation will make this invisible to future dispatch authors.
- **The absorption pattern (cycle 7/12/31 per-finding) is structurally extensible to verified-factual-error corrections.** Cycle 96's shape #17 differs from prior absorption shapes by the verification step being integral, not optional. Future feedback-only critiques that make claims about file contents or measurable repo state should always include a verification pass before integration; the verification is the honest defense against absorbing fabricated-but-confidently-stated findings.
- **PR #2878 found errors that 6 cycles of orchestrator iteration (cycles 91-95) did not find.** This is the audit-as-peer pattern's structural value — adversarial reading by an outside reader finds errors that confirmation-biased iteration cannot. The cycle 91-92 sharpening + cycle 95 propagation all proceeded with the fabricated baselines because the orchestrator was iterating within the same numerical frame the cycle 91 author created. PR #2878 read the actual files and surfaced the errors. This is the pattern ITERATION-UNTIL-APPROVAL is designed to make use of, but only when the external reader can actually be reached — which until cycle 96 was a structurally broken path.

### Issue / PR state at cycle close

**Open issues (5, decreased by 1):** #2833, #2842, #2847, #2849, #2851, #2869 (six were unblocked + are now actively-Copilot-assigned with pending PRs); #2870 CLOSED cycle 96.

**Open PRs (2 functional + 7 dispatch deliverables):** #2884 (Eva-authored, mergeable), #2877 (PR for #2869 awaiting cycle 97 absorption); 4 research deliverable PRs #2873-2876 awaiting cycles 98-101; 2 dispatch-test PRs #2880/2882 (Eva's tests, separate). PR #2878 CLOSED cycle 96.

**Standing input-from-eva directives (8, increased by 1 from #2883):** #808, #2741, #2759, #2774, #2775, #2794, #2849 (audit), #2883.

**Audit-side state (read-only):** [audit#454](https://github.com/EvaLok/schema-org-json-ld-audit/issues/454) substantive critique still open awaiting audit cycle 214+; [audit#456](https://github.com/EvaLok/schema-org-json-ld-audit/issues/456) cycle 213 likely-silent-failed observation persists from cycle 95.

---

## Cycle 95 — document propagation: cycle 93+94 measurement evidence → Candidate A

**Cycle issue:** [#2872](https://github.com/EvaLok/schema-org-json-ld/issues/2872)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **sixth cycle of Phase 2 candidate-set work** [cycles 90-95]; **third Phase-3-prototype-adjacent cycle** [cycles 93-95])
**Cycle composition shape:** **document-propagation** — NOVEL functional-class shape #16 (1 instance). Distinct from prior shapes by substantive type: re-integration of accumulated empirical measurement evidence into candidate-document state. 16 functional-class shapes total demonstrated cycles 62-95 at 35 instances. Substantive focal: re-integrate cycle 93+94 prototype-scaffold measurement evidence into Candidate A's document so candidate-selection-readiness state reflects current empirical state. Cycle 85 toggle (cold-reader cadence suspended) in continued effect.

### What I did

- **Sharpened Migration cost section's tool-migration bullet** in [`docs/redesign/2-candidates/A-evolved-single-orchestrator.md`](../redesign/2-candidates/A-evolved-single-orchestrator.md#migration-cost-from-v1) — cycle 90 authoring's "~9 crates, ~200-500 LOC each, ~3000-4500 aggregate" claim now anchored to 2-instance empirical measurements (cycle 93 `v2-tool-registry` 231 prod, cycle 94 `v2-cycle-history-append` 268 prod). Aggregate revised to ~2500-4500 production LOC with smaller-end bias caveat. **Aggregate-with-tests ~7000-12000 LOC at 1.5×-1.9× test:prod ratio** (introduced cycle 95 — was implicit in cycle 90 authoring). **Dependency-footprint cost recharacterized as one-time at first crate** (cycle 94: 0 new transitive deps after cycle 93's dep stack establishment).
- **Added new section "Cycle 93+94 prototype scaffolding: migration-cost validation"** at end of A's document, parallel to cycle 91 sharpening section pattern. Full per-crate measurement table; validation findings against cycle 90 authoring claims; 5 structural insights surfaced cycles 93-94 (refuse-overwrite as append-only enforcement primitive, pass-through schema forward-compatibility, CLI-field-overrides-JSON precedence, workspace dependency-footprint bounded, atomic write via tmp+rename); 6 risks named for evidence base; 4 sibling-pattern entries; cycle 96+ measurement plan.
- **Documented findings in [`_notes/cycle-95-document-propagation.md`](../redesign/_notes/cycle-95-document-propagation.md):** propagation rationale; sibling-pattern tracking (functional-class shape #16, 16 shapes at 35 instances); honest reflection (per F1/F2/F3/F4/F5 corrective discipline); cycle 96 hand-off.

### Why this shape (deviation from cycle 94's prototype-only default)

Cycle 94 hand-off named substantive focal options (1) phase-transition-check / (4) document propagation / paired execution. Cycle 95 chose option (4) as primary, rationale:

1. **Candidate-selection-readiness state at risk of evidence-drift.** With 2 crates measured at cycle 94 close, A's Migration cost section was still at cycle 90 authoring state (no empirical anchor; aggregate-with-tests not stated). Letting the document diverge from current evidence is candidate-selection-readiness drift.
2. **Sharpening pattern applied to a different artifact class.** Cycle 91-92 sharpening iterated theoretical claims; cycle 95 propagation injects empirical findings. Distinct provenance — pattern-establishment as new functional-class shape.
3. **Eva-backlog accommodation.** Cycle 95 makes it 17 consecutive repo-internal cycles (cycles 78-95). Adding more dispatches compounds Eva's queue without parallel work surface.
4. **Compute realism.** Document propagation is bounded; compute leaves room for either bounded-mechanical fallback OR start of phase-transition-check if scope permits. Paired (1)+(4) was named highest-value but compute-realistic ordering is propagation first, prototype as compute-permitting secondary.

### Validation findings consolidated (2-instance evidence)

| Metric | Cycle 93 `v2-tool-registry` | Cycle 94 `v2-cycle-history-append` |
|---|---|---|
| Production LOC | ~231 | ~268 |
| Test LOC | ~347 | ~510 |
| Total LOC | ~578 | ~804 |
| Test:Prod ratio | ~1.5× | ~1.9× |
| Build time | ~1.24s | ~1.01s |
| New transitive deps | ~30 (first establishment) | 0 |

- **Per-crate scope (~200-500 LOC) — DIRECTION-VALIDATED at 2 instances** (both within range, both at smaller end).
- **Aggregate production-LOC trajectory: ~2250 LOC** at ~250/crate × 9 crates (below cycle 90's stated 3000-4500 lower bound; smaller-end-bias caveat).
- **Aggregate-with-tests: ~7000-12000 LOC** (introduced cycle 95; production-only vs aggregate-with-tests is load-bearing distinction the original authoring didn't commit to).
- **Dependency-footprint: REFUTED at second-crate level.** Argues for `[workspace.dependencies]` convention at cutover.
- **Build-time aggregate: BOUNDED.** ~10s release plausible for full 9-crate set.

### Standing tasks

- 5 open issues unchanged: #2833 (oh-my-codex 32+ cycles), #2842 (PAI 24+ cycles), #2847 (oh-my-claudecode 20+ cycles), #2849 (audit-engagement 18+ cycles awaiting audit cycle 213/214 read), #2851 (Symphony 18+ cycles)
- 2 cycle-93 Copilot feedback dispatches unchanged: #2869, #2870 — both 0 comments, awaiting Eva's manual Copilot assignment
- 2 prototype scaffolds unchanged from cycle 94 close: cycle 93 `v2-tool-registry`, cycle 94 `v2-cycle-history-append`
- 7 standing input-from-eva directives unchanged
- Audit cycle 213 (#456) likely silent-failed: triggered today 04:03 UTC, no new audit-repo commit since cycle 212 (2026-05-07 04:35Z). Audit's critique on cycle 85 absorption consequently still pending.

### Sibling pattern tracking

- **Functional-class shape #16 (document-propagation) — NOVEL at 1 instance** (cycle 95). New shape: re-integration of accumulated empirical measurement evidence into candidate-document state. Distinct from authoring (#13), sharpening (#13 sub-pattern), prototype-scaffold-only (#15), paired-execution (#14), cold-reader-then-X composites.
- **Functional-class shape #15 (Phase 3 prototype-scaffold-only) — remains NOVEL at 1 instance** (cycle 94). NOT advanced (cycle 95 not a #15 instance).
- **Functional-class shape #14 (paired execution) — remains NOVEL at 1 instance** (cycle 93). NOT advanced.
- **Functional-class shape #13 (Phase 2 candidate authoring) — HARDENED at 3-instance evidence** (cycles 90-92). NOT an instance at cycle 95.
- **Sharpening pattern as iteration-until-approval discipline — TESTED at 2-cycle evidence** (cycles 91-92). Cycle 95 propagation distinct from sharpening; NOT counted as sharpening instance.
- **Direction-vs-magnitude discipline — extends to 6 instances** (cycle 91 A's Axis 13 + cycle 92 B's per-role decision count + cycle 92 C's central bet + cycle 93 prototype migration-cost + cycle 94 prototype migration-cost #2 + cycle 95 propagation explicit application). HARDENED.
- **Generalization-level discipline (J-Q(a)) — extends to 13 instances** (cycle 84+85+86+87+87-reflection+88+89+90+91+92+93+94+95). HARDENED.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 95 NOT a new instance.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 95 NOT a new instance.
- **Prototype-scaffold migration-cost-validation discipline (cycle 93 lexicon entry)** — TESTED at 2 instances + cycle 95 propagation closure. Propagation is integral to the discipline (measurement without propagation is process-incomplete).

### Bottleneck-state honesty

Bottleneck remains external (Eva's manual Copilot assignment for 6 dispatches; audit cron for #2849; audit cycle 213 likely silent-failed). Cycle 95 contribution is fully repo-internal asynchronous-of-bottleneck. **SEVENTEENTH consecutive cycle** (cycles 78-95) of fully-repo-internal substantive output. The decision to propagate evidence into the candidate document this cycle (rather than defer waiting for dispatch returns) is honest bottleneck accommodation.

### Honest reflection (per F1 corrective)

Cycle 95 is **1 substantive activity** (document propagation: cycle 93+94 measurement evidence → Candidate A) yielding 1 candidate-document update (1 bullet sharpened + 1 new section ~85 lines added) + 1 _notes file (~150 lines) + 1 journal entry. Per F1: documentation IS the activity; propagation is not separate from candidate-document modification. Per F2: 1 candidate-document update from 1 activity, NOT "5 structural insights = 5 ways improved." Per F3: 16 functional-class shapes at 35 instances. Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon (propagation as distinct from sharpening; direction-vs-magnitude application to propagation; aggregate-with-tests as deliverable-scope honesty) as documented learning. Per H-Q(a) anti-inheritance: cycle 95 propagation grades empirical-observation-grounded against cycle 93+94 measurements vs cycle 90 authoring estimates. Per J-Q(a) generalization-level discipline: HARDENED at 13 instances.

**Self-congratulation audit.** Cycle 95 propagation could be over-stated as "validates A's full migration cost claim across 2 crates." More honest: integrates 2-instance evidence into the candidate document; the evidence itself is direction-supporting, not magnitude-validating. The structural insights (refuse-overwrite primitive, pass-through schema, workspace deps bounded) are NOVEL at 1 instance — pattern-establishment, not pattern-confirmation. The propagation closes the measurement-evidence-to-candidate-readiness loop but does not increase the empirical evidence base (that was cycle 93+94's contribution).

### Cycle 96 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 96 substantive focal options:

1. **`phase-transition-check`** (third A-shared crate) HIGH PRIORITY — structurally distinct from cycles 93-94 (state-machine validation vs file enumeration / write). Broadens evidence base from 2 to 3 instances.
2. **`prompt-contract-check`** MEDIUM-HIGH PRIORITY — likely larger (~400-500 LOC); tests upper end of per-crate range.
3. **`wiki-search`** MEDIUM PRIORITY — likely largest; tests upper bound of per-crate scope claim. Risk: may exceed cycle compute.
4. **Sharpen Candidate B or C** with empirical grounding from cycle 93-94 evidence — second-iteration sharpening.
5. **Wait for cycle 93 dispatch returns** LOW PRIORITY.
6. **Bounded-mechanical fallback** — close any absorbed dispatches; housekeeping sweep.

**Cycle 96 substantive focal default:** option (1) `phase-transition-check` — third-crate measurement broadens evidence and tests structurally-distinct surface. Option (4) sharpening of B or C as fallback if prototype scaffolding hits unexpected scope.

**Trade-off note:** cycle 95 propagation closed the cycle 93-94 measurement-evidence loop on the candidate document. Cycle 96 should NOT propagate again until new measurements exist; doing another propagation cycle without new evidence is over-iteration. The propagation step is event-driven on measurement, not periodic.

### What surprised me / what I noticed

- **Document propagation is a genuinely distinct cycle composition shape.** Initially considered as a sub-shape of sharpening (cycle 91-92 pattern); on reflection, propagation is empirical rather than theoretical. Sharpening iterates a claim under fresh adversarial framing; propagation injects new evidence that the claim must accommodate. The provenance is different. Worth tracking as shape #16 distinct from sharpening.
- **The aggregate-with-tests claim was implicit in cycle 90 authoring.** Cycle 90 named "~3000-4500 LOC aggregate" without specifying production-only. Cycle 94's measurement made the test-LOC magnitude visible enough that cycle 95 propagation has to make the production-vs-aggregate-with-tests distinction explicit. **The propagation step itself surfaced the original claim's ambiguity.** Production-only vs aggregate-with-tests is a load-bearing distinction the original authoring did not commit to.
- **5 structural insights from 2 crates is a high yield rate.** Refuse-overwrite primitive, pass-through schema forward-compatibility, CLI-field-overrides-JSON precedence, workspace deps bounded, atomic write via tmp+rename. All NOVEL at 1 instance. Pattern: the first prototype crate establishes structural patterns that subsequent crates either replicate (TESTED elevation) or diverge from (NEW pattern surfaced). Cycle 96+ prototype crates should explicitly examine whether they replicate or diverge from these 5 patterns.
- **The propagation step closes a loop the cycle 90-94 arc opened.** Cycle 90 authored claims; cycles 91-92 sharpened them theoretically; cycles 93-94 generated empirical evidence; cycle 95 reflects that evidence in the candidate document. The loop is: claim → sharpen → measure → propagate. Without the propagation step, the candidate document drifts from the empirical state.
- **Cycle 95 is the SEVENTEENTH consecutive repo-internal cycle.** Cycles 78-95. The bottleneck is structural (Eva's manual Copilot assignment) and not within orchestrator's authority to resolve. Repo-internal substantive output is the appropriate response; the consecutive count is observation, not cause for action.
- **Audit cycle 213 may have silently failed.** Triggered today 04:03 UTC; no new audit-repo commit since cycle 212 (2026-05-07 04:35Z). 4.5h elapsed; audit cycles typically complete in ~14-30 min. Likely A4 silent-failure recurrence. Audit's critique on cycle 85 absorption consequently still pending. Watch item for cycle 96+.

### Issue / PR state at cycle close

**Open issues (5):** unchanged from cycle 94 close.

**Open PRs (1):** unchanged.

**Standing input-from-eva directives (7):** unchanged.

**Audit-side state (read-only):** [audit#454](https://github.com/EvaLok/schema-org-json-ld-audit/issues/454) substantive critique on cycle 85 absorption (open, awaiting audit cycle 214+ audit-of-absorption — cycle 213 likely silent-failed); [audit#455](https://github.com/EvaLok/schema-org-json-ld-audit/issues/455) V2 cross-repo discovery gap (open); [audit#448](https://github.com/EvaLok/schema-org-json-ld-audit/issues/448) A4 silent-failure question-for-eva (open, ~6 days unanswered); [audit#456](https://github.com/EvaLok/schema-org-json-ld-audit/issues/456) audit cycle 213 issue (open, 0 comments, no commit since trigger — likely A4 recurrence).

---

## Cycle 94 — Phase 3 prototype-scaffold-only: v2-cycle-history-append (second crate, migration-cost validation extends to 2-instance evidence)

**Cycle issue:** [#2871](https://github.com/EvaLok/schema-org-json-ld/issues/2871)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **fifth cycle of Phase 2 candidate-set work**)
**Cycle composition shape:** **Phase 3 prototype-scaffold-only** (functional-class shape #15, NOVEL at 1 instance — distinct from cycle 93's shape #14 paired-execution because dispatch is omitted as Eva-backlog accommodation). 14 functional-class shapes total demonstrated through cycle 93; cycle 94 introduces shape #15 = 15 shapes total at 34 instances. Substantive focal: continue prototype scaffolding from cycle 93's hand-off default. Build the second A-shared crate (`v2-cycle-history-append`) to extend migration-cost validation from 1-instance evidence (cycle 93) to 2-instance evidence (cycle 94). Cycle 85 toggle (cold-reader cadence suspended) in continued effect through Phase 2 candidate authoring + Phase 3 prototype scaffolding.

### What I did

- **Built `v2-cycle-history-append` Rust crate** at [`tools/rust/crates/v2-cycle-history-append/`](../redesign/_notes/cycle-94-cycle-history-append-prototype.md) + shell wrapper at [`tools/v2-cycle-history-append`](../../tools/v2-cycle-history-append). Functional contract: append a single cycle-history entry as JSON to `state/cycle-history/<N>.json` with refuse-overwrite append-only semantics. CLI: `--cycle-n <num>` (required), `--repo-root`, `--state-dir`, `--from-stdin`, `--from-json`, `--field KEY=VALUE` (repeatable), `--allow-overwrite` (escape hatch), `--dry-run`, `--output path|json|quiet`. Validates required fields (`cycle_number`, `model`, `started_at`); enforces payload `cycle_number` matches CLI arg; passes through unknown fields verbatim for forward compatibility. Atomic write (tmp + rename) prevents partial-write corruption.
- **Test coverage: 19 unit + 12 integration = 31 tests, all passing.** Coverage: split-field parsing (basic, numbers, negatives, floats, bools, equals-in-value, empty-key/no-equals rejection); parse_object (object/array); validate_payload (complete/missing-fields/cycle-mismatch/empty-model/non-integer-cycle); serialize round-trip; atomic_write (creates/overwrites/no-extension); end-to-end CLI behavior (refuse-overwrite, --allow-overwrite, --dry-run, --from-json passthrough, CLI-field-overrides-JSON precedence, cycle-mismatch rejection, custom --state-dir, --output modes, invalid --field).
- **Migration-cost measurement (cycle 94):** ~268 production LOC (within Candidate A's stated ~200-500 LOC range); ~510 test LOC (167 inline + 343 integration); ~804 total LOC across 4 files (Cargo.toml + main.rs + integration.rs + wrapper). Build time ~1.01s release. **Zero new transitive deps added to workspace lockfile** (vs cycle 93's ~30 transitive deps when first introducing the {clap, serde, serde_json, tempfile} dep stack).
- **Documented findings in [`_notes/cycle-94-cycle-history-append-prototype.md`](../redesign/_notes/cycle-94-cycle-history-append-prototype.md):** comparison table vs cycle 93's `tool-registry`; cumulative 2-crate measurement against A's load-bearing claims; 6 risks named for `cycle-history-append`; sibling pattern tracking; F1-F5 honest reflection.

### Why this shape (deviation from cycle 93's paired-execution default)

Cycle 93 introduced functional-class shape #14 (paired execution = bounded-dispatch + prototype-scaffold) as NOVEL at 1 instance. Cycle 94 chose **prototype-scaffold-only** (no dispatch component), rationale:

1. **Eva-backlog accommodation.** 6 dispatches awaiting Eva's manual Copilot assignment as of cycle 94 fire (4 research + 2 cycle-93 feedback). Adding more dispatches compounds the queue without parallel work surface — Eva is the bottleneck, not Copilot capacity.
2. **Cycle 93 dispatches not yet returned.** The 2 cycle-93 feedback dispatches (#2869, #2870) have 0 comments; no return to integrate. Substantive focal option (3) "wait for cycle 93 dispatch returns" therefore unavailable.
3. **Repo-internal extension is highest-leverage.** Continuing prototype scaffolding extends migration-cost evidence base. Cycle 93 measured 1 crate; cycle 94 measuring a second moves to 2-instance evidence — 100% increase in evidence basis.
4. **Iteration-mix continues evidence-via-execution diversification.** Cycles 90-92 were analysis-only sharpening; cycle 93 introduced execution-evidence; cycle 94 continues that mix shift.

### Migration-cost validation result (cumulative across 2 crates)

| Metric | Cycle 93 `tool-registry` | Cycle 94 `cycle-history-append` |
|---|---|---|
| Production LOC | ~231 | ~268 |
| Test LOC (inline + integration) | ~347 | ~510 |
| Total LOC | ~578 | ~804 |
| Test count | 15 | 31 |
| Test:Prod ratio | ~1.5× | ~1.9× |
| Build time | ~1.24s | ~1.01s |
| New transitive deps | ~30 | **0** |

**Direction:** A's per-crate scope claim (~200-500 LOC, single-cycle) **further validated** at 2 crates. **Magnitude:** aggregate prod LOC trajectory at ~250 LOC/crate × 9 crates = ~2250 LOC, **below** A's stated 3000-4500 lower bound — but the 2 measured crates are explicitly the smaller end. With test code, aggregate ~7000-12000 LOC.

**Cycle 93 risk #1 (test-code amplification): PARTIALLY SUPPORTED at higher magnitude.** Test ratio widened from 1.5× to 1.9×.

**Cycle 93 risk #3 (Cargo.lock churn): REFUTED at second-crate level.** Subsequent crates reusing the dep stack add zero transitive deps. Cost is workspace-wide one-time, not per-crate. **Implication for A's candidate document:** dependency-footprint cost is bounded; argues for `[workspace.dependencies]` convention at cutover.

### Standing tasks

- 5 open issues unchanged: #2833 (oh-my-codex 31+ cycles), #2842 (PAI 23+ cycles), #2847 (oh-my-claudecode 19+ cycles), #2849 (audit-engagement 17+ cycles awaiting audit cycle 213/214 read), #2851 (Symphony 17+ cycles)
- 2 cycle-93 Copilot feedback dispatches unchanged: #2869 (tool-surface feasibility), #2870 (sharpening-claim adversarial) — both 0 comments, awaiting Eva's manual Copilot assignment
- 2 prototype scaffolds landed: cycle 93 `v2-tool-registry`, cycle 94 `v2-cycle-history-append`
- 7 standing input-from-eva directives unchanged

### Sibling pattern tracking

- **Functional-class shape #15 (Phase 3 prototype-scaffold-only) — NOVEL at 1 instance** (cycle 94). New shape introduced this cycle. Distinct from cycle 93's shape #14 (paired execution) because dispatch is omitted as Eva-backlog accommodation.
- **Functional-class shape #14 (paired execution) — remains NOVEL at 1 instance** (cycle 93). NOT advanced because cycle 94 does not pair dispatch with prototype scaffold.
- **Functional-class shape #13 (Phase 2 candidate authoring) — HARDENED at 3-instance evidence** (cycles 90-92). NOT an instance at cycle 94.
- **Sharpening pattern as iteration-until-approval discipline — TESTED at 2-cycle evidence** (cycles 91-92). NOT an instance at cycle 94.
- **Direction-vs-magnitude discipline — extends to 5 instances** (cycle 91 A's Axis 13 + cycle 92 B's per-role decision count + cycle 92 C's central bet + cycle 93 prototype scaffold migration-cost + cycle 94 prototype scaffold migration-cost #2). HARDENED holds.
- **Generalization-level discipline (J-Q(a)) — extends to 12 instances** (cycle 84+85+86+87+87-reflection+88+89+90+91+92+93+94). HARDENED holds.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 94 NOT a new instance.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 94 NOT a new instance.
- **Prototype-scaffold migration-cost-validation discipline (cycle 93 lexicon entry)** — extends to 2 instances (cycle 93 + cycle 94). Direction validated; magnitude refined.

### Bottleneck-state honesty

Bottleneck remains external (Eva's manual Copilot assignment for 6 dispatches; audit cron for #2849). Cycle 94 contribution is fully repo-internal asynchronous-of-bottleneck. Cycle 94 is the **SIXTEENTH consecutive cycle** (cycles 78-94) whose substantive output is fully repo-internal. The decision to NOT dispatch additional Copilot feedback this cycle is honest bottleneck accommodation.

### Honest reflection (per F1 corrective)

Cycle 94 is **1 substantive activity** (Phase 3 prototype-scaffold-only — second-crate measurement, cycle-history-append) yielding 1 Rust crate (~804 LOC across 4 files including tests + wrapper) + 31 passing tests + 1 _notes documentation file + 1 journal entry + Cargo.lock update (1 entry, 0 new transitive deps). Per F1 corrective: components are deliverable form of substantive activity; documentation IS the activity. Per F2 corrective: 1 prototype crate from 1 activity, NOT "31 tests = 31 ways improved." Per F3: 15 functional-class shapes at 34 instances. Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon (refuse-overwrite as structural append-only enforcement, dependency-footprint as one-time-not-per-crate, test-code amplification at 1.9×, pass-through schema forward-compatibility) as documented learning. Per H-Q(a) anti-inheritance: cycle 94 prototype scaffold grades empirical-observation-grounded against second-crate measurement vs cycle 93's first-crate measurement. Per J-Q(a) generalization-level discipline: HARDENED at 12 instances.

**Self-congratulation audit.** Cycle 94 prototype scaffold could be over-stated as "validates A's full migration cost claim across 2 crates." More honest: validates the per-crate scope claim for 2 of 9 crates, **both at the smaller end** of A's tool surface; aggregate-LOC claim depends critically on whether larger crates (`wiki-search`, `boot-phase`, `close-phase`) come in similarly bounded. The 2-instance evidence is direction-supporting, not magnitude-validating. Risk #2 from cycle 93 (`wiki-search` likely largest) remains the structural concern not addressed by cycle 94.

### Cycle 95 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 95 substantive focal options:

1. **Continue prototype scaffolding** MEDIUM PRIORITY — `phase-transition-check` (state-machine validation; structurally different from file-write/file-enumerate) or `prompt-contract-check` (CI test for prompt drift). Third-crate measurement broadens evidence basis from 2 to 3 instances.
2. **Pause prototype scaffolding; revert to second-iteration sharpening** MEDIUM PRIORITY — apply cycle 91-92 sharpening pattern to remaining first-iteration risks.
3. **Wait for cycle 93 dispatch returns** LOW-MEDIUM PRIORITY — defer if dispatches still unassigned.
4. **Update Candidate A document with cycle 93+94 measurement evidence** MEDIUM PRIORITY — propagate test-code amplification refinement (1.7× average; aggregate-with-tests ~7000-12000) and dependency-footprint risk recharacterization (one-time, not per-crate) into A's `## Migration cost from v1` section.
5. **Bounded-mechanical fallback** — close absorbed dispatches if any deliver.

**Cycle 95 substantive focal default:** option (1) `phase-transition-check` for third crate measurement, OR option (4) document propagation of cycle 93+94 evidence. Both are valid; the former broadens evidence base, the latter consolidates evidence into the candidate-selection-readiness state. If cycle 95 compute permits, paired execution = option (1) + (4). Option (2) sharpening as fallback if prototype scaffolding hits unexpected scope.

### What surprised me / what I noticed

- **Dependency-footprint risk is bounded after first crate, not per-crate.** Cycle 93's risk #3 framing implied per-crate accretion. Cycle 94's measurement refutes that: subsequent crates using {clap, serde, serde_json, tempfile} add zero transitive deps. Substantial cost reframe. Argues for `[workspace.dependencies]` convention at cutover — declare common deps once, members reference with `{ workspace = true }`.
- **Test-code amplification ratio widened from 1.5× to 1.9×.** Direction-supporting for cycle 93 risk #1 but also magnitude-widening: aggregate test-LOC implications scale up. The 1.9× is partly explained by `cycle-history-append`'s larger surface (more CLI options, more validation paths, more error cases) — more surface area means more tests. Risk: subsequent crates with similar surface area push A's aggregate-with-tests to ~12000.
- **Refuse-overwrite as structural append-only enforcement primitive.** Cycle 94's `cycle-history-append` is the first v2 write tool; refuse-overwrite establishes a pattern other v2 write tools should follow. Tool-level enforcement is more robust than prompt-level convention (which v1 relied on).
- **CLI-field-overrides-JSON precedence is intentional and explicitly tested.** Useful for callers with mostly-fixed JSON payloads + per-invocation overrides (e.g., wrapper injecting `started_at`). Integration tests document this precedence.
- **Pass-through schema is forward-compatible without versioning.** Tool only enforces 3 required fields; rest passes through. New cycle-history schema fields can be added at orchestrator level without tool change. Deliberate looseness — strictness is a future migration if downstream consumers demand it.
- **The 0-new-transitive-deps observation is itself a workspace-architecture insight.** If A's remaining 7 crates converge on the same dep stack, workspace dependency footprint is bounded. Cutover implication: workspace-shared deps convention is naturally indicated.
- **Build time held steady (~1.0s).** Both crates build in ~1.0-1.2s release mode after dep cache is warm. Workspace property: shared deps mean only the crate's own code recompiles. Aggregate build time for A's full 9-crate set plausibly bounded at ~10s release.
- **Cycle 94 introduces shape #15 (prototype-only) distinct from cycle 93's shape #14 (paired execution).** Both are valid composition shapes; the choice between them is bottleneck-aware. When Eva-backlog is high, #15 is the right choice; when dispatch capacity is high and parallel critique is valuable, #14 is the right choice. Cycle 95 onward should choose between them based on bottleneck state, not default to one.

---

## Cycle 93 — Paired execution: 2 Copilot feedback dispatches + v2-tool-registry prototype scaffold

**Cycle issue:** [#2868](https://github.com/EvaLok/schema-org-json-ld/issues/2868)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate iteration (under [`ITERATION-UNTIL-APPROVAL`](../../.github/workflows/orchestrator-redesign-prompt.xml), **fourth cycle of Phase 2 candidate-set work**)
**Cycle composition shape:** **Paired execution = bounded-dispatch + prototype-scaffold** (functional-class shape #14, NOVEL at 1 instance). 14 functional-class shapes total demonstrated through cycle 93 at 33 instances. Substantive focal: depart from cycle 92's hand-off default (Copilot feedback dispatch alone) toward a paired-execution shape that combines bounded dispatch (2 dispatches, not the cycle-92-plan's 4) + first piece of executable redesign code (`v2-tool-registry` Rust crate as Phase 3 prototype scaffold for migration-cost validation).

### What I did

- **Filed 2 Copilot feedback dispatches**: [#2869](https://github.com/EvaLok/schema-org-json-ld/issues/2869) on tool-surface feasibility (the load-bearing migration-cost criterion across A/B/C); [#2870](https://github.com/EvaLok/schema-org-json-ld/issues/2870) on cycle 91-92 sharpening-claim adversarial critique. Both use cycle 30's `[redesign-feedback]` convention. Lens-selection rationale documented in [`_notes/cycle-93-tool-registry-prototype-and-feedback-dispatches.md`](../redesign/_notes/cycle-93-tool-registry-prototype-and-feedback-dispatches.md): tool-surface feasibility and sharpening-claim adversarial critique are the most-discriminating two of cycle 92's named four lenses; F-pattern coverage and P1-P6 ordering deferred to potential cycle 94+ dispatches if first-round returns warrant.
- **Built `v2-tool-registry` Rust crate** at [`tools/rust/crates/v2-tool-registry/`](../../tools/rust/crates/v2-tool-registry/) + shell wrapper at [`tools/v2-tool-registry`](../../tools/v2-tool-registry). Functional contract: enumerate Rust crates under `tools/rust/crates/`, parse Cargo.toml descriptions, output as markdown / JSON / names. CLI: `--repo-root` / `--format` / `--filter` / `--v2-only`. Test coverage: 9 unit + 6 integration tests, all passing (15/15). Production LOC: ~231 (within Candidate A's stated ~200-500 LOC range); test LOC: ~347 (1.5× production code ratio); aggregate ~578 LOC across 4 files. Build time: ~1.24s release.
- **Migration-cost validation result**: direction validated for one specific crate (`tool-registry` is single-cycle scope and within stated LOC range when production-only is counted). Magnitude prototype-pending across remaining 8 A-shared crates. 4 risks named at structural level (test-code amplification; `wiki-search` likely largest crate; Cargo.lock churn from new dev-deps; description-parser limitations).

### Why this shape (deviation from cycle 92 hand-off)

Cycle 92 named option (2) Copilot feedback dispatch as cycle 93 substantive focal default, with full multi-lens dispatch (4 lenses). Cycle 93 chose **paired execution = bounded application of (2) + execution of (4) prototype scaffold**, rationale:

1. **Eva backlog real but bounded.** 4 research dispatches still assigned to EvaLok (not Copilot) as of cycle 93 fire. Adding 4 more dispatches would compound the assignment-pending queue. **Bounded to 2 dispatches** with the most-discriminating lenses.
2. **Iteration-mix diversity.** Cycles 91-92 were both sharpening cycles (analysis-only). A third consecutive sharpening cycle risks the local-optimum cycling pattern that cycle 92's evaluation-discipline learnings called out. Diversifying iteration activities — moving from analysis-only toward evidence-via-execution — broadens the iteration mix per `ITERATION-UNTIL-APPROVAL`'s named activities.
3. **Prototype scaffold authorized at MEDIUM PRIORITY in cycle 92's plan**. Building the smallest A-shared crate (`tool-registry`) is within that authorization (cycle 92 explicitly framed it as "NOT a full Phase 3 prototype — that requires candidate-selection-checkpoint approval. Cycle-bounded, scoped to migration-cost validation only").
4. **Paired execution is independent**. Dispatches and prototype scaffold are non-overlapping work surfaces, fitting ~75 min of cycle compute.

### Standing tasks

- 5 open issues unchanged from cycle 92: #2833 (oh-my-codex 30+ cycles), #2842 (PAI 22+ cycles), #2847 (oh-my-claudecode 18+ cycles), #2849 (audit-engagement 16+ cycles awaiting audit cycle 213/214 read), #2851 (Symphony 16+ cycles)
- 2 NEW open Copilot feedback dispatches filed cycle 93: #2869 (tool-surface feasibility), #2870 (sharpening-claim adversarial)
- 1 prototype scaffold landed cycle 93: `v2-tool-registry` crate + wrapper
- 7 standing input-from-eva directives unchanged

### Sibling pattern tracking

- **Functional-class shape #14 (paired execution = bounded-dispatch + prototype-scaffold) — NOVEL at 1 instance** (cycle 93). New shape introduced this cycle.
- **Functional-class shape #13 (Phase 2 candidate authoring) — HARDENED at 3-instance evidence** (cycles 90-92). NOT an instance at cycle 93.
- **Sharpening pattern as iteration-until-approval discipline — TESTED at 2-cycle evidence** (cycles 91 + 92). NOT an instance at cycle 93.
- **Direction-vs-magnitude discipline — extends to 4 instances** (cycle 91 A's Axis 13 + cycle 92 B's per-role decision count + cycle 92 C's central bet + cycle 93 prototype scaffold migration-cost validation). HARDENED holds.
- **Generalization-level discipline (J-Q(a)) — extends to 11 instances** (cycle 84+85+86+87+87-reflection+88+89+90+91+92+93). HARDENED holds.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 93 NOT a new instance.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 93 NOT a new instance.

### Bottleneck-state honesty

Bottleneck remains external (Eva's manual Copilot assignment for now-6 dispatches: 4 research-only from cycles 63-77 + 2 feedback-only filed cycle 93; audit cron for #2849). Cycle 93's prototype scaffold component is fully repo-internal. Cycle 93 is the **FIFTEENTH consecutive cycle** (cycles 78-93) whose prototype-scaffold component is fully repo-internal, plus 2 cycle-93 dispatches that benefit external work in subsequent cycles. The decision to bound dispatch count at 2 (rather than the cycle-92-plan's 4) reflects honest bottleneck accommodation.

### Honest reflection (per F1 corrective)

Cycle 93 is **2 substantive activities** (paired execution = 2 feedback dispatches + 1 prototype crate scaffold) yielding 2 dispatch issues + 1 Rust crate (~578 LOC across 4 files including tests + wrapper) + 15 passing tests + 1 _notes documentation file + 1 journal entry. Per F1 corrective: components are deliverable form of substantive activity; documentation IS the activity. Per F2 corrective: 2 dispatch issues + 1 prototype crate from 2 paired activities, NOT "2 ways improved." Per F3: 14 functional-class shapes at 33 instances. Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon (paired-execution shape, prototype-scaffold migration-cost-validation discipline, test-code amplification risk, dependency footprint risk) as documented learning. Per H-Q(a) anti-inheritance: cycle 93 prototype scaffold grades empirical-observation-grounded against single-crate measurement; cycle 93 dispatches grade per cycle 30 dispatch convention. Per J-Q(a) generalization-level discipline: HARDENED at 11 instances.

**Self-congratulation audit.** Cycle 93 prototype scaffold could be over-stated as "validates A's migration cost claim." More honest: validates the single-crate scope claim for one specific crate (`tool-registry`). The aggregate-LOC claim for A's full 9-crate set is NOT validated by cycle 93 — only one crate has been measured. Risk #2 (wiki-search likely largest) is a structural concern not addressed by the prototype.

### Cycle 94 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 94 substantive focal options:

1. **Continue prototype scaffolding** MEDIUM PRIORITY — build the next-smallest A-shared crate (`cycle-history-append` or `phase-transition-check`) to extend the migration-cost validation surface beyond single-crate measurement.
2. **Second-iteration sharpening** MEDIUM-HIGH PRIORITY — apply cycle 91-92 sharpening pattern to remaining first-iteration risks: A's tool-registry-growth risk; B's coordination-overhead-magnitude; C's plan-authoring-discipline-conditional risk.
3. **Wait for cycle 93 dispatch returns** LOW-MEDIUM PRIORITY — if Eva assigns cycle 93 dispatches to Copilot before cycle 94 fires, cycle 94 can integrate returned critique. If not, cycle 94 proceeds with substantive option (1) or (2).
4. **Phase 3 prototype scaffold for next-smallest A-shared crate** — MEDIUM PRIORITY (variation of option 1).
5. **Bounded-mechanical fallback** — close absorbed dispatches if any deliver.

**Cycle 94 substantive focal default:** option (1) or (4) — continue prototype scaffolding to extend migration-cost validation. Cycle 93 measured 1 crate; cycle 94 measuring 1-2 more crates moves from single-instance evidence to 2-3 instance evidence. Fallback to option (2) sharpening if prototype scaffolding hits unexpected scope.

### What surprised me / what I noticed

- **Test code is significant fraction of total LOC.** ~347 test LOC vs ~231 production LOC = 1.5× ratio. Candidate A's stated "~3000-4500 LOC aggregate" implicitly excludes test code; with test code, aggregate is ~7500-11250. This is a **cycle-93-emergent risk** that should propagate to A's candidate document if it survives further crate measurement.
- **Hand-rolled TOML parser is acceptable for prototype but limits.** Multi-line description strings return empty (test documents this). For cutover, this should either get the `toml` crate dependency or get a more robust parser.
- **The `--v2-only` filter returns empty against actual workspace.** Only v2-tool-registry has the `v2-` prefix and it's self-excluded. The flag is only useful when ≥2 v2-prefixed crates exist; this is expected at cycle 93 (first v2 crate).
- **`tempfile` dev-dependency added ~30 transitive deps.** First-time visibility into per-crate dependency footprint. Workspace-shared dependencies should be considered as scaffolding lands across more crates.
- **BrokenPipe panic on stdio is a known Rust idiom.** Initial naïve implementation panicked when piped through `head`. Fix is straightforward (explicit stdout handle + BrokenPipe error catch). Pattern should be reused across all v2 tool prototypes that produce streaming output.
- **The dispatch-bound-to-2 decision was load-bearing.** Cycle 92's plan named 4 dispatches; cycle 93 bounded to 2. The Eva-backlog argument was decisive. Future cycles should weigh dispatch count against bottleneck state honestly rather than defaulting to "as many as the plan named."
- **Paired execution as iteration-mix diversification.** This cycle deliberately departed from the analysis-only iteration mix (cycles 90-92 were all candidate authoring + sharpening) toward execution-evidence (prototype scaffold). The diversification reflects ITERATION-UNTIL-APPROVAL's named activities being not just "sharpen / critique / stress-test" but also implicitly "produce evidence by execution." Whether this is a one-time shift or a sustained iteration shape depends on whether the prototype work produces unexpected discoveries that warrant continued execution-emphasis.

---

## Cycle 92 — Phase 2 candidate authoring (third iteration): paired sharpening of B's per-role decision count + C's central-bet validation

**Cycle issue:** [#2867](https://github.com/EvaLok/schema-org-json-ld/issues/2867)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate authoring (under [#2829](https://github.com/EvaLok/schema-org-json-ld/issues/2829) polarity inversion, **thirty-first consecutive cycle of research-corpus advancement**, cycles 62-92)
**Cycle composition shape:** **Phase 2 candidate authoring** (functional-class shape #13, third instance after cycles 90 + 91 — advances from TESTED at 2-instance evidence to **HARDENED at 3-instance evidence within the Phase 2 authoring arc**). 13 functional-class shapes total demonstrated cycles 62-92 at 32 instances. Substantive focal: paired sharpening of Candidate B's per-role decision count estimate and Candidate C's central-bet validation, applying the cycle 91 sharpening pattern. Cycle 85 toggle (cold-reader cadence suspended) in continued effect through Phase 2 candidate authoring.

### What I did (per cycle 91 hand-off paired-sharpening default)

- **Candidate B sharpening: per-role decision count empirical estimate** (~110 lines added, [`docs/redesign/2-candidates/B-decomposed-multi-role.md`](../redesign/2-candidates/B-decomposed-multi-role.md)). Per-role decision-class enumeration: planner ~6-10 / executor ~10-15 / curator ~6-12 / reconciler ~3-7 per cycle. v1 baseline measured at ~40 decisions in candidate-authoring cycle 91. B aggregate ~25-44 per-role + ~9-23 coordination = ~34-67 per cycle; lower bound below v1, upper bound ~1.7× v1. **NOT 3-4× as the M2 sub-shape-adoption count (43:6 = 7×) suggested** — sub-shape adoption count is per-cycle decision count *upper bound*, not actual rate. Most adopted sub-shapes fire bursty / situationally. Validation plan named (cycle 93+ Phase 3 prototype paired-cycle measurement); 5 risks named at structural level.
- **Candidate C sharpening: central-bet validation (F2 + F4 + F11 detection legibility)** (~120 lines added, [`docs/redesign/2-candidates/C-hybrid.md`](../redesign/2-candidates/C-hybrid.md)). Per-failure-mode metrics: F2 latency identical (no improvement); F2 reliability ~4pp improvement (95% → 99%); F4 plan-state-legibility categorical (zero named states → 3 named states) + ~3-4 cycle reduction in stale-detection lag; F11 detection rate ~5-13pp improvement (~85-90% → ~95-98%) + categorical legibility improvement. Aggregate central-bet direction validated; **F4 + F11 substantial; F2 marginal** — asymmetry across failure modes informs candidate-selection. Validation plan named (cycle 93+ Phase 3 paired-cycle prototype A 3-mode vs C 4-mode); 6 risks named at structural level. Rollback path named: if F2 + F4 + F11 below 25% estimate-magnitude, C reverts to "A + plans-as-artifacts" smaller prototype rather than full middle-path.
- **2-candidates/README.md update** (~30 lines net-add, [`docs/redesign/2-candidates/README.md`](../redesign/2-candidates/README.md)). Cycle 92 status update; Load-bearing claims sharpening tracker subsection (3-candidate × 4-column table: claim / sharpening-cycle / direction / magnitude); methodological pattern observation graduating to TESTED at 2-cycle evidence; Forward work for cycle 93+ rewritten with second-iteration sharpening targets, Copilot feedback dispatch HIGH PRIORITY, audit critique solicitation, Phase 3 prototype scaffold preview.

### Methodological refinement surfaced cycle 92

**Sub-shape adoption count vs per-cycle decision count distinction.** B's PARTIAL-FLAG language ("3-4× v1") applied to sub-shape adoption count (43:6 = 7×) but per-cycle decision count is closer to 1× when sub-shape firing rate is accounted for. Most adopted sub-shapes fire bursty (memory consolidation per ~5-10 cycles; branch-spawning per ~5-20 cycles; what-if reasoning per ~10+ cycles; plan-lifecycle promotion per ~3-10 cycles). This refinement applies to all 3 candidates' P3 grades (A unaffected; C +3 sub-shapes weaker than first appeared; B aggregate comparable to v1 not 3-4×). Cycle 92 surfaces the refinement; cycle 93+ may apply it to re-grading.

### Standing tasks

- 5 open issues unchanged from cycle 91: #2833 (oh-my-codex 29+ cycles), #2842 (PAI 21+ cycles), #2847 (oh-my-claudecode 17+ cycles), #2849 (audit-engagement 15+ cycles awaiting audit cycle 213/214 read), #2851 (Symphony 15+ cycles)
- 1 open PR: #2830 (cycle composition polarity for Phase 1) standing
- 7 standing input-from-eva directives unchanged

### Sibling pattern tracking

- **Functional-class shape #13 (Phase 2 candidate authoring) — HARDENED at 3-instance evidence within Phase 2 authoring arc** (cycles 90 + 91 + 92). Advances from TESTED at 2 instances per cycle 91.
- **Sharpening pattern as iteration-until-approval discipline — TESTED at 2-cycle evidence** (cycle 91 + cycle 92). Cycle 92 paired sharpening = 2 instances within 1 cycle.
- **Direction-vs-magnitude discipline — HARDENED at 3-instance evidence** (cycle 91 A's Axis 13 + cycle 92 B's per-role decision count + cycle 92 C's central bet).
- **Sub-shape adoption count vs per-cycle decision count — methodological distinction surfaced** (1-instance evidence cycle 92).
- **Load-bearing claims sharpening tracker — first-class candidate-selection artifact** (1-instance evidence cycle 92; methodological observation).
- **Functional-class shape #12 (artifact-resident M-item integration) — HARDENED at 4 instances** (cycles 86-89). NOT an instance at cycle 92.
- **Generalization-level discipline (J-Q(a)) — HARDENED at 10 instances** (cycle 84+85+86+87+87-reflection+88+89+90+91+92). Continues to hold.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 92 NOT a new instance.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 92 NOT a new instance.

### Bottleneck-state honesty

Bottleneck remains external (Eva's manual Copilot assignment for 4 dispatches; audit cron for #2849). Cycle 92 contribution is fully repo-internal asynchronous-of-bottleneck like cycles 78-91. Cycle 92 is the **FOURTEENTH consecutive cycle** (cycles 78-92) whose output is fully repo-internal. Cycle 92 is the **THIRD Phase 2 candidate authoring cycle**.

### Honest reflection (per F1 corrective)

Cycle 92 is **1 substantive activity** (Phase 2 paired sharpening — third Phase 2 candidate authoring cycle, second sharpening iteration after cycle 91 first sharpening) yielding 2 candidate-document sharpening sections (B ~110 lines + C ~120 lines) + 1 README expansion (~30 lines net-add) + 1 _notes documentation file + 1 journal entry. Per F1 corrective: components are deliverable form of substantive activity; documentation IS the activity. Per F2 corrective: ~230 lines of grounded enumeration + concrete metrics + validation plans + risks across 2 candidates from 1 activity, NOT "230 ways improved." Per F3: 13 functional-class shapes at 32 instances. Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon (sharpening pattern, direction-vs-magnitude discipline, sub-shape-adoption vs firing-rate distinction, load-bearing claims tracker) as documented learning. Per H-Q(a) anti-inheritance: cycle 92 sharpening grades empirical-observation-grounded against per-candidate enumeration. Per J-Q(a) generalization-level discipline: HARDENED at 10 instances.

### Cycle 93 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 93 substantive focal options:

1. **Second-iteration sharpening on first-iteration risks** MEDIUM-HIGH PRIORITY — A's tool-registry-growth risk; B's coordination-overhead-magnitude; C's plan-authoring-discipline-conditional risk.
2. **Solicit Copilot feedback dispatch on the 3-candidate set + sharpening sections** HIGH PRIORITY — multi-lens parallel dispatches (F-pattern coverage; P1-P6 ordering; tool surface feasibility; sharpening-claim adversarial critique).
3. **Audit critique solicitation** LOW PRIORITY (audit reads main per cross-repo discipline).
4. **Phase 3 prototype scaffold for the smallest A-shared crate** MEDIUM PRIORITY — `tool-registry` Rust crate as proof-of-concept (~200-500 LOC, single-cycle scope).
5. **Bounded-mechanical fallback** — close absorbed dispatches if any deliver.

**Cycle 93 substantive focal default:** option (2) Copilot feedback dispatch — cycle 92 sharpening lands first-iteration grounding for all 3 candidates; critique surface is now substantive. Sharpening should land before feedback solicitation; cycle 92 closes that gap. Fallback to option (1) or (4) if Copilot dispatch capacity exhausted.

### What surprised me / what I noticed

- **B's per-cycle decision count is comparable to v1's, not 3-4× as the M2 sub-shape-adoption count suggested.** The conflation of sub-shape adoption count with per-cycle decision count is a previously-unidentified evaluation-discipline gap. Most adopted sub-shapes fire bursty / situationally, not every cycle. The refinement weakens B's PARTIAL-FLAG severity at the per-cycle level.
- **C's F2 improvement is marginal (~4pp reliability) while F4 + F11 improvements are substantial.** This asymmetry across failure modes informs candidate-selection: if F4 + F11 are load-bearing, C is well-positioned; if F2 was the primary motivation for C's reconcile-mode, the candidate is over-engineered.
- **The sharpening pattern produces evaluation-discipline refinements as a side effect.** Cycle 92's enumeration surfaced the sub-shape-adoption vs per-cycle-decision distinction. Sharpening individual claims surfaces refinements applicable to all candidates.
- **Direction-vs-magnitude discipline scales across multiple sharpening instances.** Cycle 91 introduced it once (A's Axis 13); cycle 92 applies it twice (B + C) without modification. The discipline is a stable methodological pattern.
- **The load-bearing claims sharpening tracker naturally emerged as a first-class candidate-selection artifact.** Cross-candidate comparison artifacts (12-axis table; P1-P6 table; load-bearing claims tracker) are layered evaluation lenses, each addressing a different reviewer-question.
- **Paired sharpening produces 2 instances of the sharpening pattern in one cycle.** Session-window-dependent capacity observation: paired sharpening fits ~75min because the sharpening pattern is structurally similar across candidates. Future cycles may produce 3-instance sharpening once the pattern is more efficient.

---

## Cycle 91 — Phase 2 candidate authoring (second iteration): Candidate C + A's Axis 13 sharpening + 3-candidate side-by-side comparison

**Cycle issue:** [#2866](https://github.com/EvaLok/schema-org-json-ld/issues/2866)
**Model:** claude-opus-4-7
**Mode:** redesign Phase 2 candidate authoring (under [#2829](https://github.com/EvaLok/schema-org-json-ld/issues/2829) polarity inversion, **thirtieth consecutive cycle of research-corpus advancement**, cycles 62-91)
**Cycle composition shape:** **Phase 2 candidate authoring** (functional-class shape #13, second instance after cycle 90 — advances from NOVEL at 1 instance to **TESTED at 2-instance evidence within the Phase 2 authoring arc**). 13 functional-class shapes total demonstrated cycles 62-91 at 31 instances. Substantive focal: author Candidate C against augmented synthesis surface; sharpen Candidate A's Axis 13 extraction estimate per cycle 90 hand-off; update README with 3-candidate side-by-side comparison. Cycle 85 toggle (cold-reader cadence suspended) in continued effect through Phase 2 candidate authoring.

### What I did (per cycle 90 hand-off)

- **Candidate C — Hybrid / middle path** (~280 lines, [`docs/redesign/2-candidates/C-hybrid.md`](../redesign/2-candidates/C-hybrid.md)). Middle path: preserve v1 single-orchestrator substrate (Axis 1 = single-threaded, like A) but elevate inbound-channel reconciliation to first-class harness phase (Axis 7 = 4-mode boot/reconcile/work/close) and adopt plans-as-artifacts (Axis 5 = Yes, like B). Tool surface: ~11 new Rust crates, ~4000-6000 LOC (between A's 9 crates / 3000-4500 LOC and B's 12+ Rust + 20-40 skill / 10000-20000 LOC). P1-P6: P1 PASS, P2 PASS, P3 PASS-WITH-NOTE (~24 sub-shapes vs A's 21), P4 PASS-WITH-WORK (cluster C sub-shapes 4+5 only), P5 PASS at authoring level, P6 5/5 PASS. M3: 5/5 PRESERVED, 2 EXTENDED (S2 + S3). M2: 13 LOW + 10 MODERATE + 1 HIGH.
- **Candidate A Axis 13 extraction sharpening** (~80 lines added, [`docs/redesign/2-candidates/A-evolved-single-orchestrator.md`](../redesign/2-candidates/A-evolved-single-orchestrator.md)). Per cycle 90 hand-off naming this as the highest-leverage iteration target. Grounded P3 PASS claim with concrete extraction-vs-retention details: ~260 named procedural steps in v1 STARTUP + COMPLETION; ~130-160 deterministic-extractable; ~100-130 require orchestrator judgment. Estimated v2 prompt size: ~600-800 lines vs v1's ~2400 lines = ~67-75% byte-level reduction; ~50-62% structural extraction at named-step level. Validation plan + risks named at structural level.
- **2-candidates/README.md update** (~80 lines net-add, [`docs/redesign/2-candidates/README.md`](../redesign/2-candidates/README.md)). Cycle 91 status update; 3-candidate position summary table; side-by-side 12-axis comparison; side-by-side P1-P6 + M3 + M2 + migration comparison; most-discriminating criteria across 3 candidates; forward work for cycle 92+.

### Distinctness check (per cycle 90 hand-off question)

Cycle 90 hand-off named: "Whether C is genuinely distinct from A + targeted additions, or just a partial-B."

**Answer:** C is **genuinely distinct** from A on three axes (Axis 5, 7, 12). C is **preserved-distinct** from B on five axes (Axis 1, 2, 4, 6, 13). C is **not** A + targeted additions cosmetically, and **not** B-light — the middle path is its own design space, not a hybrid in the dilution sense.

### Side-by-side P3 ordering across 3 candidates

| Candidate | P3 grade | Sub-shapes adopted | Per-cycle decision overhead estimate |
|---|---|---|---|
| A | PASS decisively | ~21 | ~30-40% lower than v1 prompt's apparent surface |
| C | PASS-WITH-NOTE | ~24 | ~25-35% lower than v1 prompt's apparent surface |
| B | PARTIAL-FLAG | ~43 | ~3-4× v1 at system level (per-role mitigation real but unproven) |

P3 produces monotonic ordering across all 3 candidates in self-management cost direction. This validates audit#454 P3 ACCEPT verdict at higher confidence than cycle 90's 2-candidate observation revealed.

### Standing tasks

- 5 open issues unchanged from cycle 90: #2833 (oh-my-codex 28+ cycles), #2842 (PAI 20+ cycles), #2847 (oh-my-claudecode 16+ cycles), #2849 (audit-engagement 14+ cycles awaiting audit cycle 213/214 read), #2851 (Symphony 14+ cycles)
- 1 open PR: #2830 (cycle composition polarity for Phase 1) standing
- 7 standing input-from-eva directives unchanged

### Sibling pattern tracking

- **Functional-class shape #13 (Phase 2 candidate authoring) — TESTED at 2-instance evidence** (cycles 90 + 91 within Phase 2 authoring arc). Advances from NOVEL at 1 instance per cycle 90 to TESTED at 2 instances per cycle 91. If cycles 92+ produce same-shape instances, advances to HARDENED at 4-instance evidence.
- **Cross-axis dependency map admits multiple coherent endpoints** — methodological observation. 1-instance evidence (cycle 91 candidate authoring revealed C as a coherent third endpoint distinct from A and B).
- **P3 evaluation discipline produces ordering signal across 3 candidates** — methodological observation. 1-instance evidence (cycle 91 3-candidate ordering: A PASS → C PASS-WITH-NOTE → B PARTIAL-FLAG).
- **Sharpening as iteration-until-approval discipline** — methodological observation. 1-instance evidence (cycle 91 Candidate A Axis 13 extraction sharpening). Future cycles 92+ may produce same-shape instances when sharpening B's per-role decision count or C's central bet.
- **Functional-class shape #12 (artifact-resident M-item integration) — HARDENED at 4 instances** (cycles 86-89). NOT an instance at cycle 91.
- **Generalization-level discipline (J-Q(a)) — HARDENED at 9 instances** (cycle 84+85+86+87+87-reflection+88+89+90+91). Continues to hold.
- **Audit-as-peer pattern** — 2-instance evidence holds. Cycle 91 NOT a new instance.
- **Sibling pattern binary-becomes-more-structured** — HARDENED at 4 instances. Cycle 91 NOT a new instance.

### Bottleneck-state honesty

Bottleneck remains external (Eva's manual Copilot assignment for 4 dispatches; audit cron for #2849). Cycle 91 contribution is fully repo-internal asynchronous-of-bottleneck like cycles 78-90. Cycle 91 is the **THIRTEENTH consecutive cycle** (cycles 78-91) whose output is fully repo-internal. Cycle 91 is the **SECOND Phase 2 candidate authoring cycle**.

### Honest reflection (per F1 corrective)

Cycle 91 is **1 substantive activity** (Phase 2 candidate authoring + sharpening — second iteration) yielding 1 new candidate document + 1 sharpening section to existing candidate + 1 README expansion + 1 _notes documentation file + 1 journal entry. Per F1 corrective: components are deliverable form of substantive activity; documentation IS the activity. Per F2 corrective: ~180+ axis-position commitments and concrete extraction-grounding from 1 activity, NOT "180 ways improved." Per F3: 13 functional-class shapes at 31 instances. Per F4: HARDENED/TESTED/NOVEL is for redesign-process methodology only. Per F5: lexicon as documented learning. Per H-Q(a) anti-inheritance: cycle 91 candidate C grades empirical-observation-grounded against framework template + augmented synthesis surface. Per J-Q(a) generalization-level discipline: HARDENED at 9 instances.

### Cycle 92 plan

Per `ITERATION-UNTIL-APPROVAL`, cycle 92 substantive focal options:

1. **Sharpen Candidate B's per-role decision count empirical estimate** HIGH PRIORITY — apply cycle 91 sharpening pattern (concrete extraction-vs-retention; named risks; validation plan) to B. Per-role decision count compared against v1 baseline. ~80 lines addition to B-decomposed-multi-role.md.
2. **Sharpen Candidate C's central bet** HIGH PRIORITY — concrete metric for "carrying their weight" (F4 + F2 + F11 detection legibility); validation plan; risks named at structural level. ~80 lines addition to C-hybrid.md.
3. **Solicit Copilot feedback dispatch on the 3-candidate set** MEDIUM PRIORITY.
4. **Audit critique solicitation** LOW PRIORITY (audit reads main per cross-repo discipline).
5. **Bounded-mechanical fallback** — close absorbed dispatches if any deliver.

**Cycle 92 substantive focal default:** options (1) + (2) as paired sharpening (parallel sharpening of B's per-role decision count and C's central-bet validation), or option (3) Copilot feedback dispatch as alternative. Choice: sharpening is more directly load-bearing than feedback solicitation at this iteration.

### What surprised me / what I noticed

- **Candidate C's authoring confirmed cycle 90's middle-path-distinctness hypothesis.** Genuinely distinct from A on three axes; preserved-distinct from B on five axes. The middle path is its own design space, not a dilution.
- **The cross-axis dependency map admits multiple coherent endpoints.** Cycle 90 observed two; cycle 91 observes three. The dependency-graph traversal produces multiple coherent walks.
- **P3 evaluation discipline orders all 3 candidates.** A PASS → C PASS-WITH-NOTE → B PARTIAL-FLAG is monotonic in self-management cost direction. Finer-grained discrimination than 2-candidate set revealed.
- **Sharpening A's Axis 13 estimate revealed a magnitude-vs-direction distinction.** P3 PASS direction (medium-harness extraction reduces procedural surface) is validated by structural analysis; magnitude (50% vs 40% vs 30%) requires Phase 3 prototype validation. Methodological pattern useful for sharpening other load-bearing claims.
- **Audit#455 is audit-side, not main-side.** Initially read as potential critique to absorb; closer reading revealed it's audit's own STARTUP_CHECKLIST gap. All 3 candidates' P6 Criterion 4 grades already reflect this gap correctly.
- **Side-by-side comparison tables produced more discrimination signal than narrative comparison would have.** Tabular surfacing of monotonic P3 ordering, C's bridging position on F4 and F2, and migration cost ordering — prose comparison wouldn't have surfaced this. Methodological observation: comparison tables are first-class artifacts for candidate selection.

---

