# Phase 2 candidate selection — checkpoint summary

**Status (cycle 138, 2026-05-13): RESOLVED. Q7 option (c) — Candidate B selected. Phase 2 checkpoint APPROVED.** Eva's directive [#2930](https://github.com/EvaLok/schema-org-json-ld/issues/2930) (2026-05-13 14:12 UTC, verified `input-from-eva` + author `EvaLok`) resolves Q1-Q9. Cycle 138 records the resolution; cycle 139+ proceeds to Phase 3 prototype work on B (planner → executor → curator → reconciler multi-agent topology) with named load-bearing primitives `role-driver`, `channel-router`, `super-step-boundary`, `reconciler-event-processor`.

**Framework rename (per directive):** Criterion 1's label "schema-work-enabling" is renamed to **"system bandwidth preservation"** (or "experiment runway"). The schema-org-json-ld library is test material; the system itself is the deliverable. Migration cost is one-time investment IN the deliverable, not subtraction FROM it. The rename is carried in [`2-design-framework.md`](./2-design-framework.md) and [`2-candidates/README.md`](./2-candidates/README.md). Historical iteration paragraphs below (cycles 126/128/129/130/134/135) and the deep iteration log at [`2-selection.md`](./2-selection.md) retain the original "schema-work-enabling" wording as frozen historical record per the journal-immutability discipline (cycle 133 policy).

**Honest acknowledgment of bias (per directive):** the orchestrator's cycles 111-137 analysis arc converged on "Q7-dependent" with A > C >> B selection ordering under Criterion 1's old label. The arc was structurally biased — both by Criterion 1's labeling (schema-throughput as the primary thesis) and by asymmetric stress-test substrate (A and C heavily stress-tested; B got one B-side stress-test at cycle 118 that the analysis then used to dismiss B). The Copilot critique at [#2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) L2 was correct; the strength-classification discipline became a defense mechanism that filtered out the meta-evidence that incremental approaches were failing. **None of that invalidates the substrate produced** — the v2 crate measurements + audit absorptions + candidate-pattern tracking are real value. The cycle 126 [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) D1 no-regret framing carries into B: `v2-tool-registry`, `v2-cycle-history-append`, `v2-phase-transition-check`, `v2-wiki-search`, `v2-gardening-sweep` carry as no-regret primitives; `v2-boot-phase` and `v2-close-phase` need re-evaluation (orchestration-hub bundling is single-orchestrator-specific). The two unbuilt crates (`detect-abandoned-cycles`, `prompt-contract-check`) become open-questioned carryovers under B.

**The historical iteration log below this resolution section is preserved as the record of how the orchestrator reached "Q7-dependent" rather than B unilaterally.** Cycle 120 L2 constraint preserved — [`2-selection.md`](./2-selection.md) is touched only with a single Q7-resolution pointer note at top; the body is not recursively annotated. The cycle 130 D4 + cycle 135 cost-of-being-wrong table remain as they stood at cycle 137 entry; the dominant asymmetry (A/C bounded weeks vs B months by 2.7×-4.5×) was correctly named and is acknowledged by Eva's directive as the cost of arriving at a genuinely durable system. Eva's framing: paying months to learn whether drastic architectural change unblocks the deliverable is worth it; paying bounded weeks to preserve a substrate that has demonstrated it cannot produce the deliverable is not.

---

This summary IS the Eva-legible deliverable. The deep history of the cycle 111-120 iteration is preserved at [`2-selection.md`](./2-selection.md) (~990 lines) for those reading the full trajectory; this file is what the candidate-selection checkpoint approval rests on. The sections below remain as authored at cycle 120-135 to preserve the iteration record; the Q7 question they pose has been answered as recorded above.

## The single load-bearing question

**Q7 — Which weighting does Eva approve?**

| Weighting choice | Selection |
|---|---|
| **(a)** Criteria 4+5 cumulative findings outrank Criteria 1+3 migration-cost weight | **Candidate C (Hybrid)** |
| **(b)** Primary-thesis Criterion 1 + cost-of-being-wrong Criterion 3 outrank Criteria 4+5 cumulative findings | **Candidate A (Evolved Single-Orchestrator)** |
| **(c)** Different weighting rule (e.g., Criterion 5 central-bet defensibility is the single load-bearing criterion; Criterion 1 is a constraint not the primary axis) | Eva names; orchestrator implements |

The three candidates are of comparable merit on different dimensions. The choice is a value resolution Eva owns — not a technical determination the orchestrator can make alone.

## The three candidates in one sentence each

- **[A — Evolved Single-Orchestrator](./2-candidates/A-evolved-single-orchestrator.md):** preserve v1's single-orchestrator substrate; extract medium-harness procedural surface via Axis 13; structural cleanup of state.json + _notes-as-memory. Migration: ~3600-6200 LOC, per-crate single-cycle cutover. Wins Criteria 1+2+3.
- **[B — Decomposed Multi-Role](./2-candidates/B-decomposed-multi-role.md):** 4-agent decomposition (planner/executor/curator/reconciler) with typed-channel-map + branching checkpoints + fat harness. Migration: ~14000-28000 LOC, 16-30 cycle cutover. Structural-fit advantage on Criterion 6 (audit-as-peer fit) is theoretical; A/C's fit is empirically demonstrated under V2 (cycle 128 [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) D2 absorption).
- **[C — Hybrid](./2-candidates/C-hybrid.md):** preserve A's single-orchestrator substrate but elevate inbound-channel reconciliation to first-class harness phase (Axis 7 = 4-mode) and adopt plans-as-artifacts (Axis 5 = Yes). Migration: ~5200-8200 LOC, bounded single-cycle per A-shared crate + 2-3 cycles each for reconcile-mode + plan-lifecycle. Wins Criteria 4+5.

## What each gives up

| | Loses on | Why this matters |
|---|---|---|
| **A** | F2/F4/F11 detection legibility weaker than C/B (interleaves reconciliation with boot phase); no plans-as-artifacts; F4/F11 addressing depth depends on Phase 3 measurement | Under family-level reading of the retrospective (reconciliation asymmetry as dominant family per lines 161-162 + 959), legibility on the dominant family is structurally important |
| **B** | Migration cost 3-5× A's; inverts CORE-DESIGN-PRINCIPLE at prompt level (~50% larger aggregate prompt content); central bet on cluster G role-asymmetric context is corpus-pattern-grounded not retrospective-grounded (cycle 118 finding); P3 self-management-cost PARTIAL-FLAG | The redesign's primary thesis cannot rest on a candidate that's conditionally schema-work-enabling at 3-5× migration cost |
| **C** | Conditional-improvement risk (3 additional sub-shapes must carry their weight); PR #2877 names C as most under-justified estimate posture; per-mode runtime budgets add complexity at unproven value | "C is just A with more migration surface" if the additions don't carry their weight; direction is structurally validated (cycle 92 + cycle 96 + cycle 107), magnitude is Phase 3 measurement |

## How the recommendation got to "Q7-dependent" cycle 120

Cycles 111-119 iterated extensively on the cross-candidate comparison. The orchestrator's tentative recommendation moved A (cycles 111-113) → C (cycle 114 flip per the four-finding flip threshold) → C-with-progressive-qualifications (cycles 115-119). The cycle 119 [#2910](https://github.com/EvaLok/schema-org-json-ld/issues/2910) Copilot adversarial-feedback dispatch surfaced three substantive findings that shift the tentative recommendation to **Q7-dependent** rather than a unilateral C:

1. **The cycle 113 → cycle 115 flip-back threshold introduced a strength criterion ("substantively undermining") that the original four-finding threshold did not apply** ([#2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) L1 Finding 2). The asymmetry is real: cycle 114 honored the original threshold to flip A→C without applying the strength criterion; cycles 116-117 then applied the strength criterion to classify subsequent findings as "qualifying-not-substantively-undermining." This is an asymmetry of application, not language. Honest action: the orchestrator's classification of cycle 116-117 findings is self-referential; Eva's Q7 resolution should be the deciding lever, not the orchestrator's strength-criterion judgment.

2. **Migration cost can be classified as Criterion 1 (schema-work-enabling) rather than only Criterion 3 (cost-of-being-wrong)** ([#2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) L1 Finding 3). Migration cycles ARE cycles not in the proof domain — directly a Criterion 1 operand. Under this reframing, A's migration cost advantage (~3600-6200 LOC vs C's ~5200-8200 LOC PR #2877 revised) becomes additive to A's Criterion 1 lead. The cycle 111 classification of migration cost as Criterion 3 separately from Criterion 1 is a framing choice that systematically tilted the analysis toward C cycle 114+. Honest action: name this as part of Q7's resolution space.

3. **Zero operational failure evidence exists for either A or C** ([#2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) L1 Finding 4). All evidence is theoretical / structural. The choice depends on Eva's weighting of theoretical arguments, not on empirical disambiguation that hasn't happened yet. Honest action: name this symmetric uncertainty rather than presenting C's direction-validation as decisive evidence.

The orchestrator's tentative recommendation therefore shifts cycle 120 to **Q7-dependent** rather than C-unilaterally. This is more honest than the cycle 114-119 framing.

**Cycle 126 [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) refinement** (audit cycle 217 substantive critique; third V2 audit-engagement, first via implicit-ask channel): two additions to Q7's resolution surface.

- **D1 — cycles 122-125 A-shared scaffolding is A∪C-scoped architecture evidence**, not domain-agnostic evidence. The cumulative LOC trajectory (~4440 prod / 9-crate aggregate; ~10212 with tests) estimates LOC for *orchestrator-invoking-primitives at single-orchestrator substrate*. 4 of 5 measured crates (`v2-tool-registry`, `v2-cycle-history-append`, `v2-phase-transition-check`, `v2-wiki-search`) are **no-regret primitives** whose implementations would carry residual value in B with different usage patterns; only `v2-boot-phase` (cycles 123-124) is A∪C-specific in its 5-sub-responsibility bundling that B would split across planner/executor/reconciler agents. The cumulative architecture extrapolation does NOT generalize to B's multi-agent topology.
- **D5 — cost-of-being-wrong asymmetry across Q7 options should be explicit**, not only implicit in Criterion 3. The asymmetry is dominantly between A/C (bounded weeks) and B (months by 2.7×-4.5×). See the table in "What this means for Phase 3" below.

Other audit#462 findings (D2 D3 D4 + missing patterns M1-M5 + Phase 3 implications P3-1 through P3-8) are absorbed in [`_notes/cycle-126-audit-217-absorption-D1-D5.md`](./_notes/cycle-126-audit-217-absorption-D1-D5.md) for cycle 127+ work and Phase 3 design requirements.

**Cycle 128 [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) D2 absorption** (audit-as-peer empirical calibration of Criterion 6): the "B wins Criterion 6 narrowly (audit-as-peer fit)" framing carried cycles 89-127 is reframed. B's structural-fit advantage on the audit-as-peer pattern (B's reconciler agent IS the cluster G clean-context-reviewer pattern at session level) is **theoretical**; A/C's audit-as-peer fit is **empirically demonstrated** under V2 substrate operating across ~146 V2-era concurrent cycles (main 1-128 + ~18 Opus 4.7 audit cycles), evidenced by three substantive cross-repo audit-engagements ([audit#442](https://github.com/EvaLok/schema-org-json-ld-audit/issues/442) cycle 202, [audit#454](https://github.com/EvaLok/schema-org-json-ld-audit/issues/454) cycle 212 absorbed by main cycle 85 at 100% verdict-level, [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) cycle 217 currently being absorbed), the implicit-ask channel verified (#462 the first instance), audit-side commitment-thread discipline observed across ≥4 V2-era instances (cycle 207→211 / 211→212 / 212→214 / 214→215 / 216→217), and audit retrospective revision (cycle 215) naming V2 cross-repo audit-engagement format as a positive pattern. **Criterion 6 does not load-bearingly favor B.** This does NOT change Q7's resolution surface — B's Criterion 6 lead was already "narrow"; cycle 128 makes the narrowness explicitly conditional on operational falsification of A/C's empirical demonstration, which has not occurred. See [`_notes/cycle-128-audit-462-D2-absorption.md`](./_notes/cycle-128-audit-462-D2-absorption.md) for absorption details. D3 D4 + M1-M5 + P3-1 through P3-4 P3-7 P3-8 remain carried for cycle 129+ absorption.

**Cycle 129 [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) D3 absorption** (cycle 117 convergence-at-C framing qualification): the cycle 117 _notes line 176 + 314 + 410 framing "the meta-reflexive iteration is converging at C with progressively-refined framing" is qualified to **"the recursive A-side and flip-rationale stress-test is converging at non-refutation-of-C"** per audit's specific sharpening. The cycles 116-117 recursive stress-test substrate examined cycle 116's classifications of A-side findings about the cycle 114 flip rationale; it did NOT examine C's own framings recursively. The cycle 115 _notes named "cycle 116+ symmetric C-side stress-testing" as cycle 116+ priority; cycles 116-117 stress-tested *the flip rationale* (the orchestrator's argument FOR C) at object-level then meta-level rather than stress-testing *C itself*. The convergence framing imported a symmetry that the substrate did not have. **Cycle 120's shift to Q7-dependent already partially superseded this framing** by replacing "converging at C" as the load-bearing claim with "Eva resolves Q7"; cycle 129 completes the absorption by qualifying the cycle 117 framing explicitly. No symmetric C-side recursive stress-test is executed at cycle 129 — cycle 120's L2 constraint ("NO further recursive annotation of 2-selection.md") + cycle 117's own `productive-content-decreases-with-recursion-depth` pattern (HARDENED@3) preclude. **The combined cycle 120 + cycle 129 framing:** the recursive iteration (cycles 115-117) demonstrated cycle 117 _notes' bounded-recursion claim about stress-tests of A-side framings; it did NOT demonstrate convergence at C as the recommendation; Eva's Q7 resolution is the load-bearing factor. See [`_notes/cycle-129-audit-462-D3-absorption.md`](./_notes/cycle-129-audit-462-D3-absorption.md) for absorption details. D4 + M1-M5 + P3-1 through P3-4 P3-7 P3-8 remain carried for cycle 130+ absorption.

**Cycle 130 [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) D4 absorption** (migration cost methodology calibration): the PR #2877 lens-4 aggregate range revisions (A 3000-4500 → 3600-6200; C 4000-6000 → 5200-8200; B 10000-20000 → 14000-28000) are JUDGMENT-BASED adjustments following per-crate critiques (lenses 1/2/3), NOT empirical counting. The +37%/+37%/+40% upper-bound expansions across A/C/B are within 3 percentage points of each other, structurally consistent with uniform-adjustment heuristic rather than independent per-candidate measurement. PR #2877's calibration-first discipline (cycle 96 methodological lesson) applied verifiably to the top-of-file workspace calibration (38 v1 crates / median 1081 / mean 2118 — verified at cycle 97 to within 0.05%) but did NOT extend to the lens-4 aggregate range revisions, which are well-grounded planning ranges rather than verified empirical bounds (per cycle 97 absorption verdict "INTEGRATED with caveat"). The cycles 122-127 A-shared crate measurement program is what calibration-first applied to A's substrate looks like: 5 of 9 A-shared crates COMPLETED (`v2-tool-registry` cycle 93 / `v2-cycle-history-append` cycle 94 / `v2-phase-transition-check` cycle 122 / `v2-boot-phase` cycle 124 / `v2-wiki-search` cycle 127) with mean ~590 prod LOC per crate, sample standard deviation ~296 LOC, and 9-crate extrapolation ~5310 prod LOC. **A's empirical central estimate (~5310) is WITHIN PR #2877's 3600-6200 judgment range** (upper half, ~79% through) — PR #2877's well-calibrated judgment produced a range that envelopes the empirical reality for A. **C's calibration is partial** — the A-shared portion (~5310 prod LOC) is empirically anchored on the same 5-of-9 measurements; the C-specific delta (`reconcile-mode` + plan-lifecycle pair + plan-lifecycle-check + close-mode) is UNMEASURED. PR #2877's C 5200-8200 treats C ≈ A + ~0-3000 LOC C-specific delta as judgment. **B's calibration is absent** — no V2-era B-shared crates have been built (cycle 126 D1 noted this asymmetry). PR #2877's B 14000-28000 is fully judgment-based with no empirical anchor. The methodology gap is in the EVIDENCE TYPE (judgment vs empirical), not in the OUTCOME accuracy for A (PR #2877's range envelopes the empirical anchor). D4 does NOT change Q7's three options (a/b/c) — it adds an anchor-type column to the cost-of-being-wrong table below, surfacing the empirical-vs-judgment distinction explicitly. **The cost-of-being-wrong dominant asymmetry between A/C and B (bounded weeks vs months at 2.7×-4.5× ratio) holds whether judgment ranges or empirical anchoring is used; the A-vs-C differentiation narrows under calibration because the A-shared portion is empirically common.** See [`_notes/cycle-130-audit-462-D4-absorption.md`](./_notes/cycle-130-audit-462-D4-absorption.md) for absorption details. M1-M5 + P3-1 through P3-4 P3-7 P3-8 remain carried for cycle 131+ absorption.

**Cycle 134 [audit#465](https://github.com/EvaLok/schema-org-json-ld-audit/issues/465) M1 absorption** (audit retrospective A1-A6 reframed from Phase-3-deferred to Phase-2-evidence; FIFTH audit#462+#465 substantive finding absorbed — D1+D5 cycle 126 + D2 cycle 128 + D3 cycle 129 + D4 cycle 130 + M1 cycle 134): cycle 130's "M1-M5 + P3-1 through P3-* are Phase 3 design requirements rather than Q7-direct" framing conflated M1 (evidence-gathering, Phase 2) with M4 + P3-1 + P3-2 + P3-3 + P3-4 (architecture-design, Phase 3). Audit cycle 218 [audit#465](https://github.com/EvaLok/schema-org-json-ld-audit/issues/465) disambiguates: M1 specifically — audit retrospective A1-A6 unconsulted — is Phase 2 evidence the candidate evaluation should incorporate, on the same footing as main's F1-F12 / cycle 96 PR #2878 quantitative-claim corrections / cycle 117 convergence claims / PR #2877 lens-4 ranges. The deferral framing remains correct for M4 (liveness assertion mechanism), P3-1 (liveness assertion), P3-2 (commitment-thread observability), P3-3 (V2 cross-repo audit-engagement format preservation), P3-4 (state.json size discipline / F5 mitigation) — these specify mechanisms the selected candidate's design must include. M1's Phase 2 evidence character is independent. Three A-findings are load-bearing for Phase 2 candidate evaluation: **A3 (filing-rate vs evidentiary-base discipline)** extends D2's audit-as-peer empirical calibration — cycle 128 D2 named "3 substantive cross-repo audit-engagements" as empirical evidence of A/C's audit-as-peer fit under V2; cycle 218's [audit#465](https://github.com/EvaLok/schema-org-json-ld-audit/issues/465) sharpens that V2-era empirical operations themselves provide a corrective discipline V1's audit-outbound treadmill could not provide. **The total count is now 4 V2-era cross-repo audit-engagement instances** ([audit#442](https://github.com/EvaLok/schema-org-json-ld-audit/issues/442) cycle 202 / [audit#454](https://github.com/EvaLok/schema-org-json-ld-audit/issues/454) cycle 212 / [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462) cycle 217 / [audit#465](https://github.com/EvaLok/schema-org-json-ld-audit/issues/465) cycle 218), with cycle 217 absorbed at 100% verdict-level on D1-D5 across cycles 126/128/129/130. **A4 (silent zero-output cycle failures, 7/14 occurrences cycles 203-216, ~50% rate sustained; cycle 218 PASS does not falsify the empirical base over cycles 203-216)** is V2-era operational evidence of a **classifier-class failure mode** the F1-F12 retrospective does not enumerate. Pattern persists under single-orchestrator substrate without self-bounding (cycle 217 PASS did not restore the cycle 215 self-bounded hypothesis after cycle 216 FAIL). Commitment-thread cascade interaction adds a secondary failure mode: cycle N's commitment deferred 1-4 cycles when cycle N+1 silently fails. Topology-dependent predictions (neither empirically supported): A/C single-orchestrator substrate — A4 manifests at ~50% rate; cycles N+1 commitment-thread cascades cost 1-4 cycle latency per silent fail. B multi-agent fan-out — could be amplified (per-context classifier risk × agent count) OR mitigated (one agent's failure doesn't kill the cycle since other agents are still running). A4 is Phase 2 evidence for Criterion 3 (cost-of-being-wrong) + Criterion 4 (failure-mode coverage); the failure-mode coverage discussion should enumerate classifier-class as a family augmenting the v1-derived F1-F12. See `2-candidates/README.md` "Cycle 134 V2-era operational failure-mode evidence" subsection. **A5 (state.json size growth, 250KB hard limit reached cycle 217)** is V2-era operational evidence of state-growth-axis sensitivity at the named-thresholds discipline boundary. Cycle 218's audit-side entry state: state.json at 262KB ≥ 250KB hard limit per Step 13.1; `recommendations.accepted` (190 entries / ~190KB / 73% of state.json) is pending Eva direction per [audit#463](https://github.com/EvaLok/schema-org-json-ld-audit/issues/463). Per-candidate state-growth-axis predictions: A single-orchestrator state surface — F5 well-documented in main's retrospective; C single-orchestrator state surface with shared cluster catalog — F5 similar to A; B multi-agent state surface (per-agent state + coordinator state) — F5 instance count likely multiplies; different mitigation shape required. State-growth-axis is a per-candidate distinguishing dimension that the P1-P6 comparison does not currently surface explicitly; under cycle 134 absorption it is added as a failure-mode evidence dimension (see `2-candidates/README.md` cycle 134 subsection). The cycle 215 named-thresholds discipline (100KB advisory / 200KB mandatory / 250KB hard, against 256KB Read-tool ceiling) is itself a mechanism the selected candidate's design must accommodate (P3-4 cycle 130 carry — Phase 3 design). **A1 + A2 + A6 are also Phase 2 evidence but more cross-cutting**: A1 (constraint accretion in audit's checklist mirrors main's F1) is already absorbed into the F1 framework; A2 (audit constraint-patching v1 mechanism v2 deprecates) is meta-level evidence supporting the redesign's overall thesis without differentiating among A/B/C; A6 (audit accepts main's framing without independent verification) is reconciliation-asymmetry at cross-repo scale — sibling to cycle 130 D4's "main's narrative carried implicitly across many cycles" pattern. **Q7's resolution surface remains unchanged.** M1's substance is that audit's V2-era evidence has not been incorporated into the Phase 2 selection deliverable, not that the evidence flips the Q7 options. A4's classifier-class family and A5's state-growth-axis dimension augment the failure-mode evidence base; the dominant Q7 lever (Criteria 4+5 cumulative findings vs Criteria 1+3 migration-cost weight) is unchanged. The cost-of-being-wrong asymmetry between A/C (bounded weeks) and B (months by 2.7×-4.5×) is reinforced by A5 (B's multi-agent state surface adds state-growth-axis cost) and unchanged by A4 (neither topology has empirical falsification of A4-class failure). See [`_notes/cycle-134-audit-465-M1-absorption.md`](./_notes/cycle-134-audit-465-M1-absorption.md) for absorption details. M2 + M3 + M5 + P3-1 through P3-4 + P3-7 + P3-8 remain carried for cycle 135+ work as Phase 3 design requirements (independent of M1's Phase 2 evidence character).

**Cycle 135 cycle 132 6-of-9 measurement propagation** (cycle 134 forward priority #5 — calibrated-empirical migration cost row update): cycle 132 [`v2-gardening-sweep` COMPLETE](./2-candidates/A-evolved-single-orchestrator.md) advanced the A-shared crate measurement program from 5-of-9 to 6-of-9, materially shifting A's empirical anchor: sum prod LOC 2951 → 4483; mean ~590 → ~747 (+26%); sample standard deviation ~296 → ~485 (+64%, variance widening); 9-crate flat-mean extrapolation **~5310 → ~6726 (+27%)**. **The 6-of-9 empirical extrapolation exceeds PR #2877's 6200 upper bound by +8%** — first time an empirical anchor reading for A has materially exceeded PR #2877's lens-4 judgment range. The cycle 130 D4 absorption paragraph above is frozen as the 5-of-9 reading; cycle 135 propagates the 6-of-9 update to the cost-of-being-wrong table without altering the cycle 130 record. **Selection ordering A > C >> B is preserved across the update**: the dominant cost-of-being-wrong asymmetry between A/C (bounded weeks) and B (months) compresses slightly (A central estimate 5310 → 6726 narrows the A-vs-B ratio at the central estimate from ~3.95× to ~3.12×) but the scale-level asymmetry — bounded weeks vs months — is unchanged. The A-vs-C narrowing under empirical anchoring (cycle 130 framing) is preserved — the A-shared portion (now ~6726) remains empirically common to A and C; only the C-specific delta (~0-3000 LOC judgment) differentiates them. **PR #2877's `judgment-range-can-envelope-empirical-truth` claim (cycle 130 D4 candidate-emergent observation) is qualified at 6-of-9**: PR #2877's range enveloped the 5-of-9 anchor (5310 within 3600-6200, ~79% through) but is exceeded at 6-of-9 (6726 vs 6200, +8% above upper); the envelope property held at 5-of-9 but does not hold at 6-of-9. **The variance widening (std dev +64%) reflects cycle 132's third-arc significant-magnitude overshoot** (gardening-sweep 1532 prod LOC, +39% above cycle 131's ±10% upper bound; cycle 132 candidate-emergent observation `magnitude-prediction-precision-is-shape-dependent-not-flat`); each new shape-family measurement materially shifts both center and dispersion. Cost-of-being-wrong table rows (a) + (b) updated below with 6-of-9 anchor numbers. See [`_notes/cycle-135-cycle-132-measurement-propagation.md`](./_notes/cycle-135-cycle-132-measurement-propagation.md) for the propagation reasoning and [`_notes/cycle-132-gardening-sweep-complete-third-arc-closes.md`](./_notes/cycle-132-gardening-sweep-complete-third-arc-closes.md) for the underlying measurement.

## What this means for Phase 3

- **If Eva approves Q7 option (a) — select C:** Phase 3 begins with C's `reconcile-mode` + plan-lifecycle as the prototype targets; the orchestrator's tentative C recommendation is confirmed.
- **If Eva approves Q7 option (b) — select A:** Phase 3 begins with A's `boot-phase` + `wiki-search` + medium-harness extraction as prototype targets; the cycle 114 flip is reversed honestly per Q7's resolution.
- **If Eva approves Q7 option (c) — different weighting rule:** the orchestrator implements the rule and proceeds accordingly.
- **If Eva extends the iteration window (Q5):** the orchestrator continues per `ITERATION-UNTIL-APPROVAL`, with priority on legibility-restoration over more recursive annotation (the [#2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) L2 critique stands as a constraint on further iteration).

**Cost-of-being-wrong asymmetry across Q7 options** (cycle 126 D5 absorption + cycle 130 D4 anchor-type calibration per [audit#462](https://github.com/EvaLok/schema-org-json-ld-audit/issues/462)):

| Q7 option | Selection | Rollback LOC if Phase 3 refutes the central bet | Anchor type | Recovery time-frame |
|---|---|---|---|---|
| (a) | C | ~5200-8200 (PR #2877 judgment; A-shared portion ~6726 empirical at 6-of-9 per cycle 135 propagation — supersedes 5-of-9 ~5310 anchor; C-specific delta ~0-3000 LOC judgment; calibrated total ~6726-9726) | **mixed** (empirical A-shared + judgment C-specific delta) | bounded weeks |
| (b) | A | ~3600-6200 (PR #2877 judgment); empirical central estimate **~6726** (6-of-9 crates COMPLETED cycles 93/94/122/124/127/132; sample std dev ~485 LOC); **6-of-9 anchor exceeds PR #2877's 6200 upper bound by +8%** (cycle 135 propagation supersedes cycle 130's 5-of-9 ~5310 reading) | **empirically anchored** at 6 of 9 A-shared crates | bounded weeks (lowest empirical center) |
| (c) selecting B | B | ~14000-28000 (PR #2877 judgment; no V2-era B-shared crates built; +37-40% upper-bound expansions uniform across A/C/B suggest uniform-adjustment heuristic) | **judgment-only** | months (2.7×-4.5× higher) |

The asymmetry is dominantly between A/C (bounded weeks, recoverable within Phase 3's normal iteration window) and B (months). Within A/C, the difference is bounded — the A-shared portion (**~6726 prod LOC at 6-of-9 measured**, cycle 135 propagation of cycle 132 measurement) is empirically common to A and C; only the C-specific delta (`reconcile-mode` + plan-lifecycle pair + plan-lifecycle-check + close-mode, UNMEASURED) differentiates them. Selecting B materially raises the recovery cost should Phase 3 measurement refute B's central bet on cluster G role-asymmetric context. The dimension is implicit in Criterion 3 (cost-of-being-wrong) but is structurally pivotal between A/C and B — material to Q7 option (c).

## Other Eva-resolves items (lower priority than Q7)

Q1-Q9 in [`2-selection.md`](./2-selection.md#open-questions-for-eva-at-the-candidate-selection-checkpoint) detail nine resolvable questions. The cycle 120 honest assessment is that **Q7 subsumes most of the others**:
- Q1+Q2 (approve C / override to A) reduce to Q7's resolution.
- Q6+Q9 (family-level vs pattern-level dominance reading) affect Criterion 4+5 ordering under Q7 option (a).
- Q8 (strength-criterion interpretation) reduces to Q7's framing if the orchestrator's strength-classification is treated as self-referential (per [#2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) L2).
- Q3 (pick B) and Q4 (additional candidate) are separate dimensions Eva can name independently.
- Q5 (extend iteration window) is procedural — orchestrator continues iterating until approval lands.

## How to approve

- **At the candidate-selection checkpoint:** Eva resolves Q7 explicitly (option a, b, or c). The orchestrator implements per the resolution.
- **If Eva wants more time / different framing:** comment on [#2912](https://github.com/EvaLok/schema-org-json-ld/issues/2912) (the cycle 120 issue) or open an `[input-from-eva]` issue; the orchestrator continues iterating with the named focus.
- **If Eva wants to override the candidate set entirely:** name the alternative; the orchestrator authors a fourth candidate and re-runs the comparison.

## What deep-detail readers want

- **Cycle-by-cycle iteration log:** [`2-selection.md#iteration-log`](./2-selection.md#iteration-log) (cycles 111-120).
- **The cycle 114 flip rationale + cycle 115-119 qualifications:** [`2-selection.md`](./2-selection.md) sections "Cycle 114 stress-test" through "Cycle 119 6-criterion ordering structure stress-test."
- **The Copilot critique that prompted the cycle 120 recommendation shift:** [PR #2911](https://github.com/EvaLok/schema-org-json-ld/pull/2911) body.
- **The per-candidate documents:** [`2-candidates/A-evolved-single-orchestrator.md`](./2-candidates/A-evolved-single-orchestrator.md), [`B-decomposed-multi-role.md`](./2-candidates/B-decomposed-multi-role.md), [`C-hybrid.md`](./2-candidates/C-hybrid.md).
- **The candidate-comparison framework:** [`2-design-framework.md`](./2-design-framework.md) (P1-P6 criteria, F-pattern mapping, M3 strengths preservation, M2 self-management cost).
- **The retrospective grounding the failure-mode discussion:** [`0-retrospective.md`](./0-retrospective.md).
