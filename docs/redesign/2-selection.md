# Phase 2 candidate selection

**Status:** **DRAFT — cycle 111 (2026-05-10) initial authoring; cycle 112 (2026-05-10) central-bet stress-test on dominance-framing absorbed; cycle 113 (2026-05-10) central-bet stress-test on equivalent-outcomes claim absorbed; cycle 114 (2026-05-10) third central-bet stress-test on conditional-improvement-risk grounding triggered the four-finding flip threshold named cycle 113 — TENTATIVE RECOMMENDATION FLIPPED FROM A TO C; cycle 115 (2026-05-10) meta-reflexive stress-test of the cycle 114 flip rationale surfaces three motivated-reasoning surfaces (asymmetric threshold; asserted weighting; one-sided accumulation) — recommendation maintained at C with cycle 115 qualifications; symmetric flip-back threshold named for cycle 116+ object-level C-side stress-testing; cycle 116 (2026-05-11) object-level C-side stress-test of the cycle 114 flip rationale's specific claims surfaces 4 substrate-content-distinct framing-level concerns (structural-vs-empirical "evidence" framing; threshold count includes 1 derivative; Criterion 5 shift conflates articulation-failure with bet-defensibility-failure; cycle 107 closure citation conflates structural-direction-by-construction with evidence-accumulation) — findings qualify but do not substantively undermine the flip rationale per the cycle 115 symmetric threshold's strength criterion; recommendation maintained at C with cycle 116 qualifications absorbed; cycle 117 (2026-05-11) recursive stress-test of cycle 116's classifications surfaces 4 substrate-content-distinct findings + 1 meta-finding scope-narrowing/clarifying cycle 116 findings (Finding A's structural-vs-empirical scope narrowed; Finding B applies to chain narrative count not threshold count; Finding C contingent on Q6 family-level reading; Finding D's precision-vs-validation dichotomy over-precise; strength-criterion application contingent on Q6 resolution) — none substantively undermines; the `recursive-motivated-reasoning-surface` pattern HARDENS-at-3 cycle 117 with the strength-discipline gradient + productive-content-decrease both operating as natural stopping criteria across 3 consecutive cycles; recommendation maintained at C with cycle 117 qualifications absorbed; awaiting candidate-selection checkpoint approval per `ITERATION-UNTIL-APPROVAL`.**

**Authoring cycle:** 111 (2026-05-10) initial; 112 (2026-05-10) central-bet stress-test surfacing family-level vs pattern-level dominance tension; 113 (2026-05-10) central-bet stress-test surfacing detection-vs-processing distinction in the "equivalent outcomes" claim and naming the four-finding flip threshold; 114 (2026-05-10) third central-bet stress-test surfacing the cycle 111 conditional-improvement framing as omitting C's own cycle 92 + cycle 96 + cycle 107 direction-validation evidence — fourth load-bearing under-weighted dimension surfaces; flip threshold honored; tentative recommendation flipped from A to C; 115 (2026-05-10) meta-reflexive stress-test of the cycle 114 flip rationale surfaces three motivated-reasoning surfaces in the flip rationale itself (Finding 1: asymmetric threshold without named symmetric counterpart; Finding 2: Criteria 4+5 cumulative > Criteria 1+3 migration-cost weighting asserted not derived from the redesign prompt's primary thesis; Finding 3: C's "direction-validation accumulation" is predominantly orchestrator-internal work, not external validation) — recommendation maintained at C with qualifications; symmetric flip-back threshold introduced for cycle 116+ object-level C-side stress-testing; weighting choice surfaced explicitly as Eva-resolves (new Q7); 116 (2026-05-11) object-level C-side stress-test of the cycle 114 flip rationale's specific claims (5-point flip rationale + cycle 92 enumeration text fidelity + Criterion 5 ordering shift + cycle 107 Risk 2 closure load-bearingness for flip-justification) surfaces 4 substrate-content-distinct findings — adversarial review qualifies each finding; none solidly "substantively undermining" per the cycle 115 symmetric threshold's strength criterion; recommendation maintained at C with cycle 116 qualifications absorbed into the flip rationale section; 117 (2026-05-11) recursive stress-test of cycle 116's classifications (Findings A-D + strength-criterion application action) surfaces 4 substrate-content-distinct findings + 1 meta-finding scope-narrowing/clarifying cycle 116 findings — Finding 1: Finding A's structural-vs-empirical distinction over-reads (each of cycles 92/96/107 added new analytical material; accumulation is analytically-progressive); Finding 2: Finding B's count-discipline applies to chain narrative count not threshold count (round-based vs finding-based count distinction); Finding 3: Finding C contingent on Q6 family-level reading prevailing (under pattern-level reading, A's bet is itself mis-grounded); Finding 4: Finding D's precision-vs-validation dichotomy over-precise (cycle 107 contains BOTH structural-direction-reaffirmation AND measurement-specification); Meta-Finding: strength-criterion application contingent on Q6 resolution — none substantively undermines cycle 116 / flip rationale; recommendation maintains C tentatively with cycle 117 qualifications absorbed; the `recursive-motivated-reasoning-surface` pattern HARDENS-at-3 across cycles 115-117; NEW candidate-pattern `productive-content-decreases-with-recursion-depth` NOVEL@1 cycle 117 as second natural-exhaustion criterion alongside the strength-discipline gradient.
**Authored against:** [`2-candidates/README.md`](./2-candidates/README.md) post-cycle-110 state; the three candidate documents [`A-evolved-single-orchestrator.md`](./2-candidates/A-evolved-single-orchestrator.md), [`B-decomposed-multi-role.md`](./2-candidates/B-decomposed-multi-role.md), [`C-hybrid.md`](./2-candidates/C-hybrid.md); the [`2-design-framework.md`](./2-design-framework.md) v1.21+ axes / convergent constraints / failure-mode mapping / P1-P6 evaluation criteria; the cycle 96 PR #2878 sharpening-claims absorption; the cycle 97 PR #2877 tool-surface-feasibility absorption; the cycles 103/105/106/107/108/109/110 risk closures at specification level.

This document is the Phase 2 **selection** artifact named in the redesign prompt's SECTION 8 ("Output: `docs/redesign/2-candidates.md` and `docs/redesign/2-selection.md`"). Per the redesign prompt's CHECKPOINTS, candidate-selection is the second of three hard checkpoints requiring Eva's explicit approval. Per `ITERATION-UNTIL-APPROVAL`, this draft is not the deliverable until Eva approves; iteration continues until then.

## Tentative selection: Candidate C (Hybrid) — flipped from A cycle 114 per the four-finding flip threshold

**Tentative recommendation: select [`C — Hybrid`](./2-candidates/C-hybrid.md) for Phase 3 prototype work.**

This recommendation is **provisional**. It rests on the cumulative analysis across cycles 111-114 (see "Why the recommendation flipped to C (post-cycle-114)" section below). It can be overridden by:

- Eva's direct judgment at the checkpoint (Eva is authoritative; this draft is input). The most plausible override paths are (a) flip back to A if migration-cost / Criteria 1+3 weight is judged to outrank Criteria 4+5 cumulative findings; (b) flip to B if the schema-work proof-domain is judged to need multi-role decomposition for parallelism; (c) direct an additional candidate not yet authored.
- New evidence from audit critique not yet landed (audit cycle 215 named cycle 216 as the substantive Phase 2 critique cycle; landing in ~next audit cron, ~8h from cycle 114). If audit critique shifts the analysis materially, this draft updates.
- Copilot adversarial-feedback dispatch landings (the cycle 112 dispatch body file remains ready for re-attempt once question-for-eva [#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903) resolves the runtime allowlist + PAT scope friction).
- Phase 3 prototype evidence on a smallest-load-bearing crate (cycles 93-94 measured `v2-tool-registry` 231 LOC + `v2-cycle-history-append` 268 LOC; further measurements — especially of C's `reconcile-mode` if Eva pre-approves — may sharpen the migration-cost vs structural-improvement trade-off).

The recommendation is **not** that Candidate C is "best in all dimensions." It is that, given the cumulative findings across cycles 111-114 — the cycle 111 selection-rationale rested on a chain of A-favoring framings, each of which was found mis-grounded under successive rounds of stress-testing — and given the cycle 113 four-finding flip threshold being honored cycle 114 (a falsifiable pre-committed criterion that interrupts `progressive-rationale-weakening-without-flip`), Candidate C is the orchestrator's tentative recommendation. **What C gives up vs A** (Criteria 1 schema-work-enabling and Criterion 3 cost-of-being-wrong both still favor A on migration-cost dimensions) is preserved as legitimate Eva-override territory; the orchestrator's analysis names the trade-off and does not pre-empt Eva's decision.

**Concise pre-flip vs post-flip summary:**

| Aspect | Cycles 111-113 (recommendation: A) | Cycle 114 (recommendation: C) |
|---|---|---|
| Criterion 1 (schema-work-enabling) ordering | A > C > B | A > C > B (unchanged; A still favored on migration-cost dimension) |
| Criterion 2 (CORE-DESIGN-PRINCIPLE) ordering | A ≈ C > B | A ≈ C > B (unchanged) |
| Criterion 3 (cost-of-being-wrong) ordering | A > C > B | A > C > B (unchanged; A still favored on rollback-surface dimension) |
| Criterion 4 (failure-mode-addressing) ordering | C > B > A under family-level reading; B > C > A under pattern-level reading | C > B > A (family-level reading now load-bearing per cycle 112 + 114 cumulative; pattern-level reading itself shown to depend on mis-grounded F1+F7 framing) |
| Criterion 5 (central-bet defensibility) ordering | A ≈ C > B (cycle 113) | C > A > B (cycle 114; A's central-bet rationale weakened across 4 cycles; C's central bet validated by cycle 92 + cycle 96 absorption + cycle 107 closure) |
| Criterion 6 (audit-as-peer P6) ordering | B > C > A (narrow margin) | B > C > A (unchanged; narrow margin) |
| Net cumulative weight | Criteria 1+2+3 favor A; Criteria 4+5+6 contested or favor C/B | Criteria 1+2+3 favor A on bounded-migration dimensions; Criteria 4+5 favor C on cumulative-finding dimensions; Criterion 6 favors B narrowly. **The shift in Criterion 5 from A ≈ C to C > A is the load-bearing change** — A's central bet has been progressively shown mis-grounded across 4 cycles of stress-testing, while C's central bet has accumulated direction-validating evidence. |

## Cycle 114 stress-test: conditional-improvement framing weakened, four-finding flip threshold honored

This section was added in cycle 114 (2026-05-10) as the substantive output of the third A-vs-C central-bet stress-test in the cycle 111-113-named pattern. It documents the cycle 114 finding, the application of the cycle 113 four-finding flip threshold, and the reasoning for honoring the threshold (rather than reasoning around it).

### The cycle 114 stress-test target

Cycle 113 named four candidates for cycle 114+ stress-tests: (1) B's cluster G aspirational extension grounding; (2) C's conditional-improvement risk grounding; (3) B's 3-5× migration cost methodology; (4) the 6-criterion ordering structure assumptions. Cycle 114 selected target #2 — **C's conditional-improvement risk grounding** — as the most directly A-vs-C-relevant of the four, and the one whose finding would either trigger the four-finding flip threshold or stabilize the recommendation at A.

The cycle 111 "Why not C" Reason #1 framing reads:

> *"Conditional-improvement risk. C's own document names this risk: 'if the additions don't carry their weight, C is just A with more migration surface.' The 3 additional sub-shapes (plan-lifecycle + reconcile-mode + per-mode budgets) are at most 25-35% per-cycle decision overhead reduction vs A's 30-40% — C pays additional cost for additional structural depth in F4/F11."*

The stress-test question: is this framing grounded in C's document text, or is it a cycle 111 selective citation?

### The finding

The cycle 111 framing cites C-hybrid.md line 73 (C's self-acknowledged risk language) and line 116 (P3 NOTE flagging the "central uncertainty") — both genuine references. **But the framing omits C-hybrid.md lines 222-228 (cycle 92 Aggregate central-bet validation) and lines 349-372 (Risk 2 status post-cycle-107 closure), which directly address the same conditional and resolve the direction component:**

- **C-hybrid.md lines 222-228 (cycle 92):** *"C's central bet — that reconcile-mode + plans-as-artifacts carry their weight — is **structurally validated by per-failure-mode enumeration**: F2: ~4pp reliability improvement + higher legibility (real but small). F4: categorical improvement on plan-lifecycle legibility + ~3-4 cycle reduction in stale-detection lag (substantial). F11: ~5-13pp detection-rate improvement + categorical legibility improvement (substantial). **Direction:** structural advantages over A are real (validated by enumeration). The 3 additional sub-shapes adopted by C beyond A... **carry their weight on F4 and F11**; F2 improvement is more marginal."*

- **C-hybrid.md lines 349-372 (cycle 107):** Risk 2 status post-cycle-107 closes the discipline-conditional risk at specification level — *"Direction continues to hold by construction... Stale-active-plan promotion lag ≤3 cycles steady-state... Mitigation specification is now concrete enough for Phase 3 prototype validation."* The closure separates direction (validated) from magnitude (Phase 3 measurement target) with falsifiable thresholds.

- **Cycle 96 PR #2878 absorption (C-hybrid.md line 191):** the F2 reliability claim was qualified as *"ambiguous-without-prototype"* — direction holds, magnitude unmeasured. **F2 was qualified; F4 and F11 were not.** F4 + F11 direction-validation survived adversarial review.

The cycle 111 conditional-improvement framing reads as if the **full** conditional ("do the additions carry their weight at all") is open. **In fact the direction component is resolved** by cycle 92 enumeration, confirmed by cycle 96 PR #2878 absorption (F4/F11 not qualified), and bounded for Phase 3 by cycle 107's closure with falsifiable thresholds. **The remaining open conditional is magnitude (Phase 3 prototype measurement) — a smaller conditional than the cycle 111 framing suggests.**

The cycle 111 evidence ("at most 25-35% per-cycle decision overhead reduction vs A's 30-40%") substitutes a **P3 metric** (decision overhead) for the **F4/F11 detection legibility evidence** the conditional was about. P3 is one criterion among several; the conditional-improvement risk is specifically about F4/F11 carrying their weight, not P3 decision-overhead percentages.

### The chain of motivated reasoning across cycles 111-114

The cycle 111 selection draft's case for A rests on a chain of framings, each of which has now been found mis-grounded:

1. **Cycle 111 Criterion 5 framing of A's central bet** as *"directly supported by the retrospective's identification of F1+F7 as the dominant failure pattern"* — **cycle 112 found this mis-grounded** against the retrospective's explicit "reconciliation asymmetry as the dominant family" claim (lines 161-162 + 959). The retrospective does not name F1+F7 as the dominant pattern at any reading; the cycle 111 framing crosses two family boundaries (defense-accretion + procedure-overhead) to construct a cross-family pattern the retrospective does not state.

2. **Cycle 112 reframing of A-vs-C as "legibility only"** — **cycle 113 found this under-weighted** on three processing dimensions: per-mode runtime budget allocation (C explicit ≤10min reconcile-mode budget vs A shared boot-phase budget), per-channel structured handlers (C typed-deltas vs A procedurally-loose), typed-delta output discipline (C single auditable stream vs A scattered). Cycle 113 named these as detection-vs-processing distinction.

3. **Cycle 111 "Why not C" Reason #1 (conditional-improvement risk)** — **cycle 114 finds this omits C's own cycle 92 + cycle 96 + cycle 107 evidence** that resolves the direction component of the conditional. The cycle 111 framing presents the conditional as fully open when direction is in fact validated; the open conditional is magnitude (Phase 3 work).

4. **Cycle 111 "Why not C" Reason #3 ("F4/F11 addressing improvements over A are unproven as load-bearing")** — depends on F1+F7 dominance (cycle 112 finding refutes); collapses under the family-level reading (F4 + F11 are members of the reconciliation asymmetry family the retrospective names as dominant).

**The four findings are structurally distinct and substrate-content-distinct:** cycle 112 found mis-grounding against retrospective text; cycle 113 found under-weighting in cycle 112's reframing; cycle 114 found omission of C's own validation evidence; the cycle 113 + 114 findings together also dismantle Reason #3's "unproven as load-bearing" framing. Each round of stress-testing surfaces a different category of cycle 111 framing weakness while the recommendation in pre-cycle-114 drafts continued to point at A.

### The four-finding flip threshold being honored

Cycle 113's selection-draft "Honest cumulative observation" named the threshold:

> **"If a fourth round of stress-testing (cycle 114+) surfaces a fourth load-bearing under-weighted dimension favoring C over A, the recommendation should be honestly flipped to C."**

Cycle 114 surfaces such a dimension (the conditional-improvement framing's omission of C's direction-validation evidence; structurally connected to and amplified by the family-level reading from cycle 112). **The threshold is honored: the tentative recommendation flips from A to C.**

The flip is the orchestrator's tentative recommendation update; **it does not pre-empt Eva's checkpoint approval**. Eva can override (back to A on migration-cost / Criteria 1+3 weighting; to B on multi-role-decomposition; to a not-yet-authored candidate). The flip rests on Criteria 4 + 5 cumulative cycle 111-114 findings outweighing Criteria 1 + 2 + 3 + 6 in the orchestrator's tentative analysis; Eva resolves whether Criteria 4 + 5 cumulative weight does in fact outrank the Criteria 1 + 3 cumulative weight.

### Why the flip is the honest move (not reasoning-around the threshold)

The cycle 113 _notes named the failure mode explicitly:

> *"`pre-committed-flip-threshold` candidate-pattern... applicability test is whether cycle 114+ honors the threshold or reasons-around it; if reasoned-around, the threshold reveals as protective fiction; if honored, it is a load-bearing discipline."*

Reasoning-around the threshold would look like: "the cycle 114 finding is connected to cycle 112's finding so it doesn't count as a fourth dimension," or "the conditional-improvement framing was acknowledging a real risk so the omission isn't material," or "the cycle 92 validation is internal to C so it doesn't count as evidence." Each of these is plausibly arguable. None of them changes that:

- The threshold criterion was named in advance specifically to interrupt motivated-reasoning patterns.
- The cycle 114 finding meets the criterion (substrate-content-distinct from cycles 112 + 113; favoring C; load-bearing for the cycle 111 case for A).
- If the threshold is set aside whenever a finding is "merely connected" to prior findings or "merely about citation patterns," the threshold provides no actual interrupt against motivated reasoning.

**Honoring the threshold is the load-bearing structural action.** The flip is the orchestrator demonstrating that pre-committed falsifiable criteria are real disciplines, not protective fictions.

### What the flip preserves and what it changes

**Preserved:** the cumulative reasoning chain across cycles 111-114 stays in the document (the "Why not C" section retains the cycle 111 reasoning as historical record; the cycle 112 + 113 + 114 updates document the progressive findings). Eva sees the full reasoning trajectory and the cycle 114 flip rationale; the flip does not bury the prior recommendation. The "Why not B" section is unchanged (cycle 114 produced no B-side findings).

**Changed:** the tentative selection (A → C); the Criterion 5 ordering (A ≈ C > B → C > A > B); the side-by-side comparison "Recommendation" framing (orchestrator's tentative recommendation now points at C); the "Why the recommendation stays as A" section is renamed "Why the recommendation flipped to C (post-cycle-114)" with substantively rewritten body; outstanding work item #3 absorbs cycle 114; open questions #1 + #2 invert (Q1 was "Approve A?" now "Approve C?"; Q2 was "Override to C?" now "Override to A?").

### Cycle 114 outcome for the candidate-pattern tracking

- **`pre-committed-flip-threshold`** candidate-pattern (NOVEL@1 cycle 113) → **promoted to TESTED@1-by-being-honored** (cycle 114 demonstrates the threshold is a load-bearing discipline; the test was naming the criterion in advance and then honoring it when triggered, not reasoning around it).
- **`progressive-rationale-weakening-without-flip`** candidate-pattern (NOVEL@1 cycle 113) → **interrupted by the flip threshold being honored** (cycle 114 breaks the pattern at NOVEL@1 + interrupted; the pattern would have HARDENED at NOVEL@2 if cycle 114 had reasoned around the threshold).
- **`central-bet-stress-test-finds-mis-grounding`** candidate-pattern (TESTED@2 cycle 113) → **promoted to HARDENED-at-3** (cycle 112 retrospective mis-grounding + cycle 113 under-weighted dimensions + cycle 114 omitted-validation-evidence; three substrate-content-distinct findings of the same shape).
- **`discretionary-departure-from-forward-going-commitment`** candidate-pattern (HARDENED-at-3 cycle 113) → **HARDENED-at-4** (cycle 110 named cycle 111+ rotation; cycles 111, 112, 113, 114 all departed; cycle 114's substrate is the threshold-honoring action, structurally distinct from per-candidate sharpening).

## Cycle 115 meta-reflexive stress-test of the cycle 114 flip rationale

This section was added in cycle 115 (2026-05-10) as the substantive output of the meta-reflexive stress-test that cycle 114 _notes named as the cycle 115 top priority. The discipline applied: examine the cycle 114 flip rationale with the same adversarial framing that cycles 112-114 applied to the cycle 111 case for A. A flip whose own rationale is motivated reasoning would be the worst of both worlds — the orchestrator commits to C on a basis as fragile as the basis it abandoned for A.

### The stress-test target

The cycle 114 "Why the recommendation flipped to C (post-cycle-114)" section has 5 points: (1) the four-finding flip threshold was a pre-committed falsifiable criterion (honored); (2) the cumulative findings across cycles 111-114 dismantle the cycle 111 case for A; (3) C's central-bet rationale has accumulated direction-validating evidence across 4 cycles while A's rationale has progressively failed adversarial review; (4) what still favors A: Criteria 1 (schema-work-enabling) and 3 (cost-of-being-wrong) on migration-cost dimensions; (5) honoring the threshold is itself a load-bearing structural action.

Three lenses applied: **(L1)** is the four-finding flip threshold itself protective fiction — does it have a named failure mode post-flip? **(L2)** does the flip rationale under-weight Criteria 1+3 the way cycle 111 under-weighted F4/F11 validation evidence? **(L3)** is "honoring the threshold" being used as a shield against re-examining whether C is actually structurally better?

### Finding 1 (Lens 1): the four-finding flip threshold is asymmetric

Cycle 113 named the threshold as: *"if a fourth round of stress-testing surfaces a fourth load-bearing under-weighted dimension favoring C over A, the recommendation should be honestly flipped to C."* The threshold specifies a criterion for A→C but does NOT specify the symmetric criterion for C→A (or for the post-flip state where the recommendation stays at C despite findings).

Without a named symmetric counterpart, the threshold operates as a one-way ratchet: it triggered the flip A→C cycle 114 but provides no specification for when the flip should be reversed, when C should be re-examined under similar discipline, or what would count as evidence that C is wrong post-flip. The threshold's pre-flip load-bearing function was interrupting motivated reasoning toward A (the prior recommendation); the equivalent post-flip function would be interrupting motivated reasoning toward C (the new tentative recommendation). The post-flip equivalent is not specified.

**This is a real protective-fiction risk for the post-flip state.** A reader who sees the threshold honored cycle 114 might infer the discipline is settled; in fact the discipline as written applies asymmetrically. The threshold needs a symmetric counterpart to function as a genuine discipline rather than as a one-way ratchet.

### Finding 2 (Lens 2): Criteria 4+5 cumulative weight > Criteria 1+3 migration-cost weight is asserted, not derived

The cycle 114 flip rationale point 4 acknowledges Criteria 1+3 still favor A on migration cost (~3600-6200 LOC vs ~5200-8200 LOC PR #2877 revised) but states: *"the flip rests on the orchestrator's tentative analysis that Criterion 5 (central-bet defensibility) cumulative shift to C > A, combined with Criterion 4 (failure-mode addressing depth) cumulative shift to C > B > A under the family-level reading, outweighs Criteria 1 + 3 on the migration-cost dimension."*

**This weighting is asserted, not derived from the redesign prompt's primary thesis.** The redesign prompt SECTION 1 names the primary thesis as: *"the schema-org-json-ld work is the proof domain: useful output that demonstrates the system is actually doing real work, not just theorizing about itself."* Criterion 1 (schema-work-enabling) IS the primary thesis. The prompt does not specify Criteria 4+5 cumulative findings as outranking the primary thesis Criterion 1.

The weighting is a value choice. Like cycle 111's framing weighted F1+F7 dominance over the family-level reading (a value choice cycle 112 found mis-grounded against retrospective text), the cycle 114 weighting privileges Criteria 4+5 cumulative findings over the primary thesis Criterion 1. The two are structurally analogous: cycle 111 picked a reading of dominance that supported A; cycle 114 picked a weighting that supports C. Both pick an interpretation without naming the alternative as a competing reading explicitly Eva-resolves.

**Q1+Q2 partially surface this** (the "override to A on Criteria 1+3 weighting" framing in Q2). But Q1+Q2 ask about the recommendation outcome, not about the weighting. A reader could approve Q1 (select C) thinking they're approving the cumulative findings, without seeing they're also approving the meta-choice that Criteria 4+5 outrank Criteria 1+3 on the primary thesis. The weighting deserves its own surface because it's the load-bearing meta-choice between candidates of comparable merit on different dimensions.

### Finding 3 (Lens 3): C's "direction-validation accumulation" is one-sided

The cycle 114 flip rationale point 3 cites cycle 92 (Aggregate central-bet validation: "carry their weight on F4 and F11"), cycle 96 (PR #2878 absorption: F4 + F11 not qualified as ambiguous-without-prototype), and cycle 107 (Risk 2 closure at specification level with falsifiable thresholds) as direction-validating evidence accumulated for C across 4 cycles.

**All three cited sources are predominantly the orchestrator's own iterative work.** Cycle 92 is the orchestrator's per-failure-mode enumeration of C — internal authoring. Cycle 96 PR #2878 was a Copilot absorption working from the orchestrator's framing (the Copilot lens was on sharpening claims, not on adversarial critique of C's overall thesis). Cycle 107 is the orchestrator's own risk closure at specification level — internal authoring. The accumulation of "direction-validating evidence" is the accumulation of the orchestrator's own work on C, not the accumulation of external validation.

**External validation for C does not yet exist:** audit cycle 215 closed without substantive Phase 2 critique landing. The Copilot adversarial-feedback dispatch on the selection was BLOCKED by runtime allowlist gap ([#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903) open). No external review has stress-tested C's selection-draft framings the way cycles 111-114 stress-tested A's.

The "evidence accumulation" cited for C is asymmetric not because C is structurally better than A, but because cycles 111-114 stress-tested A's framings IN THE SELECTION DRAFT while equivalent stress-testing of C's framings has not occurred. Cycles 92 / 96 / 107 were per-candidate work BEFORE the selection-draft framing existed; the cycles 111-114 stress-testing was specifically on A's selection-draft framings. Cycle 116+ symmetric stress-testing of C's selection-draft framings is load-bearing for confirming whether the cumulative-evidence asymmetry reflects C's structural merit or reflects only the asymmetric stress-testing focus.

### Adversarial review of the cycle 115 findings (recursive meta-reflexive)

Per `ITERATION-UNTIL-APPROVAL`'s "examine for self-congratulation" discipline applied recursively. The cycle 115 findings could themselves be motivated reasoning to preserve perfect cycle symmetry (the pattern of every cycle's stress-test finding faults in the prior cycle's framing). Three counter-arguments tested:

**Counter-argument 1 (against Finding 1):** *"The threshold's pre-flip asymmetry is appropriate because the prior had already been tested for motivated reasoning toward A through cycles 78-110 of preceding analysis. The threshold was named to interrupt the specific pattern of keeping A despite findings; the symmetric criterion makes sense AFTER C has been similarly tested for cycles, not preemptively."*

Response: This is fair. The cycle 115 finding does not argue the threshold should have been symmetric pre-flip; it argues the threshold needs a named symmetric counterpart NOW to guide cycle 116+ work. Without that, the threshold operates asymmetrically going forward — and the asymmetry would shield the post-flip C recommendation from the same discipline that flipped A. **Finding 1 stands.**

**Counter-argument 2 (against Finding 2):** *"Q1+Q2 already surface the weighting choice to Eva implicitly. Adding explicit weighting-resolves-Eva framing would be bureaucratic transparency without changing analysis."*

Response: Q1+Q2 ask about the recommendation outcome (select C? override to A?), not about the weighting itself. A reader could approve Q1 thinking they're approving the cumulative findings, without seeing they're also approving the meta-choice that Criteria 4+5 cumulative findings outrank Criteria 1+3 migration-cost weight on the primary thesis. The weighting is the load-bearing meta-choice; surfacing it explicitly to Eva makes the value resolution visible rather than embedded. **Finding 2 stands.**

**Counter-argument 3 (against Finding 3):** *"Per-candidate work across cycles 103-110 was balanced (A:2 / B:3 / C:2 closure distribution per cycle 110). The cycle 111-114 stress-tests were on the SELECTION DRAFT'S A-framings, not on the A candidate document itself. The 'accumulation' applies to selection-draft framing stress-testing, not overall per-candidate work."*

Response: This is correct and material. The cycle 115 finding stands at the per-selection-draft framing level — cycles 111-114 stress-tested A's framings IN THE SELECTION DRAFT (Criterion 5 paragraph A; Criterion 4 dominance reading; "Why not C" Reason #1; "Why not C" Reason #3). Equivalent stress-testing of C's selection-draft framings (e.g., the new "Why the recommendation flipped to C" section, the cycle 92 enumeration cited as direction-validating in the flip rationale, the Criterion 5 ordering shift to C > A > B) has not occurred. Cycle 116+ should stress-test C's selection-draft framings symmetrically. **Finding 3 stands at the per-selection-draft level (with the qualification that it does not apply to overall per-candidate work).**

**The three findings survive adversarial review.** They are real, substrate-content-distinct from each other (Finding 1 is structural-about-the-threshold; Finding 2 is about cumulative-criteria-balance; Finding 3 is about evidence-accumulation-asymmetry), and substrate-content-distinct from the cycle 111-114 findings (which were about A's selection-draft framings; cycle 115's findings are about the flip rationale's structure).

### What the findings imply for the recommendation

The three findings do NOT establish that the cycle 114 flip is wrong. They establish that:
- The flip rationale has its own motivated-reasoning surfaces (structurally similar to but distinct from the surfaces cycles 112-114 found in cycle 111's framings).
- The flip is provisional, not load-bearing without further work.
- More iteration is needed before the candidate-selection checkpoint can be considered ready.

**The recommendation maintains C tentatively with cycle 115 qualifications.** The findings do NOT trigger a flip-back. They DO argue for three changes to the draft:

1. **Name a symmetric flip-back threshold** — a falsifiable criterion for when C → A should occur, structurally equivalent to the cycle 113 four-finding flip threshold. The symmetric threshold needs to specify what would count as a finding favoring A over C (or substantively undermining the flip rationale) and at what count the flip-back would trigger.

2. **Surface the weighting choice explicitly to Eva** — add a new question (Q7) that names the orchestrator's weighting choice (Criteria 4+5 cumulative findings > Criteria 1+3 migration-cost weight) as explicitly Eva-resolves, alongside the Q1 (Approve C?) / Q2 (Override to A?) outcome questions.

3. **Name cycle 116+ priority: symmetric C-side stress-testing** — apply the cycle 111-114 stress-test methodology to C's selection-draft framings. Targets include the "Why the recommendation flipped to C" 5-point rationale (each point stress-tested individually); the cycle 92 enumeration cited as direction-validating (does the enumeration text actually say what the flip rationale cites it as saying?); the Criterion 5 ordering shift to C > A > B (does C's central bet survive the same stress-test discipline applied to A's central bet?); the cycle 107 Risk 2 closure at specification level (do the falsifiable thresholds genuinely close the risk, or are they specification-level fiction?).

### The symmetric flip-back threshold (cycle 115)

**Threshold:** if cycle 116+ object-level stress-testing of C's selection-draft framings surfaces 4 substrate-content-distinct findings favoring A over C OR substantively undermining the cycle 114 flip rationale, the recommendation should be honestly flipped back to A.

**What counts as a finding:** substrate-content-distinct (different source material; different specific claim) findings on object-level claims about C's structural merit OR on the cycle 114 flip rationale's load-bearing claims. The cycle 115 meta-reflexive findings (about the flip rationale's structure, not about C's structural merit) are not themselves the count — they motivated naming this threshold, but the threshold counts cycle 116+ findings.

**Why the threshold is symmetric to cycle 113's:** the cycle 113 four-finding threshold for A→C used the criterion "fourth load-bearing under-weighted dimension favoring C over A." The symmetric threshold for C→A uses "fourth substrate-content-distinct finding favoring A over C OR substantively undermining the flip rationale." The four-finding count is preserved; the "favoring" direction is inverted; "substantively undermining the flip rationale" is added because the flip rationale is the load-bearing claim post-flip.

**Why this threshold is load-bearing:** without it, the cycle 113 four-finding threshold operates as a one-way ratchet — it triggers A→C but provides no specification for C→A. The cycle 115 finding (Finding 1) is that this asymmetry is itself a protective-fiction risk. Naming the symmetric threshold makes the cycle 113 discipline a genuine discipline rather than a ratchet.

**Failure mode of the symmetric threshold (named in advance):** if cycle 116+ object-level stress-testing finds nothing or finds only equivocal evidence, the threshold provides no flip-back — but it also reveals as not-yet-tested. The threshold's load-bearing function is interrupting motivated reasoning toward C; if cycle 116+ stress-testing reveals no motivated reasoning toward C, the threshold confirms C as load-bearing. If cycle 116+ reasoned-around the threshold the way cycle 114 might have reasoned around the cycle 113 threshold, the threshold reveals as protective fiction for C the way the cycle 113 threshold revealed as a load-bearing discipline for A.

### What the cycle 115 findings preserve vs change

**Preserved:**
- Tentative recommendation: C.
- The cumulative findings across cycles 111-114 (the dismantling of A's case at the selection-draft framing level).
- The 6-criterion ordering as cycle 114 updated.
- The cycle 113 four-finding flip threshold being honored cycle 114 as a real discipline.

**Changed:**
- The flip rationale acknowledges its motivated-reasoning surfaces (asymmetric threshold; asserted weighting; one-sided accumulation) — qualifying the flip as provisional rather than load-bearing without further work.
- A symmetric flip-back threshold is named for cycle 116+ object-level C-side stress-testing.
- The weighting choice (Criteria 4+5 cumulative > Criteria 1+3 migration-cost) is surfaced explicitly as Eva-resolves via new Q7.
- Outstanding work item #3 absorbs the cycle 115 findings.

### Cycle 115 outcome for the candidate-pattern tracking

- **`pre-committed-flip-threshold`** candidate-pattern (TESTED@1-by-being-honored cycle 114) → **refined with cycle 115 qualification:** the threshold operates as a one-way ratchet without a named symmetric counterpart. Pattern remains TESTED@1 but with cycle 115 qualification that the discipline's robustness requires symmetric specification. Cycle 115's introduction of the symmetric flip-back threshold extends the pattern to `pre-committed-flip-threshold-WITH-SYMMETRIC-COUNTERPART`.
- **NEW candidate-pattern named cycle 115:** `meta-reflexive-stress-test-finds-asymmetry` **NOVEL@1** (cycle 115 first instance: applying the same stress-test methodology to the flip rationale that the flip rationale applied to the prior recommendation surfaces three motivated-reasoning surfaces in the flip rationale itself — asymmetric threshold, asserted weighting, one-sided accumulation). Cycle 116+ test of the pattern: stress-testing C's selection-draft framings reveals comparable motivated-reasoning surfaces in C's framings (TESTED@2); or applies to a future flip in another phase (TESTED@2 substrate-distinct).
- **NEW candidate-pattern named cycle 115 (tentative):** `recursive-motivated-reasoning-surface` **NOVEL@1** (the meta-observation that each successive cycle's framing has motivated-reasoning surfaces revealed by the next cycle's stress-test — cycle 112 found cycle 111's framings; cycle 113 found cycle 112's reframings; cycle 114 found cycle 111's "Why not C" Reason #1; cycle 115 found cycle 114's flip rationale). The pattern's expected behavior cycle 116+ is that stress-tests of the cycle 115 framing would themselves find motivated reasoning — if so, TESTED@2. The pattern is potentially recursive without bound, which is itself a meta-observation worth tracking.
- **`central-bet-stress-test-finds-mis-grounding`** candidate-pattern (HARDENED-at-3 cycle 114) → cycle 115's substrate is the FLIP RATIONALE, not a candidate's central bet. Structurally distinct enough that promotion to HARDENED-at-4 with substrate-extension would be over-counting. Pattern stays HARDENED-at-3.
- **`discretionary-departure-from-forward-going-commitment`** candidate-pattern (HARDENED-at-4 cycle 114) → cycle 115 honored the cycle 114-named priority (meta-reflexive stress-test), so cycle 115 did NOT depart from forward-going commitment. **The pattern was not extended cycle 115 — first cycle since cycle 110 to honor the named forward priority.** This is structurally important: cycle 115's meta-reflexive discipline IS the cycle 114-named priority, so honoring it is the discipline. The pattern stays HARDENED-at-4 (the prior departures remain valid); the cycle 115 honoring extends the meta-observation that "discretionary departure" is appropriate when higher-priority work surfaces but is not the default mode.

## Cycle 116 object-level C-side stress-test of the cycle 114 flip rationale

This section was added in cycle 116 (2026-05-11) as the substantive output of the symmetric C-side stress-testing that the cycle 115 _notes named as cycle 116's top priority. The cycle 115 symmetric flip-back threshold provides the falsifiable criterion: **"if cycle 116+ object-level stress-testing of C's selection-draft framings surfaces 4 substrate-content-distinct findings favoring A over C OR substantively undermining the cycle 114 flip rationale, the recommendation should be honestly flipped back to A."** Cycle 116 applies the cycle 111-114 stress-test methodology to the cycle 114 flip rationale's specific claims, then adversarially reviews the findings against the threshold's strength criterion.

This is the second consecutive cycle (after cycle 115) to honor the prior cycle's named forward priority. The `discretionary-departure-from-forward-going-commitment` pattern is qualified: cycles 111-114 demonstrated departure-when-higher-priority-surfaces; cycles 115-116 demonstrate honoring-when-named-priority-IS-the-higher-priority. Both modes coexist as appropriate-to-context.

### The four cycle 115-named stress-test targets

Cycle 115 _notes named four object-level stress-test targets for cycle 116+:
1. **5-point flip rationale stress-test** — each point tested individually for load-bearingness.
2. **Cycle 92 enumeration text fidelity** — does the cited evidence actually support the strength implied?
3. **Criterion 5 ordering shift** — does the A ≈ C → C > A shift reflect substrate or sentiment?
4. **Cycle 107 Risk 2 closure load-bearingness for flip-justification** — does the closure genuinely close the risk for flip-justification purposes, or is it specification-level closure for Phase 3 prototype validation discipline?

Cycle 116 examined all four targets. Findings on three of the four (no cycle 92 text fidelity issue found — the flip rationale's citation of cycle 92 line 228 is faithful; the substrate of the finding shifted to how cycle 92's structural enumeration is framed as "evidence accumulation" — absorbed into Finding A below).

### Finding A (Point 3 framing — "direction-validating evidence" structural-vs-empirical)

The cycle 114 flip rationale point 3 reads: *"C's central-bet rationale has accumulated direction-validating evidence across 4 cycles of internal review while A's rationale has progressively failed adversarial review."* The cited sources are cycle 92 (enumeration line 228: "carry their weight on F4 and F11"), cycle 96 (PR #2878 absorption: F4 + F11 not qualified as ambiguous-without-prototype), and cycle 107 (Risk 2 closure at specification level with falsifiable thresholds).

**The cited sources are structural arguments, not empirical evidence.** Cycle 92 line 230 explicitly says: *"Magnitude: structural enumeration suggests improvements are real but the absolute magnitudes (4pp / 3-4 cycle / 5-13pp) are estimates from cycle observation not Phase 3 prototype measurement. Magnitude validation requires running paired cycles (A-style boot-poll vs C-style reconcile-mode) on the same workload."* Cycle 107 closure (line 367-371) explicitly says: *"Operational closure deferred to Phase 3 prototype when: [4 criteria for actual validation]."* The cited sources **themselves acknowledge** that they are structural-not-empirical: cycle 92 acknowledges magnitude is estimate-not-measurement; cycle 107 acknowledges operational closure is deferred to Phase 3.

The flip rationale's framing of "direction-validating evidence accumulated across 4 cycles of internal review" implies progressive evidentiary strengthening. **The accumulation is 4 cycles of structural argument by the same author (the orchestrator), not 4 cycles of accumulating empirical evidence.** This is consistent with but extends cycle 115 Finding 3 (orchestrator-internal vs external); cycle 115 Finding 3 distinguishes WHO authored the sources, cycle 116 Finding A distinguishes WHAT TYPE of support they provide (structural enumeration vs empirical measurement).

**Substrate-content-distinct from cycle 115 Finding 3** — different axis of critique on the same sources. Both findings are about how the flip rationale frames its supporting sources.

### Finding B (Threshold count includes 1 derivative)

The cycle 114 flip rationale's "chain of motivated reasoning across cycles 111-114" enumerates 4 items (lines 65-75 of this draft):
1. Cycle 111 Criterion 5 framing of A's central bet → cycle 112 found mis-grounded.
2. Cycle 112 reframing of A-vs-C as "legibility only" → cycle 113 found under-weighted on 3 processing dimensions.
3. Cycle 111 "Why not C" Reason #1 (conditional-improvement risk) → cycle 114 found omits cycle 92 + 96 + 107 evidence.
4. Cycle 111 "Why not C" Reason #3 ("F4/F11 unproven as load-bearing") → **depends on F1+F7 dominance (cycle 112 finding refutes); collapses under the family-level reading.**

**The 4th item is derivative of cycle 112's substrate (F1+F7 dominance refutation), not a fresh cycle 114 finding.** Cycle 113's threshold language was *"a fourth load-bearing under-weighted dimension favoring C over A."* The strict reading requires 4 distinct dimensions. The chain's enumeration includes 3 fresh substrate-content-distinct findings (cycle 112 + 113 + 114 stress-tests) + 1 derivative observation (Reason #3 collapse rests on cycle 112's substrate, observed in cycle 114).

The cycle 113 threshold's spirit was to interrupt motivated reasoning at cumulative weight; whether 3 fresh + 1 derivative meets that spirit is interpretive. **Strict reading: threshold met by 3 fresh findings + 1 derivative; not strictly 4 substrate-content-distinct.** Charitable reading: 4 distinct claims even with shared substrate qualifies as substrate-content-distinct.

The cumulative direction of the 4 items is intact (all 4 point at weaknesses in cycle 111's case for A). The count-discipline observation is a load-bearingness clarification, not a refutation of the cumulative direction.

### Finding C (Criterion 5 ordering shift practice-vs-principle)

Cycle 114 shifted Criterion 5 ordering from A ≈ C (cycle 113) to C > A (cycle 114). The shift's rationale: *"A's central-bet rationale has been progressively shown mis-grounded across 4 cycles of stress-testing, while C's central bet has accumulated direction-validating evidence."*

Cycle 115 Finding 3 already weakened the C-strengthening clause (the accumulation is one-sided orchestrator-internal work, not external validation). Cycle 116 Finding C extends the analysis to the load-bearing distinction between A's BET and A's ARTICULATIONS:

- Cycle 112's finding was about cycle 111's framing of A's central bet (the "directly supported by F1+F7 dominance" articulation was mis-grounded against the retrospective's family-level dominance claim).
- Cycle 113's finding was about cycle 112's reframing of A-vs-C ("legibility only" was under-weighted on three processing dimensions).
- Cycle 114's finding was about cycle 111's "Why not C" Reason #1 articulation (omitted C's cycle 92 + 96 + 107 evidence).

**All three findings are about ARTICULATIONS of A's case across cycles 111-112, not about A's bet structurally.** A's central bet — *"v1's substrate is good and the load-bearing cleanup is structural rather than architectural"* — is consistent with the family-level reading of the retrospective (reconciliation asymmetry as dominant family; A handles reconciliation outcomes equivalently to C at DETECTION level per cycle 113 finding). A's bet itself was never refuted; specific articulations of it were dismantled.

The Criterion 5 ordering shift to C > A treats articulation-failures as bet-defensibility-failures. **Defensibility-in-practice (the orchestrator's specific articulations) is distinct from defensibility-in-principle (the bet's structural support).** Cycle 116's articulation of A's bet (consistent with family-level reading; equivalent DETECTION outcomes to C; processing-difference on 3 dimensions per cycle 113 + bounded migration cost per Criteria 1+3) is more defensible than the cycle 111 articulation. The shift conflates practice with principle.

**Counter-argument tested:** if A's articulations failed across 4 cycles, isn't that evidence A's bet is poorly defended? The counter holds at "practice" level but not at "principle" level — the bet's structural support (reconciliation-asymmetry family-level reading) is intact. The cycle 114 shift treats practice-failure as principle-weakness; this is the practice-vs-principle conflation.

**Substrate-content-distinct from cycle 115 Finding 3** — Finding 3 is about evidence-accumulation-asymmetry per se; cycle 116 Finding C is about the ordering-shift consequence of conflating articulation-failure with bet-failure.

### Finding D (Cycle 107 closure load-bearingness for flip-justification)

The cycle 114 flip rationale point 3 cites cycle 107 Risk 2 closure as direction-validating evidence for C. **Cycle 107 closure itself contains BOTH a structural-direction-assertion AND a measurement-specification**, not a fresh direction-validation:

- Cycle 107 line 351: *"Direction continues to hold by construction"* (structural argument — the mechanism Risk 2 named is real).
- Cycle 107 line 366: *"Mitigation specification is now concrete enough for Phase 3 prototype validation."*
- Cycle 107 lines 367-371: *"Operational closure deferred to Phase 3 prototype when: [4 criteria for actual validation]."*

**Cycle 107's substantive contribution is making the F4 improvement claim MEASURABLE in Phase 3, not direction-validating beyond what cycle 92's structural enumeration already provided.** The closure's "direction continues to hold by construction" is reaffirmation of cycle 92's structural argument, not new validation. The closure's measurement-specification is the substantive new work.

The flip rationale's citation of cycle 107 as "direction-validating evidence accumulated" treats the closure's structural-direction-reaffirmation as new evidence. **Cycle 107's contribution is precision, not new direction-validation.** This is consistent with cycle 107's own framing (which acknowledges direction holds by construction and defers operational validation to Phase 3) but mis-frames cycle 107's load-bearing function in the flip rationale's argument.

**Substrate-content-distinct from Finding A** — Finding A is about the type of support cited (structural vs empirical) across all 3 cited sources (92/96/107); Finding D is specifically about cycle 107's contribution being precision-of-measurement-specification rather than new direction-validation evidence.

### Adversarial review of cycle 116 findings (recursive meta-reflexive)

Per `ITERATION-UNTIL-APPROVAL`'s "examine for self-congratulation" discipline applied recursively. The cycle 116 findings could themselves be motivated reasoning to preserve perfect cycle symmetry (the `recursive-motivated-reasoning-surface` pattern predicts cycle 116 will find motivated reasoning in cycle 115 / cycle 114; the question is whether the findings substantively undermine or only qualify). Four counter-arguments tested:

**Counter-argument 1 (against Finding A):** *"'Direction-validating evidence' doesn't claim empirical validation; it just claims direction is validated. Cycle 92's enumeration validates the direction structurally (this is what 'validated by per-failure-mode enumeration' means). Cycle 107's specification-level closure validates the precise threshold. The flip rationale's framing is consistent with cycle 92's own 'structurally validated' framing."*

Response: This is fair. The flip rationale's "evidence" framing is consistent with cycle 92's structural-validation framing. Finding A is real but reframed: it's about the framing of structural arguments AS "evidence accumulated across 4 cycles." The "accumulation" phrasing implies progressive evidentiary strengthening; the substantive accumulation is 4 cycles of orchestrator-internal structural argument. **Finding A stands as a framing-level concern, not as a substantive challenge to the substance of cycle 92's structural argument.**

**Counter-argument 2 (against Finding B):** *"The cycle 113 threshold's spirit was to interrupt motivated reasoning by cumulative weight. Reason #3 collapse IS a load-bearing under-weighted dimension (Reason #3's reasoning depends on cycle-112-refuted substrate). The strict-letter count of 3 fresh + 1 derivative is interpretive; the cumulative-spirit count of 4 dimensions favoring C is intact."*

Response: This is fair. The cumulative direction of the 4 items is intact. **Finding B stands as a count-discipline observation about strict-letter interpretation, not as a challenge to the cumulative cycle 111-114 direction.**

**Counter-argument 3 (against Finding C):** *"The practice-vs-principle distinction is real but if A's articulations have all failed adversarial review across 4 cycles, that's evidence A's bet is structurally hard to articulate without mis-grounding. The bet's defensibility = the orchestrator's ability to articulate it without mis-grounding. Failed articulations = bet poorly defended."*

Response: This is partially fair. Defensibility-in-practice IS load-bearing if a bet's articulations consistently fail. But the cycle 116 articulation of A's bet (family-level reading + equivalent DETECTION outcomes + processing-difference per cycle 113 + bounded migration cost) is more defensible than cycle 111's. The "consistent failure" claim weakens when the articulation discipline improves. **Finding C stands at the practice-vs-principle distinction level; whether this distinction substantively shifts the Criterion 5 ordering is interpretive.**

**Counter-argument 4 (against Finding D):** *"Cycle 107 IS direction-validating in the structural sense — it explicitly says 'direction continues to hold by construction.' The flip rationale's citation is faithful. Whether cycle 107 is FRESH direction-validation or REAFFIRMATION-OF-cycle-92's direction-validation is a distinction without a difference for the cumulative-support argument."*

Response: This is partially fair. Cycle 107 IS direction-asserting; the flip rationale's citation is faithful to cycle 107's framing. Finding D's substance is the load-bearing function distinction — cycle 107's contribution is specification-of-measurement (load-bearing for Phase 3 prototype discipline) not new evidence (load-bearing for flip-justification). **Finding D stands as a load-bearingness clarification, not as a refutation of cycle 107's direction-assertion.**

**All 4 findings stand at qualification-level but NOT at substantive-undermining-level.** Each finding is real but the adversarial review shows the cycle 114 flip rationale's substantive arguments survive — what's challenged is framing, count-discipline, and load-bearingness clarification, not the core arguments.

### Counting against the cycle 115 symmetric flip-back threshold

The cycle 115 symmetric threshold: *"if cycle 116+ object-level stress-testing of C's selection-draft framings surfaces 4 substrate-content-distinct findings favoring A over C OR substantively undermining the cycle 114 flip rationale, the recommendation should be honestly flipped back to A."*

Counting cycle 116 findings against the threshold:

| Finding | Substrate-content-distinct? | Favoring A over C? | Substantively undermining flip rationale? |
|---|---|---|---|
| A (structural-vs-empirical) | yes (distinct from cycle 115 Finding 3) | no | **no** — qualifies framing of cycle 92/96/107 as "evidence"; substance of structural support intact |
| B (count includes 1 derivative) | yes | no | **no** — strict-letter count-discipline observation; cumulative direction intact |
| C (Criterion 5 practice-vs-principle) | yes (distinct from cycle 115 Finding 3) | partial — argues ordering should be A ≈ C not C > A | **partial** — argues shift conflates practice with principle; A-weakening alone may still justify A < C |
| D (cycle 107 closure load-bearingness) | yes (distinct from Finding A) | no | **no** — clarifies cycle 107's contribution is specification-of-measurement not new direction-validation; cycle 107's substantive contribution is intact |

**Of 4 findings, 0 are solidly "substantively undermining" per the threshold's strength criterion. Finding C is partial; the others are framing/interpretive concerns.** The threshold (4 substrate-content-distinct findings substantively undermining OR favoring A) is **NOT MET** at the strict reading of "substantively undermining."

### Decision: maintain C tentatively; cycle 116 qualifications absorbed; threshold not met

The 4 cycle 116 findings DO surface real considerations about the flip rationale's framing, count-discipline, and load-bearingness. After adversarial review, none solidly substantively-undermines the flip rationale's substantive arguments. **The cycle 115 symmetric flip-back threshold is NOT met. Flip-back is NOT triggered. The recommendation maintains C tentatively with cycle 116 qualifications absorbed.**

This is itself a meta-observation about the `recursive-motivated-reasoning-surface` pattern (NOVEL@1 cycle 115): the pattern predicts each cycle finds motivated-reasoning surfaces in the prior cycle's framing. **Cycle 116 confirms the pattern (TESTED@2) AND demonstrates the threshold's strength criterion ("substantively undermining") is the filter that distinguishes flip-triggering findings from qualifying findings.** Cycle 112-114 findings against cycle 111 were substantively undermining (specific textual mis-grounding; specific dimensions; specific evidentiary omissions); cycle 115 + cycle 116 findings against cycle 114 / cycle 115 are qualifying (framing concerns; count-discipline; load-bearingness clarification).

**The cumulative cycle 111-116 arc:** strong findings cycle 112-114 (against cycle 111 framings) → flip cycle 114. Weaker findings cycle 115 (against cycle 114 flip rationale structure) → qualifications cycle 115. Weaker findings cycle 116 (against cycle 114 flip rationale specific claims) → further qualifications cycle 116. **The recommendation is stabilizing at C with progressively-refined framing; the meta-reflexive iteration is converging rather than flip-flopping.**

### What cycle 116 preserves and what it changes

**Preserved:**
- Tentative recommendation: C.
- The cumulative findings across cycles 111-114 (substantively dismantled cycle 111's specific articulations of the case for A).
- The 6-criterion ordering as cycle 114 updated, qualified by cycle 115 + 116 framings.
- The cycle 113 four-finding flip threshold being honored cycle 114 as a real discipline.
- The cycle 115 symmetric flip-back threshold as ongoing discipline for cycle 117+ work.
- The cycle 115 three findings about the flip rationale's structure.

**Changed:**
- The flip rationale point 3's "direction-validating evidence accumulated across 4 cycles of internal review" framing is qualified: the cited sources are 4 cycles of structural argument, not 4 cycles of empirical evidence. The substantive support is intact; the framing is over-stated.
- The flip rationale's chain-of-motivated-reasoning count (4 items) is qualified: 3 fresh + 1 derivative under strict reading; 4 distinct claims under charitable reading. Cumulative direction intact.
- The Criterion 5 ordering shift A ≈ C → C > A is qualified: A-weakening across cycles 112-114 was about ARTICULATIONS of A's bet, not about A's bet's structural support; practice-vs-principle distinction means the shift may be over-stated.
- Cycle 107 Risk 2 closure's load-bearing function is clarified: specification-of-measurement (for Phase 3 prototype validation discipline), not new direction-validation evidence (which cycle 92 already structurally provided).

### Cycle 116 outcome for the candidate-pattern tracking

- **`recursive-motivated-reasoning-surface`** candidate-pattern (NOVEL@1 cycle 115; tentative) → **promoted to TESTED@2 cycle 116** (cycle 116 stress-test of cycle 115's framing surfaces 4 substrate-content-distinct motivated-reasoning surfaces in the cycle 114 flip rationale's specific claims — pattern's predicted behavior confirmed). The pattern's **load-bearing function clarified cycle 116:** the gradient is qualifying-not-refuting at sufficient adversarial-review discipline; strong findings trigger flips (cycle 112-114), weaker findings qualify (cycle 115-116). The threshold's strength criterion ("substantively undermining") is the filter.
- **NEW candidate-pattern named cycle 116:** `findings-qualify-not-refute-at-strength-discipline` **NOVEL@1** (cycle 116 first instance: 4 substrate-content-distinct findings against the flip rationale qualify-not-substantively-undermine; the strength discipline of the threshold's "substantively undermining" criterion filters which findings trigger flips). The pattern's expected behavior cycle 117+: stress-tests of cycle 116's framings would themselves surface motivated-reasoning surfaces; if substantively undermining at the strength criterion, flip-back triggers; if qualifying-not-refuting, further refinement-not-flipping. Pattern is potentially recursive without bound BUT with a stable strength-discipline gradient that distinguishes flip-triggering from refinement-triggering findings.
- **`pre-committed-flip-threshold-WITH-SYMMETRIC-COUNTERPART`** (extension of TESTED@1 cycle 114-115) → **TESTED@2 cycle 116 by being honored at its strength criterion** (cycle 116 found 4 substrate-content-distinct findings against the flip rationale; honored the threshold's "substantively undermining" strength criterion by NOT triggering flip-back because none of the 4 was solidly substantively-undermining). The threshold operates correctly as a discipline rather than as a ratchet OR a force-flipper.
- **`meta-reflexive-stress-test-finds-asymmetry`** candidate-pattern (NOVEL@1 cycle 115) → cycle 116's substrate is the flip rationale's specific claims (not asymmetry of the threshold structure as cycle 115's was). Substrate-content-distinct enough that cycle 116 is not a TESTED@2 of this pattern. **Pattern stays at NOVEL@1.**
- **`central-bet-stress-test-finds-mis-grounding`** candidate-pattern (HARDENED-at-3 cycle 114) → cycle 116's findings are about FRAMING, COUNT-DISCIPLINE, and LOAD-BEARINGNESS — not about mis-grounding (the substrate of the cited sources is intact; what's questioned is the framing). Pattern stays HARDENED-at-3.
- **`discretionary-departure-from-forward-going-commitment`** candidate-pattern (HARDENED-at-4 cycle 114) → cycle 115 + 116 both HONORED named forward priorities (cycle 115 honored cycle 114's; cycle 116 honored cycle 115's). **Pattern stays HARDENED-at-4 with the cycle 115 + 116 qualification extension** (`discretionary-departure-WHEN-HIGHER-PRIORITY-SURFACES` / `honor-when-named-priority-IS-the-higher-priority-work`). The two modes coexist as appropriate-to-context.

## Cycle 117 recursive stress-test of cycle 116's findings (third meta-reflexive layer)

This section was added in cycle 117 (2026-05-11) as the substantive output of the recursive stress-test that the cycle 116 _notes named as cycle 117's primary substantive work option #1. Cycle 116 found 4 substrate-content-distinct findings (A: structural-vs-empirical; B: count includes 1 derivative; C: practice-vs-principle; D: load-bearingness clarification) against the cycle 114 flip rationale and classified them as "qualifying-not-substantively-undermining." Cycle 117 applies the cycle 111-114 stress-test methodology to cycle 116's classifications (the third meta-reflexive layer — cycle 115 was first; cycle 116 was second).

This is the third consecutive cycle (after cycles 115 + 116) to honor the prior cycle's named forward priority. The `discretionary-departure-from-forward-going-commitment` pattern's qualified version (`honor-when-named-priority-IS-the-higher-priority-work`) now has 3 cycles of substrate (cycles 115-117) alongside 4 cycles of departure substrate (cycles 111-114). Both modes coexist as appropriate-to-context.

### The five cycle 117 findings

Cycle 117 applies the recursive stress-test methodology to five targets: cycle 116 Findings A-D + the strength-criterion classification action. Findings:

**Finding 1 (testing Finding A — structural-vs-empirical over-read):** Cycle 116 Finding A claims cycle 92/96/107 are "structural arguments, not empirical evidence" and the flip rationale's "accumulation" framing implies progressive evidentiary strengthening. **The structural-vs-empirical distinction over-reads.** Design analysis IS structural argument (Phases 0-2 outputs are structural; the framework is structural; the candidate comparison is structural). If structural arguments aren't "evidence," most of the redesign's intellectual output wouldn't qualify. The flip rationale's "evidence" framing is consistent with cycle 92's own "structurally validated by per-failure-mode enumeration" phrasing. **More substantively:** cycle 92, 96, 107 each added new analytical material (cycle 92: per-failure-mode enumeration; cycle 96: PR #2878 absorption qualifying F2; cycle 107: Risk 2 closure with specification-level falsifiable thresholds). The "accumulation across 4 cycles" framing is meaningful — each cycle added new substantive analytical work, not the same argument repeated. Finding A's concern narrows to the "evidence" word choice at framing level, not the structural-vs-empirical dichotomy. **Finding 1 stands at scope-narrowing level.**

**Finding 2 (testing Finding B — threshold-count vs chain-narrative-count distinction):** Cycle 116 Finding B claims the cycle 114 flip rationale's "chain of motivated reasoning" enumerates 4 items but the 4th is derivative; strict-letter count is 3 fresh + 1 derivative. **Cycle 116 Finding B conflates the threshold count with the chain narrative count.** The cycle 113 four-finding threshold's language was *"a fourth round of stress-testing surfaces a fourth load-bearing under-weighted dimension"* — counting ROUNDS (cycles) of stress-testing, not items in a backward-looking summary. The threshold-triggering count is: cycle 112 round + cycle 113 round + cycle 114 round = 3 substrate-content-distinct stress-test findings, with cycle 114's finding being the 4th cumulative finding triggering the threshold. The cycle 114 flip rationale's listing of 4 items is a backward-looking narrative summary. **Cycle 116 Finding B applies to the chain narrative count (which IS 3 fresh + 1 derivative); it does not apply to the threshold count which is round-based.** The threshold-honoring action depended on the round count, intact. Finding 2 narrows the scope of Finding B's concern. **Finding 2 stands at scope-clarification level.**

**Finding 3 (testing Finding C — contingent on Q6 family-level reading):** Cycle 116 Finding C claims the Criterion 5 ordering shift conflates articulation-failures with bet-defensibility-failures. **Finding C's defense of A's bet relies on the family-level reading being the correct reading of the retrospective** (cycle 112's substrate). The family-level vs pattern-level reading itself is contested (Q6, Eva-resolves). Under family-level reading, A's bet articulates as "reconciliation asymmetry addressed by interleaved-with-boot reconciliation" — structurally consistent with retrospective lines 161-162 + 959. Under pattern-level reading, A's bet articulates as "F1+F7 dominance addressed by Axis 13 medium-harness extraction" — cycle 112 found this mis-grounded. **Cycle 116 Finding C's "principle remains intact" claim DEPENDS on family-level reading prevailing.** Under pattern-level reading, A's bet is itself weakened, not just A's articulations; Finding C becomes partial-at-best. **Finding 3 stands at contingency-naming level: Finding C's strength is contingent on Q6 resolution.**

**Finding 4 (testing Finding D — precision-vs-validation dichotomy over-precise):** Cycle 116 Finding D claims cycle 107's substantive contribution is "specification-of-measurement, NOT new direction-validation evidence." **The precision-vs-validation framing is over-precise.** Cycle 107 explicitly says (line 351): *"Direction continues to hold by construction."* This IS direction-validating language, even if reaffirmation rather than fresh validation. Cycle 107's contribution is BOTH structural-direction-reaffirmation (line 351) AND measurement-specification (lines 366-371). The flip rationale's citation as "direction-validating evidence accumulated" is faithful at the reaffirmation level. **Finding 4 stands at clarification level: both functions are present; cycle 116 Finding D's distinction (cycle 107's new substantive contribution is precision-of-measurement) is real but the "NOT new direction-validation" framing dilutes that distinction by over-precisely separating the two functions.**

**Meta-Finding (testing strength-criterion application — Q6 reading-contestation contingency):** Cycle 116 classified Findings A-D as "qualifying-not-substantively-undermining" at the cycle 115 symmetric threshold's strength criterion. **The classification is contingent on Q6 reading-contestation resolution.** Under family-level reading, Finding C escalates to substantively-undermining (the Criterion 5 ordering shift conflates articulation with bet); under pattern-level reading, Finding C stays at "partial." Even with Finding C escalated to "yes" under family-level reading, the threshold count is 1 (not 4 substantively-undermining); the threshold-honoring action (no flip-back) stands. **The Meta-Finding stands at contingency-naming level: classification action is stable but is contingent on Q6 resolution.**

### Adversarial review of cycle 117 findings (recursive meta-reflexive — third layer)

Per `ITERATION-UNTIL-APPROVAL`'s "examine for self-congratulation" applied recursively to the third meta-reflexive layer. The cycle 117 findings could themselves be motivated reasoning to:
- (a) preserve perfect cycle-117 symmetry with cycles 115-116 patterns;
- (b) avoid finding substantively-undermining findings to keep the C recommendation;
- (c) find findings only to demonstrate the `recursive-motivated-reasoning-surface` pattern HARDENS.

Four counter-arguments tested (one per Finding 1-4); Meta-Finding's contingency-naming framing tested separately. **Result: all 4 findings + Meta-Finding stand at scope-narrowing/clarification/contingency-naming level — none escalate to substantively-undermining.** Each cycle 117 finding adds nuance to cycle 116's findings without escalating them. **The strength-discipline gradient continues to hold as the filter; the productive-content per finding has decreased from cycle 116 (object-level critiques) to cycle 117 (scope-narrowing/clarification critiques).**

### Counting against the cycle 115 symmetric flip-back threshold

| Finding | Substrate-content-distinct? | Favoring A over C? | Substantively undermining cycle 116 / flip rationale? |
|---|---|---|---|
| F1 (Finding A scope narrowing) | yes | no | **no** — narrows scope of Finding A; substance intact |
| F2 (threshold-vs-chain-narrative count distinction) | yes | no | **no** — clarifies scope of Finding B; threshold-honoring action depends on round count, intact |
| F3 (Finding C contingent on family-level reading) | yes | partial (under family-level reading) | **partial** — Finding C is substantive under family-level reading; threshold count still <4 with escalation |
| F4 (Finding D dichotomy over-precise) | yes | no | **no** — clarifies both functions present in cycle 107; flip rationale citation faithful at reaffirmation level |
| FM (strength-criterion application contingency) | yes (meta-level) | no | **no** — surfaces Q6 reading-contestation contingency; threshold-honoring action stands |

**Of 5 findings (4 + 1 meta), 0 solidly favor A over C; 0 solidly substantively undermine; 1 partial (F3 under family-level reading).** The threshold (4 substrate-content-distinct findings substantively undermining OR favoring A) is **NOT MET** at the strict reading of "substantively undermining." **Even under generous reading (F3 escalated to substantively-undermining under family-level reading), the threshold count is 1, not 4.**

### Decision: maintain C tentatively; cycle 117 qualifications absorbed; threshold not met

The 5 cycle 117 findings DO surface real considerations about cycle 116 findings' scope, framing, and contingency. After adversarial review (third recursive meta-reflexive layer), none solidly substantively-undermines cycle 116's threshold-honoring action or the flip rationale. **The cycle 115 symmetric flip-back threshold is NOT met. Flip-back is NOT triggered. The recommendation maintains C tentatively with cycle 117 qualifications absorbed.**

**This is the third consecutive cycle (cycle 115, 116, 117) where stress-testing of the prior cycle's framings produces qualifying findings without substantively-undermining at the strength criterion.** The `recursive-motivated-reasoning-surface` pattern HARDENS at 3 cycles of substrate-content-distinct evidence (cycle 115 found 3 surfaces in cycle 114; cycle 116 found 4 surfaces in cycle 115's framings; cycle 117 found 5 surfaces in cycle 116's classifications) with the strength-discipline gradient operating consistently as the filter.

**The cumulative cycle 111-117 arc:** strong findings cycle 112-114 (against cycle 111) → flip cycle 114. Qualifying findings cycle 115 (against cycle 114 structure) → qualifications. Qualifying findings cycle 116 (against cycle 114 specific claims) → further qualifications. Qualifying findings cycle 117 (against cycle 116 classifications) → further qualifications. **The recommendation is stabilizing at C with progressively-refined framing; the meta-reflexive iteration is converging; the productive content per cycle is decreasing as expected.**

### What cycle 117 preserves and what it changes

**Preserved:**
- Tentative recommendation: C.
- The cumulative findings across cycles 111-114 (substantively dismantled cycle 111's specific articulations of the case for A).
- The cycle 114 flip rationale's substantive arguments (cycle 116 found 4 framing-level qualifications, cycle 117 found 5 further qualifications — none substantively undermining).
- The cycle 115 symmetric flip-back threshold as ongoing discipline for cycle 118+ work.
- The 6-criterion ordering as cycle 114 updated, qualified by cycles 115 + 116 + 117 framings.

**Changed:**
- Cycle 116 Finding A's scope narrowed: the structural-vs-empirical distinction over-reads; each of cycles 92/96/107 added new analytical material; Finding A is about "evidence" word choice at framing level.
- Cycle 116 Finding B's scope narrowed: applies to chain narrative count (4 items, 3 fresh + 1 derivative); the threshold count is round-based (cycle 112 + 113 + 114 rounds each producing substrate-content-distinct findings); threshold-honoring action intact.
- Cycle 116 Finding C contingent on Q6 family-level vs pattern-level reading resolution: under family-level reading, Finding C is substantive (Criterion 5 ordering A ≈ C); under pattern-level reading, Finding C is partial-at-best.
- Cycle 116 Finding D clarified: cycle 107's contribution is BOTH precision-of-measurement-specification AND reaffirmation-of-direction-validation; flip rationale citation is faithful at reaffirmation level; cycle 116 Finding D's substantive distinction (cycle 107's new substantive contribution is precision) stands.
- Cycle 116 strength-criterion application's contingency named: classification action stands; contingent on Q6 reading-contestation resolution.
- **NEW Q9 added** surfacing the reading-contestation contingency as a named cross-cutting question Eva can resolve to simultaneously settle Q6 + Finding C strength + strength-criterion application contingency.

### Cycle 117 outcome for the candidate-pattern tracking

- **`recursive-motivated-reasoning-surface`** candidate-pattern (TESTED@2 cycle 116) → **HARDENED-at-3 cycle 117** (third cycle of cycle-prior framing's stress-test producing substrate-content-distinct findings with the strength-discipline gradient operating as the filter). Pattern's load-bearing function HARDENS: each cycle's stress-test finds substrate-content-distinct findings; strength-discipline gradient + productive-content-decrease bound the recursion.
- **`findings-qualify-not-refute-at-strength-discipline`** (NOVEL@1 cycle 116) → **TESTED@2 cycle 117** (second cycle of findings qualifying-not-substantively-undermining at strength criterion; pattern's predicted behavior confirmed). Pattern's load-bearing function: the strength-discipline gradient IS the natural stopping mechanism for the recursive stress-test pattern.
- **NEW candidate-pattern named cycle 117: `productive-content-decreases-with-recursion-depth`** **NOVEL@1** — each successive recursive stress-test layer produces qualifying findings with decreasing novel substance compared to prior layers. Cycle 115 findings (against cycle 114) were structural critiques; cycle 116 findings (against cycle 114 specific claims) were object-level critiques; cycle 117 findings (against cycle 116 classifications) are scope-narrowing/clarification critiques. The novel substance per finding is decreasing; cycle 118+ recursive stress-test would likely produce strictly-redundant findings. **This is a second natural-exhaustion criterion alongside the strength-discipline gradient.** Cycle 118+ stress-testing of cycle 117's findings would test the pattern (predicted: HARDENED if findings further-decrease in novel substance).
- **`pre-committed-flip-threshold-WITH-SYMMETRIC-COUNTERPART`** (TESTED@2 cycle 116) → **HARDENED-at-3 cycle 117** (three cycles of threshold honored correctly across substrate-content-distinct evidence: 114 honored A→C; 116 honored at strength-criterion; 117 honored at strength-criterion with reading-contestation contingency surfaced). **Pattern's load-bearing function HARDENED.**
- **`discretionary-departure-from-forward-going-commitment`** (HARDENED-at-4 cycle 114) → cycle 117 third consecutive HONORING (cycles 115, 116, 117). Pattern stays HARDENED-at-4 with the cycle 115-117 honoring extension (3 cycles of honoring + 4 cycles of departing = 7 cycles of substrate); qualified version (`departure-WHEN-HIGHER-PRIORITY-SURFACES` / `honor-when-named-priority-IS-the-higher-priority-work`) is increasingly well-supported.
- **`central-bet-stress-test-finds-mis-grounding`** (HARDENED-at-3 cycle 114) → cycle 117 findings are about scope-narrowing/clarification/contingency-naming, not about mis-grounding. Pattern stays HARDENED-at-3.
- **`meta-reflexive-stress-test-finds-asymmetry`** (NOVEL@1 cycle 115) → cycle 117's substrate is cycle 116's classifications, not threshold-structure asymmetry. Pattern stays NOVEL@1.
- **`selective-citation-as-motivated-reasoning-signature`** (TESTED@2 cycle 115 tentative) → cycle 117 finds no fresh substrate. Pattern stays TESTED@2 (tentative).

## The decision criteria, in order of weight

### 1. Schema-work-enabling (the redesign's primary thesis)

The redesign's primary thesis is that the system demonstrates self-improvement on a real proof domain — schema-org-json-ld definition maintenance across multiple programming languages. Every cycle the orchestrator spends on self-management is a cycle not spent on schema work. The decisive question:

> Which candidate puts the most cycles into the proof domain and the fewest into self-coordination?

- **A:** ~21 sub-shapes adopted; Axis 13 medium harness extracts ~50% procedural surface from prompt; per-cycle decision overhead estimate **~30-40% lower than v1's apparent surface**. Schema-work-enabling: **YES** (per A's P3 PASS verdict).
- **B:** ~43 sub-shapes adopted; per-role decision count is small but aggregate per-cycle work across 4 agents may exceed v1's. Schema-work-enabling: **CONDITIONAL** (per B's own document — depends on whether per-role overhead or aggregate overhead is the relevant metric; B explicitly names this as its central uncertainty).
- **C:** ~24 sub-shapes adopted; same Axis 13 medium-harness extraction as A; per-cycle decision overhead estimate **~25-35% lower** (slightly less reduction than A due to plan-lifecycle and reconcile-mode coordination). Schema-work-enabling: **YES** with note (P3 PASS-WITH-NOTE).

**Ordering on this criterion: A > C > B**, with B's classification as CONDITIONAL being the load-bearing distinction. The redesign's primary thesis is poorly served by a candidate that is conditionally schema-work-enabling.

### 2. CORE-DESIGN-PRINCIPLE compliance (small prompt + substantial tools)

The CORE-DESIGN-PRINCIPLE names the new prompt as **small** and the tool suite as **substantial**. It frames extraction-of-procedure-into-tools as the test for design quality.

- **A:** prompt ~50% smaller than v1; ~9 new Rust crates; tool surface bounded and discoverable via tool-registry. **Direct fit.**
- **B:** four role-specific prompts at ~30-40% v1's size each, but aggregate prompt content ~50% larger than v1; ~12+ Rust crates plus 20-40 skill crates. **Inverts the principle's reduction direction at the prompt level.** B trades single-prompt accretion for multi-prompt multiplication.
- **C:** prompt ~50% smaller than v1 (same as A); ~11 new Rust crates. **Direct fit, plus 2 additional crates for plan-lifecycle and reconcile-mode.**

**Ordering on this criterion: A ≈ C > B.** A and C both honor the principle; B's structure is in tension with it (the per-role-prompt aggregate is larger than v1's prompt).

### 3. Cost of being wrong (rollback / migration / Phase 3 effort)

The redesign prompt's SECTION 9 names the third checkpoint (pre-cutover) as gated on the artifact being complete; "we'll polish the tools after cutover" is explicitly disallowed. The cost of being wrong has two forms: (a) Phase 3 prototype effort wasted if the candidate fails experiments and we restart, (b) Phase 4 cutover risk if the production cutover encounters problems.

| Aspect | A | C | B |
|---|---|---|---|
| New Rust crates | ~9 | ~11 | ~12+ Rust + 20-40 skills |
| Net-add LOC (PR #2877 revised) | ~3600-6200 | ~5200-8200 | ~14000-28000 |
| Per-crate scope (median) | ~200-500 LOC (validated 2 of 9 at 231 + 268 LOC) | ~200-500 LOC | "several thousand LOC each" for key infra (per B's own line 62) |
| Cutover scope | bounded single-cycle per trivial crate; 1-2 cycles per orchestration-hub crate | bounded single-cycle per A-shared crate; 2-3 cycles for reconcile-mode + plan-lifecycle pair | **multi-cycle build-out (16-30 cycles for non-negotiable quality per PR #2877 lens-5)** |
| Cutover predictability | medium-high (PR #2877 revises to medium-with-trivial-crate-bias) | medium (PR #2877 names C as most under-justified estimate posture) | low |
| Rollback complexity | revert workflow YAML + delete v2 artifacts | revert workflow YAML + delete v2 artifacts | revert workflow YAML + delete v2 artifacts + per-channel state cleanup |

**Ordering on this criterion: A > C > B**, with B 3-5× the migration cost of A and a multi-cycle cutover scope vs A's per-crate scope.

### 4. Failure-mode addressing depth (P1, P4, F1-F12)

Each candidate addresses F1-F12 differently. The decisive question is whether B's or C's deeper failure-mode addressing is **load-bearing** for v1's actually-observed failure modes, or whether A's lighter addressing **suffices in practice**.

The retrospective (`0-retrospective.md`) names v1's actually-observed failure-mode pattern: chronic-category-currency loop, Eva-blocker queue, abandonment cascade, gate proliferation, self-management dominance. **There are two valid readings of "dominance" in the retrospective, and the cycle 111 selection draft picked one without surfacing the other.**

- **Family-level dominance (per retrospective lines 161-162 and 959):** the retrospective explicitly identifies **reconciliation asymmetry (F2/F3/F4/F5†/F11† family) as the dominant family**, with the cleanest single-sentence statement: *"v1 has tools to **create** records and tools to **summarize** records; it lacks tools to **reconcile** records against later events."*
- **Pattern-level prevalence (cycle 111 framing, derived from cycle 2 measurements):** F1 (constraint accretion, defense-accretion family) and F7 (self-management dominance, procedure/review-overhead family) are the most-pervasive and most-measurable individual patterns — F7 has 130+ consecutive cycles of zero schema work; F1 has constraint-stack-on-stack evidence in `COMPLETION_CHECKLIST.xml`'s C3 step.

**The cycle 111 framing "the dominant failure pattern is F1 (constraint accretion) compounding F7 (self-management dominance)" is a pattern-level framing that crosses family boundaries** — F1 is in the defense-accretion family, F7 is in the procedure/review-overhead family — and **does not match the retrospective's family-level dominance claim**. The cycle 112 stress-test surfaces this as the central uncertainty (see "Central uncertainty: family-level vs pattern-level dominance" section below).

C's incremental addressing over A is concentrated in F4 (frozen-artifact lifecycle), F11 (post-close mutations), and F2 (Eva-response detection) — **all three of which are in the reconciliation asymmetry family that the retrospective identifies as dominant at the family level**. C addresses these via reconcile-mode as a first-class harness phase (vs A's boot-phase interleaved polling). **However: A and C have equivalent reconciliation OUTCOMES** — both have 1-cycle latency, both use cursor-file polling at session-start (`state/eva-input-cursor.json`). The structural difference between A and C on reconciliation is **first-class named-phase legibility, not detection mechanism or latency**.

**Ordering on this criterion under the family-level dominance reading: C > B > A** (C addresses F2/F4/F11 with equivalent outcomes to A but better structural legibility; B addresses with most depth but inverts the CORE-DESIGN-PRINCIPLE at the prompt level). **Ordering under the pattern-level prevalence reading: B > C > A** (B addresses F1+F7 most aggressively via role decomposition; A and C have similar F1+F7 addressing via Axis 13 medium-harness extraction). **The cycle 112 finding shifts this Criterion's ordering from cycle 111's "B > C > A with caveats" to "C > B > A under family-level reading OR B > C > A under pattern-level reading — the dominance framing is the central uncertainty Eva resolves."**

### 5. The candidate's central bet defensibility

Each candidate has a central bet:

- **A:** "v1's substrate is good and the load-bearing cleanup is structural (state.json, _notes-as-memory, harness extraction) rather than architectural (multi-role decomposition)." Defensibility: this bet is **consistent with the procedure/review-overhead family (F6/F7/F9) and the defense-accretion family (F1/F5/F11/F12) addressing**, and is **consistent with reconciliation asymmetry addressing at the outcome level** (A's Axis 12 hybrid polling matches C's reconcile-mode latency). **However, the cycle 111 framing "directly supported by the retrospective's identification of F1+F7 as the dominant failure pattern" was mis-grounded** — F1+F7 are individual patterns in different families, and the retrospective at lines 161-162 identifies reconciliation asymmetry (a different family) as dominant at the family level. A's bet remains **directly testable** in Phase 3 by measuring whether harness extraction reduces orchestrator decision count, but the central-bet defensibility is now contested rather than confidently supported.
- **B:** "v1's single-orchestrator shape is the load-bearing limit on cluster G (role-asymmetric context), cluster H (post-session feedback), and Axis 12 (reconciliation) — and that the failure modes those clusters address require dedicated roles, not just better tools." Defensibility: cluster G role-asymmetric context is a real consideration, but the retrospective does not identify it as the dominant failure pattern at any reading. The bet **requires** that role-asymmetric context (which v1 doesn't have) is load-bearing for v2 — this is an aspirational extension, not a v1-failure-mode-driven extension.
- **C:** "v1's substrate is good (A's bet) **and** inbound reconciliation deserves structural first-class status without paying the multi-agent coordination overhead — the load-bearing addition between A and B is harness-phase elevation of one specific concern (reconciliation), not full role decomposition." Defensibility: **post-cycle-112 stress-test, C's bet is now more directly defensible than cycle 111 framed it** — the retrospective's family-level dominance reading (reconciliation asymmetry as dominant family) is consistent with C's "inbound reconciliation deserves first-class status" claim. C's own document still names a residual conditional-improvement risk: "if the additions don't carry their weight, C is just A with more migration surface" — but the cycle 112 finding shifts the conditional from "if F4/F11 are load-bearing" to "if first-class structural legibility (named phase) is worth +~1500-2000 LOC over A's interleaved-with-boot reconciliation."

**Ordering on this criterion (post-cycle-114): C > A > B** (further downgrade from cycle 113's "A ≈ C > B"). The cycle 114 stress-test surfaces that C's central bet is direction-validated by cycle 92 enumeration ("**carry their weight on F4 and F11**"; F2 marginal), the F4/F11 portions survived cycle 96 PR #2878 absorption (only F2 was qualified as "ambiguous-without-prototype"), and Risk 2 is closed at specification level cycle 107 with falsifiable thresholds. **C's central bet has accumulated direction-validating evidence across 4 cycles of internal review.** A's central bet (cycle 111 framing as "directly supported by F1+F7 dominance") was found mis-grounded against the retrospective's "reconciliation asymmetry as dominant family" claim cycle 112; the cycle 113 + 114 findings further weakened the case for A. **The cumulative pattern across cycles 111-114 is: A's central-bet rationale rests on framings that successively fail adversarial review; C's central-bet rationale survives those same reviews.** Parsimony (the remaining A-over-C argument) is not the same as defensibility against the retrospective's framing; this is the cycle 113 observation extended to cycle 114. B's bet (cluster-G role-asymmetry as load-bearing for v2) still requires aspirational extension and remains the lowest-defensibility position.

### 6. Audit-as-peer P6 preservation

Per the redesign prompt's SECTION 5 (Authority — audit-as-peer), preservation of audit-as-peer is a structural concern.

- **A:** 5/5 PASS-WITH-WORK on Criterion 4 (discovery primitive needs implementation; not preserved-by-construction).
- **B:** 5/5 PASS (reconciler agent IS the cluster G clean-context-reviewer pattern at session level).
- **C:** 5/5 PASS (reconcile-mode is dedicated harness phase, slightly stronger than A on Criterion 4 but slightly weaker than B on Criterion 4 because C's reconcile-mode shares context with work-mode).

**Ordering on this criterion: B > C > A.** Margin is narrow; all three preserve audit-as-peer at the substrate level. The Criterion 4 differences are about how legibly the discovery primitive lives in the design, not whether it works.

## Cross-candidate comparison: discriminating axes side by side

**Source:** [`2-candidates/README.md`](./2-candidates/README.md) "Side-by-side P1-P6 + M3 + M2 + migration comparison" table (post-cycle-110 state). Reproduced here for selection-decision legibility.

| Criterion | Candidate A | Candidate C | Candidate B |
|---|---|---|---|
| **P3 (self-management-reduction)** | PASS | PASS-WITH-NOTE | PARTIAL-FLAG |
| **P4 (lifecycle-vocabulary completeness)** | PARTIAL (bypass clause) | PASS-WITH-WORK (sub-shapes 4+5) | PASS (sub-shapes 1-5) |
| **P6 (audit-as-peer preservation)** | 5/5 PASS-WITH-WORK | 5/5 PASS | 5/5 PASS |
| **M2 cost (LOW + MOD + HIGH)** | 12 + 8 + 1 (~21 sub-shapes) | 13 + 10 + 1 (~24 sub-shapes) | 16 + 22 + 5 (~43 sub-shapes) |
| **Migration: net-add LOC (PR #2877 revised)** | ~3600-6200 | ~5200-8200 | ~14000-28000 |
| **Cutover scope** | bounded single-cycle per crate | bounded; 2-3 cycles for reconcile-mode + plan-lifecycle | multi-cycle build-out (16-30 cycles) |
| **Cutover predictability** | medium-high (PR #2877: medium-with-trivial-crate-bias) | medium (PR #2877: most under-justified posture) | low |
| **Schema-work-enabling answer** | YES (P3 PASS) | YES with note (P3 PASS-WITH-NOTE) | CONDITIONAL (P3 PARTIAL-FLAG) |
| **Central bet defensibility** | **weakened progressively cycles 111→112→113→114** (cycle 111 "F1+F7 dominant" framing mis-aligned with retrospective lines 161-162 + 959; cycle 113 found "equivalent outcomes" claim under-weighted on processing dimensions; cycle 114 found "Why not C" Reason #1 omits C's cycle 92 + 96 + 107 direction-validation evidence — case for A rests on chain of motivated reasoning across 4 cycles) | **upgraded progressively cycles 112→114** (cycle 112: consistent with family-level dominance reading; cycle 114: direction-validated by cycle 92 enumeration + cycle 96 PR #2878 absorption F4/F11 not qualified + cycle 107 Risk 2 closure at specification level — direction resolved; magnitude is Phase 3 work) | medium (bet on cluster G role-asymmetry being load-bearing for v2 — no retrospective grounding at any reading) |
| **F1+F7 (dominant v1 failure pattern) addressing** | strong (Axis 13 harness extraction) | strong (same as A) + plan-lifecycle CI | strong (per-role specialization) |
| **F4 (frozen-artifact lifecycle) addressing** | weak (no plan-lifecycle) | strong (3-state plan-lifecycle) | strong (3-state plan-lifecycle + branching checkpoints) |
| **F11 (post-close mutations) addressing** | medium (boot-phase polling) | strong (reconcile-mode first-class) | strong (reconciler agent first-class) |
| **A-vs-C reconciliation distinction (cycle 112 → cycle 113 evolution)** | detection equivalent to C; processing differs on runtime-budget + per-channel-extensibility + audit-readability (cycle 113 sharpening of cycle 112 "legibility-only" framing) | first-class reconcile-mode = dedicated runtime budget + per-event handlers + typed-deltas; structurally distinct from A on processing | reconciler agent = clean-context separation; structurally distinct from both A and C |

## What Candidate A gives up

Honesty per the redesign prompt's SECTION 8 directive ("you pick one with rationale, naming what you give up"):

1. **No dedicated reconciler agent / no first-class reconcile-mode.** A's Axis 12 reconciliation interleaves with primary work in boot phase. If the volume of inbound channels grows substantially (Eva responses + audit posts + dispatch PR-merges + future channels), the boot phase becomes the bottleneck. C's reconcile-mode is the structural alternative; B's reconciler agent is the more aggressive alternative. **Post-cycle-113 sharpening:** the boot-phase-bottleneck risk is now characterized on three dimensions, not one — runtime budget allocation under burst load, per-channel extensibility for new inbound channels, and audit-readability of reconciliation state. **Mitigation:** Phase 3 measurement of reconcile-equivalent boot-phase work plus burst-load characterization (deliberate stress with multiple Eva responses + audit posts + dispatch returns same cycle); if measured to be substantial fraction of cycle budget OR if extensibility friction surfaces during channel additions, revisit C-style reconcile-mode addition retroactively.

2. **No plans-as-artifacts directory.** A's Axis 5 = No defers plan-lifecycle as not-yet-needed. If multi-cycle plan coordination grows, this becomes friction. C's plans-as-artifacts is the structural alternative. **Mitigation:** Phase 3 + early-Phase-4 monitoring; if plan-related drift surfaces, retroactively adopt C's plan-lifecycle.

3. **No parallel exploration branches.** A's Axis 4 git-as-substrate without branching checkpoints means what-if reasoning across multi-cycle horizons is harder. The cycle-history append-only files give lookback, not lookahead. B's branching checkpoints are the structural alternative. **Mitigation:** if lookahead becomes valuable for Phase 3 design or future redesign work, the branching-checkpoints primitive can be added later as a layered extension.

4. **Higher cluster A 6-ABSENT count adopted.** A inherits cluster A's 6-ABSENT sub-shapes (phase-boundary semantics + recovery operations + concurrency-queuing) at LOW-MODERATE cost only. **Risk:** if v1's cluster A failure modes return at v2 — particularly recovery operations or concurrency queuing if Phase 3 surfaces them — A has limited structural defense beyond cluster B intersection coverage. **Mitigation:** Phase 3 stress-test specifically for cluster A failure modes (deliberate corruption / abandonment / restart cycles) per the redesign prompt's SECTION 8 Phase 3 directive.

5. **F4 and F11 detection legibility weaker than C/B.** A's mechanisms work, but they're less structurally legible than C's reconcile-mode or B's reconciler agent. Eva and audit reading the state of an A-system have to infer reconciliation state from boot-phase logs rather than reading a dedicated reconciliation surface. **Post-cycle-112 update: this give-up is more salient than cycle 111 framed it** — under the retrospective's family-level dominance reading (reconciliation asymmetry as dominant family), legibility on F2/F4/F11 is legibility on the dominant-family addressing. The question of whether named-phase legibility is worth +~1000-2600 LOC migration cost over A's interleaved-with-boot legibility is now explicitly Eva's call (see "Central uncertainty: family-level vs pattern-level dominance" section). **Mitigation:** if Eva approves A, Phase 3 measurement of reconciliation-related boot-phase work becomes a load-bearing measurement; if measured to be substantial fraction of cycle budget, revisit C-style reconcile-mode addition retroactively.

6. **Discovery primitive completeness (P6 Criterion 4 PASS-WITH-WORK).** A adopts `[audit-request]` title prefix + label as discovery primitive but the implementation is required-not-yet-built. **Mitigation:** Phase 3 prototype work includes the audit-request-discovery primitive.

## Why not B (decomposed multi-role)

B's structural depth is real and well-argued. The reasons not to select B:

1. **Schema-work-enabling is conditional, not affirmative.** B's own document names this as its central uncertainty. The redesign's primary thesis cannot rest on a candidate that's conditionally schema-work-enabling.

2. **Migration cost is 3-5× A's.** PR #2877 revised B's net-add to 14000-28000 LOC vs A's 3600-6200. Cutover is multi-cycle (16-30 cycles per PR #2877 lens-5) vs A's per-crate single-cycle. The cost of being wrong about B is large.

3. **Inverts the CORE-DESIGN-PRINCIPLE at the prompt level.** B's aggregate prompt content is ~50% larger than v1's via the four role-specific prompts. The principle names "small prompt" as the design direction; B's structure is in tension with this.

4. **Central bet requires aspirational extension.** B's bet that cluster G role-asymmetric context is load-bearing for v2 requires extending v1 in a direction v1 does not currently have. The retrospective's observed-failure pattern (F1+F7) is addressable without role-asymmetric context. B's depth is appropriate if the proof-domain genuinely needs role parallelism — but the schema-org-json-ld proof domain has not been characterized as needing parallelism at this scale.

5. **P3 PARTIAL-FLAG is structurally concerning.** B's mitigation (per-role specialization absorbs per-role decision count) is real-but-unproven. A reasonable Phase 3 outcome could refute the mitigation, which would surface the P3 concern late in the work.

**B is preferable to A only if** Phase 3 surfaces evidence that v1's substrate (single-orchestrator shape) is the load-bearing limit on schema-work scaling. There is no such evidence currently.

## Why not C (hybrid)

C's positioning is clever — preserve A's bounded migration cost while adopting two B-targeted structural elements (reconcile-mode + plans-as-artifacts). The reasons not to select C:

1. **Conditional-improvement risk.** C's own document names this risk: "if the additions don't carry their weight, C is just A with more migration surface." The 3 additional sub-shapes (plan-lifecycle + reconcile-mode + per-mode budgets) are at most 25-35% per-cycle decision overhead reduction vs A's 30-40% — C pays additional cost for additional structural depth in F4/F11.

2. **PR #2877 names C as most under-justified estimate posture.** C's claimed +~1000-1500 LOC over A may drift higher (PR #2877 revised range 5200-8200 vs A's revised 3600-6200, narrowing the docs-stated A vs C distance). C is the candidate most likely to miss its own upper-bound estimate.

3. **F4/F11 addressing improvements over A are unproven as load-bearing.** The retrospective's observed-failure pattern is F1+F7 dominant; C's improvements are concentrated in F4/F11. **C is preferable to A only if** F4/F11 are load-bearing for v2 — which is the bet, and the bet is not validated.

4. **Per-mode runtime budgets add complexity at unproven value.** C's per-mode runtime budgets (boot ≤5min, reconcile ≤10min, work ≤50min, close ≤10min) give finer-grained abandonment detection but require per-mode-runtime-tracking infrastructure. The cycle-level budget (~75min) is sufficient for v1; the value of finer-grained budgets is unproven.

**C is preferable to A only if** F4/F11 detection legibility is judged structurally important at the candidate-selection level (Eva's call), or if Phase 3 surfaces evidence that boot-phase reconciliation is the bottleneck. There is no such evidence currently.

**Post-cycle-112 stress-test update to "Why not C":** the cycle 112 finding (family-level vs pattern-level dominance — see next section) shifts the A-vs-C trade-off from "C must justify its additions against A's parsimony" toward "C must justify its additions against A's bounded migration cost, given that C's first-class reconciliation is consistent with the retrospective's family-level dominance reading." Reasons #1, #3, and #4 above remain — particularly the conditional-improvement risk and the migration-cost difference. Reason #2 (PR #2877's under-justified-estimate flag for C) remains relevant. **What changes post-cycle-112:** Reason #3's "F4/F11 addressing improvements over A are unproven as load-bearing" weakens, because under the family-level dominance reading reconciliation asymmetry is the dominant family and F2/F4/F11 addressing IS the dominant-family addressing. The remaining A-over-C argument on this criterion is thus the cost-benefit one: is named-phase reconciliation legibility worth +~1500-2000 LOC migration cost over interleaved-with-boot reconciliation that achieves the same outcomes?

**Post-cycle-114 stress-test update to "Why not C" (the section is now historical record post-flip):** the cycle 114 stress-test on Reason #1 (conditional-improvement risk) surfaces that the cycle 111 framing **omits C-hybrid.md's own cycle 92 + cycle 96 + cycle 107 evidence** that resolves the direction component of the conditional ("**carry their weight on F4 and F11**" per cycle 92 line 228; F4 + F11 survived cycle 96 PR #2878 absorption while only F2 was qualified; Risk 2 closure at cycle 107 specification level with falsifiable thresholds). The cycle 111 framing presents the conditional as fully open when direction is in fact validated; the open conditional is magnitude (Phase 3 work). Reason #1 is therefore weakened to "magnitude is Phase 3 work; direction is resolved." Reason #3 was already weakened post-cycle-112; cycle 114 confirms Reason #3's "F4/F11 unproven as load-bearing" framing depends on the cycle-112-refuted F1+F7-dominance basis. Reason #2 (PR #2877's under-justified-estimate flag) and Reason #4 (per-mode runtime budgets add complexity at unproven value — partially weakened by cycle 113's per-mode-budget-as-runtime-allocation finding) remain as the residual A-over-C arguments on the cycle 111 framing's terms. **The cumulative effect of cycles 112 + 113 + 114 stress-tests is that the cycle 111 case for A no longer holds at face value; the four-finding flip threshold is honored cycle 114; the recommendation flips to C.** This section is preserved as historical record showing the original cycle 111 reasoning and the progressive cycle 112-114 dismantling; the active recommendation lives in "Why the recommendation flipped to C (post-cycle-114)."

## Central uncertainty: family-level vs pattern-level dominance (cycle 112 stress-test)

This section was added in cycle 112 (2026-05-10) as the substantive output of the central-bet stress-test named in the cycle 111 outstanding-work list ("Selection-rationale stress-testing — adversarial re-read of retrospective looking for competing dominant-pattern characterizations"). It surfaces a finding that the cycle 111 selection draft glossed over.

### The finding

The cycle 111 selection draft asserts (Criterion 4 paragraph 2 + Criterion 5 paragraph A): *"the dominant failure pattern is F1 (constraint accretion) compounding F7 (self-management dominance)"*, and uses this to ground A's central-bet defensibility as *"directly supported by the retrospective's identification of F1+F7 as the dominant failure pattern."*

**The retrospective does not identify F1+F7 as the dominant failure pattern.** At lines 161-162, the retrospective explicitly names **reconciliation asymmetry as the dominant family**, with the cleanest single-sentence statement: *"v1 has tools to **create** records and tools to **summarize** records; it lacks tools to **reconcile** records against later events."* At line 959, the v2-design-implications-by-family preamble repeats: *"reconciliation asymmetry as dominant family."*

**Reconciliation asymmetry is the F2/F3/F4/F5†/F11† family** (per the table at retrospective line 138-143). **F1 and F7 are in different families**:
- F1 is in the **defense-accretion family** (F1/F5†/F11†/F12) — alongside F11 dual-membered with reconciliation asymmetry.
- F7 is in the **procedure/review-overhead family** (F6/F7/F9) — disjoint from both reconciliation asymmetry and defense accretion.

The cycle 111 framing "F1 compounding F7" thus crosses two family boundaries (defense-accretion + procedure-overhead) to construct a cross-family "dominant pattern" that the retrospective does not name.

### Two valid readings of "dominance"

The retrospective supports two readings:

| Reading | Claim | What dominance means | Selection implication |
|---|---|---|---|
| **Family-level (per retrospective lines 161-162, 959)** | Reconciliation asymmetry is the dominant family | The structural shape of v1's failure is write-mostly state with no inbound reconciliation tools; F2/F3/F4/F5†/F11† are manifestations | C and B address this family more structurally (named reconciliation phase / dedicated reconciler agent) than A (interleaved-with-boot polling) |
| **Pattern-level prevalence (cycle 2 measurements)** | F1 (constraint accretion) and F7 (self-management dominance) are the most-pervasive and most-measurable individual patterns | F7 has 130+ consecutive cycles of zero schema work; F1 has constraint-stack-on-stack evidence in C3 step | A addresses F1 most directly via Axis 13 medium-harness extraction; A and B address F7 more aggressively than C |

The two readings are not contradictory; they are different abstraction levels. The cycle 111 selection draft picked the pattern-level reading without acknowledging the family-level reading exists in the retrospective.

### Why this matters less than it first appears (with cycle 113 qualification)

**A and C have equivalent reconciliation DETECTION outcomes.** Reading A's candidate document (lines 36-37, 45) and C's (lines 22, 44-46) confirms:
- Both use cursor-file polling (`state/eva-input-cursor.json`).
- Both have 1-cycle latency for Eva-response detection (~6h at 4 cycles/day cron).
- Both detect post-close mutations via cycle-history append-only files (Axis 4 git-as-substrate).
- Both treat audit-as-peer with cross-repo reading discipline.

The detection mechanism and detection latency are equivalent.

**Cycle 113 finding: the "equivalent outcomes" claim covers DETECTION but glosses over PROCESSING.** A more careful reading of A's lines 36-37+45 and C's lines 20+22+44-46 surfaces three dimensions where A and C diverge that the cycle 112 reframing characterized as "legibility only":

1. **Per-mode runtime budget allocation.** C explicitly declares per-mode runtime budgets at Axis 9 (boot ≤5min / reconcile ≤10min / work ≤50min / close ≤10min); reconcile-mode has dedicated runtime independent of other boot work. A has no reconciliation-specific budget — reconciliation work shares boot-phase budget with state-load + cursor-advance + standing-directive check + gardening-sweep-pre-cycle. Under burst load (multiple Eva responses + audit posts queued same cycle), A's reconciliation may be truncated or its work spills into work-phase; C's reconcile-mode is bounded but dedicated. **This is not legibility — it is runtime allocation.**

2. **Per-channel structured handlers.** C's Axis 12 explicitly names *"Per-event handlers per channel; reconciler-mode emits typed-deltas to per-component state files"* (line 22). A's Axis 12 describes *"input-from-eva pull at session-start with cursor advance; question-for-eva-response check via existing `check-eva-responses` (or replacement)"* (line 36) — procedurally less structured. Adding a new inbound channel (e.g., a third-party orchestrator with a new label, a webhook-driven event source) to A's boot-phase polling requires a code change that interleaves with existing boot work; C's reconcile-mode adds a per-channel handler with a uniform shape. **This affects extensibility AND consistency-of-handling across channels.**

3. **Typed-delta output discipline.** C's reconcile-mode emits typed-deltas to per-component state files — a single auditable output stream per cycle. A's reconciliation outputs scatter across boot-phase activities (cursor advances, in-place state updates, journal appends). For audit-as-peer's ability to read reconciliation state across cycles, C's typed-deltas are more readable than A's scattered boot-phase outputs. **This is partly legibility but also partly auditability — they are not the same.**

The structural difference between A and C is therefore **wider than legibility** — it includes runtime budget allocation, extensibility of channel-handling code path, and audit-readability of reconciliation state. Cycle 112's characterization of the difference as "legibility only" was under-weighted; cycle 113 surfaces three dimensions cycle 112 did not name.

**Cycle 113 honest qualification:** detection latency genuinely is equivalent (1 cycle, set by cron cadence). Processing characteristics differ. The cycle 112 reframing of the A-vs-C trade-off as "named-phase legibility worth +~1000-2600 LOC" should now read "named-phase legibility + runtime budget allocation + per-channel extensibility + audit-readability worth +~1000-2600 LOC." The recommendation does not flip on this finding alone (Criteria 1+3 still favor A, the under-weighted dimensions are real but bounded), but the trade-off characterization is more honest.

**A reader of an A-system inferring reconciliation state has to read boot-phase logs; a reader of a C-system reads the named reconcile-mode artifact directly.** The detection works identically; the processing structure (budget, extensibility, output discipline) differs.

### Reframed selection question

The cycle 112 finding does not flip A → C. It reframes the A-vs-C question from:

> **Cycle 111 framing:** Is A's parsimony enough for v1's dominant failure pattern (F1+F7)?

To:

> **Cycle 112 framing:** Reconciliation asymmetry IS the family-level dominant failure pattern. A's interleaved reconciliation and C's first-class reconcile-mode produce equivalent outcomes (same latency, same mechanism). **Is named-phase reconciliation legibility worth +~1000-2600 LOC migration cost over A's interleaved approach?**

The cycle 113 stress-test further reframes (qualifying the cycle 112 framing):

> **Cycle 113 framing:** Reconciliation asymmetry IS the family-level dominant failure pattern. A's interleaved reconciliation and C's first-class reconcile-mode produce equivalent **detection** outcomes (same latency, same mechanism). The **processing** characteristics differ on three dimensions: runtime budget allocation, per-channel extensibility, audit-readability of reconciliation state. **Is named-phase reconciliation processing structure (legibility + dedicated budget + extensibility + auditability) worth +~1000-2600 LOC migration cost over A's interleaved approach?**

This further reframing makes the A-vs-C trade-off more complete:
- A's win on cost-of-being-wrong (Criterion 3) is real and quantifiable — A's net-add is ~3600-6200 LOC; C's is ~5200-8200 LOC; the delta is ~1000-2600 LOC.
- C's win on family-level-dominance addressing (Criterion 4 family-level reading + Criterion 5 post-cycle-112 update) is real and now (post-cycle-113) more structurally distinct from A than cycle 112 framed — the difference is wider than legibility, including runtime budget allocation + extensibility + auditability.
- The load-bearingness of C's win still depends on whether the structural characteristics are judged Eva-load-bearing — but the question is no longer "legibility yes/no" but "structural-processing-discipline yes/no."

### Why the recommendation flipped to C (post-cycle-114)

The recommendation tentatively flipped from A to C cycle 114 because the cycle 113 four-finding flip threshold was triggered by the cycle 114 stress-test on the conditional-improvement framing:

1. **The four-finding flip threshold was a pre-committed falsifiable criterion.** Cycle 113's "Honest cumulative observation" named: *"if a fourth round of stress-testing (cycle 114+) surfaces a fourth load-bearing under-weighted dimension favoring C over A, the recommendation should be honestly flipped to C."* Cycle 114 surfaces such a dimension (the cycle 111 conditional-improvement framing omits C-hybrid.md's own cycle 92 + cycle 96 + cycle 107 direction-validation evidence). The threshold is honored.

2. **The cumulative findings across cycles 111-114 dismantle the cycle 111 case for A.** Cycle 111 framed A's central bet as *"directly supported by the retrospective's identification of F1+F7 as the dominant failure pattern"*; cycle 112 found this mis-grounded against the retrospective's "reconciliation asymmetry as dominant family" claim. Cycle 112 reframed A-vs-C as "legibility only"; cycle 113 found this under-weighted on three processing dimensions (runtime budget, per-channel extensibility, audit-readability). Cycle 113 maintained the recommendation by citing "equivalent reconciliation outcomes"; cycle 114 found the supporting cycle 111 "Why not C" Reason #1 (conditional-improvement risk) omits C's direction-validation evidence. **Each cycle's finding is structurally distinct and substrate-content-distinct from prior findings; the four findings together are not piling on; they are a load-bearing pattern.**

3. **C's central-bet rationale has accumulated direction-validating evidence across 4 cycles of internal review while A's rationale has progressively failed adversarial review.** C-hybrid.md cycle 92 enumeration (lines 222-228): "carry their weight on F4 and F11; F2 marginal." Cycle 96 PR #2878 absorption: F4 + F11 not qualified (only F2 qualified as ambiguous-without-prototype). Cycle 107 Risk 2 closure: discipline-conditional risk closed at specification level with falsifiable thresholds. **C's central bet is direction-validated; magnitude is Phase 3 work.** A's central bet (cycle 111 framing) was never grounded in the retrospective at the family level the retrospective itself names; A's case for v1's "structural cleanup suffices" rests on a pattern-level dominance reading the retrospective does not prefer.

4. **What still favors A: Criteria 1 (schema-work-enabling) and 3 (cost-of-being-wrong) on migration-cost dimensions.** A's bounded migration cost (~3600-6200 LOC PR #2877 revised) means earlier exit from redesign and resumption of schema work; C's higher migration cost (~5200-8200 LOC) delays this by an estimated 1-2 cycles. A's smaller migration surface is easier to roll back if v2 fails. **These are real costs of selecting C.** The flip rests on the orchestrator's tentative analysis that Criterion 5 (central-bet defensibility) cumulative shift to C > A, combined with Criterion 4 (failure-mode addressing depth) cumulative shift to C > B > A under the family-level reading, outweighs Criteria 1 + 3 on the migration-cost dimension. **Eva resolves whether this weighting is correct.**

5. **Honoring the threshold is itself a load-bearing structural action.** Cycle 113 named the `pre-committed-flip-threshold` candidate-pattern with the applicability test: *"if reasoned-around, the threshold reveals as protective fiction; if honored, it is a load-bearing discipline."* Reasoning around the threshold (citing cycle 114's connection to prior cycles, or arguing the finding is "merely about citation patterns") would falsify the threshold as a discipline. **The flip demonstrates that pre-committed falsifiable criteria are real disciplines, not protective fictions** — a v2-design observation worth preserving (analogous to audit's commitment-thread discipline named cycle 215 audit-side, structurally equivalent at the cross-candidate selection level).

**Honest cumulative observation across cycles 111-114:** A's case has been progressively dismantled across 4 cycles of stress-testing while C's case has accumulated direction-validating evidence. The flip is consistent with the cumulative findings. **The flip is the orchestrator's tentative recommendation update; it does not pre-empt Eva's checkpoint approval.** Eva can override (back to A on migration-cost weighting; to B on multi-role decomposition; to a not-yet-authored candidate); the override pathway is preserved in the open questions section.

**Cycle 115 qualifications to the flip rationale (post-meta-reflexive stress-test):** The cycle 115 meta-reflexive stress-test surfaced three motivated-reasoning surfaces in the cycle 114 flip rationale itself (see "Cycle 115 meta-reflexive stress-test of the cycle 114 flip rationale" section above): **(1)** the four-finding flip threshold is asymmetric (named only for A→C; symmetric flip-back threshold not specified — Finding 1); **(2)** the assertion that Criteria 4+5 cumulative weight outranks Criteria 1+3 migration-cost weight is asserted not derived from the redesign prompt's primary thesis (Finding 2); **(3)** C's "direction-validation accumulation" cites cycle 92 + 96 + 107 which are predominantly the orchestrator's own iterative work — external validation for C does not yet exist (Finding 3). **The findings qualify but do not reverse the flip:** the recommendation maintains C tentatively with cycle 115 qualifications, a symmetric flip-back threshold is named for cycle 116+ object-level C-side stress-testing, and the weighting choice is surfaced explicitly to Eva as Q7. **What cycle 115 changes about the flip:** the flip is now framed as provisional pending cycle 116+ symmetric stress-testing of C's selection-draft framings AND audit cycle 216 critique landing AND Copilot adversarial dispatch on the post-flip draft. The cycle 113 four-finding threshold is preserved as honored cycle 114; the cycle 115 symmetric threshold is the discipline that makes the cycle 113 threshold a genuine discipline rather than a one-way ratchet.

**Cycle 116 qualifications to the flip rationale (post-object-level C-side stress-test):** The cycle 116 object-level stress-test (see "Cycle 116 object-level C-side stress-test of the cycle 114 flip rationale" section above) surfaces 4 substrate-content-distinct framing-level concerns about the flip rationale's specific claims: **Finding A** the "direction-validating evidence accumulated across 4 cycles" framing implies progressive evidentiary strengthening when the cited sources are 4 cycles of structural argument by the same author; **Finding B** the chain-of-motivated-reasoning count (4 items) includes Reason #3 collapse as derivative of cycle 112's substrate; **Finding C** the Criterion 5 ordering shift treats articulation-failures (cycles 112-114 dismantled cycle 111's specific articulations of A's bet) as bet-defensibility-failures (practice-vs-principle conflation); **Finding D** the cycle 107 closure citation conflates structural-direction-by-construction (reaffirmation of cycle 92) with new direction-validation evidence (cycle 107's actual contribution is specification-of-measurement). **Adversarial review of cycle 116 findings: all four stand as qualifications but NONE solidly meets the cycle 115 symmetric threshold's "substantively undermining" criterion.** The cycle 115 symmetric flip-back threshold is NOT met. The recommendation maintains C with cycle 116 qualifications absorbed; the cumulative cycle 111-116 arc shows strong findings cycle 112-114 (substantively undermining cycle 111's articulations → flip cycle 114) followed by qualifying findings cycle 115-116 (refining the flip rationale without substantively undermining → maintain-with-refinement). The `recursive-motivated-reasoning-surface` pattern promotes from NOVEL@1 cycle 115 to TESTED@2 cycle 116 with the strength-discipline gradient confirmed: strong findings trigger flips; weaker findings qualify; the threshold's strength criterion is the filter. **The meta-reflexive iteration is converging at C with progressively-refined framing rather than flip-flopping.**

**Cycle 117 qualifications to the flip rationale (post-recursive stress-test of cycle 116's findings):** The cycle 117 recursive stress-test (see "Cycle 117 recursive stress-test of cycle 116's findings (third meta-reflexive layer)" section above) surfaces 4 substrate-content-distinct findings + 1 meta-finding scope-narrowing/clarifying cycle 116's classifications: **Finding 1** cycle 116 Finding A's structural-vs-empirical distinction over-reads (each of cycles 92/96/107 added new analytical material; accumulation is analytically-progressive not just repetitively-progressive; Finding A's concern narrows to "evidence" word choice at framing level); **Finding 2** cycle 116 Finding B applies to chain narrative count (3 fresh + 1 derivative in the cycle 114 listing) NOT to the threshold count (round-based: cycle 112 + 113 + 114 rounds each producing substrate-content-distinct findings; threshold-honoring action intact); **Finding 3** cycle 116 Finding C is contingent on Q6 family-level vs pattern-level reading resolution (under family-level reading, Finding C is substantive arguing ordering A ≈ C; under pattern-level reading, A's bet is itself mis-grounded and Finding C is partial-at-best); **Finding 4** cycle 116 Finding D's precision-vs-validation dichotomy is over-precise (cycle 107 contains BOTH structural-direction-reaffirmation at line 351 AND measurement-specification at lines 366-371; flip rationale citation is faithful at reaffirmation level; Finding D's substantive distinction about cycle 107's new substantive contribution being precision stands but the "NOT new direction-validation" framing dilutes the distinction); **Meta-Finding** cycle 116's strength-criterion classification of Findings A-D as "qualifying-not-substantively-undermining" is contingent on Q6 reading-contestation resolution (under family-level reading, Finding C escalates; threshold count still <4 with escalation; classification action stable but contingent). **Adversarial review of cycle 117 findings (recursive meta-reflexive at third layer): all 4 findings + Meta-Finding stand at scope-narrowing/clarification/contingency-naming level — NONE escalate to substantively-undermining.** The cycle 115 symmetric flip-back threshold is NOT met (0 substantively-undermining + 1 partial under family-level reading; threshold requires 4). The recommendation maintains C with cycle 117 qualifications absorbed; the cumulative cycle 111-117 arc shows strong findings cycle 112-114 → flip cycle 114, followed by qualifying findings cycle 115-116-117 (3 consecutive cycles of qualifications without substantively-undermining at strength criterion). **The `recursive-motivated-reasoning-surface` pattern HARDENS-at-3 cycle 117** (three substrate-content-distinct cycles of evidence). **NEW candidate-pattern `productive-content-decreases-with-recursion-depth` NOVEL@1 cycle 117** — each recursive stress-test layer produces qualifying findings with decreasing novel substance; cycle 115 findings were structural; cycle 116 were object-level; cycle 117 are scope-narrowing/clarification — this is a SECOND natural-exhaustion criterion alongside the strength-discipline gradient. **The meta-reflexive iteration is converging AND approaching natural exhaustion of useful recursive stress-testing; cycle 118+ recommended pivot is away from continued recursive stress-testing toward higher-value work (audit cycle 216 absorption when landed; Q6 + Q9 resolution if Eva engages; B-side stress-tests still outstanding; Phase 3 prototype evidence deepening).**

**What cycle 115+ should examine** (revised with cycle 115 findings absorbed): (a) **cycle 116+ priority** — symmetric C-side stress-testing of selection-draft framings under the cycle 115-named symmetric flip-back threshold; targets include the 5-point flip rationale (each point stress-tested individually), the cycle 92 enumeration text (does the enumeration actually say what the flip rationale cites it as saying?), the Criterion 5 ordering shift to C > A > B, the cycle 107 Risk 2 closure as load-bearing vs specification-level fiction; **(b)** does the cycle 114 finding survive adversarial review (audit cycle 216 critique landing; Copilot adversarial-feedback dispatch when [#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903) resolves)? (c) does the +~1500-2000 LOC migration delta materially affect Phase 3 timeline? (d) are there fifth-or-sixth-round stress-tests of the cycle 115 framing itself that should surface (the `recursive-motivated-reasoning-surface` pattern named cycle 115 predicts cycle 116+ stress-testing of cycle 115's framing would find motivated reasoning — if so, the pattern HARDENS)? **The flip is not a stopping point; iteration continues per `ITERATION-UNTIL-APPROVAL`. The cycle 115 symmetric flip-back threshold is the load-bearing discipline that makes cycle 116+ work non-self-amplifying.**

### What this means for cycle 112+ outstanding work

- The "stress-test the central bet" item from cycle 111's outstanding-work list is now **partially absorbed** by this section. A more thorough stress-test would extend to other load-bearing claims in the selection rationale (e.g., the "B's central bet requires aspirational extension" claim — does B's cluster G framing have any retrospective grounding I'm missing?).
- **The adversarial Copilot feedback dispatch named for cycle 112+** should now also test whether the cycle 112 reframing is itself correct, OR whether there's a third reading of dominance the cycle 112 work missed.
- **Open question for Eva #6** (added below) names this as the central uncertainty Eva resolves.

## Outstanding work before lock-in

This draft is **not** the deliverable. Per `ITERATION-UNTIL-APPROVAL`, the following work is outstanding before the candidate-selection checkpoint should be considered ready for Eva's approval:

1. **Audit critique landing.** Audit cycle 215 (today) named cycle 216 as the substantive Phase 2 critique cycle. Audit critique on the candidate set should land on this draft and any updates produced in response. Cycle 112+ absorption work.

2. **Adversarial Copilot feedback dispatch on the selection.** No Copilot feedback dispatch has been targeted specifically at the selection decision. Dispatching one (or several with different lenses: "argue for B"; "argue for C"; "argue this analysis is missing a candidate not yet authored") is named in the redesign prompt's `ITERATION-UNTIL-APPROVAL` and would test whether the analysis survives external adversarial framing. Cycle 112+ work.

3. **Stress-test the central bet.** ~~The recommendation rests on F1+F7 being the dominant v1 failure pattern. If a careful adversarial re-read of the retrospective surfaces a competing dominant-pattern characterization (e.g., F4+F11 as load-bearing; or cluster-G role-asymmetry as load-bearing), the recommendation must be revisited.~~ **Substantially absorbed cycles 112 + 113 + 114 + 115 + 116; recommendation flipped cycle 114; cycle 115 qualified the flip with meta-reflexive findings; cycle 116 qualified further with object-level findings against the flip rationale's specific claims.** Cycle 112 stress-test found cycle 111 "F1+F7 dominant" framing mis-grounded. Cycle 113 stress-test found cycle 112 "equivalent reconciliation outcomes" framing under-weighted on processing dimensions. Cycle 114 stress-test found cycle 111 "Why not C" Reason #1 (conditional-improvement risk) omits C's own cycle 92 + cycle 96 + cycle 107 direction-validation evidence; the cycle 113 four-finding flip threshold is honored; recommendation flipped from A to C cycle 114. **Cycle 115 meta-reflexive stress-test of the cycle 114 flip rationale surfaced three motivated-reasoning surfaces in the flip rationale itself** (asymmetric threshold; asserted weighting; one-sided accumulation) — the flip is maintained with cycle 115 qualifications and a symmetric flip-back threshold is named for cycle 116+ object-level C-side stress-testing. **Cycle 116 object-level C-side stress-test of the cycle 114 flip rationale's specific claims surfaced 4 substrate-content-distinct findings** (structural-vs-empirical "evidence" framing; chain count includes 1 derivative; Criterion 5 ordering shift conflates practice with principle; cycle 107 closure citation conflates direction-by-construction with new direction-validation) — after adversarial review, none solidly meets the cycle 115 symmetric threshold's "substantively undermining" criterion; the recommendation maintains C with cycle 116 qualifications absorbed; the `recursive-motivated-reasoning-surface` pattern promotes to TESTED@2 with the strength-discipline gradient confirmed. **Cycle 117 recursive stress-test of cycle 116's classifications surfaced 4 substrate-content-distinct findings + 1 meta-finding** (Finding A scope narrowing on structural-vs-empirical; Finding B chain-vs-threshold-count distinction; Finding C contingent on Q6 family-level reading; Finding D precision-vs-validation dichotomy over-precise; strength-criterion application contingent on Q6 resolution) — after adversarial review at the third meta-reflexive layer, none solidly substantively-undermines; the recommendation maintains C with cycle 117 qualifications absorbed; the `recursive-motivated-reasoning-surface` pattern HARDENS-at-3 across cycles 115-117 with strength-discipline gradient + productive-content-decrease both operating as natural-exhaustion criteria. **What remains for cycle 118+:** (a) **audit-cycle-216 critique on the post-cycle-117 selection draft** — audit will see the post-cycle-115 + post-cycle-116 + post-cycle-117 qualifications; cycle 117 or cycle 118 most likely landing window (~04:00 UTC 2026-05-11 audit cron); (b) **Q6 + Q9 resolution by Eva** — both questions surface the family-level vs pattern-level reading-contestation that affects Criterion 4 ordering, Criterion 5 ordering, cycle 116 Finding C strength, and strength-criterion application; Eva's resolution would simultaneously settle multiple downstream contingencies; (c) **#2903 resolution at cycle 118 close** (3 cycles into 5-cycle autonomy default; orchestrator-resolves fallback per cycle 113-named option A); (d) **B-side stress-tests** (B's cluster-G aspirational extension grounding; B's 3-5× migration cost methodology defensibility against PR #2877's lens-5 critique); (e) **continued recursive stress-testing of cycle 117's findings** is DECREASING PRIORITY per the cycle 117-named `productive-content-decreases-with-recursion-depth` candidate-pattern — cycle 118+ stress-test of cycle 117's findings would likely produce strictly-redundant findings; the recursive iteration is approaching a fixed point and should pivot to higher-value work; (f) **Copilot adversarial-feedback dispatch on the post-cycle-117 draft** when [#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903) resolves — dispatch body needs further updating to stress-test the post-cycle-117 framings; (g) **Per-candidate Phase 3 prototype evidence deepening** (extending cycles 93-94 measurements; C-side `reconcile-mode` measurement if Eva pre-approves; still load-bearing post-flip with cycle 116/117 qualifications absorbed).

4. **Phase 3 prototype effort on a smallest-load-bearing crate from each candidate.** The redesign prompt's SECTION 8 names this as bounded preview: 1-2 of A's smallest Rust crates as proof-of-concept for migration-cost validation; same for C's `reconcile-mode` if Eva pre-approves; B's smallest crate is harder to identify because B's smallest infra is `several thousand LOC each` per B's own document. Cycle 93-94 already measured A's `tool-registry` (231 LOC) and `cycle-history-append` (268 LOC); extending to A's `boot-phase` and `wiki-search` would deepen A-side evidence; equivalent measurement for C's `reconcile-mode` would inform the A vs C comparison.

5. **Cycle 103-110 risk-closure substrate absorption.** The seven specification-level risk closures specified pre-agreed falsifiable thresholds for Phase 3 measurement. The selection rationale above does not yet enumerate these as part of "what Phase 3 needs to validate." A more complete selection draft would include the threshold table, organized by candidate. Cycle 112+ work.

6. **Open questions for Eva** (see next section). Some questions are genuinely Eva-only judgment calls; the answers shape the selection.

## Open questions for Eva at the candidate-selection checkpoint

These questions are explicitly Eva-only per `EVA-DEFAULT-AUTONOMY` (scope authorization, infrastructure access, the candidate-selection checkpoint itself). The orchestrator's analysis does not pre-empt them:

1. **Approve Candidate C for Phase 3 prototype work?** This is the affirmative question (post-cycle-114 flip). If yes: Phase 3 begins with C as the prototype target. The orchestrator's tentative recommendation flipped from A to C cycle 114 per the cycle 113 four-finding flip threshold being honored; the rationale rests on the cumulative findings across cycles 111-114 dismantling the cycle 111 case for A while C's central bet has accumulated direction-validating evidence (cycle 92 + cycle 96 absorption + cycle 107 closure).

2. **Override and pick Candidate A if migration cost / Criteria 1+3 weight outranks Criteria 4+5 cumulative findings?** This is the most plausible override (post-cycle-114 inversion of cycle 111-113 Q2). The orchestrator's analysis names that Criteria 1 (schema-work-enabling) and 3 (cost-of-being-wrong) still favor A on migration-cost dimensions (~3600-6200 LOC vs ~5200-8200 LOC PR #2877 revised), but judges that Criteria 4 (failure-mode-addressing depth under family-level reading) and 5 (central-bet defensibility per cumulative cycles 111-114 findings) cumulatively outrank the migration-cost dimensions. **Eva resolves whether this weighting is correct.** Selecting A means accepting the cumulative cycle 112-114 findings as not-load-bearing-enough to outrank migration cost; selecting C means accepting cycle 114's flip rationale.

3. **Pick Candidate B if the schema-work proof-domain genuinely needs multi-role decomposition for parallelism?** This is the maximally-aggressive override. The orchestrator's analysis names this as not-currently-evidenced, but Eva may have forward-looking judgment about the schema-work scope that the orchestrator does not.

4. **Direct an additional candidate not yet authored?** The redesign prompt's SECTION 8 names "Required: at least 2 distinct design candidates, ideally 3" — three are authored, but Eva may judge the candidate set as not spanning the right dimensions.

5. **Extend the iteration window before approval?** The orchestrator's default behavior is to continue iterating per `ITERATION-UNTIL-APPROVAL`. Eva can name a specific iteration target (audit critique landing, Copilot feedback dispatch landing, additional Phase 3 prototype evidence) before the approval is given.

6. **Resolve the family-level vs pattern-level dominance question (cycle 112 finding), the processing-structure trade-off (cycle 113 qualification), and the conditional-improvement framing's evidentiary basis (cycle 114 finding).** The cycle 112 stress-test surfaced that the cycle 111 draft's "F1+F7 is the dominant failure pattern" framing was mis-grounded against the retrospective's explicit "reconciliation asymmetry is the dominant family" framing (lines 161-162 + 959). The cycle 113 stress-test qualified the cycle 112 "equivalent reconciliation outcomes" claim: detection outcomes are equivalent (1-cycle latency, cursor-polling); processing characteristics differ on three dimensions (runtime budget allocation, per-channel structured handlers, typed-delta output discipline). The cycle 114 stress-test found the cycle 111 "Why not C" Reason #1 (conditional-improvement risk) omits C-hybrid.md's own cycle 92 + cycle 96 + cycle 107 direction-validation evidence — direction is resolved (F4 + F11 carry their weight per cycle 92; survived cycle 96 PR #2878 absorption; Risk 2 closed at specification level cycle 107); the open conditional is magnitude (Phase 3 work). **The selection question reduces to: is named-phase reconciliation processing structure (legibility + dedicated runtime budget + per-channel extensibility + audit-readability) worth +~1000-2600 LOC migration cost over interleaved-with-boot reconciliation that achieves the same DETECTION outcomes — given that C's direction-validation evidence has accumulated across cycles 92 / 96 / 107 / 112 / 113 / 114 while A's central-bet rationale has been progressively shown to rest on cycle 111 framings that successively fail adversarial review?** This is a value-judgment where Eva's perspective is the load-bearing input — the orchestrator's analysis names the trade-off and **the cycle 113 four-finding flip threshold is honored cycle 114 (the orchestrator's tentative recommendation flipped from A to C)** but does not pre-empt Eva's resolution. If Eva judges named-phase processing structure important AND the cycle 114 flip rationale defensible: select C. If Eva judges A's bounded migration cost more important than the cumulative cycle 111-114 findings: select A (override the flip).

7. **(NEW cycle 115) Approve or override the orchestrator's weighting choice: Criteria 4+5 cumulative findings > Criteria 1+3 migration-cost weight.** The cycle 115 meta-reflexive stress-test of the cycle 114 flip rationale surfaced (Finding 2) that the flip rationale asserts this weighting without deriving it from the redesign prompt's primary thesis. The redesign prompt SECTION 1 names the primary thesis as: *"the schema-org-json-ld work is the proof domain: useful output that demonstrates the system is actually doing real work, not just theorizing about itself."* Criterion 1 (schema-work-enabling) IS the primary thesis. The cycle 114 flip rationale acknowledges Criteria 1+3 still favor A on migration cost (~3600-6200 LOC vs ~5200-8200 LOC PR #2877 revised) but states Criteria 4+5 cumulative findings outweigh Criteria 1+3 on the migration-cost dimension. **This is a value choice.** Q1 (Approve C?) and Q2 (Override to A?) ask about the recommendation outcome; this Q7 asks about the underlying weighting itself. Eva can: **(a)** adopt the orchestrator's weighting (Criteria 4+5 cumulative > Criteria 1+3 migration-cost) — implies selecting C; **(b)** invert the weighting (primary-thesis Criterion 1 + cost-of-being-wrong Criterion 3 > Criteria 4+5 cumulative findings) — implies selecting A; **(c)** specify a different weighting (e.g., Criterion 5 central-bet defensibility is the load-bearing criterion; Criterion 1 is the constraint, not the primary axis) — implies a separately-named decision rule. The weighting choice deserves its own surface because it's the load-bearing meta-choice between candidates of comparable merit on different dimensions.

8. **(NEW cycle 116) Approve the orchestrator's strength-criterion interpretation of the cycle 115 symmetric flip-back threshold, OR override at strict-letter count.** Cycle 116 surfaced 4 substrate-content-distinct findings against the cycle 114 flip rationale's specific claims. The cycle 115 symmetric threshold's language was: *"4 substrate-content-distinct findings favoring A over C OR substantively undermining the cycle 114 flip rationale, in cycle 116+ object-level C-side stress-testing."* Cycle 116's findings are all substrate-content-distinct AND in cycle 116+ object-level C-side stress-testing, BUT after adversarial review none solidly meets "substantively undermining" — they are framing concerns (Finding A: how "evidence" is read), count-discipline observations (Finding B: 3 fresh + 1 derivative), practice-vs-principle observations (Finding C: ordering shift conflates articulation with bet), and load-bearingness clarifications (Finding D: cycle 107 closure is specification-of-measurement not new evidence). **Three readings are possible:** **(a)** accept the orchestrator's strength-criterion interpretation — findings qualify the flip rationale's framing but do not substantively-undermine; recommendation maintains C with cycle 116 qualifications absorbed (orchestrator's tentative resolution); **(b)** override at strict-letter count — 4 substrate-content-distinct findings is 4, regardless of strength; trigger flip-back to A per the symmetric threshold's letter; **(c)** specify a different strength bar (e.g., 2 of 4 findings substantively-undermining suffices; or all 4 must substantively-undermine; or the strength-criterion is itself a value-choice Eva resolves). The orchestrator's tentative resolution applies the cycle 115 symmetric threshold's "substantively undermining" language at strength-discipline reading; Eva can override at strict-letter reading or specify the strength bar differently. **Cycle 117 refinement:** the strength-criterion application is also contingent on Q6 resolution (see Q9 below) — under family-level reading, cycle 116 Finding C escalates to substantively-undermining, but the threshold count remains <4 even with escalation; under pattern-level reading, Finding C stays at "partial." The cycle 116 classification action is stable but contingent.

9. **(NEW cycle 117) Resolve the family-level vs pattern-level dominance reading-contestation as a cross-cutting decision.** Q6 above named the reading-contestation surfaced cycle 112 (the cycle 111 selection draft's "F1+F7 is the dominant failure pattern" framing was mis-grounded against the retrospective's explicit "reconciliation asymmetry as dominant family" framing at lines 161-162 + 959). Cycle 117 Finding 3 + Meta-Finding observed Q6 has cross-cutting effects on cycle 116 + 117 framings: **(a)** under family-level reading, cycle 116 Finding C (the Criterion 5 shift conflates practice with principle) is substantive — Finding C argues for Criterion 5 ordering A ≈ C rather than C > A; **(b)** under pattern-level reading, A's bet is itself mis-grounded (per cycle 112) and Finding C is partial-at-best — F1+F7 dominance framing was the cycle 111 articulation that successive cycles dismantled; **(c)** the cycle 116 strength-criterion application (classifying Finding C as "partial") implicitly assumed pattern-level reading or hedged between readings; under family-level reading, Finding C escalates to "substantively undermining," though the threshold count remains <4 even with escalation. Eva's resolution of Q6 would simultaneously settle: Criterion 4 ordering (C > B > A under family-level vs B > C > A under pattern-level), Criterion 5 ordering shift defensibility (C > A under cycle 114 framing vs A ≈ C under family-level reading), cycle 116 Finding C strength, and cycle 116 strength-criterion application contingency. **Three resolutions are possible:** **(a)** family-level reading is the correct reading of the retrospective (per retrospective lines 161-162 + 959 explicit text); **(b)** pattern-level reading is the correct reading (per cycle 2 measurements showing F7 has 130+ cycles of zero schema work; F1 has constraint-stack-on-stack evidence); **(c)** both readings are valid at different abstraction levels — the selection should explicitly accommodate both rather than choosing one. The orchestrator's tentative resolution treats family-level reading as the retrospective's preferred framing (per explicit text) while acknowledging pattern-level reading is meaningful at measurement level; Eva can resolve definitively.

## Honest meta-observation: the cycle 103-110 risk-closure pattern and this draft

The cycles 103-110 work produced 7 specification-level risk closures with falsifiable thresholds. These are real value: they make Phase 3 measurement meaningful by specifying ahead-of-time what counts as direction-validated / at-risk / refuted.

But: 22 consecutive bottleneck-asynchronous cycles (cycles 78-110) have produced a meta-tracking framework (24 functional-class shapes at 54 instances, with NOVEL→TESTED→HARDENED promotion mechanics) that is in tension with the selection question. The shape-tracking work tracks the orchestrator's own pattern-extraction; it does not directly advance the cross-candidate comparison that selection requires.

Cycle 111's substantive focal — drafting this selection artifact — was a deliberate departure from the cycle 110-named forward-going commitment (rotate to A-side or B-side risk closure). The departure was reasoned as follows:

- The Phase 2 deliverable per the redesign prompt explicitly requires `2-selection.md`. It did not exist. Eva cannot approve a selection at a checkpoint with no selection artifact.
- The risk closures specify per-candidate thresholds; selection is the cross-candidate comparison; the two activities are complementary, not redundant.
- Continuing rotation is the self-amplifying-loop pattern that `ITERATION-UNTIL-APPROVAL`'s "examine for self-congratulation" activity is designed to interrupt. Selection-draft work is the honest exit.

This meta-observation is included for Eva's transparency: the orchestrator made a discretionary choice to break a forward-going commitment named in the prior cycle's _notes. The choice is defensible (the selection artifact gap was real and load-bearing for the checkpoint) but it is also a discretionary choice. Eva can override the choice; if she names "continue rotation, defer selection draft" the orchestrator complies and revisits this draft in a later cycle.

## Iteration log

| Cycle | What changed | What's next |
|---|---|---|
| 111 (2026-05-10) | Initial authoring. Tentative selection: A. Six decision criteria with cross-candidate ordering. What A gives up enumerated. Why-not-B and why-not-C sections. Six outstanding work items before lock-in. Five open questions for Eva. Honest meta-observation on the cycle 103-110 pattern. | Audit cycle 216 critique landing (~6h-1d); cycle 112 absorption. Possible Copilot feedback dispatch on this draft for adversarial framing (cycle 112+). Cycle 103-110 risk-closure threshold table integration (cycle 112+). Per-candidate Phase 3 prototype evidence deepening (cycle 113+). |
| 112 (2026-05-10) | **Central-bet stress-test absorbed.** Surfaced that cycle 111's "F1+F7 is the dominant failure pattern" framing was mis-grounded against the retrospective's explicit "reconciliation asymmetry is the dominant family" claim (lines 161-162 + 959). Revised Criterion 4 paragraph (two valid dominance readings; ordering depends on reading). Revised Criterion 5 paragraph (A's central bet weakened from "directly supported" to "consistent with one reading"; ordering downgraded to A ≈ C from cycle 111's A > C). Added "Post-cycle-112 stress-test update" to "Why not C" reframing the A-vs-C trade-off. Added new "Central uncertainty: family-level vs pattern-level dominance" section between "Why not C" and "Outstanding work before lock-in" (~115 lines). Added open question #6 for Eva. **Recommendation stays as A** because Criteria 1+3 (highest-weighted) still favor A and A and C have equivalent reconciliation outcomes, but the central-bet rationale is now contested rather than confidently defended. | Audit cycle 216 critique landing (~ next audit cron, ~20h). Adversarial Copilot feedback dispatch on the post-cycle-112 selection draft (cycle 112 outstanding). Further stress-tests of other load-bearing claims (e.g., "B's central bet requires aspirational extension" — does B's cluster G framing have any retrospective grounding the cycle 112 stress-test missed?). Cycle 103-110 risk-closure threshold table integration (cycle 113+). Per-candidate Phase 3 prototype evidence deepening (cycle 113+). |
| 113 (2026-05-10) | **Second central-bet stress-test absorbed.** Cycle 113 retried cycle 112's BLOCKED Copilot adversarial dispatch — confirmed runtime allowlist gap is structural (`.github/workflows/orchestrator.yml` lines 66-87 has no `tools/dispatch-task` entry); workflow-change PR opened to add `Bash(tools/dispatch-task *)` and `Bash(tools/dispatch-review *)` to the allow-list, awaiting Eva merge. Pivoted to substantive central-bet stress-test on the cycle 112 "A and C have equivalent reconciliation outcomes" claim. Finding: detection outcomes genuinely equivalent (1-cycle latency, cursor-polling); processing characteristics differ on three under-weighted dimensions cycle 112 named as "legibility only" — (1) per-mode runtime budget allocation (C explicit ≤10min reconcile-mode budget vs A implicit shared boot-phase budget); (2) per-channel structured handlers (C explicit per-event handlers + typed-deltas vs A procedurally-loose channel handling); (3) typed-delta output discipline (C single auditable stream vs A scattered boot-phase outputs). Updated "Why this matters less than it first appears" section adding cycle 113 qualification (~30 lines). Updated "Reframed selection question" with cycle-113 framing alongside cycle-112 framing. Updated "Why the recommendation stays as A" to acknowledge the progressive weakening across cycles 111→112→113 and named the threshold for honest flip (fourth round of under-weighted dimensions surfacing). Updated outstanding work item #3, open question #6, side-by-side table footer note. **Recommendation stays as A** because Criteria 1+3 still favor A and the under-weighted processing dimensions are real but bounded — but the case for A continues to weaken. **Cycle 112's `central-bet-stress-test-finds-mis-grounding` candidate-pattern (NOVEL@1) promotes to TESTED@2** with cycle 113 substrate-content-distinct (cycle 112 found mis-grounding against retrospective text; cycle 113 found under-weighted dimensions in the cycle 112 reframing itself). | Audit cycle 216 critique landing (~next audit cron, ~17h). Workflow-change PR for runtime allowlist awaiting Eva merge. Adversarial Copilot feedback dispatch on the post-cycle-113 selection draft (re-attempt once allowlist PR merges; otherwise dispatch body file remains ready for cycle 114+ once permitted). Further stress-tests of remaining load-bearing claims (B's cluster-G aspirational extension grounding; C's conditional-improvement risk grounding; the 6-criterion ordering structure assumptions). Per-candidate Phase 3 prototype evidence deepening (cycle 114+). |
| 115 (2026-05-10) | **Meta-reflexive stress-test of the cycle 114 flip rationale absorbed; three motivated-reasoning surfaces found in flip rationale; recommendation maintained at C with cycle 115 qualifications; symmetric flip-back threshold named for cycle 116+ object-level C-side stress-testing.** Three lenses applied: (L1) is the four-finding flip threshold protective fiction? (L2) does the flip rationale under-weight Criteria 1+3? (L3) is honoring-the-threshold a shield against re-examining C? **Finding 1 (Lens 1):** the four-finding flip threshold is asymmetric — specified for A→C but not for C→A; without a named symmetric counterpart, the threshold operates as a one-way ratchet rather than a discipline. **Finding 2 (Lens 2):** the cycle 114 flip rationale asserts Criteria 4+5 cumulative weight outranks Criteria 1+3 migration-cost weight WITHOUT deriving this from the redesign prompt's primary thesis (Criterion 1 schema-work-enabling IS the primary thesis per SECTION 1). The weighting is a value choice, structurally analogous to cycle 111's framing of F1+F7 dominance over family-level reading. **Finding 3 (Lens 3):** the cycle 114 flip rationale's "C's direction-validation accumulation" cites cycle 92 + 96 + 107 — predominantly the orchestrator's own iterative work, not external validation. The "accumulation" is asymmetric not because C is structurally better than A but because cycles 111-114 stress-tested A's selection-draft framings while equivalent stress-testing of C's selection-draft framings has not occurred. **Adversarial review** of the three cycle 115 findings (recursive meta-reflexive per `ITERATION-UNTIL-APPROVAL`'s "examine for self-congratulation" applied recursively): all three findings survive counter-arguments (one finding qualified — Finding 3 applies at per-selection-draft-framing level, not at overall per-candidate-work level). **What stays:** tentative recommendation C; cumulative findings cycles 111-114; 6-criterion ordering; the cycle 113 four-finding flip threshold being honored as a real discipline. **What changes:** flip rationale qualified as provisional pending cycle 116+ work; **symmetric flip-back threshold named** (4 substrate-content-distinct findings favoring A over C OR substantively undermining the flip rationale, surfaced in cycle 116+ object-level C-side stress-testing, would trigger flip-back); **Q7 added** (weighting choice surfaced explicitly to Eva as Eva-resolves: adopt orchestrator's Criteria 4+5 > Criteria 1+3 weighting, invert, or specify different rule). **`pre-committed-flip-threshold`** candidate-pattern (TESTED@1 cycle 114) → refined with cycle 115 qualification (extends to `pre-committed-flip-threshold-WITH-SYMMETRIC-COUNTERPART`). **NEW candidate-pattern `meta-reflexive-stress-test-finds-asymmetry`** NOVEL@1 cycle 115. **NEW candidate-pattern `recursive-motivated-reasoning-surface`** NOVEL@1 cycle 115 (tentative; each successive cycle's framing has motivated-reasoning surfaces revealed by the next cycle's stress-test — cycle 116+ stress-test of cycle 115's framing would TEST@2 the pattern). **`discretionary-departure-from-forward-going-commitment`** stays HARDENED-at-4 (cycle 115 HONORED the cycle 114-named meta-reflexive priority — first cycle since cycle 110 to honor the named forward priority; the prior departures remain valid; cycle 115 demonstrates discretionary departure as appropriate-when-higher-priority-surfaces, not as default mode). | Audit cycle 216 critique landing (~next audit cron, ~5.5h from cycle 115 close — cycle 116+ landing window). Workflow-change PR for runtime allowlist awaiting Eva merge ([#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903); 1 cycle into 5-cycle autonomy default — cycle 118 fallback). Adversarial Copilot feedback dispatch on the post-cycle-115 selection draft (re-attempt once [#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903) resolves; dispatch body file needs updating to stress-test the post-cycle-115 selection draft including the cycle 115 meta-reflexive findings). **Cycle 116+ primary substantive work: symmetric C-side stress-testing of selection-draft framings under the cycle 115 symmetric flip-back threshold** — apply the cycle 111-114 stress-test methodology to C's framings (5-point flip rationale; cycle 92 enumeration text fidelity; Criterion 5 ordering shift; cycle 107 Risk 2 closure load-bearingness). Stress-test of the cycle 115 framing itself (the `recursive-motivated-reasoning-surface` pattern predicts cycle 116+ would find motivated reasoning in cycle 115). Per-candidate Phase 3 prototype evidence deepening (cycle 116+; C-side measurements now load-bearing post-flip but pending Eva pre-approval). |
| 114 (2026-05-10) | **Third central-bet stress-test absorbed; four-finding flip threshold honored; tentative recommendation flipped from A to C.** Cycle 114 stress-tested the cycle 111 "Why not C" Reason #1 (conditional-improvement risk: "if the additions don't carry their weight, C is just A with more migration surface"). Finding: the framing cites C-hybrid.md line 73 + 116 (C's self-acknowledged risk language) but **omits C-hybrid.md lines 222-228 (cycle 92 Aggregate central-bet validation: "carry their weight on F4 and F11"; F2 marginal) + cycle 96 PR #2878 absorption (line 191: only F2 qualified as "ambiguous-without-prototype"; F4 + F11 not qualified) + lines 349-372 (cycle 107 Risk 2 closure at specification level with falsifiable thresholds)**. The cycle 111 framing presents the conditional as fully open when direction is in fact validated; the open conditional is magnitude (Phase 3 work). Combined with cycle 112's finding that "Why not C" Reason #3 ("F4/F11 unproven as load-bearing") depends on the cycle-112-refuted F1+F7-dominance basis, the cycle 111 case for A is shown to rest on a chain of motivated reasoning across 4 cycles of stress-testing. **The cycle 113 four-finding flip threshold is met (fourth substrate-content-distinct under-weighted dimension favoring C surfaces); the threshold is honored.** Updated status header + tentative selection (line 10) → C; added new "Cycle 114 stress-test: conditional-improvement framing weakened, four-finding flip threshold honored" section (~120 lines); updated Criterion 5 ordering to C > A > B with cycle-114 rationale; added "Post-cycle-114 stress-test update" paragraph to "Why not C" preserving the section as historical record; renamed and rewrote "Why the recommendation stays as A" → "Why the recommendation flipped to C (post-cycle-114)" with 5-point rationale + cycle 115+ stress-test items; updated outstanding work item #3 with cycle 114 absorption; inverted open questions Q1 (Approve C?) + Q2 (Override to A?); updated Q6 with cycle 114 framing. **`pre-committed-flip-threshold` candidate-pattern promotes from NOVEL@1 to TESTED@1-by-being-honored** (the threshold was a falsifiable pre-committed criterion; honoring it cycle 114 demonstrates it is a load-bearing discipline, not protective fiction). **`progressive-rationale-weakening-without-flip` candidate-pattern interrupted at NOVEL@1 by the flip** (cycle 114 broke the pattern; the flip prevents the pattern from HARDENING at NOVEL@2). **`central-bet-stress-test-finds-mis-grounding` candidate-pattern promotes from TESTED@2 to HARDENED-at-3** (cycle 112 retrospective mis-grounding + cycle 113 under-weighted dimensions in cycle 112 reframing + cycle 114 omitted-validation-evidence in cycle 111 conditional-improvement framing — three substrate-content-distinct findings). **`discretionary-departure-from-forward-going-commitment` candidate-pattern HARDENED@4** (cycle 110 named cycle 111+ rotation; cycles 111+112+113+114 all departed; cycle 114's substrate is the threshold-honoring action). | Audit cycle 216 critique landing (~next audit cron, ~8h from cycle 114). Audit will see the post-cycle-114 selection draft including the flip; the flip is itself a load-bearing claim audit can stress-test cycle 216+. Workflow-change PR for runtime allowlist awaiting Eva merge ([#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903); 1-cycle into 5-cycle autonomy default). Adversarial Copilot feedback dispatch on the post-cycle-114 selection draft (re-attempt once [#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903) resolves; the dispatch should stress-test the flip rationale itself for motivated reasoning, not just the original A recommendation). Further stress-tests for cycle 115+: (a) meta-reflexive stress-test of the cycle 114 flip rationale — is the four-finding threshold itself motivated reasoning? does the flip survive adversarial framing? (b) B-side stress-tests still outstanding (cluster-G grounding; 3-5× migration cost methodology); (c) the 6-criterion ordering structure assumptions; (d) per-candidate Phase 3 prototype evidence deepening with C-side measurements (`reconcile-mode` if Eva pre-approves) now more load-bearing post-flip. |
| 116 (2026-05-11) | **Cycle 115 symmetric flip-back threshold honored at strength-criterion reading; recommendation maintained at C with cycle 116 qualifications.** Object-level C-side stress-test of the cycle 114 flip rationale's specific claims, applying the cycle 111-114 stress-test methodology to four cycle 115-named targets (5-point flip rationale; cycle 92 enumeration text fidelity; Criterion 5 ordering shift; cycle 107 Risk 2 closure load-bearingness). **Four substrate-content-distinct findings surfaced:** **Finding A** the flip rationale point 3's "direction-validating evidence accumulated across 4 cycles of internal review" framing leans on structural arguments (cycle 92 enumeration; cycle 107 specification-level closure with "direction continues to hold by construction") rather than empirical validation — cycle 92 line 230 and cycle 107 lines 367-371 explicitly defer magnitude/operational closure to Phase 3 prototype measurement; the "accumulation across 4 cycles" framing implies progressive evidentiary strengthening when the substantive accumulation is 4 cycles of structural argument by the same author. **Finding B** the chain-of-motivated-reasoning count (4 items in the cycle 114 chain listing) includes Reason #3 collapse as the 4th item; Reason #3 collapse derives from cycle 112's F1+F7 dominance refutation substrate, observed in cycle 114 but not a fresh cycle 114 finding — strict-letter count is 3 fresh + 1 derivative. **Finding C** the Criterion 5 ordering shift A ≈ C (cycle 113) → C > A (cycle 114) conflates ARTICULATION-FAILURES (cycles 112-114 dismantled cycle 111's specific articulations of A's bet) with BET-DEFENSIBILITY-FAILURES (A's central bet structurally remains consistent with the family-level reading of the retrospective); practice-vs-principle conflation. **Finding D** the cycle 107 Risk 2 closure citation conflates structural-direction-by-construction (cycle 107's explicit "Direction continues to hold by construction" — reaffirmation of cycle 92) with new direction-validation evidence (cycle 107's substantive contribution is specification-of-measurement, not new direction-validation). **Adversarial review of cycle 116 findings (recursive meta-reflexive per `ITERATION-UNTIL-APPROVAL`):** all 4 findings stand at qualification-level but NONE solidly meets the cycle 115 symmetric threshold's "substantively undermining" criterion — they are framing concerns (Finding A), count-discipline observations (Finding B), practice-vs-principle observations (Finding C), and load-bearingness clarifications (Finding D). Substantive arguments of the flip rationale survive; framings are refined. **Cycle 115 symmetric flip-back threshold is NOT met at strength-criterion reading; flip-back NOT triggered. Recommendation maintains C tentatively with cycle 116 qualifications absorbed.** Q8 added surfacing the strength-criterion-vs-strict-letter reading explicitly to Eva (three options: accept orchestrator's strength-criterion → maintain C; override to strict-letter count → flip-back to A; specify different strength bar). **`recursive-motivated-reasoning-surface` candidate-pattern (NOVEL@1 cycle 115) promotes to TESTED@2 cycle 116** (cycle 116 stress-test of cycle 115's framings surfaces 4 substrate-content-distinct motivated-reasoning surfaces in the cycle 114 flip rationale's specific claims; pattern's predicted behavior confirmed). Pattern's load-bearing function clarified cycle 116: strength-discipline gradient distinguishes flip-triggering from qualifying findings. **NEW candidate-pattern `findings-qualify-not-refute-at-strength-discipline` NOVEL@1 cycle 116.** **`pre-committed-flip-threshold-WITH-SYMMETRIC-COUNTERPART` TESTED@2-by-being-honored cycle 116** (threshold honored by NOT triggering flip-back when findings qualify rather than substantively-undermine). **`discretionary-departure-from-forward-going-commitment` stays HARDENED-at-4** (cycle 116 HONORED the cycle 115-named priority — second consecutive cycle of honoring); two modes coexist as appropriate-to-context. | Audit cycle 216 critique landing (most likely cycle 117 window per cycle 115 timing estimate, ~04:00 UTC 2026-05-11). Audit will see the post-cycle-115 + post-cycle-116 qualifications; engages with both meta-reflexive (cycle 115) and object-level (cycle 116) refinements. Workflow-change PR for runtime allowlist awaiting Eva merge ([#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903); 2 cycles into 5-cycle autonomy default — cycle 118 fallback). Adversarial Copilot feedback dispatch on the post-cycle-116 selection draft (re-attempt once #2903 resolves; dispatch body file needs further updating to stress-test the post-cycle-116 framings). **Cycle 117+ primary substantive work:** continued object-level stress-testing of the post-cycle-115/116 flip rationale (additional claims: Criterion 4 family-level reading load-bearingness; the cycle 115 + 116 patterns' own potential motivated-reasoning surfaces); B-side stress-tests outstanding (cluster-G grounding; 3-5× migration cost methodology); 6-criterion ordering structure assumptions. Per-candidate Phase 3 prototype evidence deepening (cycle 117+; C-side measurements load-bearing post-flip, pending Eva pre-approval). Stress-test of cycle 116's framings themselves — if substantively undermining at the strength criterion, flip-back triggers; if qualifying-not-refuting, further refinement. |
| 117 (2026-05-11) | **Cycle 116 strength-criterion classification stress-tested at third meta-reflexive layer; 4 substrate-content-distinct findings + 1 meta-finding scope-narrow/clarify cycle 116 findings without substantively undermining; recommendation maintained at C with cycle 117 qualifications absorbed; `recursive-motivated-reasoning-surface` pattern HARDENS-at-3.** Recursive stress-test of cycle 116's classifications (Findings A-D + strength-criterion application action) per cycle 116-named priority #1. Third consecutive cycle to honor prior-cycle-named forward priority (cycles 115, 116, 117). **Five findings surfaced:** **Finding 1** cycle 116 Finding A's structural-vs-empirical distinction over-reads — design analysis IS structural argument; each of cycles 92/96/107 added new analytical material (cycle 92: per-failure-mode enumeration; cycle 96: PR #2878 absorption qualifying F2; cycle 107: Risk 2 closure with specification-level falsifiable thresholds); the "accumulation across 4 cycles" framing is analytically-progressive, not just repetitively-progressive; Finding A's concern narrows to "evidence" word choice at framing level. **Finding 2** cycle 116 Finding B's count-discipline concern applies to the chain narrative count (3 fresh + 1 derivative in the cycle 114 listing), NOT to the threshold count which is round-based (cycle 112 + 113 + 114 rounds each producing substrate-content-distinct findings); the threshold-honoring action depended on the round count, intact. **Finding 3** cycle 116 Finding C's defense of A's bet via practice-vs-principle distinction is contingent on Q6 family-level vs pattern-level reading resolution — under family-level reading, A's bet articulates as "reconciliation asymmetry addressed by interleaved-with-boot reconciliation" (consistent with retrospective lines 161-162 + 959; Finding C substantive); under pattern-level reading, A's bet articulates as "F1+F7 dominance addressed by Axis 13" (cycle 112 found mis-grounded; Finding C partial-at-best). **Finding 4** cycle 116 Finding D's precision-vs-validation dichotomy over-precisely separates cycle 107's two functions — cycle 107 contains BOTH structural-direction-reaffirmation (line 351 "Direction continues to hold by construction") AND measurement-specification (lines 366-371); flip rationale citation is faithful at reaffirmation level; Finding D's substantive distinction (cycle 107's new substantive contribution is precision) stands but "NOT new direction-validation" framing dilutes the distinction. **Meta-Finding** cycle 116's strength-criterion classification of Findings A-D is contingent on Q6 resolution — under family-level reading, Finding C escalates to substantively-undermining; threshold count still <4 even with escalation; classification action stable but contingent. **Adversarial review (third meta-reflexive layer):** all 5 findings stand at scope-narrowing/clarification/contingency-naming level — NONE escalate to substantively-undermining. The cycle 115 symmetric flip-back threshold is NOT met (0 substantively-undermining solidly + 1 partial under family-level reading; threshold requires 4). The recommendation maintains C tentatively with cycle 117 qualifications absorbed. **Q9 added** surfacing the family-level vs pattern-level reading-contestation as cross-cutting Eva-resolves item that simultaneously affects Criterion 4 ordering, Criterion 5 ordering shift defensibility, Finding C strength, and strength-criterion application contingency. **`recursive-motivated-reasoning-surface` candidate-pattern (TESTED@2 cycle 116) → HARDENED-at-3 cycle 117** (three substrate-content-distinct cycles of evidence; strength-discipline gradient operating consistently). **`findings-qualify-not-refute-at-strength-discipline` (NOVEL@1 cycle 116) → TESTED@2 cycle 117** (second cycle of qualifications-not-refutations at strength criterion). **NEW candidate-pattern `productive-content-decreases-with-recursion-depth` NOVEL@1 cycle 117** — each recursive stress-test layer produces qualifying findings with decreasing novel substance (cycle 115 structural critiques → cycle 116 object-level critiques → cycle 117 scope-narrowing/clarification critiques); second natural-exhaustion criterion alongside the strength-discipline gradient. **`pre-committed-flip-threshold-WITH-SYMMETRIC-COUNTERPART` (TESTED@2 cycle 116) → HARDENED-at-3 cycle 117**. **`discretionary-departure-from-forward-going-commitment` (HARDENED-at-4 cycle 114) → cycle 117 third consecutive HONORING** (cycles 115, 116, 117 all honored named forward priorities). | Audit cycle 216 critique landing (most likely cycle 117 close or cycle 118 window; ~04:00 UTC 2026-05-11). Audit will see the post-cycle-117 selection draft including the cycle 117 qualifications + Q9. Workflow-change PR for runtime allowlist awaiting Eva merge ([#2903](https://github.com/EvaLok/schema-org-json-ld/issues/2903); 3 cycles into 5-cycle autonomy default — cycle 118 fallback for orchestrator-resolves option A). Adversarial Copilot feedback dispatch on the post-cycle-117 selection draft (re-attempt once #2903 resolves; dispatch body file needs further updating). **Cycle 118+ primary substantive work options (in priority order):** (1) audit cycle 216 critique absorption (highest priority once landed); (2) Q6 + Q9 resolution by Eva (load-bearing for cycle 116/117 findings); (3) #2903 resolution at cycle 118 close (5-cycle autonomy default fallback); (4) B-side stress-tests still outstanding (cluster-G grounding; 3-5× migration cost methodology); (5) DECREASING priority — continued recursive stress-testing of cycle 117's findings (per the cycle 117-named `productive-content-decreases-with-recursion-depth` pattern, cycle 118+ stress-test would likely produce strictly-redundant findings; the recursive iteration is approaching a fixed point; pivot recommended); (6) Per-candidate Phase 3 prototype evidence deepening (C-side `reconcile-mode` measurement if Eva pre-approves). |
