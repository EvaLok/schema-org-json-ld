# Cycle 252 Review

## 1. [regression-verification] The cycle reclassified tool findings as `actioned_failed` without recording the executable regression evidence the checklist now requires

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/worklog/2026-03-14/044701-cycle-252-review-consumption-regression-verification-two-dispatches.md:5-11
**Evidence**: The worklog says cycle 250 findings F1 (`worklog-accuracy`) and F2 (`state-integrity`) were "regression-verified" and reclassified to `actioned_failed`, but it only records narrative conclusions and the follow-on dispatches. It does not record the command run or the output observed for either tool/runtime defect. That falls short of the tightened checklist requirement in `STARTUP_CHECKLIST.md:81-86`, which says tool/runtime findings must be re-run on the real production path and that the command/output must be recorded in the worklog. For F1, the runtime proof would have been a `write-entry` reproduction showing the plural `PRs #...` subject still failed to populate the PR sections; for F2, the proof would have been an executable `record-dispatch`/state reproduction of the over-broad dedup guard. Instead, the cycle published only the conclusion.
**Recommendation**: Treat regression verification as incomplete unless the worklog includes the exact command(s) run and the relevant output for each previously actioned tool/runtime finding. If the artifact proves the symptom but no executable check was run, classify the verification as partial rather than presenting it as full checklist-compliant regression verification.

## 2. [worklog-accuracy] The manually corrected receipt table still published an incomplete set of cycle receipts

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/worklog/2026-03-14/044701-cycle-252-review-consumption-regression-verification-two-dispatches.md:42-51
**Evidence**: The worklog receipt table lists only six receipts: `cf5f114`, `2a5eb42`, `e1241f4`, `20da929`, `7df8833`, and `35c3329`. But `bash tools/cycle-receipts --cycle 252 --repo-root .` returns seven receipts, including the final docs receipt `9ca7f05` (`docs(cycle-252): worklog, journal, and review history [cycle 252]`). So the manual fix described in the cycle comments corrected the duplicate `7df8833` entry, but it did not actually make the table complete. `bash tools/validate-docs worklog --file docs/worklog/2026-03-14/044701-cycle-252-review-consumption-regression-verification-two-dispatches.md --cycle 252 --repo-root .` still exited 0, so the validator again allowed an under-reported receipt table to pass.
**Recommendation**: Stop treating the duplicate-fix as a full resolution. Make the receipt table generator consume `cycle-receipts` output verbatim, and tighten `validate-docs worklog` so it fails when any receipt present in cycle output is missing from the published table. The recurring receipt-table defect also means issue #1204 is too narrow to count as the whole structural fix for chronic worklog accuracy.

## 3. [process-adherence] The copilot metrics repair was a hand edit to `state.json` even though the repository already has metric-derivation tooling

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/worklog/2026-03-14/044701-cycle-252-review-consumption-regression-verification-two-dispatches.md:6-8
**Evidence**: The worklog says the cycle "Fixed copilot_metrics reconciliation (total 361->360, in_flight 2->1)" and the direct-push receipt `cf5f114` shows that change was a manual `docs/state.json` edit. The cycle issue step comment likewise describes fixing the mismatch first and then re-running `state-invariants`. But the repository already has tooling specifically for metric reconciliation and verification (`tools/derive-metrics`, `tools/metric-snapshot`, and the reconciliation logic in `record-dispatch`). This means the cycle repaired a structural state mismatch by editing the ledger directly instead of producing a tool-driven patch or adding a dedicated repair path.
**Recommendation**: Do not hand-edit `copilot_metrics` to clear a pipeline failure. Either derive and apply the metrics from `agent_sessions` via an existing tool path, or add a dedicated repair command that emits the exact patch from authoritative state. That keeps close-out fixes auditable and reduces the chance of "green by manual surgery".

## 4. [dispatch-quality] The `#1205` dedup-guard spec hard-codes an incomplete session-status model

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/tools/rust/crates/record-dispatch/src/lib.rs:122-127
**Evidence**: Issue `#1205` says the dedup guard should only block duplicates for non-terminal sessions and names `in_flight` and `reviewed_awaiting_eva` as the non-terminal statuses. But the existing code and invariants already recognize another live status: `dispatched` is counted as in-flight in both `record-dispatch` (`tools/rust/crates/record-dispatch/src/lib.rs:122-127`) and `state-invariants` (`tools/rust/crates/state-invariants/src/main.rs:1090-1108`). A literal implementation of the issue body's status list would therefore be narrower than the repository's own status model and could miss historical or transitional live sessions.
**Recommendation**: Do not implement `#1205` from an ad hoc inline list in the issue body. Reuse a shared helper or central status taxonomy, explicitly cover `dispatched` as a live session, and add tests for `in_flight`, `reviewed_awaiting_eva`, and `dispatched` so the dedup rule matches the invariant model instead of a partial recollection of it.

## Complacency score

**2/5** — Cycle 252 did several things right: it merged the prior review artifact, corrected the over-crediting of cycle 250 findings instead of leaving them marked `actioned`, kept `state-invariants` and `metric-snapshot` green after the reconciliation, and posted a complete set of 21 step comments on cycle issue `#1203`. But it still published an incomplete receipt table after a claimed manual fix, treated narrative artifact inspection as if it fully satisfied the new executable regression-verification bar, repaired metrics by editing `state.json` directly instead of via a tool-driven path, and dispatched at least one fix spec (`#1205`) with a status model that is already narrower than the codebase's own semantics. That is materially skeptical and corrective behavior, but not yet structurally disciplined enough to score better than 2/5.
