# Cycle 366 Review

## 1. [worklog-accuracy] The published cycle summary is still a pre-dispatch snapshot and is stale against the cycle’s final committed state

**File**: `docs/worklog/2026-03-26/050526-cycle-366-merge-backlog-review-fixes-pipeline-accuracy-dispatches.md:29-39`
**Evidence**: The worklog explicitly freezes a “Snapshot before review dispatch” with `In-flight agent sessions: 2`, `Copilot metrics: 578 dispatches, 519 merged`, and next steps only for `#1793` and `#1795`. The final committed state after C6/C8 is different: `docs/state.json:5163-5183` records the new review dispatch `#1797`, and `docs/state.json:5402-5412` ends the cycle with `dispatch_log_latest` set to `#1797`, `in_flight` at `3`, and `total_dispatches` at `579`. The cycle issue’s C6 step comment also says “Review dispatched as #1797” (`https://github.com/EvaLok/schema-org-json-ld/issues/1792#issuecomment-4131726541`). This is the same chronic worklog-accuracy drift that cycle 365 already flagged; cycle 366 dispatched `#1795` for it, but the cycle 366 artifact still ships stale state as the published summary.
**Recommendation**: Regenerate the state/next-steps block from final state after C6/C8, or publish a distinct final close-out summary after review dispatch so the committed cycle record reflects the completed cycle rather than the pre-dispatch checkpoint.

## 2. [review-history-accuracy] Cycle 366 dispatched fixes for the cycle 365 findings but left the review history marked as fully deferred

**File**: `docs/state.json:9103-9117`
**Evidence**: The cycle 365 review history entry still says `deferred: 2`, with both finding dispositions recorded as `deferred`, and the note still says both findings “require” future tool fixes. But cycle 366’s own artifacts say those tool-fix dispatches already happened: the worklog records `#1793` as the receipt-accuracy fix and `#1795` as the worklog-accuracy fix at `docs/worklog/2026-03-26/050526-cycle-366-merge-backlog-review-fixes-pipeline-accuracy-dispatches.md:8-9`, and the journal says those dispatches “address chronic review findings flagged in cycles 364 and 365” at `docs/journal/2026-03-26.md:92`. The C2 step comment on issue `#1792` shows why the state stayed stale: the orchestrator ran `record-dispatch --issue 1793` and `record-dispatch --issue 1795` without `--addresses-finding`, even though PR `#1789` had just restored that linkage path (`https://github.com/EvaLok/schema-org-json-ld/issues/1792#issuecomment-4131693687`).
**Recommendation**: When a new dispatch is created specifically to address a review finding, require `record-dispatch --addresses-finding CYCLE:INDEX` so the corresponding history entry flips to `dispatch_created` in the same cycle. If that linkage is optional by design, add a post-dispatch reconciliation step so state history cannot keep reporting “deferred” after the worklog and journal claim the follow-up was dispatched.

## 3. [receipt-accuracy] The receipt note still does not describe the actual receipt filter that produced the table

**File**: `docs/worklog/2026-03-26/050526-cycle-366-merge-backlog-review-fixes-pipeline-accuracy-dispatches.md:43-52`
**Evidence**: The note says post-C5.1 commits in the categories “docs, record-dispatch, review-body, cycle-tagged” are structurally excluded. But the same cycle’s step comments show that description is not the real filter set: C5 says the docs commit `4f322f3` already happened before C5.1 (`https://github.com/EvaLok/schema-org-json-ld/issues/1792#issuecomment-4131724935`), and C2 shows the `record-dispatch` commits for `#1793` and `#1795` also happened before C5.1 (`https://github.com/EvaLok/schema-org-json-ld/issues/1792#issuecomment-4131693687`). Meanwhile the table itself already includes a `cycle-tagged` row (`0b00249`) at line 52, and `bash tools/cycle-receipts --cycle 366 --repo-root .` reports the only additional excluded receipt as the later `cycle-tagged` commit `9865a2b`. So the prose is still hand-written approximation rather than a faithful description of the actual included/excluded receipt set.
**Recommendation**: Generate the note from the same filtered receipt data used to render the table, including explicit excluded SHAs if needed, or remove the prose note entirely and trust the auto-generated receipt table. The chronic receipt-accuracy finding is not actually closed while the explanatory note still drifts from the real receipt filter.

## Complacency score

**3/5** — The cycle did real work: PRs `#1787` and `#1789` were focused and test-backed, `bash tools/state-invariants` and `bash tools/metric-snapshot` pass, the full repo validation passes locally, and issue `#1792` has 25 cycle-366 step comments with the mandatory steps present. But the cycle still published the same stale pre-dispatch worklog pattern that cycle 365 had already flagged, still shipped a receipt note that does not match its actual filter logic, and then failed to record its own new follow-up dispatches back into review history even after merging the `--addresses-finding` repair. That is meaningful recurring drift, not just bad luck.
