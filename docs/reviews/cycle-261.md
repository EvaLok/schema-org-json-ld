# Cycle 261 Review

## 1. [worklog-accuracy] The published receipt note still certifies the wrong scope after the table was patched

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/worklog/2026-03-14/222215-cycle-261-review-consumption-cascade-merge-2-dispatches-chronic-rewrite.md:41-55
**Evidence**: The receipt note says “Docs and record-dispatch commits are structurally excluded (created post-worklog),” but that is not what the cycle history shows. `git show --stat 58bf927 6d98cd1 73f3b8c bc5293c` proves both record-dispatch commits landed before the docs commit, and `bash tools/cycle-receipts --cycle 261 --repo-root . --json` returns canonical receipts for `d663048`, `7867b57`, and `e58aadc` in addition to the earlier cycle receipts. The first published worklog commit (`73f3b8c`) omitted those three post-cycle-complete state-repair receipts entirely; `bc5293c` patched them back in later, but the explanatory note was never updated. Worse, the repo’s own validator still passed the incomplete `73f3b8c` artifact: in a temp worktree pinned to `73f3b8c`, `bash tools/validate-docs worklog --file docs/worklog/2026-03-14/222215-cycle-261-review-consumption-cascade-merge-2-dispatches-chronic-rewrite.md --cycle 261 --repo-root .` exited 0 even though `bash tools/cycle-receipts --cycle 261 --repo-root .` listed receipts the table did not contain.
**Recommendation**: Stop hard-coding receipt-scope boilerplate in the worklog. Generate the note directly from `cycle-receipts`/`receipt-validate` output, and make `validate-docs` fail when the published table omits required post-cycle-complete state-repair receipts or when the note contradicts the actual included/excluded commit classes.

## 2. [process-adherence] Non-review dispatches pushed the cycle into `complete`, and the cycle had to be manually rewound and repaired afterward

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/state.json:3447-3459,3646-3663
**Evidence**: The final state on disk is `close_out`, but the cycle only got there after a repair sequence. `git show 58bf927 -- docs/state.json` shows record-dispatch for issue `#1256` appended a new in-flight session and also advanced `cycle_phase.phase` from `close_out` to `complete` at `2026-03-14T22:23:36Z`. `git show 6d98cd1 -- docs/state.json` did the same again for `#1258` at `22:23:42Z`, raising `copilot_metrics.in_flight` to 3. The next three commits are explicit state surgery: `d663048` rewound `cycle_phase` back to `close_out`, `7867b57` changed issue `#1253` from `in_flight` to `merged` and corrected `in_flight` from 3 to 2, and `e58aadc` recalculated merged/resolved/produced PR metrics. The worklog’s “What was done” and “Current state” sections present only the cleaned-up end state, even though the cycle briefly moved into an invalid post-dispatch phase and needed three repair commits to recover. This is exactly the process hazard the issue asked to examine: record-dispatch for non-review tasks advanced the phase prematurely, and the cycle relied on manual cleanup rather than preventing the bad transition.
**Recommendation**: Make record-dispatch fail closed for non-review dispatches when the cycle is still in `close_out`, or teach it to leave `cycle_phase` untouched unless the dispatch is the review-dispatch that truly completes the cycle. Also require the worklog to explicitly disclose any post-cycle repair commits that were needed to restore state integrity instead of narrating only the repaired final state.

## 3. [journal-quality] The “structural” review-evidence fix is still only a dispatch, but the journal/state already present it as verified

**File**: /home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/state.json:4026-4032
**Evidence**: The chronic `review-evidence` entry now claims a structural fix and marks `verification_cycle` as `261`, with rationale stating that a state-invariants extension “makes the requirement machine-verified.” But no such enforcement landed in cycle 261. The only concrete action in this cycle was dispatching issue `#1258`, and GitHub still shows `#1258` open. The cycle’s journal repeats the same overclaim: `/home/runner/work/schema-org-json-ld/schema-org-json-ld/docs/journal/2026-03-14.md:440-452` says the real fix is two-part and treats the structural half as established, while the sole next-step commitment is merely to “Review and merge #1256 and #1258 next cycle.” That is not a verified structural fix; it is a plan. Marking the chronic response as verified before the invariant exists collapses the distinction between “dispatched” and “fixed,” which is exactly how chronic problems get cosmetically rewritten instead of measurably closed.
**Recommendation**: Revert this chronic entry to an in-progress status until the invariant/write-entry fix is merged and demonstrated on a later cycle. Journal commitments should name an observable completion condition—e.g. “merge the invariant and confirm the next merged PR has an auditable GitHub review event without manual checking”—instead of narrating an open dispatch as if the structural fix already exists.

## Complacency score

**2/5** — Cycle 261 is not a total no-op: PR `#1252` was actually reviewed on GitHub and shipped with focused regression coverage, so there was some real engineering work. But the cycle still published a worklog whose receipt note contradicted the actual receipt scope, relied on a validator that missed the omission, let non-review dispatches push `cycle_phase` into `complete`, repaired state manually afterward, and then rewrote the chronic `review-evidence` response as if the structural fix were already verified when it was only dispatched. That is some improvement in effort, but not much improvement in verification discipline.
