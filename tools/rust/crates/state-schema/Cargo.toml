[package]
name = "state-schema"
version = "0.1.0"
edition = "2021"
description = "Shared serde schema for docs/state.json"

[dependencies]
chrono = { version = "0.4", features = ["clock"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
