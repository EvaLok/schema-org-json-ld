[package]
name = "receipt-validate"
version = "0.1.0"
edition = "2021"
description = "Validate worklog receipt tables against canonical cycle receipts"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

