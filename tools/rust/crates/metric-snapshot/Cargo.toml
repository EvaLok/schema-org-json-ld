[package]
name = "metric-snapshot"
version = "0.1.0"
edition = "2021"
description = "Verifies docs/state.json metrics against repository filesystem state"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
state-schema = { path = "../state-schema" }
