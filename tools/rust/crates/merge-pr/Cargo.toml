[package]
name = "merge-pr"
version = "0.1.0"
edition = "2021"
description = "Automate the full PR merge workflow"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
