[package]
name = "check-field-inventory"
version = "0.1.0"
edition = "2021"
description = "Field inventory completeness check for state.json — replaces check-field-inventory.jq"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
