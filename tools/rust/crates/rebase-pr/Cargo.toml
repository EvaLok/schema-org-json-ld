[package]
name = "rebase-pr"
version = "0.1.0"
edition = "2021"
description = "Rebase a Copilot PR branch onto current origin/master and force-push it safely"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
