[package]
name = "post-step"
version = "0.1.0"
edition = "2021"
description = "Post individual orchestrator checklist step outcomes as issue comments"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
state-schema = { path = "../state-schema" }
