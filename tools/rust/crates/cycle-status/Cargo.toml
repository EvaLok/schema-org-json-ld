[package]
name = "cycle-status"
version = "0.1.0"
edition = "2021"
description = "Consolidated startup checklist report for orchestrator cycles"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
