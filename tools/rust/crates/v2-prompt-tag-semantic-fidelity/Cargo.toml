[package]
name = "v2-prompt-tag-semantic-fidelity"
version = "0.1.0"
edition = "2021"
description = "Verifies v2 role prompts' top-level tag-content semantic alignment against a TOML manifest (design scope: docs/redesign/_notes/v2-prompt-tag-semantic-fidelity.md, cycle 168)"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
quick-xml = "0.31"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
